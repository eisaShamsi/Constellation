[{"n": 1, "sev": "APP-KILLER", "klass": "content-corruption", "file": "src/lib/components/NoteEditor.svelte", "line": 447, "summary": "The new MIG-101 applyShape is the only write path in NoteEditor with no `if (readOnly) return` guard, so read-only hosts (second screen, Index preview) durably write a compose of their own stale, never-edited model over the note on disk.", "scenario": "The ⋯ breadcrumb menu and its four new shape items render unconditionally in NotePane (NotePane.svelte:1388-1427 — no readOnly gate), and NoteEditor routes shapeScrap/shapePage/shapeClear/shapeRevert through the always-handled switch block (NoteEditor.svelte:538-566) into applyShape. handlePromote:183, handleSave:247, handleFlush:304 and handleTitleChange:377 all open with `if (readOnly) return;` — applyShape:447 does not. Concrete: the Index preview mounts `<NoteEditor tab={indexNoteTab} readOnly={true}>` (+layout.svelte:7529) with tab id `index_preview_<ts>`, so ensureModel creates a SECOND model for a path that may already be open in a real tab with a different id (the dedup invariant is keyed on openTabs and does not cover this host). Open note N in a tab, edit and save it, then open the Index and preview N (its preview model is seeded from disk at preview time — now older), then cli"}, {"n": 2, "sev": "APP-KILLER", "klass": "cross-window-clobber", "file": "src/lib/libraries/store.ts", "line": 2298, "summary": "The display-only second-screen window can be handed a DIRTY note model (markModelRecoveredFromNet on any path with a write-ahead net) and its three departure-flush sites (openNoteTab in-place reuse :2239, closeTab :2627, loadTabHistoryEntry) durably write that model to disk — an unguarded write from the read-only window that clobbers newer main-window content with no error.", "scenario": "PJ-108 made every second-screen note-open PRESERVE the shared crash-recovery net (store.ts:2149, preserveNet = displayOnlyWindow), but resolveNoteContent still returns `recoveredFromNet: true` for that path (store.ts:2065), so openNoteTab unconditionally calls markModelRecoveredFromNet (store.ts:2278 reuse branch / :2298 new-tab branch), which does `m.version++` (noteModel.ts:266) — the SS model is born DIRTY. The net is genuinely shared across windows: setWriteAhead persists to localStorage (store.ts:255-264) and both webviews are same-origin (the code itself notes 'a second window sharing the localStorage origin', store.ts:291), and the main window leaves a net for EVERY viewed note on editor teardown (NoteEditor.svelte:334-338, `if (!needsDiskSave) { setWriteAhead(...); return; }`). Concrete recipe: (1) main window opens note X, edits, saves, then switches tabs — teardown stashes net("}, {"n": 3, "sev": "APP-KILLER", "klass": "content-corruption", "file": "src/lib/editor/yamlDoc.ts", "line": 145, "summary": "serializeLine() has no `nested-object-list` branch, so the G4 CST compose path flattens a structured ikhtilāf seq-of-maps into a single scalar summary string on disk — the legacy reconstructFrontmatter (store.ts:1749-1768) DID serialize this type correctly, so the G4 swap silently dropped the serializer.", "scenario": "A note carries a structured ikhtilāf block (parsed by store.ts:1614-1675 into type 'nested-object-list' + nestedObjects):\n---\nikhtilāf:\n  - school: Hanafī\n    position: permissible\n  - school: Mālikī\n    position: discouraged\n---\nThe user opens it and changes 'discouraged' to 'prohibited' in the §A.3 nested-row widget. updateNestedField (PropertyEditor.svelte:689-697) rebuilds nestedObjects AND prop.value (the ' | '-joined summary), then debouncedSave → saveTabContent → editNoteProps → setProps → compose → composeFrontmatter. The diff comparator (yamlDoc.ts:247-249) sees op.value !== np.value → the key is NOT skipped. findItem() resolves the CST item whose value token type is 'block-seq', not 'scalar', so the scalar-set branch (line 252) is skipped and the ELSE branch (lines 255-257) SPLICES the entire ikhtilāf block-seq out of the CST and pushes serializeLine('ikhtilāf', np). serializeL"}, {"n": 4, "sev": "HIGH", "klass": "index-divergence", "file": "src-tauri/src/libraries.rs", "line": 929, "summary": "In-app FOLDER rename does no DB path cascade AND the gated rename suppresses the watcher event that is the only path which would have healed it — every descendant note's index rows stay at now-dead paths for the rest of the session.", "scenario": "User right-clicks folder `Lib/Research` in the file tree → Rename → `Studies`. `rename_item` takes the non-`.md` branch (libraries.rs:922-931) and calls `gate_rename(old,new,\"rename_folder\")` only — there is NO note_meta/note_links/note_aliases/note_embeddings/review_schedule path UPDATE for any of the 40 notes inside (the `.md` branch at :1047-1095 does all of that; the folder branch does none). The healing path that normally covers an EXTERNAL folder rename is `watcher.rs` → `library-changed` → `reindex_changed_paths` → `reindex_md_descendants` (new side) + `delete_rows_under_prefix` (old side). But `gate_rename` (write_gate.rs:582-583) calls `watcher_suppress::mark(old)` and `mark(new)` for BOTH folder paths, and Windows `ReadDirectoryChangesW` emits ONE rename event for the directory (never per-descendant), so `was_recent` (watcher.rs:85) filters out the only signal and `changed_path"}, {"n": 5, "sev": "HIGH", "klass": "silent-data-loss", "file": "src-tauri/src/libraries.rs", "line": 1747, "summary": "move_item reindexes a moved note by delete-then-add, which DESTROYS its review_schedule row (✓ history, interval, ease, snooze) and orphans its note_aliases rows — rename_item explicitly migrates both and documents why.", "scenario": "User drags note `Lib/Inbox/Hub.md` into `Lib/Projects/` (or uses right-click → Move). `move_item` calls `reindex_delete_note(&search_state, &source_path)` (libraries.rs:1747; :1740 for every descendant of a moved folder) and then `reindex_single_note` at the new path. `reindex_delete_note` (search.rs:9228-9231) calls `crate::review::delete_schedule_row(conn, note_path)` — it hard-deletes the note's review_schedule row. `reindex_single_note` re-creates only a DEFAULT schedule row; last_reviewed, interval, ease and snooze are gone. The note's entire spaced-review history is destroyed by an ordinary drag-and-drop, with no error, no dialog, no journal anomaly (the journal records a clean `renamed`). This is provably not intended: rename_item's tail goes out of its way to preserve exactly this (libraries.rs:1077-1095, `UPDATE review_schedule SET path = ?2 WHERE path = ?1`, with the comment \"c"}, {"n": 6, "sev": "HIGH", "klass": "content-loss", "file": "src/lib/components/BacklinksPanel.svelte", "line": 181, "summary": "`linkMention` writes a `[[wikilink]]` straight to a note's .md on disk with no check for an open model, no reindex, and the whole operation wrapped in `catch { /* ignore */ }`.", "scenario": "User has note B open in a tab (split view, or simply a second tab) and has typed in it, so B's model is dirty/its baseline is the pre-edit bytes. In the Backlinks panel of note A the user clicks the \"link mention\" action on B. Line 181 does `invoke('write_note', {filePath: B, ...})` — a raw disk write BEHIND the open model. Because `gate_write` calls `watcher_suppress::mark` (write_gate.rs:249-250), the file watcher emits nothing, so `adoptExternalChangeIntoTabs`/the PJ-070 conflict-sidecar path never runs and B's model never learns. 1500 ms later B's debounced `editor_save` composes from the model (whose body still lacks the wikilink) and writes it over the top: the wikilink the user just created is gone from disk, from screen, and from the index, with no error and no `.conflict` sidecar. Even in the closed-note case the write is never paired with `constellation_search_reindex`, so `not"}, {"n": 7, "sev": "HIGH", "klass": "false-success", "file": "src-tauri/src/search.rs", "line": 9289, "summary": "`reindex_single_note` returns `Ok(())` after skipping all work when `state.db` is `None` — every caller, including the save path's `constellation_search_reindex`, resolves as success while the index was never touched.", "scenario": "`let db = state.db.lock()...; if let Some(conn) = db.as_ref() { ...index_note... } Ok(())` — the `None` arm falls straight through to `Ok(())` at line 9359. `state.db` is set to `None` on every universe switch (search.rs:8423) and is `None` until first init. Scenario: the user edits a note, then switches universe (or the switch's teardown lands) inside the 1500 ms save debounce. `noteSession.save` writes the new bytes durably to disk, `markSaved` marks the model clean, then `standardSaveEnv.onSaved` fires `invoke('constellation_search_reindex', ...)`, which reaches `reindex_single_note` with `db == None` and resolves **successfully**. The frontend's `.catch(() => {})` (NoteEditor.svelte:266, :356; +layout.svelte:1583) has nothing to catch. The note's disk content is now newer than its `note_meta`/`notes_fts`/`note_links` row — the note is still findable by its OLD text and unfindable by "}, {"n": 8, "sev": "HIGH", "klass": "content-loss", "file": "src/lib/libraries/store.ts", "line": 3522, "summary": "renameItem force-reseeds the model from disk (openNoteModel) with no re-check of dirty, silently discarding any keystrokes typed during the rename's three awaits — and the write-ahead net for those bytes was just cleared two lines earlier.", "scenario": "User is typing in note A (model dirty from the per-keystroke setBody). They rename it (title bar, sidebar Rename, or FocusPane title blur → handleRenameComplete → renameItem). Line 3466-3470 flushes the model and awaits the write; line 3476 awaits `rename_item`; line 3512 awaits `readNote`. The editor is NOT frozen during any of this — +layout.svelte:6390 raises cascadeFreeze only AFTER renameItem returns (its own comment says raising it earlier aborted the cascade), and markCascading/isCascading gates saves, not typing. So every character typed across those awaits lands in the model via onDocChange→editBody. Line 3509-3510 then clearWriteAhead(oldPath) + clearWriteAhead(effectivePath) destroys the recovery net, and line 3522 openNoteModel(t.id, effectivePath, fresh) replaces the whole model from disk. Those keystrokes now exist nowhere — not on screen (the tab also takes fresh + a reloa"}, {"n": 9, "sev": "HIGH", "klass": "silent-data-loss", "file": "src/lib/components/NoteEditor.svelte", "line": 447, "summary": "`applyShape` is the only ⋯-menu write handler with NO `readOnly` guard, so a read-only display (Index preview, second screen) silently rewrites the note's frontmatter on disk.", "scenario": "Every other write path in this component early-returns on `readOnly` (handlePromote:183, handleSave:247, handleFlush:304, handleTitleChange:377, and even onDocChange:625). `applyShape` (447) does not. The breadcrumb ⋯ menu that hosts the four new shape items (NotePane.svelte:1409-1429) is rendered unconditionally — it is NOT gated on `readOnly`. Concrete: open the Index panel preview (mounted `readOnly={true}` at src/routes/+layout.svelte:7529), click ⋯ → \"Page\". applyShape runs `editProps` + `compose` + `saveNoteSession(... 'shape_set')` and writes `shape: page` into the real .md on disk from a surface contractually guaranteed to perform zero writes (Display-not-Domain, G3). No error, no dialog, no visual difference in the read-only pane. Worse on the second screen (SecondScreenPage.svelte:1129/1295): that window's model writes the file while the MAIN window holds its own model for the "}, {"n": 10, "sev": "HIGH", "klass": "content-loss", "file": "src/lib/libraries/store.ts", "line": 3522, "summary": "renameItem force-re-seeds the note model from disk (openNoteModel) after clearing its cascade gate and after an intervening await, with no re-check that the model went dirty during the long rename_item await — keystrokes typed while the rename is in flight are silently destroyed.", "scenario": "Note A is open in NotePane. The user renames it from the NotePane title bar (+layout.svelte:8061 onTitleRename → handleRenameComplete → renameItem). renameItem flushes the model at store.ts:3466-3469 (model now clean), then awaits invoke('rename_item') at 3476 — an (async) dual-locked command the caller's own watchdog allows up to 30s (+layout.svelte:6351-6355). cascadeFreeze is NOT yet raised (that happens only after renameItem returns, +layout.svelte:6390), so there is no overlay and no read-only state: the user continues typing into the body. Each keystroke reaches editBody (NoteEditor.svelte:625 — editBody is never cascade-gated) and the model becomes dirty again. rename_item returns; the finally at 3478 clears the cascade mark; readNote is awaited at 3512; then line 3522 calls openNoteModel(t.id, effectivePath, fresh), which unconditionally REPLACES the model with disk content (note"}, {"n": 11, "sev": "HIGH", "klass": "cross-window-clobber", "file": "src/routes/+layout.svelte", "line": 6413, "summary": "The cascade's freeze set and cascading marks are point-in-time snapshots of tabsInLibrary taken before the walker runs; a tab opened during the multi-second cascade window is neither frozen, nor gated, nor in the flush-exclude set, so its edits are force-adopted away and its ungated save can revert the walker's rewrite.", "scenario": "The user renames note B in a large library. handleRenameComplete snapshots the freeze set at :6390 (cascadeFreeze.set(new Set(tabsInLibrary(lib.path)...))) and the gate set at :6413-6414 (const tabs = tabsInLibrary(lib.path); markCascading each), then flushes those tabs at :6417 and awaits updateLinksOnRename at :6424 — a full recursive filesystem walk of the library (libraries.rs:5555, the ~7s scan the overlay text acknowledges). CascadeFreezeOverlay is mounted per-pane only (+layout.svelte:7530/8068/8317); the sidebar file tree is NOT covered and openNoteTab (store.ts:2105) has no cascade gate. During the walk the user clicks note C in the tree. C opens with the content read at that instant, mints a fresh model, and mounts a fully editable NotePane — C is in none of the three snapshots, so no overlay, no markCascading, no exclusion. The user types. Two silent damages follow: (1) C's de"}, {"n": 12, "sev": "HIGH", "klass": "concurrency-race", "file": "src/routes/+layout.svelte", "line": 6505, "summary": "cascadeFreeze is overwritten wholesale on entry and cleared to empty on exit with no refcount, so with two overlapping renames the first to finish lifts the read-only freeze off every pane while the second cascade is still rewriting files.", "scenario": "renamesInFlight (+layout.svelte:6293) is keyed by oldPath, so it only blocks a duplicate rename of the SAME item — renaming a second, different note while the first cascade runs is permitted, and the file tree is not covered by the pane overlay so the user can do exactly that. Rename X starts and sets cascadeFreeze to snapshot S1 (:6390). Rename Y starts and OVERWRITES the store with its own snapshot S2 (:6390 uses .set, not a union). Y's walk is short and finishes first; its inner finally runs cascadeFreeze.set(new Set()) at :6505, emptying the store unconditionally. X's walk is still running. Every pane's CascadeFreezeOverlay disappears (CascadeFreezeOverlay.svelte:25) and focusFrozen goes false (+layout.svelte:1514), so FocusPane drops out of hard read-only — while X's walker is still rewriting the same files. The user, seeing 'Updating…' vanish, resumes typing in a backlink note; tho"}, {"n": 13, "sev": "HIGH", "klass": "silent-data-loss", "file": "src/lib/libraries/store.ts", "line": 1201, "summary": "saveTabContent drops a frontmatter edit entirely — it returns before pushing the props into the model, so a property change made during a cascade window is lost from disk AND from memory with a resolved promise.", "scenario": "PropertyEditor edits route through saveTabContent. Line 1201 `if (isCascading(filePath)) return;` fires BEFORE line 1217 `editNoteProps(tabId, updatedProps, filePath)`, so during a rename cascade the edit is not merely deferred — it never reaches the model, which is the single source of truth. The function returns a resolved Promise<void>, so the caller sees success. Reachable because the property surfaces are not all behind the pane freeze: the standalone PropertyEditor / right-sidebar properties panel is outside the CascadeFreezeOverlay mounts (+layout.svelte:7530/8068/8317 are pane-local), so during the ~7s walk the user can change a stage/maturity/typed-link property and watch the UI accept it. The write never happens, the model never holds it, and when the cascade's reloadTabsFromDisk re-seeds the model from disk (store.ts:815) any optimistic UI state is replaced by the unchanged di"}, {"n": 14, "sev": "HIGH", "klass": "index-divergence", "file": "src-tauri/src/search.rs", "line": 5217, "summary": "The indexer never strips heading/block anchors from wikilink targets, so `[[Note#Heading]]` is stored as target_name \"note#heading\" — an edge that can never match any note's name_lower, silently vanishing from incoming_count, backlinks, sky_links and every connectivity predicate, while the same link navigates correctly in the UI.", "scenario": "Note A contains `[[Stone Age#Tools]]` (the dominant link form in any imported Obsidian vault — importers.rs is a first-class path). index_note → extract_typed_links → parse_link_body takes the predicate-LAST/untyped branch at search.rs:5216-5217, which does `fold_match_key(parts[0].trim())` with no '#' or '^' split, producing target \"stone age#tools\". That string is INSERTed into note_links.target_name (search.rs:6135/6142) and mirrored into sky_links by note_links_sky_ai (search.rs:3711). Afterwards maintain_incoming_after_save runs incoming_aggregate_assignments (search.rs:1333), which matches `nl.target_name_lower = COALESCE(note_meta.name_lower, LOWER(note_meta.name))` — \"stone age#tools\" ≠ \"stone age\", so no match. Result on note \"Stone Age\": incoming_count stays 0, incoming_link_types/_json stay '{}', incoming_top_rank stays at the sentinel, and no sky_links edge resolves. connecti"}, {"n": 15, "sev": "HIGH", "klass": "false-success", "file": "src/lib/editor/yamlDoc.ts", "line": 213, "summary": "On a note whose frontmatter eemeli-yaml rejects (parseDocument().errors non-empty), composeFrontmatter returns the ORIGINAL yaml bytes verbatim and silently DISCARDS every property edit — yet the save reports success, marks the model clean, and clears the recovery net; nothing in the app reads FmDoc.hasErrors, so the documented '+ reported' half of the H1 contract (yamlDoc.ts:18-22) does not exist in code.", "scenario": "An Obsidian-imported / externally-edited note has frontmatter the tolerant hand-rolled reader accepts but the YAML library rejects — e.g. `title: Foo: bar` (nested mapping in a compact mapping), a tab-indented list, an unterminated quote, or a duplicate key (uniqueKeys). store.ts:1573 parseFrontmatter parses it happily (key 'title', value 'Foo: bar'), so PropertyEditor shows the full editable row set and the user edits a property (adds a tag, sets a stage, types an alias). debouncedSave → saveTabContent → editNoteProps (model.props now hold the edit) → noteSession.saveUnchained → compose → composeModel → composeFrontmatter. Line 213 fires FIRST, before any diff is applied, and returns `---{rawYaml}---{body}` — the pre-edit frontmatter. e.write() succeeds, so markSaved(id, r.version) clears dirty, noteDiskSynced re-baselines, clearNetIf wipes the write-ahead net, and onSuccess reindexes. "}, {"n": 16, "sev": "HIGH", "klass": "content-corruption", "file": "src/lib/components/PropertyEditor.svelte", "line": 361, "summary": "editableProps overrides each prop's parsed `type` with the library-wide persisted registry type (getRegisteredType) and with a forced 'list' for parent/contains, but composeFrontmatter's skip test (yamlDoc.ts:247) compares `type` — so an UNTOUCHED key whose registered type differs from its parsed type produces a spurious diff, and the ELSE branch splices its CST node and re-emits it from the lossy scalar projection, destroying nested maps and block scalars the user never edited.", "scenario": "propertyTypeRegistry is per-library and persisted to the universe's property-types.json (propertyTypeRegistry.ts:65-69), so a type the user picks once on note A applies to that key in EVERY note of the library. Setup: on note A the user clicks the type icon for key `notes` and picks Date. Note B, in the same library, has that key as a nested map authored elsewhere:\n---\ntitle: B\nnotes:\n  reviewed: 2026-01-02\n  by: Eisa\n---\nparseFrontmatter projects `notes` as {value:'', type:'text'} (the indented children are skipped by the !startsWith(' ') guard at store.ts:1600), and that same projection is captured as the model's write base (noteModel.ts:113-117). The user opens B and edits something completely unrelated — adds one tag. The sync $effect (PropertyEditor.svelte:361-377) stamps type='date' onto `notes` from the registry; debouncedSave passes editableProps to saveTabContent → setProps. In "}, {"n": 17, "sev": "HIGH", "klass": "silent-data-loss", "file": "src-tauri/src/review.rs", "line": 762, "summary": "save_pulse_data writes review-pulse.json with a plain non-atomic fs::write (truncate-then-write), while load_pulse_data silently falls back to ReviewPulseData::default() on any read/parse failure — so a partial write wipes the entire review history with no error, and the reconcile then propagates the empty state into review_schedule.", "scenario": "Every ✓Reviewed / snooze / dismiss is a whole-file read-modify-write: load_pulse_data → mutate → save_pulse_data (review.rs:689/713/736). fs::write at review.rs:762 truncates review-pulse.json to zero and then re-writes it, so there is a crash window on EVERY review action. User clicks ✓Reviewed; the app is killed / loses power / an AV scanner locks the file after the truncate but before the write completes. review-pulse.json is now partial. On next boot load_pulse_data (review.rs:747-757) hits `if let Ok(pulse) = serde_json::from_str(&data)` — the parse fails, the branch is skipped, and it returns ReviewPulseData::default() = EMPTY: every last_reviewed date, every interval on the 1·3·7·14·30 ladder, every snooze and every dismissal is gone. No error is raised anywhere. It then compounds twice: (a) the periodic reconcile calls recompute_all_in(conn, pulse, today) (review.rs:1233) with th"}, {"n": 18, "sev": "HIGH", "klass": "content-loss", "file": "src-tauri/src/sources/mod.rs", "line": 441, "summary": "rewrite_frontmatter_sources (and its twin rewrite_frontmatter_content_type at :1017) strips the note's ENTIRE existing `sources:` / `content_type:` block and re-emits only values that match the built-in taxonomy, so any user-authored value not in HORIZONTAL_NODES/VERTICAL_NODES is silently erased from the .md on disk.", "scenario": "A note imported from Obsidian (or hand-authored) uses the generic YAML key `sources:` for citations: `sources:\\n  - \"Kant, Critique of Pure Reason\"\\n  - https://plato.stanford.edu/x`. The user opens the Cataloger and clicks Accept on that note's suggestion. sources_set_manual(merge=true) → rewrite_note_sources_on_disk (:571) → union_preserve_order(&extract_sources(content), sources). extract_sources → push_source (:286) drops both citation strings because is_valid_source_id() is false, so `existing` is EMPTY; the union is just the suggested taxonomy id. rewrite_frontmatter_sources then deletes the whole `sources:` block (:481-489) and writes only the validated id (:508-514). Both citation lines are gone from the file. gate_rmw returns Ok, the journal records a normal Ok write, and index_note re-derives note_meta from the mutilated frontmatter — so no surface anywhere shows the loss. `sou"}, {"n": 19, "sev": "HIGH", "klass": "cross-window-clobber", "file": "src/lib/components/NoteEditor.svelte", "line": 447, "summary": "applyShape() is the only write path in NoteEditor with no `if (readOnly) return` guard, so the read-only second screen composes and durably rewrites the whole .md from its own (possibly stale) model.", "scenario": "ssReadOnly is hard-coded true (SecondScreenPage.svelte:177) and NoteEditor is mounted read-only at SecondScreenPage.svelte:1295/:1129. The new shape buttons (NotePane.svelte:1412-1427) are rendered unconditionally — NotePane knows `readOnly` (prop at :120, used at :495) but does not gate the ⋯ menu — and NoteEditor.handleMoreAction (:538-546) routes them into applyShape in the ALWAYS-handled block, bypassing the readOnly checks its four siblings have (:183, :247, :304, :377). Concrete failure: note N is open in the main window and the user has been typing for 20s (model dirty, 1500ms debounce pending or mid-session). The second screen shows N; its model was seeded by ensureModel (NoteEditor.svelte:166) from the last-saved disk bytes. User clicks ⋯ → Page on the second screen. applyShape takes the `SINGLE_OWNERSHIP && model` branch (:454) and calls saveNoteSession (:478), which composes f"}, {"n": 20, "sev": "HIGH", "klass": "concurrency-race", "file": "src/lib/components/BacklinksPanel.svelte", "line": 176, "summary": "`linkMention` does an unguarded read-modify-write to ANOTHER note's .md file (readNote → regex replace → write_note) with no open-model check, no cascade check, no markRecentWrite, and a bare `catch { /* ignore */ }` — the exact anti-pattern every sibling writer in the codebase explicitly guards against.", "scenario": "Note B is open in a second tab and the user typed into it 400 ms ago (model dirty, 1500 ms debounce still pending). From note A's Backlinks → Unlinked mentions, the user clicks the link-mention button for B. `readNote(mentionPath)` at :176 returns B's LAST-SAVED disk bytes (no typed edits); :181 writes those bytes + `[[A]]` back to B. B's debounced model save then fires, composes from the model's own byte-base (which never learned about the wikilink) and overwrites the file — the `[[A]]` link silently vanishes. The UI showed no error and the row disappears from Unlinked mentions on the next refresh, so the user believes the link was made. The reverse interleaving is equally silent: `markRecentWrite(mentionPath)` is never called, so the watcher classifies the app's own write as EXTERNAL and `adoptExternalChangeIntoTabs` remounts B from disk. Compare `+layout.svelte:6125-6141` (addTagToNot"}, {"n": 21, "sev": "HIGH", "klass": "freeze-hang", "file": "src-tauri/src/provenance.rs", "line": 101, "summary": "`compute_note_origins` is a SYNC `#[tauri::command]` that recursively walks an entire library and `fs::read_to_string`s every .md file, so the whole scan runs on the Tauri main dispatch thread and freezes both windows. Its three siblings in the very same frontend loop (`compute_note_strata` strata.rs:83, `compute_note_maturity` maturity.rs:38, `scan_note_stages` libraries.rs:3820) were all converted to `(async)` on 2026-07-03 with comments naming this exact freeze; this one was missed.", "scenario": "User with a 7,600-note universe opens Sky View, then picks Stratum or Maturity in the GraphMind legend empty-state. That fires `onRequestEnrichment` (+layout.svelte:7860) -> `enrichNodesBackground` -> the per-library loop at +layout.svelte:4000 `await invoke('compute_note_origins', ...)`. Strata/Maturity/Stages return promptly because they are `(async)` and run off-thread. `compute_note_origins` instead executes inline on the main thread: `scan_notes_recursive` (provenance.rs:148) does `fs::read_to_string` on all 7,600 files plus a regex pass over each, then an O(N) ancestor-chain walk. For the whole duration the WebView2 message pump is starved: no repaint, no keystroke, no spinner, the `await yieldToUI()` between libraries never gets a chance to run, and the second-screen window freezes too. Nothing is logged, and the `catch { /* skip */ }` around the invoke means even a failure is sil"}, {"n": 22, "sev": "HIGH", "klass": "freeze-hang", "file": "src-tauri/src/libraries.rs", "line": 5299, "summary": "`collect_library_notes` is a SYNC `#[tauri::command]` that recursively walks a whole library and, for every canonical note, calls `extract_frontmatter_title_quick` - which despite its doc comment claiming 'reads first 1KB only' does a full `fs::read_to_string` (libraries.rs:5340). It therefore reads every canonical note in full on the main dispatch thread.", "scenario": "The second screen's `loadAllData()` (SecondScreenPage.svelte:421) loops over every library awaiting `invoke('collect_library_notes', ...)` at line 433. Because the command is sync, each call blocks the process-wide Tauri main thread for a full recursive walk plus a full read of every canonical .md. On a multi-library, 7,600-note universe this freezes BOTH the second screen and the main editor window. Worse, it is not a one-shot boot cost: `loadAllData()` is re-armed on library change behind a 3s debounce (SecondScreenPage.svelte:685 and 695), so a Syncthing/git pull that touches several libraries re-triggers the whole-universe re-read and re-freezes the app while the user is mid-sentence in NotePane. The `.catch(() => [])` on line 433 swallows any error, so nothing is ever surfaced."}, {"n": 23, "sev": "MED", "klass": "index-divergence", "file": "src-tauri/src/libraries.rs", "line": 6191, "summary": "Deleting a FOLDER with the local-.trash setting purges only the folder path from the index (which matches no row); every descendant note keeps its note_meta/FTS/sky/review rows, and the gated rename again suppresses the watcher's healing signal.", "scenario": "Settings → Deleted files = \"local .trash\" (`trashDestination: 'local'` → `deleteWithSetting` sends mode `'trash'`, store.ts:3611-3626). User deletes folder `Lib/Archive` (30 notes). `delete_path` routes to `move_into_trash_folder` (libraries.rs:6180-6183), which calls `gate_rename(source, dest, \"delete_trash\")` at :6232 — a single directory move into `.trash`. Then the only index cleanup is `reindex_delete_note(&search_state, &path)` at :6191 with the FOLDER path, which matches zero note_meta rows (a folder is never a note). All 30 descendant rows survive at `Lib/Archive/*.md`. As in the folder-rename case, `gate_rename` marks BOTH paths in `watcher_suppress`, and a directory move emits ONE directory event on Windows, so `reindex_changed_paths`/`delete_rows_under_prefix` — the exact mechanism written for this shape — never fires. The two sibling modes DO heal (`permanent` → `remove_dir_a"}, {"n": 24, "sev": "MED", "klass": "index-divergence", "file": "src/lib/libraries/store.ts", "line": 1143, "summary": "`flushAllTabsInLibrary` flushes every dirty tab to disk with a bare `standardSaveEnv({origin:'flush_all'})` that has no `onSaved` — a durable disk write with no paired reindex, the exact divergence `navFlushEnv` was written to fix for the nav path only.", "scenario": "The rename cascade calls `flushAllTabsInLibrary(libraryPath)` before rewriting `[[Old]]`→`[[New]]`. Every dirty open tab in the library is written to disk here. Note C is dirty but contains no link to the renamed note, so the cascade walker never rewrites it and `libraries.rs:1684`'s `reindex_single_note` never runs for it. C's new body is now on disk; its `note_meta`/`notes_fts` row still holds the pre-flush body. The gated write is watcher-suppressed (write_gate.rs:249) so no watcher reindex fires either, and `reconcile.rs:157` never revisits a present-but-stale row. Result: silent, permanent index↔disk divergence for every dirty non-linking tab in the library on every rename. The comment on `navFlushEnv` (store.ts ~line 505) names this precise hazard — 'a bare flush (like flushAllTabsInLibrary / rename_flush) omits the onSaved side effects, which would leave the just-flushed note's SE"}, {"n": 25, "sev": "MED", "klass": "index-divergence", "file": "src/lib/components/ExpressionForge.svelte", "line": 144, "summary": "`createNote` + `writeNote` create a full composition note whose content is never indexed — the same shape at SenseMakingCanvas.svelte:271 ('canvas_export').", "scenario": "User exports a composition from the Expression Forge. `createNote` calls Rust `create_note` (`gate_create_exclusive`, watcher-suppressed) and emits `note-created`, whose only listener (+layout.svelte:3342) refreshes the file tree — no reindex. `writeNote(newPath, content, 'expression_forge')` then writes the real body, also watcher-suppressed and also unpaired with `constellation_search_reindex`. For the rest of the session the composition exists on disk and in the sidebar but is absent from `note_meta`/`notes_fts`/`note_links`: it cannot be found in Search Hub or the Quick Switcher, contributes no backlinks to the notes it cites, and is missing from Sky/Index/tag counts — with no error anywhere. It self-heals only at the next boot, when `reconcile_filesystem` classifies it as an orphan (a file absent from `note_meta`) and re-adopts it (reconcile.rs:248)."}, {"n": 26, "sev": "MED", "klass": "cross-note-bleed", "file": "src/lib/components/NoteEditor.svelte", "line": 553, "summary": "shapeRevert resolves the `undo_shape` IPC asynchronously and then calls applyShape, which re-reads `tab.path`/`getModel(tab.id)` at callback time — so a navigation during the IPC writes note A's reverted shape into note B's frontmatter.", "scenario": "`tab` is a Svelte 5 prop whose `.path` reflects the tab's CURRENT note; openNoteTab's in-place reuse re-seeds the same tab id onto a new path (store.ts:2252-2276). User on note A clicks ⋯ → Revert shape; `invoke('undo_shape', { filePath: A })` is in flight; the user clicks note B in the file tree (same tab id, model reseeded to B). The promise resolves and calls applyShape(target, false), which reads getModel(tab.id) — now B's model — and tab.path — now B. Both editProps(tab.id, newProps, tab.path) at :467 and compose(tab.id, tab.path) at :468 see a matching path, so the identity guards that exist precisely to refuse this all pass, and saveNoteSession durably writes `shape: <A's previous value>` into B. B silently acquires A's shape, A's revert never lands, and the Rust-side undo step was already consumed so it cannot be replayed. Every other shape op is synchronous; only this one crosse"}, {"n": 27, "sev": "MED", "klass": "silent-data-loss", "file": "src/lib/components/NoteEditor.svelte", "line": 248, "summary": "handleSave's `if (saving) return` drops a debounced save outright with no re-arm, leaving the write-ahead net holding the PREVIOUS burst's content while the newest edits live only in volatile memory.", "scenario": "The write-ahead net is only ever refreshed by noteSession.save's setNet (noteSession.ts:186), which runs inside the save this guard skips. Type burst 1 → the 1500ms debounce fires handleSave → saving=true and a slow write (network/synced drive, AV scan) is in flight. Type burst 2 → 1500ms later NotePane's debounce fires again → `if (saving) return` drops it before editBody/markRecentWrite/saveNoteSession, and NotePane's debounce does not re-arm without further typing. The user stops. The model is dirty with burst 2, but the net still holds burst-1 content and disk holds burst-1 content. A crash / power loss / webview kill before any departure (nav, tab close, app close) loses burst 2 entirely — and the documented recovery route silently restores burst 1 as if it were the latest work, with no banner or marker. The guard is now also redundant: noteSession.save serializes concurrent saves p"}, {"n": 28, "sev": "MED", "klass": "index-divergence", "file": "src-tauri/src/libraries.rs", "line": 1092, "summary": "The rename-tail DB cascade migrates note_meta / note_links / note_aliases / note_embeddings / review_schedule to the new path but NOT shape_history, so renaming a note silently and permanently disables its shape revert.", "scenario": "`shape_history` is keyed by absolute `path` (shape.rs:68-82) and every read/write query filters `WHERE path = ?1` (record_change:241/244, undo_shape:366, get_shape_history:415). The rename cascade at libraries.rs:1049-1092 repaths five tables and omits this one. Concrete: set a note's shape to Page, then rename the note (sidebar rename or the delegated title rename at NoteEditor.svelte:399). All history rows still carry the OLD path. ⋯ → Revert now invokes `undo_shape` with the NEW path, the query returns no row, and Rust returns Err(\"Nothing to undo for this note.\") — which the frontend deliberately swallows as a normal end state (NoteEditor.svelte:562-566, `if (!msg.includes('Nothing to undo'))`). The revert gesture therefore does nothing, forever, with no message anywhere. Reversibility is the stated safety premise of the entire shape feature (shape.rs:1-15), and it is silently void a"}, {"n": 29, "sev": "MED", "klass": "silent-data-loss", "file": "src-tauri/src/style_presets.rs", "line": 51, "summary": "save_style_presets persists the entire preset array with a plain non-atomic fs::write, and load_style_presets explicitly swallows a corrupt-JSON parse error into `[]` — a crash mid-write silently deletes every style preset the user has ever saved.", "scenario": "save_style_presets (style_presets.rs:48-52) serializes the FULL presets array and calls fs::write(&path, data) on {app_data_dir}/style-presets.json — truncate-then-write, no temp file, no fsync, no rename. The user saves a new preset in the Style Setter (stylePresets.ts:134 → this command); the machine loses power or the process is killed after the truncate but before the data lands. On next launch load_style_presets (style_presets.rs:36-43) does `serde_json::from_str(&data).unwrap_or_else(|e| { eprintln!(...); json!([]) })` — the parse failure is deliberately swallowed to an empty array and the eprintln is invisible in a release build (devtools are DEV-ONLY per project rule). The Style Setter opens showing zero presets, as though the user had never created any. The very next save then writes that empty-plus-one array back over the file, cementing the loss. The file is app-GLOBAL, so thi"}, {"n": 30, "sev": "MED", "klass": "silent-data-loss", "file": "src/lib/libraries/store.ts", "line": 5712, "summary": "saveSettings debounces the settings.json write by 300ms (and persistPropertyTypes by 500ms), but the app-close handshake flushes only the session snapshot and dirty note models — a pending timer is destroyed with the WebView, silently discarding the user's last settings / property-type change.", "scenario": "saveSettings (store.ts:5710-5714) sets a 300ms setTimeout before invoking save_universe_settings; persistPropertyTypes (propertyTypeRegistry.ts:42-49) does the same with 500ms. The graceful-close handshake in +layout.svelte:2905-2913 handles the 'session:final-flush' event by awaiting persistSessionNow() then flushAllForAppClose() then acking — it never touches saveSettingsTimer or the property-type timer, and no flushSettings/saveSettingsNow function exists anywhere in src (grep confirms saveSettingsTimer is referenced only at store.ts:5708/5711/5712). Concrete case: the user flips a setting in the Settings modal — or drags a Style-Setter slider, which routes through setStyleOverride → saveSettings (store.ts:5726) — and immediately closes the window. A settings change does not alter the tab arrangement, so persistSessionNow returns early on an unchanged signature, and with no dirty note"}, {"n": 31, "sev": "MED", "klass": "silent-data-loss", "file": "src-tauri/src/libraries.rs", "line": 197, "summary": "save_libraries does temp-write + rename but omits the fsync that universe.rs:121-131 documents as required — power loss can land the rename while the data blocks are unflushed, leaving a zero-length libraries.json that resolve_libraries_recursive silently reads as zero libraries.", "scenario": "save_libraries (libraries.rs:193-201) writes the serialized library list to libraries.json.tmp with fs::write then fs::rename over the target. It never calls sync_all on the temp file. universe.rs:121-131 spells out why that is insufficient: 'fsync BEFORE the rename — otherwise power loss can land the rename while the data blocks are still unflushed, leaving a zero-length/garbage file under the FINAL name (the exact failure the temp+rename dance exists to prevent).' Scenario: the user adds or renames a library (any path that calls save_libraries); the rename metadata commits to the journal but the data blocks for the temp file have not been flushed when power is lost. On reboot libraries.json exists under its final name and is zero-length or garbage. resolve_libraries_recursive (universe.rs:423-428) does `if let Ok(libs) = serde_json::from_str::<Vec<LibraryInfo>>(&data) { all_libraries.e"}, {"n": 32, "sev": "MED", "klass": "content-corruption", "file": "src-tauri/src/sources/mod.rs", "line": 481, "summary": "The frontmatter rewriters match `line.trim_start().starts_with(\"sources:\")` / `\"content_type:\"` with no YAML nesting or block-scalar awareness, so ANY frontmatter line at ANY indentation whose trimmed text begins with those tokens is deleted from the note.", "scenario": "A note's frontmatter contains a nested mapping — `meta:\\n  sources: reading-list\\ntitle: Foo` — or a literal block scalar whose text happens to start with the token — `abstract: |\\n  sources: books and letters\\n  - Kant`. On any Accept / PropertyEditor set / bulk approve, the loop at :479-501 trims the indented line, sees the `sources:` prefix, and `continue`s (:488) — the line is dropped from new_fm_lines and never re-emitted. If the nested value was empty (`meta:\\n  sources:`), skip_block is additionally set true (:486) and every following `- ` line is eaten too (:491), so a sibling list under an unrelated key is destroyed as well. The rewritten file is written through gate_rmw as a normal Ok write; index_note then re-derives note_meta.properties_json from the damaged frontmatter, so the SQLite mirror agrees with the corruption and nothing flags it. Identical code path at :1052-1071 fo"}, {"n": 33, "sev": "MED", "klass": "freeze-hang", "file": "src-tauri/src/cece/orchestrator.rs", "line": 142, "summary": "run_one_safe's documented per-cataloger timeout is not actually enforced: std::thread::scope blocks until the spawned worker terminates, so recv_timeout only manufactures an abstain trail while the call still parks for the full duration of the hung cataloger.", "scenario": "The Graph and Semantic catalogers acquire the single global SearchState.db mutex inside the scoped worker (cece/wiring.rs:151 knn_classified_neighbors, :218 load_typed_neighbors — the former also loads EVERY note_embeddings row and cosine-scores it while holding that mutex). During a large-universe reindex or a Sky/link backfill that holds the writer mutex for tens of seconds, the worker thread blocks on `state.db.lock()`. `rx.recv_timeout(2s)` fires at :172, prints the reassuring \"exceeded timeout — treating as silent\" line, and returns an abstain trail — but `std::thread::scope` (:153) then JOINS the still-blocked worker before the closure's value can be returned, so classifier_suggest_for_note parks for the whole lock hold. With the `on_save` classifier setting or a running background scan (classifier/scan_job.rs:189) firing this per note, several `#[tauri::command(async)]` invocation"}, {"n": 34, "sev": "MED", "klass": "false-success", "file": "src-tauri/src/search.rs", "line": 9289, "summary": "`reindex_single_note` returns Ok(()) when `state.db` is None, and its IPC entry point `constellation_search_reindex` (search.rs:9177) never calls `ensure_search_db_ready` — so every editor save that lands while the DB is uninitialized silently skips its entire index update and reports success.", "scenario": "User switches universe (universe.rs → invalidate_search_state, search.rs:8425 sets `*db = None`; cold re-init through init_db takes 20–40 s per the MIG-078 §B1 comment at search.rs:8663). Inside that window a 1500 ms debounced editor save fires: NoteEditor.svelte:268 writes the .md to disk successfully, then invokes `constellation_search_reindex`. search.rs:9182 takes the lock, `db.as_ref()` is None, the `if let` body is skipped, and line 9359 returns `Ok(())`. The promise resolves; no error, no log, no status-bar signal. note_meta/notes_fts/note_links/sky_nodes keep the pre-edit body, title, tags and links. Nothing heals it: the watcher drops the app's own writes (watcher_suppress), `reconcile::maybe_schedule` only removes rows whose file is *gone*, and +layout.svelte:2593 forbids any boot-time reconcile walk. The note is silently unfindable by its new text (and findable by text it no l"}, {"n": 35, "sev": "LOW", "klass": "index-divergence", "file": "src/lib/libraries/store.ts", "line": 2468, "summary": "`drainCidEnsure` flushes the dirty model with a no-`onSaved` env and then calls `ensure_cid_cn_cmd`, which does its own gated disk write (canonical.rs:1256) — two disk writes, zero reindexes.", "scenario": "User types in a note that has no `cid_cn` yet, then activates another tab. `drainCidEnsure` runs: the dirty model is flushed to disk via `standardSaveEnv({origin:'cid_ensure_flush'})` with no `onSaved`, so no reindex. Then `ensure_cid_cn_cmd` reads the file, injects `cid_cn:` into the frontmatter and writes it back through `gate_write` (canonical.rs:1256) — also with no reindex, and watcher-suppressed. The note's disk bytes are now two writes ahead of `note_meta`, and specifically `note_meta.cid_cn` stays at its `''` default. Beyond the stale full-text/links, that empty `cid_cn` disables the note's identity-based recovery: `reconcile.rs`'s `orphan_by_cid` relocate (which migrates a `note_meta` row + its `review_schedule`/aux rows when a rename loses its tail) can only match on `cid_cn`, so this note's review history and link aux rows are silently unrecoverable on a lost-tail rename. Noth"}, {"n": 36, "sev": "LOW", "klass": "toctou", "file": "src-tauri/src/cece/reliability.rs", "line": 185, "summary": "sweep_tmp_orphans deletes every `.cataloger_reliability.*.tmp` in the Library's .constellation dir, and is reached from load_or_default OUTSIDE the RELIABILITY_LOCK that serializes save(), so it can unlink the NamedTempFile a concurrent save is mid-write.", "scenario": "The user accepts a classification card: cece_record_correction_for_card → update_reliability_from_correction takes RELIABILITY_LOCK (:319) and calls save(), which creates `.cataloger_reliability.<rand>.tmp` in the Library's `.constellation/` (:155-165) and writes+syncs it. Concurrently the background scan is running and calls classifier_suggest_for_note, which at classifier/mod.rs:164 calls `load_or_default(lr)` once PER NOTE — and load_or_default's first statement is `sweep_tmp_orphans` (:96), which holds NO lock and removes any file matching the prefix/suffix (:198-199), i.e. the live temp file. `tmp.persist(&path)` then fails and is swallowed with a bare `eprintln!` (:175-177). The user's correction is recorded in the append-only NDJSON but the accumulated per-cataloger accuracy histogram silently never updates — and corrections are never replayed, so the calibration data is unrecover"}, {"n": 37, "sev": "LOW", "klass": "toctou", "file": "src/lib/components/NoteEditor.svelte", "line": 553, "summary": "MIG-101 shape Revert consumes the undo step in Rust FIRST (`undo_shape` marks `undone=1` under the DB lock and returns), then hands it to `applyShape`, which has three unconditional early-return guards — a consumed-but-never-applied step is silently destroyed and the undo stack desynchronises from disk.", "scenario": "A wikilink rename cascade is running over the note's library (`isCascading(tab.path)` true — a state the user cannot see beyond a brief freeze overlay). The user clicks ⋯ → Revert shape. `invoke('undo_shape')` at :553 runs to completion: it SELECTs the newest `undone = 0` row and UPDATEs it to `undone = 1` (shape.rs:392-404), permanently consuming that step. The promise resolves and `applyShape(target, false)` at :554 returns immediately at :448 on the cascade guard. Nothing is written to disk, nothing is logged, no `.catch` fires (the invoke succeeded), and the menu closes normally. The note's shape is unchanged but its top history step is gone. The user clicks Revert again: `undo_shape` now hands back the step BEFORE it, and the note jumps two states backwards to a shape the user never selected — and can never reach the intermediate one again. The same consume-then-abandon window exist"}]