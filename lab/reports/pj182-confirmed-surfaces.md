### APP-KILLER  —  libraries.rs:1767  —  update_frontmatter_title
CONFIRMED by running the real code; every refutation avenue closed. (1) Extracted libraries.rs:1731-1847 verbatim with sed (no transcription) plus extract_frontmatter_title (1714-1728), compiled with rustc -O, ran on the ground-truth probe with old_title computed exactly as rename_item computes it (extract_frontmatter_title -> "Imported Probe"). Output contains `aliases:` then `  - "Imported Probe"` then the zero-indent `- Old Name` / `- Older Name` items orphaned beneath it. Fed that to the repo's own yaml@2.9.0: doc.errors.length === 3 ("A block sequence may not be used as an implicit map key" / "Implicit keys need to be on a single line" / "Implicit map keys need to be followed by map values"); YAML.parse THROWS. The 2-space-indent control yields 0 errors and {"tags":["alpha","beta"],"aliases":["Old Name","Older Name","Imported Probe"]}. (2) "The code never sees raw disk" refuted: write_gate.rs:705 gate_rmw_rename does fs::read_to_string(old) and passes it straight to the closure at libraries.rs:1308-1335 — no normalizer anywhere between disk and the function. The frontend's only pre-rename write (store.ts:3856 rename_flush) is CONDITIONAL on the tab being dirty and goes through the CST-preserving composeFrontmatter, which leaves undiffed keys byte-perfect; a sidebar rename of a CLOSED note has no flush at all. (3) "Another guard catches it" refuted: write_gate.rs:708 atomic_write(old, &updated) performs no YAML validation, and grepping all of src/ shows `hasErrors` (yamlDoc.ts:141) has ZERO consumers outside yamlDoc.ts itself — no banner, no log, nothing surfaced to the user. (4) Line number verified: libraries.rs:1767 is verbatim `let is_list_item = indented && t.starts_with("- ");`, and there is no regex to "actually match" — `line.starts_with(' ') || line.starts_with('\t')` is plainly false for `- Old Name`. (5) `git diff src-tauri/src/libraries.rs` is 17 inserted lines at @@ -6836 (a different agent's temp test on set_frontmatter_parent) — lines 1731-1847 are identical to HEAD. (6) Blast radius PROVEN with a vitest against the real parseFrontmatterDoc/composeContent (3/3 green, file deleted afterwards): the PRE-rename file parses cleanly (tags ['alpha','beta'], aliases ['Old Name','Older Name']); the POST-rename bytes give hasErrors=true and props=[]; a subsequent stage promote through composeContent returns the input BYTE-FOR-BYTE — `stage: spark-seed` survives and `growing` never appears — i.e. every later property edit is silently discarded forever, with no error path. SCOPING CORRECTION (ran 5 variants through the real function): the defect fires ONLY when the ROOT `aliases:` key is itself a zero-indent block sequence. A zero-indent `tags:` list alone is SAFE (it is pushed through verbatim and the appended `aliases:` block is well-formed) — variants A and B stayed valid YAML. Three alias sub-shapes all corrupt: aliases mid-block, aliases as the LAST key, and single-item aliases. Variant C also exposes a second symptom: when the old title is already present in a zero-indent alias list the dedup branch never runs, so it is appended again. APP-KILLER: a plain sidebar rename of an imported/PyYAML-authored note silently converts the note's frontmatter into unparseable YAML, orphaning the authored aliases and permanently swallowing every future tag/stage/property edit on that note with no surfaced error.

### APP-KILLER  —  PropertyEditor.svelte:795  —  addNestedRow(propIdx) — the ikhtilāf widget
Could not refute — confirmed by running the real code (vitest probe, since deleted). READ: store.ts:1896-1901 gates the IKHTILAF branch on /^\s+-\s/ against the NEXT line, so a zero-indent `- school: X` never enters it; the PJ-136 nested-map branch (/^\s/) and the block-list branch (/^\s+-\s/) fail identically; the key then falls to the scalar path where detectPropertyType (store.ts:176) returns 'nested-object-list' for any IKHTILAF key regardless of value. PropertyEditor.svelte:1301 renders the '+ Add school' button OUTSIDE the `{#if prop.nestedObjects && length>0}` rows block, so it is reachable with zero rows; line 795 is exactly the quoted spread; PropertyEditor.svelte:184-187 seeds from getModel().props — the same projection, no corrected upstream copy. yamlDoc.ts:183 `immutableBlockKeys` explicitly `continue`s on STRUCTURED_LIST_KEYS, so the 2026-07-24 seq-of-maps guard does not apply. RAN on a real zero-indent ikhtilāf note with two authored rows (Hanafī/permissible, Mālikī/discouraged): parseFrontmatter yields {key:'ikhtilāf', value:'', type:'nested-object-list'} with nestedObjects ABSENT, plus two garbage top-level props keyed '- school'. Applying a verbatim replica of addNestedRow then composing gives — legacy whole-array path: `ikhtilāf:\n  - school: ""\n    position: ""` (both authored rows DELETED); live MIG-107 path (PROPS_SINGLE_OWNERSHIP=true): `ikhtilāf: "school:  / position: "` — the entire block-seq spliced out of the CST and replaced by a SCALAR summary of the one blank row (worse than claimed, because propsCommit's `set` op carries only value/type/listItems and never nestedObjects, so serializeLine's MIG-101 branch cannot fire). A 2-space-indent control note preserves its rows on both paths, so the defect is specific to zero indent. Both corrupted outputs re-parse as clean YAML, with no error surfaced. Reachability: requires one of the three STRUCTURED_LIST_KEYS ('ikhtilāf' / 'ikhtilaf' / 'الاختلاف') written at zero indent — Constellation itself always emits 2-space indent, so the file must come from a hand edit or an external emitter (PyYAML), exactly the ground-truth premise. Trigger is one click on 'Add school' in a widget that is showing the user zero rows.

### APP-KILLER  —  PropertyEditor.svelte:756  —  addTag(idx, tag)
CONFIRMED by running the panel's real path (vitest, scratch files deleted): seed $effect (:428-446) -> addTag (:744-761) -> commitAndSave (:921-949) -> saveTabContent(propsAlreadyInModel=true), capturing the write_note payload. CONTROL (2-space-indented probe) wrote "tags:\n  - alpha\n  - beta\n  - gamma". PROBE (zero-indent probe) wrote "tags:\n  - gamma" — alpha+beta silently DELETED, and the result re-parses cleanly.

Every refutation avenue closed: (1) PROPS_SINGLE_OWNERSHIP is literally `true` in src/lib/editor/ownershipFlag.ts, so the intent path is live — and the legacy branch (saveTabContent -> buildFullContent) loses the list too. (2) No upstream normalizer: noteModel.ts:157 openModel calls store.ts parseFrontmatter directly on the disk bytes; the observed seeded row is {key:'tags', value:'', type:'list', listItems:[]}. (3) The propsCommit guards do not help: seededKeys/touchedSince only protect keys the panel never saw or never touched — the user DID touch `tags`, so the []-derived value is an authorized SET. (4) composeFrontmatter's refusal guard only covers type 'nested-map'; this row is 'list'. (5) Line 756 is verbatim `const items = [...(p.listItems ?? []), item];`. (6) The claim's `??` remark is a side observation (|| would behave the same) but the conclusion holds because the row genuinely carries [].

TWO AGGRAVATIONS BEYOND THE CLAIM. (a) The loss is invisible beforehand: the template at :1199 renders chips only when listItems.length > 0, so the row draws with zero chips and the "Empty" placeholder (:1207) — the file's real tags are hidden, making typing into the apparently-empty box the natural action. (b) Structural lists are destroyed WITHOUT the user touching them: observed parseFrontmatter yields zero-indent `parent`/`contains` as {value:'', type:'text'} (no listItems), and the seed at :436-442 coerces them text->list with listItems=[]; touchedSince (propsCommit.ts:150) sees the type change and marks them touched with no user edit. Running a commit that edited ONLY the neighbouring `stage` scalar wrote `parent: []` and `contains: []`, deleting [[Big Note]], [[Child A]], [[Child B]]. So on a zero-indent note ANY property edit in the panel destroys the structural link lists.

Severity APP-KILLER: silent, unsurfaced loss of the user's authored knowledge (tags, aliases, and PJ-065 structural parent/contains links) on imported/PyYAML-authored vaults, with clean re-parse so nothing downstream notices.

### APP-KILLER  —  yamlDoc.ts:183  —  immutableBlockKeys() — STRUCTURED_LIST_KEYS exemption
REFUTATION FAILED — reproduced by running the real code (3 vitest harnesses under tests/pj-182/, since deleted). (1) COUNTERFACTUAL isolates line 183 as sole cause: with byte-identical zero-indent seq-of-maps frontmatter, key `authors` (not in STRUCTURED_LIST_KEYS) is fully protected — both a SET and a REMOVE compose to the block verbatim; keys `ikhtilāf`/`ikhtilaf`/`الاختلاف` (all exempt at 183) lose it — SET → `ikhtilāf: CLOBBERED`, REMOVE → block spliced out. (2) The exemption's stated premise is false: `parseDocument` on the zero-indent block returns 0 errors, 0 warnings and the correct 2-row seq-of-maps (so `isBlock` at :188 WOULD be true — only the 183 `continue` skips it), while store.ts parseFrontmatter's nested-object-list branch (store.ts:1896-1901, regex /^\s+-\s/ requires leading whitespace) does NOT fire, projecting `{type:'nested-object-list', value:'', nestedObjects: undefined}`. (3) LIVE chain reproduces the claimed blast radius verbatim: openModel → touchedSince/plan/apply (propsCommit.ts) → real intents → compose. The panel (PropertyEditor.svelte:1263-1304) renders only "+ Add school" because nestedObjects is undefined; clicking it emits a `set` op, and PropOp.set (propsCommit.ts:33) carries only value/type/listItems — never nestedObjects — while setPropValue (noteModel.ts:278) spreads `...cur`, so nestedObjects stays undefined; serializeLine:228 `prop.nestedObjects?.length` is falsy → scalar path → composed disk content is `ikhtilāf: "school: Shāfiʿī / position: obligatory"`. Both authored schools and positions gone, no error, re-parses cleanly. REMOVE is reachable too (the × is rendered since type !== 'nested-map', PropertyEditor.svelte:1322) and splices the whole block. Refutations that FAILED: line number correct (183); yaml lib does not error (no H1 passthrough); no upstream normalizer; no other guard — immutableBlockKeys IS the guard and 183 is the bypass; the /^\s+-\s/ regex does not match. Scope limit (confirmed, does not save it): editing an UNRELATED key composes the block byte-perfect (unification holds), and the properly-indented control survives — the trigger is the user touching the widget the panel falsely shows as empty. Severity APP-KILLER: silent, unrecoverable loss of the user's authored knowledge with a cleanly-reparsing result. Side finding at the same input: store.ts:1882 also emits two phantom top-level props keyed `- school`.

### APP-KILLER  —  noteModel.ts:157  —  openModel() → parseFrontmatter
CONFIRMED by running the real modules, not by reasoning. (1) Line check: noteModel.ts:157 is `const { properties, body } = parseFrontmatter(content);`, feeding `props: cloneProps(properties)` (:162) AND `base: baseOf(content, properties)` (:166) — claim verbatim accurate. (2) Refutation "an upstream normalizer fixes the YAML" FAILS: `read_note` (src-tauri/src/libraries.rs:526) is a bare fs::read_to_string; `ensure_cid_cn` (src-tauri/src/canonical.rs:1256) is a pure string splice that never re-serializes YAML; grep for normalizeFrontmatter|normalize_frontmatter|rewriteFrontmatter|reindent across src/ and src-tauri/src/ = zero hits; raw disk bytes reach the model via readNote -> tab.content -> ensureModel(tab.id, tab.path, tab.content) (NoteEditor.svelte:186). (3) Refutation "a downstream guard catches it" FAILS — ran a temporary vitest (tests/pj-182/, deleted after): openModel on the zero-indent probe yields tags {value:'',type:'list',listItems:[]} and aliases likewise, and base.props is IDENTICAL, so both the live authority and the write base are blind. Untouched compose is byte-identical to disk; body-only edit, neighbour-key (stage) edit and addProp are all safe — but setPropValue('tags',…) then compose emits `tags:\n  - gamma` with alpha+beta DELETED, valid YAML, no error. (4) Refutation "the live UI doesn't edit tags that way" FAILS — re-ran through the app's own pipeline: model.props -> seededRows/editableProps (PropertyEditor.svelte:185 sourceProps) -> the verbatim addTag shape (PropertyEditor.svelte:745-758) -> touchedSince+plan+apply (src/lib/editor/propsCommit.ts) -> noteModel intents -> compose. Observed OPS = [{"op":"set","key":"tags","value":"gamma","type":"list","listItems":["gamma"]}] and the disk-bound content reported alpha survived=false, beta survived=false. The panel renders ZERO chips for tags/aliases while the file holds four items — the PJ-136 "Empty is an invitation" shape. SINGLE_OWNERSHIP and PROPS_SINGLE_OWNERSHIP are both true (src/lib/editor/ownershipFlag.ts), so this projection IS the app-wide truth and the write base. Root cause is store.ts parseFrontmatter's three /^\s+-\s/ and /^\s/ regexes (store.ts:1900/1981/2009) requiring a leading space, but noteModel:157 is the surface that promotes it to system-of-record and to the byte-perfect write base. APP-KILLER: ordinary tag-add silently deletes the user's authored tags/aliases from the .md, and the result re-parses cleanly so nothing surfaces the loss.

### APP-KILLER  —  store.ts:2009  —  parseFrontmatter — branch 3 (multi-line list) entry test
CONFIRMED by running the real code, not by reading. Read store.ts:1877-2102 (full parseFrontmatter), yamlDoc.ts in full, noteModel.ts (openModel/setPropValue/compose, USE_YAML_DOC=true), propsCommit.ts (plan/apply/touchedSince), PropertyEditor.svelte:1225-1330 + updateValue/removeProperty/commitAndSave. RAN two vitest harnesses (deleted after). (1) parseFrontmatter on `---\ncid_cn: ABCD\ntitle: Imported Probe\nsources:\n- Ibn Khaldun, Muqaddimah\n- Al-Ghazali, Ihya\nstage: spark-seed\n---` → {key:'sources', value:'', type:'text', listItems:undefined}; no phantom keys. Indented control → type:'list' with both items. The /^\s+-\s/ at 2009 requires a leading space, so the key falls to the scalar path at 2087 and detectPropertyType returns 'text'. (2) THE REAL WRITE PATH, not just composeUpdatedContent: openModel → propsCommit.plan (ops = [{op:'set',key:'sources',value:'z',type:'text'}]) → setPropValue → noteModel.compose → composeFrontmatter. Output: `---\ncid_cn: ABCD\ntitle: Imported Probe\nstage: spark-seed\nsources: z\n---` — BOTH authored values deleted, no error, and the result re-parses cleanly (keys: cid_cn,title,stage,sources). REFUTATION ATTEMPTS, ALL FAILED: (a) line number — 2009 is exactly the entry test, verified by Read; (b) another guard — yamlDoc.immutableBlockKeys:188 deliberately leaves a seq of pure scalars editable, so it never fires, observed by the write succeeding; (c) upstream normalizer — grep found no normalizeFrontmatter/rewriteFrontmatter anywhere in src/ or src-tauri/src/, and libraries are read in place; (d) the panel protects it — PropertyEditor.svelte:1305 is the {:else} fallback, an ordinary text input with the 'Empty' placeholder, wired to updateValue → debouncedSave; (e) propsCommit filters it — no, touchedSince marks the row touched on the first keystroke and plan emits the set. CONTROL: editing only `title` left the block byte-intact (CST preserved the zero indent verbatim), so the loss is specific to touching this key. ADDITIONAL PATH the sweep agent did not name: because type !== 'nested-map', the row keeps its ✕ delete button (PropertyEditor.svelte:1322); I ran it — ops = [{op:'remove',key:'sources'}] and the whole block is spliced off disk from a row the panel labels 'Empty'. ONE CORRECTION to the agent's evidence: the composed shape differs — the key is spliced from its position and re-appended at the END of the block, not written in place; the data loss is identical. Severity APP-KILLER: silent, unreported deletion of the user's authored frontmatter values, and the rewritten YAML re-parses cleanly so no surface ever notices. Applies to every user-defined list key outside the fixed LIST_KEYS set at store.ts:123-132 (sources, authors, refs, evidence, …).

### APP-KILLER  —  store.ts:1900  —  parseFrontmatter — branch 1 (nested-object-list / ikhtilāf) entry test
REFUTATION FAILED — confirmed by running the real live path in vitest (harness written, run, then deleted). RAN: openModel() on `---\ntitle: Zakat on Crypto\nikhtilāf:\n- school: Hanafī\n  position: permissible\n- school: Mālikī\n  position: discouraged\ntags:\n- fiqh\n---\n\nbody text\n` → seeded panel rows from model.props (PropertyEditor:184 sourceProps = getModel().props; noteModel:157 builds them from store.parseFrontmatter) → PropertyEditor's verbatim addNestedRow (:792) → the REAL commit path touchedSince/plan/apply with the real noteModel intents (PropertyEditor.commitAndSave :921) → compose(id,path). OBSERVED: parse gives {key:'ikhtilāf', type:'nested-object-list', value:'', nestedObjects:UNDEFINED} plus two phantom top-level keys '- school'; the panel shows 0 rows yet renders '+ Add school' (PropertyEditor:1301 is outside the {#if prop.nestedObjects && length>0} at :1271); one click emits ops=[{op:'set',key:'ikhtilāf',value:'school:  / position: ',type:'nested-object-list'}] and composes to DISK as `ikhtilāf: "school:  / position: "` — Hanafī=false, Mālikī=false, and the result re-parses without error. INDENTED control on the identical click: both schools survive. Refutation avenues all closed: (1) the regex /^\s+-\s/ at store.ts:1900 genuinely requires a leading space so branch 1 never fires, while detectPropertyType (store.ts:176) stamps the type anyway from IKHTILAF_KEYS; (2) no upstream normalizer exists — grep for normalizeFrontmatter|normalizeYaml|reindent across src/ and src-tauri/src/ returns zero hits, files are read verbatim; (3) immutableBlockKeys (yamlDoc.ts:170) WOULD protect any seq-of-maps, but yamlDoc.ts:183 explicitly `continue`s for STRUCTURED_LIST_KEYS = {ikhtilāf, ikhtilaf, الاختلاف}, so these three keys alone are unguarded; (4) line numbers verified exact (store.ts:1900, yamlDoc.ts:183/228, PropertyEditor.svelte:1301, propsCommit.ts:33, ownershipFlag.ts:33 PROPS_SINGLE_OWNERSHIP=true). The claim's only inaccuracy is the composed BYTES: it predicted a rewritten `- school: Shāfiʿī` block (that is the direct composeFrontmatter route), whereas the shipping path is WORSE — PropOp 'set' (propsCommit.ts:33) carries value/type/listItems but no nestedObjects, so setPropValue (noteModel.ts:278) leaves nestedObjects undefined, serializeLine's `prop.nestedObjects?.length` guard (yamlDoc.ts:228) falls through, and the whole block is replaced by the flat display SUMMARY STRING. Silent, single-click, total loss of every recorded scholarly position, no error surfaced, output re-parses cleanly.

### APP-KILLER  —  store.ts:2087  —  parseFrontmatter — block scalars (`|`, `>`, `|-`, `>-`) projected as editable TEXT whose value is the indicator character
Could not refute on any of five fronts; confirmed by running the real code in a vitest harness (since deleted). (1) Line 2087 verified = `const type = detectPropertyType(key, value);`. (2) The three multi-line branches at store.ts:1896/:1981/:2009 are all gated on `!value`; for `desc: |` the value is `'|'` (truthy) so none fire, and the content lines are skipped by the outer `!line.startsWith(' ')` guard at :1882. RAN: store.parseFrontmatter -> {key:'desc', type:'text', value:'|'} while yamlDoc.parseFrontmatterDoc (eemeli) -> 'Line one of the abstract.\nLine two of the abstract.\n'. (3) No write-path refusal: yamlDoc.ts immutableBlockKeys computes isBlock = isMap(v) || (isSeq(v) && !all-scalar); a block scalar is a Scalar node, so isBlock is false and the key is not immutable. (4) No read-only widget: PropertyEditor.svelte gates the read-only render on `prop.type === 'nested-map'` (:1232) and suppresses delete only for that type (:1322); a block scalar arrives typed 'text' and falls to the final {:else} editable <input type=\"text\"> plus a delete button. (5) The UI path does reach it: updateValue -> debouncedSave -> commitAndSave (:921) -> editPropValue -> noteModel.setPropValue -> composeFrontmatter. RAN the edit both via composeUpdatedContent and via the real noteModel path (openModel -> setPropValue('desc','hello') -> compose): both produced `---\ncid_cn: ABCD\ntitle: Imported Probe\ntags:\n  - alpha\nstage: spark-seed\ndesc: hello\n---` — both authored lines gone, key reordered to the end, no error, result re-parses cleanly. RAN delete of the row: block gone entirely. RAN buildFullContent: emits `desc: \"|\"` (quoteIfNeeded, store.ts:2113), poisoning tab.content and every read surface. Folded `>` -> value '>' and strip `|-` -> value '|-' behave identically. Controls confirm the scope: editing `title` only, or adding a tag, leaves the block byte-intact — the loss requires the user to edit or delete that key, which the lone-`|` display actively invites. Only deviation from the sweep's report: I did not observe the claimed stray blank line on the `|-` edit (cosmetic, does not affect the verdict). Same class the repo already ruled APP-KILLER for nested-map (PJ-136) and seq-of-maps (2026-07-24 inspection): silent loss of the user's authored multi-line prose with no error.

### APP-KILLER  —  store.ts:2048  —  parseFrontmatter — branch 3 isFlatList test demotes a block list containing a YAML comment to nested-map
CONFIRMED by running the app's own code (vitest importing $lib/libraries/store, $lib/editor/yamlDoc, $lib/editor/noteModel; scratch files deleted). (1) Projection: for `---\ntitle: T\ntags:\n  - alpha\n  # a comment\n  - beta\nstage: spark-seed\n---`, /^\s+-\s/.test('  # a comment') is false so contentLines.every(...) at 2048 is false, the block is demoted at 2067, and the child-key regex at 2063 matches neither '  - alpha' nor '  # a comment' — observed store output {key:'tags', type:'nested-map', value:'', nestedKeys:[]}; eemeli truth in the same run {tags:['alpha','beta']}. (2) The strongest refutation avenue — PJ-136's rule that composeFrontmatter refuses to write/splice nested-map — INVERTS here and is the reason the defect fires: immutableBlockKeys (yamlDoc.ts:170) deliberately classifies from the FILE via eemeli, which sees tags as a seq of pure scalars (a YAML comment is not a seq item), so isSeq(v) && items.every(isScalar) is true, tags is NOT in immutableKeys, and the SET/ADD branch (yamlDoc.ts:365-371) finds a non-scalar value, splices the CST item and appends serializeLine. Read parser says 'nested-map, immutable'; write guard says 'ordinary list, editable'. (3) Both real Add-tag paths RAN and lost the data: CLOSED note (+layout.svelte:6541 composeUpdatedContent) -> `---\ntitle: T\nstage: spark-seed\ntags:\n  - gamma\n---`; OPEN note (+layout.svelte:6535 saveTabContent -> store.ts:1479 editNoteProps -> noteModel.setProps -> compose) -> identical. alpha, beta AND the comment deleted, no error, re-parses cleanly. (4) Control with the comment removed preserves alpha+beta+gamma — the single '#' line is the sole differentiator. (5) Comment BEFORE the first item takes branch 2 (1981) and loses the same data (the claimed stray blank line did not reproduce — cosmetic only). (6) Editing a neighbouring key (title) is safe: alpha, beta, comment all survive. (7) Reachability refutation failed: no Rust-side frontmatter normalizer exists, readNote passes raw bytes, and addTagToNote is reachable from the file-tree right-click AND the batch-tag loop (+layout.svelte:9645-9656), so one batch tag can strip every affected note in a selection. (8) Line/symbol correct: 2048 is exactly `contentLines.every((l) => /^\s+-\s/.test(l)) &&`. Read side also confirmed: PropertyEditor.svelte:1252-1262 draws the empty nestedKeys row as 'nestedMapEmpty' read-only and 1322 hides delete, so tags go invisible before the next add erases them. Distinct from the known zero-indent finding (that input never reaches branch 3; this one does and is demoted at 2048). Silent loss of the user's authored data.

### APP-KILLER  —  store.ts:1981  —  parseFrontmatter — branch 2 (nested-map) swallows an indented flow sequence
Refutation FAILED on every angle; confirmed by running the real code, not by reading. RAN a vitest harness on `---\ntitle: T\ntags:\n  [alpha, beta]\n---`: store.parseFrontmatter -> {key:'tags', value:'', type:'nested-map', nestedKeys:[], nestedRaw:['  [alpha, beta]']} while yamlDoc.parseFrontmatterDoc -> {tags:['alpha','beta']}. READ store.ts:1878-2106: at 1981 `!value` holds (`tags:` has empty value), `/^\s/` matches `  [alpha, beta]`, and `!/^\s+-\s/` holds, so branch 2 fires; the child scan at 1988 (`cur.indexOf(':')`) finds no colon -> empty summary. Branch 3 (2009) cannot be the anchor (it requires `/^\s+-\s/` on the next line) and the scalar fall-through would have typed it `text`, so 1981 is the correct line. READ yamlDoc.ts:170-192: `isBlock = isMap(v) || (isSeq(v) && !v.items.every(isScalar))` — a flow seq of pure scalars is all-scalar, so `immutableBlockKeys` does NOT protect it; the PJ-136 write-path refusal is absent here. RAN the closed-note path (+layout.svelte:6541 addTagToProps -> composeUpdatedContent): `existing` = [] because a nested-map row has neither listItems nor value, output = `---\ntitle: T\ntags:\n  - gamma\n---` — alpha and beta DELETED, no error, re-parses cleanly. ALSO RAN the OPEN-note path the claim did not test (openModel -> setProps -> compose, i.e. +layout.svelte:6535 saveTabContent): identical loss, so both branches of addTagToNote destroy the data. Reachable from the single right-click Add-tag (+layout.svelte:9656) and the batch tag loop (:9651). Controls prove specificity: inline `tags: [alpha, beta]` round-trips correctly (alpha+beta+gamma all kept) and editing a neighbouring key leaves the flow seq byte-perfect. Blast-radius claim verified too — PropertyEditor.svelte renders nested-map read-only (~1232) and hides the delete button (~1322), so the row looks protected while the tag-add path wipes it. ONE NARROWING FACT, investigated not assumed: I could not find an emitter that produces this shape — eemeli `yaml` (flow style forced, lineWidth 80/40/20/10) and PyYAML (default_flow_style True/None, widths 5-200) always keep `[` on the key line — so the trigger is hand-authored YAML rather than tool-emitted, which lowers likelihood versus the ground-truth zero-indent block list but not the impact. Severity APP-KILLER per the given definition: total, silent, unrecoverable loss of the user's authored tag values, and the rewritten YAML re-parses cleanly so nothing ever notices. SEPARATE adjacent defect found while probing (NOT at 1981, emitter-reachable): verbatim PyYAML `default_flow_style=None, width=60` output `tags: [alpha, beta, a-fairly-long-tag-value, another-long-tag-value,\n  third-long-tag]` — value is non-empty so branch 2 is skipped, the inline-list branch takes it and the outer `!line.startsWith(' ')` guard at 1882 skips the continuation line; store yields listItems ["[alpha","beta","a-fairly-long-tag-value","another-long-tag-value"] — third-long-tag dropped AND alpha corrupted to `[alpha`; after an Add-tag the .md is written as `- "[alpha"` … `- gamma` with third-long-tag gone. Scratch harness files deleted.

### APP-KILLER  —  canonical.rs:299  —  merge_frontmatter
TRIED TO REFUTE, FAILED ON THE CORE CLAIM — confirmed by RUNNING the code, not by reasoning.

WHAT I RAN. I extracted the four functions VERBATIM by line range out of canonical.rs (inject_frontmatter 175-197, build_frontmatter 206-225, merge_frontmatter 228-360, escape_yaml_string 363-365 — no re-implementation, a programmatic byte-for-byte copy), compiled with `rustc -O`, and ran 5 inputs. (I also added a temp #[test] to the crate's test module and started `cargo test --lib`; the crate compile exceeded 10 min so I stopped it and REVERTED the edit — `git diff src-tauri/src/canonical.rs` is now empty, and all scratch files are deleted.)

EMIT SIDE — CONFIRMED. Line numbers are right: L299 `let alias_line = format!("  - \"{}\"", escape_yaml_string(alias));` (mid-block exit branch) and L318 (end-of-frontmatter branch). Both reproduce. On `---\ncid_cn: ABCD\ntags:\n- alpha\n- beta\naliases:\n- Old Name\n- Older Name\nstage: spark-seed\n---` with fields.aliases=["Imported Probe"], the real code emitted:
    aliases:
    - Old Name
    - Older Name
      - "Imported Probe"
    stage: spark-seed
The 2-space-indented control case (aliases already at 2-space) emitted correctly — so the indent is genuinely hardcoded, not derived from the block.

PARSE SIDE — CONFIRMED, AND IT IS SILENT. I fed the REAL emitted frontmatter to the app's OWN parser, `yaml` 2.9.0 from ./node_modules (the eemeli library yamlDoc.ts is built on) and to PyYAML 6.0.3. Both agree, both with ZERO errors/warnings: aliases = ["Old Name", "Older Name - \"Imported Probe\""]. The user's authored alias "Older Name" is mutated in place and the injected alias vanishes as a distinct entry. Nothing raises. (Control: the same list all at column 0 parses to three clean entries; a QUOTED last item instead errors loudly — so the silence is specific to the plain-scalar case, which is the common one.)

REACHABILITY — CONFIRMED, no upstream normalizer. canonicalize_execute (canonical.rs:485) skips only `is_canonical_filename` files (L522) — an imported/hand-authored vault note is NOT canonical, so it is processed; fields.aliases = vec![original_name] from `file_stem` (L540-544, L573). canonicalize_adopt L763 and importers.rs L907/910 (`aliases: vec![clean_title]`) are the same shape. inject_frontmatter only requires a well-formed `---` block (L178-186). I read `write_gate::gate_write` (write_gate.rs:387-447): it does an identity (cid_cn) check and `atomic_write` — NO YAML validation, NO normalization. So the mangled bytes reach disk.

WHAT I DID REFUTE (narrows the claim, does not exonerate it):
1. The dedup guard at L301-305 uses `l.contains(alias.as_str())` against ALL already-emitted lines. When existing frontmatter carries `title: Imported Probe` and the filename stem is "Imported Probe", the append is suppressed entirely — that case emitted clean output. But the corruption still fires in the two normal shapes: (a) NO `title:` key in frontmatter — the Obsidian default, since the injected `title:` is only added at L327 AFTER the alias loop, so it is not in `lines` yet; (b) `title:` present but different from the filename stem. Both reproduced.
2. The sweep's blast-radius claim "the mangled string is what lands in note_aliases" is WRONG. `search.rs::extract_aliases` (search.rs:5446) trims leading whitespace (L1459/L1480 `trim_start` / `strip_prefix("- ")`) and is indent-agnostic, so the SQLite index reads BOTH "older name" and "imported probe" correctly. The real shape is the inverse and arguably worse: the index is right, the FILE on disk is corrupt, so nothing in the app ever surfaces the divergence.

SEVERITY. The `.md` file is the declared source of truth ("File Over App"). A user-authored alias string is silently rewritten on disk, at bulk-import / bulk-canonicalize scale, with no error and no index signal — and the next PropertyEditor write (yamlDoc.ts, which reads the mangled value) would serialize the corruption back permanently. APP-KILLER.

### HIGH  —  store.ts:1882  —  parseFrontmatter
CONFIRMED by running the real chain (vitest harness, since deleted). Line 1882 is exactly `if (colonIdx > 0 && !line.startsWith(' ') && !line.startsWith('\t'))`. RAN parseFrontmatter on `---\nauthors:\n- name: X\n  role: Y\ntitle: T\n---` -> THREE props: {authors,text,''}, {'- name',text,'X'}, {title,text,'T'}; `role: Y` invisible. eemeli truth (parseFrontmatterDoc) projects only {title} and preserves the seq in the CST. Refutations all failed: (a) no earlier branch fires — the 2026-07-24 seq-of-maps comment at store.ts:2013-2015 names this exact authors/name/role shape but guards with /^\s+-\s/, so it fixed only the INDENTED variant; (b) no filtering in the panel — PropertyEditor.svelte:432 `editableProps = sourceProps.map(...)` renders every model prop and a type='text' row gets a live `<input oninput={updateValue}>` (lines 1183/1196), wired at :931 to editPropValue -> noteSession.ts:112 M.setPropValue; (c) no upstream normalizer (only key-scoped rewrite_frontmatter_sources/_content_type in Rust). RAN the REAL shipped write path (SINGLE_OWNERSHIP=true, USE_YAML_DOC=true): openModel -> setPropValue('- name','Z') -> compose -> `---\nauthors:\n- name: X\n  role: Y\ntitle: T\n"- name": Z\n---` — a junk top-level key written into the user's authored .md, silently, re-parsing cleanly. RAN 4 successive rounds editing whatever phantom row the panel then shows: the file accumulates `"- name": V1`, `'"- name"': V2`, `"'\"- name\"'": V3`, `"\"'\\\"- name\\\"'\"": V4` — unbounded compounding. Sub-claims REFUTED: editing the SAME row twice updates the existing quoted key (no second append), and the two-row seq yields two identically-keyed rows whose edit dedupes to a NO-OP write (the user's edit silently discarded). Severity HIGH not APP-KILLER: on the live path no authored value is deleted or overwritten (authors seq + `role: Y` survive; editing the parent `authors` row is a proven no-op). The deletion vector — buildFullContent emitting `authors: \n- name: X\ntitle: T`, dropping `role: Y` (RAN, confirmed) — is confined to the tab.content display cache and the SINGLE_OWNERSHIP=false legacy branch (NoteEditor.svelte:323 marks it dead), and cannot become a write base: restoreSessionTabs (store.ts:2976) seeds models from resolveNoteContent disk bytes, not the cached tab.content.

### HIGH  —  libraries.rs:899  —  merge_initial_frontmatter
REFUTATION FAILED ON ALL SIX ANGLES; confirmed by RUNNING the real code end to end (rustc harness with merge_initial_frontmatter libraries.rs:892-919, create_note composition :944-964, template_content_from_note Whole arm universe.rs:1996-2001, and bases.rs frontmatter_span/remove_frontmatter_property/update_frontmatter_property/format_yaml_value copied verbatim; driven by node using the REAL regex from +layout.svelte:4976). Scratch files deleted. (1) "Code never sees this input" — DEFEATED, and worse than claimed: template_content_from_note (universe.rs:1980) is a byte-offset line splice, NOT a YAML round-trip, so "Save as template" on any imported note EMITS a template still carrying `tags:\n- alpha\n- beta`. Constellation manufactures the poisoned template itself; no hand-authored template is needed. (2) "Upstream normalizer" — read_note (libraries.rs:526-533) is a bare fs::read_to_string; tplFm is the raw regex capture passed verbatim. (3) "Downstream normalizer" — create_note writes through the gate then ONLY reindexes (:964-1016), no rewrite; a repo-wide case-insensitive grep for any frontmatter normalizer returned ZERO matches. (4) "Line number wrong" — libraries.rs:899 is exactly `let is_top_level = !line.starts_with(' ') && !line.starts_with('\t');`. (5) "Guard catches it" — for `- alpha` at column 0, is_top_level evaluates TRUE, trim_start() yields "- alpha", no identity prefix matches, line pushed unchanged. (6) "Not UI-reachable" — command palette `new-note-from-template` (+layout.svelte:2367) -> prompt (:9331) -> newNoteFromTemplate (:4949) -> createNote(folder, title, initialFm, body) (:4980). OBSERVED new note on disk: `---\ntitle: "New Note"\ncid_cn: ...\nkind: note\ncreated: ...\ntags:\n- alpha\n- beta\naliases:\n- Old Name\n- Older Name\nstage: spark-seed\nborn_from: "Research Note"\n---`. Control with an indented source list emitted `  - alpha`, proving the shape is inherited and never normalized. SECONDARY DEFECT observed at the same line (OBS_B): when a FILTERED identity key's value is a zero-indent block list, the key is dropped but its items survive (they too test is_top_level==true) — input `kind:\n- template\ntags:\n- alpha` produced a bare `- template` sequence entry between `created:` and `tags:`, i.e. structurally INVALID YAML in a brand-new note; contrived input, so severity does not rest on it. SEVERITY = HIGH not APP-KILLER, stated honestly: this site does not itself lose data (the bytes it writes are valid YAML 1.2 with the user's tags intact) — it is the PROPAGATION VECTOR. It answers question (1) affirmatively, converting the store.ts defect from "only imported vaults" to "natively-created notes too", turning one file into unbounded exposure: every note cast from that template is born in the state where the first tag-add silently deletes the list. The deletion itself occurs at the already-established store.ts write path. HOUSEKEEPING for the parent: libraries.rs:6840 carries a test `tmp_pj182_zero_indent_block_list_observation` (a sibling agent's scratch, not mine) that must be removed before any commit.

### HIGH  —  search.rs:5642  —  extract_frontmatter_typed_links
CONFIRMED BY RUNNING THE REAL FUNCTION, not by reading. I added a temp #[cfg(test)] probe into search.rs's own tests_mig086_frontmatter_links module and executed the compiled lib-test binary directly (target/debug/deps/constellation_lib-b523964e15e7b887.exe --exact search::tests_mig086_frontmatter_links::pj182_zero_indent_probe --nocapture). Input `---\ntitle: X\nsupports:\n- "[[Alpha]]"\n- "[[Beta]]"\ncontains:\n- "[[Kid]]"\n---\nbody text`. Observed output:
  ZERO-INDENT frontmatter typed links => []
  TWO-SPACE   control                 => [("contains","kid"),("supports","alpha"),("supports","beta")]
  BODY typed links (also merged by index_note) => []
  extract_wikilinks(full content) => ["alpha","beta","kid"]
Scratch test has been deleted; `git diff --stat src-tauri/src/search.rs` is now empty.

REFUTATIONS ATTEMPTED, ALL FAILED:
(1) "The body scan still catches them as associative" — NO. index_note:6161 runs extract_typed_links(&body) where `body` comes from parse_frontmatter (search.rs:4898-4900, `body = body_slice.trim()`, i.e. frontmatter-STRIPPED). Observed [] above. The MIG-086 comment at 6151-6153 says this on purpose: the old whole-file scan was deliberately removed, so the associative safety net no longer exists.
(2) "An upstream normalizer re-serializes the YAML first" — NO. index_note:6069 does std::fs::read_to_string(path) straight off disk. canonical::ensure_cid_cn (canonical.rs:1256) is a line splice (format!("---\n{}\ncid_cn: …", fm)) that preserves every other line byte-for-byte; libraries.rs::remove_frontmatter_contains_item (1911) likewise pushes `line.to_string()` verbatim. Nothing normalizes indentation.
(3) "Some other writer populates note_links" — NO. Every other `INSERT … note_links` in src-tauri (cache.rs 2028/2049/2175, connectivity.rs, links_backfill.rs 525/696/716, link_life_*.rs, reconcile.rs, review.rs, libraries.rs 5513/7538, incoming_links_backfill.rs) is inside #[cfg(test)]/bench code. index_note is the sole production writer from file content.
(4) "Wrong line number" — NO. 5642 is `let is_indented = line.len() != trimmed.len();` inside the fn spanning 5628-5665; the reset is at 5644-5647 and the list-item branch at 5659-5662, exactly as claimed.
(5) "It's deliberate" — NO. Its own doc comment (5626) says "Block-aware like `extract_aliases`", and extract_aliases (5446-5486) trims and accepts `- ` at ANY indent including zero. This function diverges from the model it cites.

PARTIAL MITIGATION (does not refute): extract_wikilinks(&content) at 6072 runs over the FULL file, so the bare target names DO reach note_meta.links_json (observed ["alpha","beta","kid"]). Only the TYPED graph is lost. Backlinks are unaffected by that mitigation — cache.rs::backlink_rows_in_schema (504-536) selects FROM note_links, so the target note shows no backlink either.

ESCALATION FOUND WHILE VERIFYING (read, not run): index_note's incremental diff at search.rs ~6613-6620 does `DELETE FROM note_links WHERE source_path=?1 AND target_name=?2 AND link_type=?3` for every old_edges key absent from new_edges. So if a note whose typed links were already indexed (indented) is later reflowed to zero-indent by any external tool — PyYAML default_flow_style=False emits exactly this shape — the next re-index DELETES those rows, destroying weight / traversal_count / last_traversed / confidence promotions / status='archived'. Per CLAUDE.md those fields live ONLY in search.db (no LINK-file disk layer exists), so that is irreversible, silent loss of the earned half of the Living Link Architecture.

SEVERITY = HIGH, not APP-KILLER: this function never writes to the .md, so the user's authored file content is not corrupted or lost on disk — the confirmed damage is a silent index↔disk divergence (declared relationships absent from note_links, hence from backlinks, Structure/TOC, Sky, tension, strata, outgoing counts) that a re-index would repair once the parser is fixed. It becomes APP-KILLER-grade only via the reflow path above, which I did not reproduce.

TWIN SURFACE (Whole-Ecosystem Fix Law): link_types.rs:396-399 `structural_frontmatter_targets` carries the identical `let is_indented = line.len() != trimmed.len();` gate, with the inverse effect — a zero-indent `parent:`/`contains:` target is NOT skipped, so strata.rs / inspector360.rs miscount it as a cognitive outgoing link. Both must be fixed in the same pass (both should trim and accept `- ` at any indent, as extract_aliases already does).

### HIGH  —  bases.rs:541  —  update_frontmatter_property
CONFIRMED by running real code at every link; one leg of the claimed blast radius REFUTED. (1) Extracted bases.rs 412-420/431-444/456-500/517-571/754-767 verbatim, compiled with rustc, ran: on the zero-indent probe, update_frontmatter_property(src,"aliases","New Name") emits `aliases: New Name` with `- Old Name`/`- Older Name` ORPHANED at column 0. Line 541's guard cannot fire because a zero-indent item IS top-level (is_top_level = !starts_with(' ') && !starts_with('\t')). Controls clean: 2-space list, TAB-indented list, neighbour-key edit, new-key append. remove_frontmatter_property (bases.rs:471) has the identical guard and orphaned the same items. (2) yaml@2.9.0 from the repo's node_modules: PRE = 0 errors, aliases parses as ["Old Name","Older Name"] (valid YAML, legitimate note); POST = 7 errors (MULTILINE_IMPLICIT_KEY, UNEXPECTED_TOKEN, MISSING_CHAR, DUPLICATE_KEY) and toJS gives aliases:"New Name" plus junk ""=null — authored values gone from every reader. (3) Permanent consequence verified live, not theorised: composeFrontmatter IS the shipped write path (G4 Phase 3 — store.ts:2207, noteModel.ts:136; the useYamlDoc flag no longer exists in code), and yamlDoc.ts:300 `if (parseDocument(rawYaml).errors.length) return ---rawYaml---body` silently passes the broken block through, so every later PropertyEditor edit on that note is discarded with no error. (4) REACHABILITY proven by extracting and running search.rs::parse_frontmatter (+split_frontmatter, decode_yaml_scalar, collect_block_scalar) verbatim: a block list under any key EXCEPT tags lands in properties_json as an empty string ("aliases"="", "notable_works"=""); discover_keys (lens/query.rs:468-474) excludes only '- %', '"%', blank and cid/cid_cn/kind, so those keys ARE offered in the Bases add-column picker, and isPropColumn (tableModel.ts:42) makes the cell editable (BaseTab.svelte:237,697) -> commitEdit -> updateNoteProperty -> bases.rs:394. The cell renders EMPTY, so the user has no signal a list is behind it. REFUTED: the claimed universe.rs:2345 leg is wrong — I ran bases::parse_frontmatter on the probe and it DOES parse zero-indent lists ("tags"="alpha, beta", "aliases"="Old Name, Older Name"), so merge_fields_into_template's already-present gate skips list keys and never reaches the writer. Also refuted: the sweep's literal `tags` example is unreachable via the picker (the indexer consumes `tags:` into the tag vector, never into properties_json) — aliases/notable_works/etc. are the reachable keys. SEVERITY HIGH not APP-KILLER: I scanned the Boss's corpus at E:\Cognitive Knowledge — 7,672 notes with frontmatter, ZERO zero-indent block lists, 7,662 using the safe 2-space form; the active universe (كون عيسى) has 5 notes and no list fields. Constellation's own writers cannot emit the form (canonical.rs:213-216 writes '  - '; yaml@2.9.0 defaults to indentSeq), so the input must arrive from an external tool (PyYAML/script/hand-authoring) — real under file-over-app, but absent from the user's data today, and no automatic (non-user-initiated) call site reaches it. All scratch files deleted; repo untouched (remaining uncommitted files are other sweep agents').

### HIGH  —  libraries.rs:1887  —  set_frontmatter_parent
RAN THE REAL FUNCTION IN THE REAL CRATE (not a copy): added a temp test to tests_pj065_resolve, `cargo test --lib refute182 -- --nocapture`. Input `---\ntitle: A\nparent:\n- "[[Old Parent]]"\n- "[[Older Parent]]"\nstage: seed\n---\nbody`, set parent "New Parent". OUTPUT verbatim: `parent: "[[New Parent]]"` then BOTH `- "[[Old Parent]]"` and `- "[[Older Parent]]"` orphaned at col 0, then `stage: seed`. ORPHANS=["- \"[[Old Parent]]\"", "- \"[[Older Parent]]\""]. The 2-space control correctly consumes them. Line 1887 is exactly `if indented && line.trim_start().starts_with("- ")` — verified by Read; the +64 lines other agents have uncommitted are all in test modules at ~6820+, so the number is right. Test removed; libraries.rs back to its prior working-tree state; scratch binaries deleted.

HARM CONFIRMED, not theorised: I ran the exact library composeFrontmatter uses (`yaml`, node) on the orphan output — `YAMLParseError: Implicit keys need to be on a single line at line 3`. The control parses fine. And yamlDoc.ts:300 is `if (parseDocument(rawYaml).errors.length) return `---${eol}${rawYaml}---${eol}${body}`; // H1` — so after this write EVERY later property edit on that note is silently passed through verbatim, i.e. discarded with no error. write_gate.rs:643-671 `gate_rmw` has NO validity guard: it atomic_writes whatever the closure returns and journals it as WriteOutcome::Ok. So the corruption is silent end-to-end. The comment at libraries.rs:1869-1881 documents fixing this exact orphan class on 2026-07-22 — for the indented case only. The sweep's irony is accurate.

WHAT I DID REFUTE — the claimed blast radius ("any note whose `parent:` block came in zero-indent") is too wide. Reachability is a three-way conjunction:
(1) Only one caller: libraries.rs:2012 `"parent" => set_frontmatter_parent` ← resolve_structural_conflict ← store.ts:4915 ← StructuralOutlinePanel.svelte:118 `resolveMove` — the "Move here" button on a CONTESTED row. Nothing else calls it (grepped Rust + Svelte).
(2) A zero-indent `parent:` list is NOT INDEXED AT ALL. search.rs:5638 extract_frontmatter_typed_links computes `is_indented = line.len() != trimmed.len()` and only attributes a list item to `current_key` when indented; a col-0 `- "[[X]]"` resets current_key to None and emits nothing. link_types.rs:394-411 structural_frontmatter_targets has the identical assumption. So no `parent` edge exists for such a note.
(3) With no own-parent edge, structural.rs:139 parent_of falls through to the smallest-path `contains:` claim, and structural.rs:103-108 sets contested ONLY when a `contains:` claimant loses. So the "Move here" button appears only if TWO notes `contains:`-claim the same child. The direct "child's own parent overrules the claimant" route — the common one — is closed precisely because the shape is unindexable.
(4) REAL DATA: I walked the Boss's live Universe (E:\Cognitive Knowledge) — 7,667 notes with frontmatter, ZERO carry any zero-indent frontmatter list item, ZERO carry a zero-indent `parent:` list. The hazard is latent (Obsidian/PyYAML import, hand-authoring), not live.
(5) No upstream normalizer saves it: yamlDoc.ts:263-273 composeFrontmatter is a CST splice that keeps untouched keys byte-perfect, and I confirmed by running the yaml lib that a full doc.toString() WOULD normalize to 2-space but the CST path does not — so an imported zero-indent list survives neighbouring-key edits indefinitely.

SEVERITY CALL: the outcome is the app-killer class (silent unparseable frontmatter + every subsequent property edit silently discarded, no error surfaced) and is the same defect the 2026-07-22 inspection rated APP-KILLER for the indented case. I rate it HIGH rather than APP-KILLER only because reachability is materially narrower here — the shape that breaks the writer is the same shape the indexer cannot see, closing the common contested route — and because zero instances exist in the live 7,667-note Universe. Promote to APP-KILLER if the parent weighs class over reachability. Fix is one line: drop the `indented &&` conjunct at 1887 (a col-0 `- ` immediately after `parent:` is a block-sequence item of that key), matching the same latent assumption in extract_frontmatter_typed_links and structural_frontmatter_targets — Whole-Ecosystem Fix Law applies, all three share the "a list item must be indented" assumption.

### HIGH  —  canonical.rs:292  —  merge_frontmatter — the `if in_aliases_block { if trimmed.starts_with("- ") }` branch (:291-310) plus the hardcoded 2-space alias append at :299 and :318
CONFIRMED by running, not reasoning. (1) Compiled inject_frontmatter/build_frontmatter/merge_frontmatter/escape_yaml_string VERBATIM from canonical.rs:175-365 (byte-containment-verified against clean HEAD; git diff canonical.rs is empty — the sibling agent's probe test was reverted) with rustc -O over 9 shapes. Zero-indent aliases reproduces exactly: "aliases:\n- Old Name\n- Older Name\n  - \"My Import\"". Same for the last-key branch (:316-320), for a single-item list, and for Arabic. The branch at :292 trims so it RECOGNISES the column-0 items, but :299/:318 hardcode a 2-space indent for the new one. (2) Parsed the outputs with TWO independent YAML implementations — yaml@2.9.0 from the repo's own node_modules (the lib src/lib/editor/yamlDoc.ts is built on) and PyYAML. Both: 0 errors, 0 warnings, aliases => ["Old Name","Older Name - \"My Import\""]. Single-alias case => ["Old Name - \"My Import\""] (the user's ONLY alias destroyed and the intended new one never exists). Silent, error-free on-disk corruption of authored frontmatter. (3) Reachability traced end to end: ImporterModal.svelte:65 -> importWithCanonical (src/lib/importers/store.ts:92) -> import_with_canonical (importers.rs:811); markdown|folder|obsidian|notion all reach inject_frontmatter at importers.rs:910 with aliases: vec![clean_title] (never empty); second entry canonical.rs:582 with vec![original_name]. REFUTATIONS THAT FAILED: no upstream normalizer (fs::read_to_string straight in; clean_notion_content is body-only); no downstream healer (repair_external_libraries_on_startup is gated once-per-install by localStorage 'constellation:canonical-repair-done' at +layout.svelte:3156, and its remove_canonical_fields at canonical.rs:1081-1088 DELETES the entire aliases block rather than re-indenting); line number correct. REFUTATIONS THAT PARTLY SUCCEEDED — blast radius corrected: (a) Constellation's OWN extractor is immune — I ran search.rs:5446 extract_aliases verbatim on the mangled block and got ["Old Name","Older Name","My Import"] (it trim_start()s then strip_prefix("- ")), so note_aliases, wikilink resolution and backlinks are NOT broken; the claim's "the note is unreachable by its original filename" is wrong for Constellation and right only for Obsidian/spec-compliant readers and Constellation's PropertyEditor read path (yamlDoc.ts -> parseDocument). (b) The dedup guard at canonical.rs:301-305 (l.contains(alias)) suppresses the append when an already-emitted line contains the file stem — observed: existing "title: My Import" + stem "My Import" => no append, no corruption; but Obsidian exports typically carry no title: key, and that case was observed corrupting. Severity HIGH not APP-KILLER: the authored bytes remain visible in the .md (hand-recoverable) and Constellation's index is unaffected — but the file's YAML meaning is silently changed for every spec-compliant consumer at bulk-import time, and is baked in permanently the first time the aliases property is re-serialised.

### MED  —  PropertyEditor.svelte:973  —  debouncedSave() → tab.content = buildFullContent(editableProps, body)
MECHANISM CONFIRMED BY RUNNING (vitest harness under tests/pj-182/, deleted after): openModel() on the zero-indent note yields tags/aliases as {type:'list', listItems:[]}; PropertyEditor's seed transform (:431-447) copies [] through; buildFullContent then emits "---\ncid_cn: ABCD\ntitle: Imported Probe\ntags: \naliases: \nstage: spark-seed\n---\nbody text\n" — both lists deleted from the cached tab.content. Line 973 text verified: `if (tab) tab.content = buildFullContent(editableProps, body);`. BUT THE CLAIMED BLAST RADIUS IS REFUTED: (a) NoteEditor `parsed`/freshProps/freshBody and applyTemplateToCurrentNote (+layout.svelte:5072) read tab.content through the SAME lossy store.parseFrontmatter — observed identical output before and after ({key:'tags',value:'',type:'list',listItems:[]}), so they lose nothing they had not already lost; (b) the legacy flag-off write paths are dead code — SINGLE_OWNERSHIP and PROPS_SINGLE_OWNERSHIP are both true in ownershipFlag.ts, so saveTabContent:1552 and the FocusPane writeNote at +layout.svelte:8615 are unreachable; (c) NO live disk write reads tab.content — I enumerated every writeNote call site; props/body/stage/flush all compose from the model, whose base.rawYaml holds the TRUE disk bytes (observed: compose after an unrelated edit re-emits `tags:\n- alpha\n- beta` byte-perfectly); every openModel/ensure site (store.ts:956,1694,2659,2684,2873,2976,3930) passes disk-read content, and NoteEditor:186's ensureModel(tab.id, tab.path, tab.content) early-returns on the m.path===path guard; reloadTabsFromDisk:935 and drainCidEnsure:2871 treat the poisoned cache as differing from disk and HEAL it by re-seeding, and the conflict test uses diskDiffersFromBaseline (the model), so no spurious .conflict either. WHAT SURVIVES: parsed.rawYaml is verbatim bytes and is handed to NotePane (NoteEditor:710) which renders it at NotePane.svelte:1636 `<pre class="e-props-source">{rawYaml}</pre>` — observed before "tags:\n- alpha\n- beta\naliases:\n- Old Name\n- Older Name" vs after "tags: \naliases: ", so on the next openTabs.update() the user sees the lists disappear from the on-screen frontmatter source while disk still has them (on-screen != disk, the Editor-Surface Gate class); and the poisoned cache is a latent loss vector — a model opened from it composes `tags: ` straight to disk (observed), reachable only if any future caller seeds openModel from tab.content. Severity MED, not APP-KILLER: display divergence plus a latent gun, with no reachable silent disk loss under today's flags.

### MED  —  link_types.rs:397  —  structural_frontmatter_targets
CONFIRMED by running the real code, not by reasoning. (1) cargo test --lib with a temporary probe inside src-tauri/src/search.rs (since reverted; git diff on search.rs now shows only a sibling agent's test) printed: structural_frontmatter_targets on "---\\ntitle: Part I\\ncontains:\\n  - \\\"[[Chapter One]]\\\"\\n  - \\\"[[Chapter Two]]\\\"\\n---" = {"chapter one","chapter two"}, but on the ZERO-INDENT form ("contains:\\n- \\\"[[Chapter One]]\\\"\\n- \\\"[[Chapter Two]]\\\"") = {} (EMPTY). Caller-level: extract_wikilinks(indented) = [] but extract_wikilinks(zero-indent) = ["chapter one","chapter two"] — the structural TOC targets are written into note_meta.outgoing_links_json as COGNITIVE links. (2) Reproduced independently with a standalone rustc binary containing verbatim copies of structural_frontmatter_targets, resolve_wikilink_type, frontmatter_byte_len and extract_wikilinks (identical output); both scratch files deleted. Line 397 is exactly `let is_indented = line.len() != trimmed.len();` and the list-item arm at :407 (`else if current_structural`) is unreachable for a zero-indent `- "[[X]]"`; the `!is_indented` arm clears current_structural, finds no colon, yields None. Refutations attempted and closed: no upstream normalizer (importers.rs::copy_full_tree uses fs::copy byte-for-byte; strata.rs:182, inspector360.rs:356 and search.rs:6086 all pass raw fs::read_to_string content); the registry is live (STRUCTURAL_SEED_IDS=["parent","contains"] at link_types.rs:61, both seeds structural:true, so the early-return at :391 never fires); no downstream guard (search.rs:5409 gates on `!struct_fm.is_empty()`, so an empty set is a no-op and the polluted column feeds map.rs:110 and the cache.rs:1334 boot fallback). Sibling parser extract_frontmatter_typed_links (search.rs:5628-5663) has the identical indent requirement, so the same note is also missing its real contains/parent edge in note_links — both directions wrong, as claimed. Note two corrections to the claim: there are THREE call sites, not four (search.rs:5401, strata.rs:199, inspector360.rs:368 — map.rs consumes the polluted column, it does not call the function); and severity is not APP-KILLER because all three call sites are read/index paths that never write the user's .md — the damage is a silent index↔disk divergence in derived cognitive views, fully repairable by a reindex after the parser is fixed. Contrast worth noting for the fix: search.rs::parse_frontmatter:4952 handles list items with `trimmed.starts_with("- ")` and is indent-agnostic, so link_types.rs:397 is the outlier against this repo's own convention.

### LOW  —  bases.rs:471  —  remove_frontmatter_property
RAN a byte-verbatim copy of bases.rs line_end_inclusive/frontmatter_span/remove_frontmatter_property/update_frontmatter_property/format_yaml_value/parse_frontmatter + universe.rs template_content_from_note, compiled with rustc 1.94 and executed. MECHANISM CONFIRMED, line/symbol correct: is_top_level = !starts_with(' ') && !starts_with('\t'), so a column-0 "- alpha" IS top-level and the `!is_top_level` conjunct at :471 makes the skip fail. remove_frontmatter_property(PROBE,"tags") reproduced the claimed output exactly — `tags:` removed, `- alpha`/`- beta` dangling at column 0 between two mapping keys. BLAST RADIUS REFUTED: I ran the actual template_content_from_note on the exact probe for all three kinds. Whole → "---\ntitle: My Template\ntags:\n- alpha\n- beta\naliases:\n- Old Name\n- Older Name\nstage: spark-seed\nkind: template\ntemplate_kind: whole\n---\nbody text\n"; Frontmatter → same block, body dropped; Snippet → minimal block. ALL THREE ARE VALID YAML WITH tags AND aliases INTACT. No dead template is written. Reason observed: cid_cn/cid/created are scalars, so skipping_list_items is cleared by the very next top-level line (verified by running remove of cid_cn and of the absent key `created` — both byte-clean). The claim concedes this and then rests on a hypothetical FUTURE caller. EXHAUSTIVE CALLER SET (Grep over src-tauri/src, and no frontend call at all — grep of src/ for remove_frontmatter_property returns zero): shape.rs:196 (SHAPE_KEY) and universe.rs:1996-1998 / 2016-2018 (cid_cn, cid, created). Only ONE live path can fire the orphan: shape.rs:196 via clear_note_shape (shape.rs:270) / revert_note_shape (:300) → apply_shape(to=None), and ONLY if the note's own `shape:` key is a zero-indent list. Observed: remove_frontmatter_property("---\ncid_cn: ABCD\nshape:\n- scrap\n- page\ntags: x\n---\nbody\n","shape") → "---\ncid_cn: ABCD\n- scrap\n- page\ntags: x\n---\nbody\n". No upstream guard: Rust parse_frontmatter (bases.rs:149 uses trim_start(), unlike store.ts) reads zero-indent lists, so read_shape_from_disk returned Some("scrap, page"), from != to, and the remove executed. BUT `shape` is Constellation's own key and every Constellation write emits it as a scalar (update_frontmatter_property + format_yaml_value); the 2-space-indented control removes cleanly. So triggering requires an externally-authored note carrying a top-level key literally named `shape` written as a YAML block sequence, then a user clear/revert. I did not find, and do not claim, that such data exists. Hence LOW: a real latent defect worth fixing (drop the `!is_top_level` conjunct, or track the removed key's own indent), not an APP-KILLER — the named template path is observationally safe and no current caller loses authored data on Constellation-written notes. SIBLING (different line, same hole, for whoever fixes this): bases.rs:541 in update_frontmatter_property — setting a shape on such a note yielded "shape: page\n- scrap\n- page\n". HOUSEKEEPING: bases.rs:879-900 (mod pj182_refute_zero_indent) and shape.rs:696-713 (pj182_probe_remove_list_key, ends in panic!) are uncommitted probe modules left by other agents and must not be committed; my own scratch (scratchpad/refute471.rs + binaries) is deleted, no repo file was modified by me.
