# Whole-app safety sweep register — 2026-07-14 (`wf_8b0a5104-6e8`, 83 agents, 55 confirmed)

Ran at the SS-Cockpit §1 build boundary. **The §1 diff's own findings: ZERO.** Register below, APP-KILLER first.

## [APP-KILLER] `src/lib/libraries/store.ts:2039` (notemodel-ownership)

openNoteTab's ensure_cid_cn step silently replaces write-ahead-recovered content with stale disk content AFTER resolveNoteContent has already consumed (deleted) the recovery net — the manual-reopen crash/failed-save recovery contract is structurally dead for every .md note.

**Scenario:** A note's save fails (locked .md) or the app crashes mid-debounce; the write-ahead net (memory + localStorage) holds the ONLY copy of the last edits; the tab is closed (closeTab's documented contract at store.ts:2418: 'the write-ahead net + save-health banner preserve a failed write and restore it on reopen'). User reopens the note from the file tree → resolveNoteContent finds the identity-proven wab, CLEARS it (store.ts:1939, manual-open consume semantics) and returns the recovered content → openNoteTab then calls ensure_cid_cn_cmd, whose Rust impl (canonical.rs:1226-1227) ALWAYS returns the full DISK content when cid_cn is already present → `updated !== content` is true precisely when the wab held unsaved edits → `content = updated` discards the recovered content. Tab + model open on the OLD disk content, model born clean, net already destroyed. The user's unsaved edits are gone from sc

## [APP-KILLER] `src/routes/+layout.svelte:3436` (editor-lifecycle)

Dirty BACKGROUND note models are never flushed at app close: the MIG-100 graceful-close handshake ('session:final-flush') calls only persistSessionNow() (tab arrangement), and beforeunload/visibilitychange handlers exist only on the MOUNTED NotePane/FocusPane — a note edited and then switched away from holds its only current copy in the in-memory noteModel Map with no disk write and no write-ahead net.

**Scenario:** User types a burst in note A (edits live only in model A; debounced save pending), clicks tab B within the 1.5s debounce. NotePane A's teardown flush is dropped by the filePath!==tab.path staleness guard (NoteEditor.svelte:305 — tab prop already points at B during the keyed teardown), so no doSave, no setWriteAhead; A's debounce+idle timers are cleared with the destroyed pane. Model A stays dirty in RAM for the rest of the session — switchTab (store.ts:2473) is not one of the three APP-KILLER-#2 flush sites and correctly keeps the model, but nothing ever persists it. User works in B for an hour, closes the app: Rust holds CloseRequested, the final-flush listener persists session.json only, ack, window dies. Up to ~30s of typing in A (everything since its last durable save) vanishes with zero error; next launch restores A from stale disk. Universe switch DOES sweep via flushAllDirtyTabs (

## [APP-KILLER] `src-tauri/src/review.rs:762` (persisted-json-state)

save_pulse_data writes review-pulse.json (the source of truth for all review history: last_reviewed/intervals/snoozed/dismissed) with a plain non-atomic fs::write, and load_pulse_data (lines 747-757) silently swallows any read/parse failure and returns an empty default — with no log and no backup, unlike the G6-hardened loaders in libraries.rs/universe.rs. This file was missed by the G6 atomic-write sweep.

**Scenario:** User clicks ✓ Reviewed → mark_reviewed → save_pulse_data truncates review-pulse.json then writes; app crash / power loss / AV lock lands mid-write → partial JSON on disk. Next review action (or the periodic reconcile): load_pulse_data's serde parse fails → silently returns ReviewPulseData::default() → mark_reviewed/snooze/dismiss then save_pulse_data writes the near-empty default OVER the corrupt file — every last_reviewed date, interval position, snooze, and the entire dismissed list permanently gone with zero surfaced error. Worse, recompute_all_in (line 1231) rebuilds the derived review_schedule table from that same empty pulse on the next reconcile, so the whole corpus silently reverts to never_reviewed and dismissed notes resurface.

## [APP-KILLER] `src-tauri/src/universe.rs:1099` (persisted-json-state)

open_existing_universe (both the already-registered early-return at lines 1031-1036 and the new-entry path at 1097-1099) and link_library_as_universe (lines 1183-1185) flip UniverseState.active_path directly WITHOUT the switch protocol set_active_universe implements: no invalidate_search_state, no switch_lock, no frontend onBeforeSwitch dirty-tab flush, no frontend reload — and set_active_universe's already-active guard (lines 699-701) then permanently blocks a repair call because the path already matches.

**Scenario:** Universe A is active and loaded. User opens Universe Manager → 'Open Existing' → picks Universe B. Rust: active_path=B, registry.active_id=B, command returns Ok. Frontend (UniverseManager.handleOpenExisting) only calls refresh() — never onSwitch — so the UI keeps showing A's tabs/sidebar while every ambient-keyed backend command now resolves to B: the next settings tweak / collection edit / workspace save (save_universe_settings/collections/workspaces via active_constellation_dir) silently OVERWRITES B's collections.json / settings.json with A's in-memory state — B's real collections destroyed with no error. Clicking B in the list to 'switch' no-ops (id === activeId in handleSwitch). Simultaneously SearchState.db still holds A's search.db (ensure_search_db_ready guards only on db.is_some(), search.rs:8644-8656), so any reindex writes B-paths into A's index — silent index↔universe diverge

## [APP-KILLER] `src/routes/+layout.svelte:4767` (frontend-write-callers)

Template-insert fallback writes `tab.content + template` straight to disk via raw invoke('write_note', origin 'template_insert') when no `.note-pane.active .cm-editor` DOM node is found — bypassing the single-ownership model entirely (no editNoteBody, no saveNoteSession, no markRecentWrite, no model reload); it then patches only the display-state tab.content (line 4768), leaving the model stale.

**Scenario:** User is in Focus mode (or any layout where `.note-pane.active .cm-editor` doesn't exist) with unsaved keystrokes in the model, and runs Insert Template from the command palette. The fallback writes the STALE tab.content (pre-edit display copy) + template to disk — the recent keystrokes vanish from disk. Then either (a) commitFocusSave/autosave composes from the dirty model and overwrites disk, silently discarding the inserted template (user believes it was inserted; nothing errors), or (b) the un-suppressed watcher sees the app's own write as external against a dirty model and raises a phantom .conflict sidecar. Either branch is the exact Recipe-O two-writers clobber shape the SINGLE_OWNERSHIP migration exists to forbid.

## [HIGH] `src/lib/components/ExpressionForge.svelte:144` (note-save-index)

Forge export does createNote (which synchronously indexes the template stub, stamping note_meta.modified at second resolution) then immediately writeNote's the real composition with NO reindex — and because both land in the same second, the boot reconcile's mtime cache-gate skips the note forever.

**Scenario:** User exports a composition: create_note (libraries.rs:806→832) writes + indexes the empty template at second T; writeNote at line 144 overwrites with the full composition milliseconds later, still in second T. gate_write suppresses the watcher for the path (write_gate.rs:249-250), so no watcher reindex; no frontend reindex call exists on this path; onClose() fires and the UI reports success. note_meta/notes_fts now hold the empty template body while disk holds the composition. At every subsequent boot, reconcile_filesystem → index_note(force=false) hits the mtime gate (search.rs:5688-5697: existing_mod == modified, same second) and returns Ok without reading the file — the divergence is PERMANENT unless the user happens to edit the note. The user's synthesized composition is silently invisible to full-text search, backlinks, wikilink extraction (note_links), tags, and sky — with zero err

## [HIGH] `src/lib/components/SenseMakingCanvas.svelte:271` (note-save-index)

Canvas item promote is the identical createNote-then-writeNote-same-second shape with no reindex: the promoted note's content is permanently absent from the index unless the user later edits the note.

**Scenario:** User promotes a canvas item to a note: create_note indexes the frontmatter-only template at second T; writeNote (line 271) overwrites with frontmatter+item.content in the same second; watcher suppressed by the gate; openNoteTab (line 278) does not reindex (only lazy cid injection). note_meta.modified equals disk mtime, so every boot walk's mtime gate cache-hits (search.rs:5695-5697) and never re-reads. The promoted knowledge exists on disk but silently never enters notes_fts/note_meta/note_links/sky. If the user closes the tab without typing (promote is often the last action on that note), the divergence persists across reboots indefinitely — search for the promoted text returns nothing, no error ever surfaced.

## [HIGH] `src/lib/libraries/store.ts:1929` (notemodel-ownership)

On MANUAL open, resolveNoteContent's rejection branch destroys (clearWriteAhead) an identity-unproven or empty-body write-ahead recovery copy — the exact copy the adjacent MIG-100 comment says 'may be the ONLY copy of unsaved edits' and promises 'stays for manual recovery'; only the restore path (preserveNet) was fixed.

**Scenario:** A deferred-cid note (session-restored, Gate #8 skipped the cid write, or ensure_cid failed on a locked file) is edited; the save fails; the cid-less recovery copy sits in the net (identity-unproven by construction). The user closes the tab (net retained per closeTab contract) and later reopens the note by clicking it in the tree → resolveNoteContent: wabCid=null → identityProven=false → rejection branch runs `if (!opts.preserveNet) clearWriteAhead(filePath)` → the sole copy of the unsaved edits is deleted and disk wins the view. Only a console.warn fires — devtools are disabled in release builds, so the user sees nothing. Same line also permanently reverts a deliberate delete-all-body edit whose save failed (emptyResurrection rejection).

## [HIGH] `src/lib/libraries/store.ts:1283` (notemodel-ownership)

loadTabHistoryEntry (Alt+Back/Forward) bypasses the B1 one-path-one-tab dedup that openNoteTab enforces (DEDUP_ALL_TABS_ENABLED, store.ts:2003) — history nav can land a tab on a path already open in ANOTHER tab, minting two independent writable models for one file whose saves silently clobber each other.

**Scenario:** Tab1 visits note A then navigates in-place to B (tab1.history=[A,B]). User Ctrl+clicks A → no tab currently has path A, so a new Tab2 opens A. User presses Alt+Back in Tab1 → loadTabHistoryEntry re-seeds Tab1 to A with NO dedup check → A now backs two tabs/models. User types edit-2 in Tab2 (autosave writes A₀+e2 to disk), then types edit-1 in Tab1 whose model still holds A₀ → its save composes A₀+e1 and overwrites disk, silently erasing e2; markRecentWrite suppresses the watcher echo so Tab2 never learns — Tab2's clean model still DISPLAYS e2 while disk no longer contains it (screen↔disk divergence), and Tab2's next save in turn erases e1. Last-writer-wins ping-pong, no conflict sidecar, no error — the exact W2-2 class the B1 fix closed for openNoteTab, reachable through the un-guarded history-nav door.

## [HIGH] `src/lib/libraries/store.ts:1914` (editor-lifecycle)

resolveNoteContent's write-ahead-vs-disk arbiter proves IDENTITY (cid_cn equal + non-empty body) but has no RECENCY signal, while clean-note teardowns keep stashing write-ahead entries (NoteEditor.svelte:337, the needsDiskSave=false path) that closeTab (store.ts:2414) never clears — so a time-stale localStorage wab silently beats NEWER external disk content for the same note on the next open/restore, and the next save clobbers the external edit on disk.

**Scenario:** Session 1: note A (content C0, saved/clean) is the active tab; entering Focus mode or app close triggers NotePane teardown/beforeunload doFlush -> handleFlush !needsDiskSave -> setWriteAhead(A, C0) persisted to localStorage; no later durable save of A runs, so clearWriteAheadIf never fires. App closed. Syncthing/Git (the project's stated sync model) pulls an edit from another device: disk A = C1, same cid_cn (sync edits never change cid). Session 2: MIG-100 restore calls resolveNoteContent(A,{preserveNet,requireDisk}) (store.ts:2351) — wab C0 hit, disk C1 read, cids equal -> identityProven -> wab C0 WINS the view (console.warn only fires on the rejection branch; acceptance is silent, and devtools is disabled in release anyway). Model born clean from C0 with diskBaseline C0; no watcher event exists for the pre-boot change, so no adopt and no .conflict sidecar. User edits -> durable save w

## [HIGH] `src/routes/+layout.svelte:1538` (rename-cascade-integrity)

FocusPane sits outside every rename-cascade gate: the focusMode branch (:7984-8027) has no CascadeFreezeOverlay, FocusPane onchange pushes editNoteBody ungated (:8005), and commitFocusSave (:1538) writes via saveNoteSession which is NOT gated by isCascading (store.ts:884's own comment) — the F2 post-cascade-stomp gate that protects NotePane (NoteEditor.svelte:250/:311) has no Focus-mode equivalent, violating Editor-Surface Gate #4.

**Scenario:** Note B (backlinks [[A]]) is open in FOCUS mode; user types (model dirty, 1500ms focus-save timer armed), then right-click-renames note A in the file tree (tree stays interactive in focus mode). handleRenameComplete raises cascadeFreeze — but no overlay covers FocusPane, so typing continues and the armed commitFocusSave fires mid-cascade with no isCascading check. Interleaving (a): the focus save lands AFTER the walker rewrote B on disk -> saveNoteSession durably writes the pre-cascade body, silently reverting [[A v2]] back to [[A]]; the subsequent reloadTabsFromDisk read adopts the reverted content, so nothing ever surfaces the loss. Interleaving (b): keystrokes typed in Focus during the walk leave the model dirty when reloadTabsFromDisk(:6339) force-reseeds it via openNoteModel (store.ts:723 — the exact PJ-092 forbidden dirty-reseed) and focusReseed remounts (:6346) — the last seconds o

## [HIGH] `src-tauri/src/search.rs:6107` (derived-index-triggers)

index_note's edge-diff rebuild silently RESURRECTS every user-archived link on the next save of the source note: the `identical` check requires o_status=='active', so an archived edge (status='archived', weight=0.0 — set by constellation_link_archive, which touches ONLY the DB, never the .md body) always fails the check and is DELETE+re-INSERTed with status hardcoded 'active' (lines 6141/6147), undoing the archive.

**Scenario:** User archives the A→B link in the Backlinks/Outgoing panel (store.ts archiveLink → constellation_link_archive: status='archived', weight=0.0; the [[B]] text stays in A's body by design — 'soft delete, reversible'). User then types ONE character anywhere in note A; the 1.5s debounced save calls reindex_single_note → index_note(force=true). The [[B]] wikilink still parses into new_edges; old_edges has it archived → the `unchanged` fast-path fails (line 5988 comment: 'the rebuild forces active, so an archived edge would NOT be a no-op') → per-edge loop: identical=false (o_status!='active') → row deleted and re-INSERTed with status='active' (weight 0.0 via the preserved branch since w!=1.0). The note_links_sky_ai trigger (WHEN NEW.status='active') re-adds it to sky_links. Result: the archived link silently reappears ACTIVE in Sky View, backlinks, incoming/outgoing aggregates, and vanishes fr

## [HIGH] `src-tauri/src/search.rs:6058` (derived-index-triggers)

User-set link confidence (e.g. 'contested') and the link's original `created` date are silently reset to 'hypothesis'/now whenever a never-traversed edge is re-INSERTed, because the `preserved` snapshot only keeps rows with (traversal_count > 0 || weight != 1.0) — an unearned edge whose confidence was set via constellation_link_set_confidence (search.rs:8172, which changes ONLY the confidence column) never qualifies, so the re-insert falls to the fresh-defaults branch (line 6146: confidence='hypothesis', created=now).

**Scenario:** User marks the A→B link 'contested' via the shared ConfidencePicker (setLinkConfidence → UPDATE note_links SET confidence only; traversal_count stays 0, weight stays 1.0). Later ANY parse-derived field of that edge changes and forces a re-insert: the user edits the link's annotation, OR renames note A (source_name changes on EVERY edge of A), OR target note B gets its cid_cn lazily injected at first open (target_cid_cn now resolves, o_tcc != tcc), OR the archived-resurrect path above fires. On A's next save, index_note computes identical=false → DELETE + re-INSERT; `preserved.get()` misses (tc=0 && w==1.0 at line 6058) → INSERT with confidence='hypothesis' and created=now (lines 6146-6148). The user's epistemic judgment — 'contested', the input to the tension/CCS-contested surfaces and one of the Eight Properties of the Living Link — plus the link's birth date are silently destroyed with

## [HIGH] `src/lib/editor/yamlDoc.ts:213` (frontmatter-property-writes)

On any YAML parse error (duplicate keys, tab indentation — common in imported Obsidian vaults), composeFrontmatter passes the ORIGINAL frontmatter bytes through verbatim, silently discarding every property edit, yet noteSession.save (noteSession.ts:140) still writes, marks the model version CLEAN (markSaved), re-baselines, and runs onSuccess — no onError, no banner, nothing surfaced.

**Scenario:** Note imported from Obsidian has two `tags:` keys (or a tab char) in its frontmatter — the hand-rolled parseFrontmatter happily projects it as editable, but eemeli-yaml parseDocument reports errors. User opens the note, adds a tag / changes stage / edits any property in PropertyEditor -> debouncedSave -> saveTabContent -> composeModel hits the H1 passthrough -> disk receives old-frontmatter+current-body -> save succeeds -> model marked clean, 'saving' indicator completes, reindex runs. The edit shows on screen until the tab is closed; on reopen it is gone. Every future property edit on that note vanishes the same way, forever, with zero surfaced error. Body edits persist, so the user has no reason to suspect frontmatter writes are dead.

## [HIGH] `src/lib/components/PropertyEditor.svelte:809` (frontmatter-property-writes)

Property edits live ONLY in local editableProps for the 800ms debounce window — they are pushed to the noteModel only inside saveTabContent (store.ts:1125). The APP-KILLER #2 departure flushes (closeTab/nav) see a CLEAN model and proceed; closeTab disposes the model (store.ts:2437) BEFORE PropertyEditor.onDestroy fires, so the onDestroy flush hits editNoteProps-no-model / compose no_model refusal (noteSession.ts:130) which returns without onError, and saveTabContent ignores the SaveOutcome — the edit is dropped with no error, no banner, no retry.

**Scenario:** User types a new property value (or adds a tag chip) and within 800ms closes the tab (Ctrl+W) or clicks a wikilink that reuses the tab: flushOutgoing finds the model clean (props never pushed), closeTab/openNoteTab re-seeds or deletes the model, the pending debounce/onDestroy saveTabContent then no-ops on the path-guard or missing model and resolves normally. The property edit silently never reaches disk. The exact durability gap the nav-flush migration closed for BODY edits remains open for PROPERTY edits.

## [HIGH] `src/lib/editor/yamlDoc.ts:155` (frontmatter-property-writes)

serializeLine has no 'nested-object-list' branch: any edit to an ikhtilaf structured property makes composeFrontmatter splice the real YAML seq-of-maps out of the CST (line 256) and append the flat display summary string instead — the MIG-022 structured data is silently flattened on disk (a G4 regression: the retired reconstructFrontmatter at store.ts:1629-1648 handled this type correctly).

**Scenario:** Note has `ikhtilāf:` with rows `- school: Hanafī / position: permissible`, `- school: Mālikī / position: discouraged`. User edits one position field in the PropertyEditor ikhtilaf widget -> debouncedSave -> saveTabContent -> composeModel diff fires (summary value changed) -> CST item is a block-seq, not a scalar -> spliced + re-added via serializeLine as `ikhtilāf: "Hanafī: permissible | Mālikī: discouraged"`. On next open, parseFrontmatter sees a plain scalar — nestedObjects gone, the widget shows no rows, the structured scholarly-disagreement data is permanently flattened. No error anywhere.

## [HIGH] `src-tauri/src/libraries.rs:197` (persisted-json-state)

save_libraries does temp-write + rename but NO fsync before the rename — the exact hardening universe.rs's own atomic_write documents as mandatory (universe.rs:122-125: 'power loss can land the rename while the data blocks are still unflushed, leaving a zero-length/garbage file under the FINAL name'). libraries.json is the library-registration source of truth.

**Scenario:** User adds/removes a library → save_libraries writes tmp, renames over libraries.json; the filesystem commits the rename metadata but the data blocks are still in the page cache; power loss / hard crash within the flush window → libraries.json is zero-length/garbage under the final name. Next boot: load_libraries backs the corrupt file up and returns vec![] — the user's entire library list vanishes from the UI with only an eprintln (dev-only console; release builds have no visible surface per the devtools-dev-only rule). ensure_universe_notes_folder then re-inserts only a fresh universe_notes entry, so the next save persists a registry missing every external library — the user's additional libraries silently disappear from the Universe.

## [HIGH] `src-tauri/src/sources/mod.rs:571` (cece-sources-derived)

Accepting a classification rewrites an OPEN note's frontmatter on disk through a side channel the editor model never learns about (gate_rmw suppresses the watcher; the frontend accept flow does not refresh the model), so the next debounced editor save composes from the stale open-time frontmatter base and silently erases the just-accepted sources:/content_type: from disk, after which reindex reverts note_meta and the suggestion row is already cleared — the user's accepted classification vanishes everywhere with no error.

**Scenario:** Note X is open in a tab. User clicks 'Classify open note' in the right rail, then Accept (SourceReviewPanel.svelte:786 invokes sources_set_manual/content_type_set_manual; same for cece_resolve_disambiguation and bulk Approve-All hitting an open note). rewrite_note_sources_on_disk (sources/mod.rs:561-582) runs gate_rmw -> atomic_write, which calls watcher_suppress::mark(path) (write_gate.rs:250), so the watcher event that would drive adoptDisk (noteModel.ts:305) never arrives; acceptSuggestion also never calls replaceContent/setProps. The open model's G4 write-base (noteModel.ts base.rawYaml) still holds the open-time frontmatter WITHOUT sources:. User types one character (or a debounced save was already pending because the model was dirty at accept time) -> save composes via composeFrontmatter(base.rawYaml ...) -> frontmatter without sources: -> gate_write self-attests (same cid) and in 

## [HIGH] `src/lib/components/BacklinksPanel.svelte:197` (frontend-write-callers)

linkMention() does a raw disk read-modify-write (readNote → replace → invoke write_note origin 'link_mention') on a note that may be OPEN with unsaved edits, bypassing every established recipe: no open-tab check, no flushOpenTabOrAbort, no markCascading, no markRecentWrite, no reloadTabsFromDisk, no reindex, and the whole op is wrapped in `catch { /* ignore */ }` (line 199).

**Scenario:** The unlinked-mention note is open in a background tab with unsaved keystrokes (model dirty, autosave armed ~1500ms). User clicks 'Link it': write_note puts stale-disk-body+wikilink on disk. The dirty model never learns of it; the armed autosave then composes from the model and overwrites disk — the wikilink the user just inserted is silently reverted, no error anywhere (or, if the watcher's 300ms debounce wins instead, a phantom .conflict sidecar is raised for the app's own write since markRecentWrite was never called). If the write itself fails, the catch swallows it entirely and the UI proceeds as if linked. Contrast: addLinkToNote (store.ts:976-1001) — the sibling closed-note link writer — has the isCascading gate, markRecentWrite, byte-perfect composeUpdatedContent, and a logged reindex; linkMention has none.

## [HIGH] `src-tauri/src/canonical.rs:932` (reactivity-concurrency)

Boot-time de-canonicalize sweep (repair_external_libraries_on_startup, canonical.rs:1313 → de_canonicalize_library) runs (async) on a tokio worker concurrently with the live editing session and does an UNLOCKED read(old canonical path):932 → gate_write(NEW human-named path):947 → remove_file(old path):951 per note — no path lock spans the cycle, so a concurrent durable editor save to the old path is silently destroyed, and the gate's watcher_suppress on the new path hides the rename from the index.

**Scenario:** State: an external library still contains canonical-named .md files (the exact state this repair exists to fix) and localStorage 'constellation:canonical-repair-done' is unset (fresh install / second device / cleared webview storage). Boot: +layout.svelte:3021 fires the repair fire-and-forget BEFORE initializeApp; MIG-100 session restore opens the user's tabs while the sweep walks the library (seconds — one fsync per file). Timing: (a) sweep reads note X's old canonical path at :932; (b) the user's 1.5s-debounced save lands on the old path via write_note — durability gate resolves, model marked CLEAN, write-ahead net cleared, save-health green; (c) sweep gate_writes the PRE-EDIT content to the new human-named path at :947; (d) sweep deletes the old path at :951. The durably-confirmed edit now exists nowhere on disk; the model believes it is clean so nothing retries. Additionally the new 

## [MED] `src-tauri/src/write_gate.rs:587` (rename-move-delete-gate)

gate_rename never re-checks destination existence under its locks, and std::fs::rename on Windows is MoveFileExW(MOVEFILE_REPLACE_EXISTING) — an existing destination FILE is silently replaced. Every gate_rename caller (move_item libraries.rs:1711, move_to_trash libraries.rs:6072, move_into_trash_folder libraries.rs:6191) does its dest-exists / de-collide check BEFORE the locks are acquired. gate_rmw_rename explicitly fixed this exact TOCTOU for rename_item ('dest-exists check now happens UNDER the lock'), but gate_rename retained the unguarded shape.

**Scenario:** User A (or the second window / a template flow) creates note 'X.md' in folder F via gate_create_exclusive at the same moment a move_item of a different 'X.md' into F is in flight: move_item's dest.exists() pre-check at libraries.rs:1711 runs while F/X.md does not yet exist → passes; gate_create_exclusive then acquires F/X.md's lock, verifies not-exists, writes the new note, journals CreatedExclusive, releases; move_item's gate_rename now acquires both locks and fs::rename REPLACES F/X.md — the just-created note's entire content is destroyed. Both operations return Ok, both journal success outcomes, no error anywhere. Same window exists for a sync-tool (Syncthing/iCloud) file landing at dest between the check and the rename, and for the trash de-collide loops (candidate picked as non-existent, then clobbered).

## [MED] `src-tauri/src/libraries.rs:5585` (rename-move-delete-gate)

update_links_on_rename detaches the post-cascade reindex loop to tauri::async_runtime::spawn_blocking with the JoinHandle dropped — no durability, no retry, per-file failures only eprintln (invisible in a release build). This is the same detached-tail shape as the MIG-098 rename_item tail but a DIFFERENT, unregistered site: the wikilink-cascade's index tail.

**Scenario:** User renames a hub note linked from 100+ notes. The cascade rewrites all 100 files on disk (gate_rmw) and the IPC settles immediately; the detached loop then needs 100 sequential SearchState writer-lock acquisitions to reindex them. User closes the app (or it crashes, or the writer lock is held long by a backfill) before the loop finishes → for every un-reindexed source, note_meta.body_text / notes_fts / outgoing_links_json / note_links.target_name still carry the OLD wikilink target. The watcher cannot heal: gate_rmw marked every rewritten path in watcher_suppress, so the app's own rewrites never re-enter the freshness pipeline. Backlinks/Outgoing/Index/search silently show the pre-rename link graph until each affected note is manually edited or the user runs Settings → Rebuild Index. No error is ever surfaced (eprintln only).

## [MED] `src/lib/libraries/store.ts:1051` (note-save-index)

flushAllTabsInLibrary flushes dirty tabs through standardSaveEnv WITHOUT the onSaved reindex hook, so a durably-flushed note that the rename cascade does NOT rewrite gets its new disk content but never a reindex — the exact index↔disk divergence navFlushEnv's own comment (store.ts:421-424) names, fixed only for the three nav sites.

**Scenario:** User has note X open with ~1.5s of un-autosaved edits, then renames note Y (X does not link Y). The pre-cascade flush (called from +layout.svelte:6311) durably writes X's edits to disk via the bare env — no onSaved, so no constellation_search_reindex. gate_write suppresses the watcher and markRecentWrite suppresses the frontend flush; flushOutgoingModel marks the model CLEAN, so the editor's pending debounce never re-saves; the cascade walker only rewrites/reindexes notes containing [[Y]], which excludes X. Result: X's flushed edits are on disk but note_meta/notes_fts hold the pre-flush body for the rest of the session — search, backlinks, tags, counts silently stale with no error. Heals at next boot via the mtime walk EXCEPT when the flush lands in the same second as X's last indexed autosave (rename immediately after typing), where the second-resolution gate makes it permanent.

## [MED] `src-tauri/src/tasks.rs:523` (note-save-index)

toggle_task validates the WRITE against ALL libraries (validate_path_in_any_library → load_all_libraries, federated cUniverses included) but resolves the reindex library via load_libraries (own libraries.json only) — a task toggle on a federated cUniverse note writes disk, silently skips the reindex entirely, and the `let _ =` at line 532 swallows any error for own-library notes too.

**Scenario:** User toggles a checkbox in a note living in a linked cUniverse library (the Boss's real setup: federation under E:\Cognitive Knowledge). tasks.rs:453 validate_path_in_any_library passes (load_all_libraries spans federation), gate_rmw writes the toggled line to disk, then the lib_name lookup at 523-528 iterates load_libraries — own libraries.json only — finds no match, and the entire MIG-080 §C reindex block is skipped with no log and no error; Ok(final_content) returns and the UI shows the toggle succeeded. notes_fts/note_meta body (and the ✅ completion date) silently diverge from disk for the whole session; task dashboards re-reading note_meta show the stale state. Additionally line 532's `let _ = reindex_single_note(...)` swallows real reindex failures even for own-library notes — the create_note site (libraries.rs:825) explicitly forbids this exact pattern ('Not `let _ =`: an index fa

## [MED] `src/lib/libraries/store.ts:3290` (notemodel-ownership)

renameItem's pre-rename flush is a SINGLE saveNoteSession (not the bounded flushOutgoingModel loop that flushAllTabsInLibrary uses for exactly this H2 race), and clearCascading lifts the gate BEFORE the readNote await — a keystroke landing in the flush-write/rename_item/readNote windows re-dirties the model, which openNoteModel(t.id, effectivePath, fresh) then force-reseeds from disk, silently discarding it after clearWriteAhead already wiped the net (3277-3278).

**Scenario:** The open, dirty note is renamed from the sidebar (no CascadeFreezeOverlay on this path — the freeze belongs to the D2 title-rename orchestration). Flush at :3236 composes and awaits the write; the user refocuses the editor and types during the awaited write (the H2 window: save returns ok:true but the model is dirty again — the single call never re-checks), or during the subsequent `await rename_item` / `await readNote` windows where clearCascading (:3246) has already re-enabled input paths. renameFlushOk stays true → clearWriteAhead(oldPath/effectivePath) destroys the net → openNoteModel force-replaces the re-dirtied model with disk content. The during-rename keystrokes vanish with no banner (the dev tripwire in openModel is DEV-only and doesn't even fire for canonical renames where effectivePath === oldPath). Same class as the filed PJ-099 (loadTabHistoryEntry) but a distinct, unfiled 

## [MED] `src/lib/libraries/store.ts:849` (notemodel-ownership)

adoptExternalChangeIntoTabs clears the write-ahead net for every clean adopter — but a session-restored tab whose model was born (clean) from identity-PROVEN wab recovery content is a 'clean' model whose compose ≠ disk, so ANY watcher event (even a spurious fs touch: Defender, sync re-stat) makes adoptDisk replace the displayed recovered content with stale disk AND delete the recovery net.

**Scenario:** Crash with unsaved edits on a cid-bearing note → net holds recovered content. Boot restore: resolveNoteContent(preserveNet) proves identity → the tab and model are seeded from the WAB content but openModel marks it CLEAN (version=0=savedVersion) with diskBaseline set to the wab bytes — nothing ever writes the recovered content to disk. Before the user types, any fs event on that file reaches the 300ms watcher flush → adoptExternalChangeIntoTabs: model 'clean', composeModel(m)=wab content !== disk → adoptDisk adopts the OLD disk, the {#key} remount repaints the pre-crash content, and line 849 clearWriteAhead(p) destroys the only recovery copy ('invariant #10 — only adopters' assumes a clean adopter has nothing to lose, which is false for a net-seeded restore tab). The recovered edits vanish from screen, net, and disk silently — no conflict sidecar (the model wasn't dirty), no banner.

## [MED] `src/routes/+layout.svelte:7990` (editor-lifecycle)

FocusPane's ontitlechange callback is not wired at its only call site — the title input in Focus mode is fully editable (bind:value, blur handler fires ontitlechange?.(titleValue) in FocusPane.svelte:90) but the optional-chained callback is undefined, so a title typed or edited in Focus mode is silently discarded on exit; no rename, no frontmatter update, no error.

**Scenario:** User enters Focus mode on an untitled capture note, writes a paragraph, pauses 3s (title input fades in per the progressive-disclosure design — the UI actively INVITES titling), types 'Q3 Strategy Draft' as the title, presses Esc to exit. FocusPane is destroyed; handleTitleBlur fired ontitlechange?.('Q3 Strategy Draft') into undefined. Back in NotePane the note still shows the old title/filename; the typed title exists nowhere — not in frontmatter, not as a rename, not in any buffer. The user believes the note was titled (they saw it on screen in Focus) and can no longer find it by that name in search/quick-switcher — silent loss of user input with a false on-screen acknowledgment. Fix locus: wire ontitlechange on the FocusPane instance (+layout.svelte:7990) to the same delegated rename path NoteEditor uses (handleRenameComplete via handleTitleChange semantics, guarded by focusSessionPat

## [MED] `src/routes/+layout.svelte:6399` (rename-cascade-integrity)

cascadeFreeze is last-writer-wins while its paired save-gate cascadingPaths is refcounted for exactly this overlap: a second concurrent rename's cascadeFreeze.set(new Set(...)) (:6284) replaces the first cascade's frozen set, and whichever rename finishes first clears ALL freezes (:6399 set(new Set())) while the other cascade is still walking/reloading — renamesInFlight (:6187) only dedupes the same oldPath, so two different-note renames overlap freely.

**Scenario:** User renames note A (7,600-note library -> walker runs for seconds), then renames note C (tree row rename is not frozen) before A's cascade completes. C's flow either replaces A's frozen tab set (cross-library: A's tabs unfreeze instantly) or C's shorter cascade finishes first and :6399 clears the freeze for A's still-running cascade. The user types into a now-unfrozen backlink tab of A; markCascading still blocks its SAVE, but nothing blocks the model going dirty; A's reloadTabsFromDisk then force-reseeds the dirty model via openNoteModel (store.ts:723) — the keystrokes typed in the unfreeze window are silently discarded with no error (the PJ-092 forbidden dirty-reseed, reachable because the freeze that upholds the invariant is not refcounted).

## [MED] `src/routes/+layout.svelte:6308` (rename-cascade-integrity)

The cascade's freeze and write-gate are snapshots: cascadeFreeze (:6284) and the markCascading loop (:6307-6308) capture tabsInLibrary at cascade start, so a tab OPENED during the multi-second cascade window (the file tree is never frozen) is neither frozen nor marked cascading — its NoteEditor accepts typing and its debounced handleSave passes the isCascading gate.

**Scenario:** Rename note A in a large library; while the freeze overlay covers existing tabs, the user clicks backlink note B in the still-interactive file tree. B opens un-frozen and un-marked, seeded with pre-rewrite disk content, and the user types. Either (a) B's 1.5s debounced save lands after the walker rewrote B -> the save composes from the pre-cascade model and silently reverts the [[A v2]] rewrite on disk, or (b) reloadTabsFromDisk(:6339) matches B's now-open tab (store.ts:698 checks CURRENT tabs) and force-adopts the walker output over the dirty model via openNoteModel (store.ts:723), silently discarding the typed keystrokes. No error in either interleaving.

## [MED] `src/lib/components/SecondScreenPage.svelte:960` (cross-window-integrity)

Every second-screen companion note click mints a fresh `${Date.now()}` tab id (dash-note-* :784, dash-tag-* :960, idx-term-* :1007, idx-cmp-* :1061, peek-* :313) whose NoteEditor mount creates a full-content noteModel (NoteEditor.svelte:166 ensureModel) that is NEVER disposed — SecondScreenPage never calls closeModel/closeAll for companions (only the store closeTab path does, which companions bypass), and the SS window is hidden, not destroyed, on close (lib.rs:264-269), so the module-level models Map grows for the entire app lifetime.

**Scenario:** User browses the SS dashboard/tag/index companions over a long session: each note click adds a NoteModel holding ~2x the full note content (body Text + diskBaseline + base.rawYaml) under a never-reused id. Closing and reopening the SS does not clear them (window is only hidden). Hundreds of clicks on large notes = tens-to-hundreds of MB retained silently in the SS webview with no error, no visible symptom until the webview degrades. Additionally each leaked model still matches by path in adoptFreshDiskIntoSS's openTabs loop cost (Map lookups), so the leak also grows adopt work.

## [MED] `src/lib/components/PropertyEditor.svelte:364` (frontmatter-property-writes)

editableProps are seeded with `type: forced ?? registeredType ?? p.type` — a library-registered or structural type override makes newProps.type differ from the model base's parser-derived type, so composeFrontmatter's unchanged-check (yamlDoc.ts:247, which requires op.type === np.type) sees a FALSE DIFF and rewrites a key the user never touched; when the override is 'list' with no listItems, serializeLine splits the scalar value on commas, corrupting it into multiple list items.

**Scenario:** User once set key `author` to type 'list' in the library registry (setType persists it library-wide). Another note in the library has `author: "Smith, John"`. User opens it and edits a completely different property -> save -> the diff fires on `author` (text vs list) -> serializeLine writes `author:` as a block list of TWO items `Smith` and `John`. An unedited scalar was silently split/corrupted on disk; repeat for every registry-typed key on every note in the library.

## [MED] `src/lib/libraries/store.ts:1583` (frontmatter-property-writes)

parseFrontmatter's inline flow-list parser splits `[...]` naively on ',' with per-fragment quote stripping, so a quoted item containing a comma (`aliases: ["Smith, John"]`) projects as two broken items; the corruption is latent (no diff while unedited) but is written to disk the next time the user edits that list (add/remove a chip), silently splitting the alias/tag and diverging note_aliases.

**Scenario:** Note has `aliases: ["Ibn Taymiyyah, Taqī al-Dīn"]`. User adds a second alias via the PropertyEditor chip input -> addTag builds listItems from the mis-split projection -> composeFrontmatter writes a block list of THREE items: `Ibn Taymiyyah`, `Taqī al-Dīn`, plus the new one. The original compound alias is destroyed on disk and in the note_aliases index; wikilink resolution via that alias silently breaks. Same pattern corrupts comma-containing wikilink targets in addTypedLinkToProps (store.ts:915) and addTagToProps (+layout.svelte:6005).

## [MED] `src/lib/libraries/store.ts:1596` (frontmatter-property-writes)

parseFrontmatter projects a top-level nested MAP (or a `key: |` block scalar) as an ordinary editable text property with value '' (or '|'), defeating the yamlDoc 'preserve + read-only' decision (projectProps deliberately skips these, but PropertyEditor uses THIS projection): typing anything into that innocuous-looking empty field makes composeFrontmatter splice the entire hidden structure and replace it with the typed scalar (yamlDoc.ts:256).

**Scenario:** Note has `metadata:` with nested `author:`/`year:`/`publisher:` children (e.g. from a Zotero/Obsidian plugin). PropertyEditor shows a row `metadata` with an EMPTY value — the children are invisible. User types 'imported' into the empty field -> save -> the whole nested map is deleted from disk and replaced by `metadata: imported`. The user never saw the data they destroyed and gets no warning. Same for multi-line block-scalar values shown as a bare '|'.

## [MED] `src-tauri/src/sources/mod.rs:480` (frontmatter-property-writes)

rewrite_frontmatter_sources (and its content_type twin at line 1053) strips lines by `trim_start().starts_with("sources:")` — a purely line-based match that also deletes INDENTED look-alike lines living inside a nested map or a block scalar, silently removing user content that merely mentions the key at any depth.

**Scenario:** Note frontmatter contains a nested map `citation:\n  sources: primary\n  page: 12` (or a block scalar whose literal text includes a line `sources:`). User accepts a CECE suggestion or sets sources manually -> sources_set_manual -> the rewriter drops the nested `sources: primary` line out of the citation map (and, if the matched line had an empty value, also swallows the following `- ` lines), then appends its own top-level block. The nested structure is silently mutilated on disk; no error, and the note still parses, so nothing ever flags it.

## [MED] `src-tauri/src/universe.rs:757` (persisted-json-state)

The same-name consolidation in set_active_universe moves each child entry (including the .constellation directory) with a swallowed `let _ = fs::rename`, then UNCONDITIONALLY repoints the registry entry to the parent (lines 764-770) and saves it (line 887) even when renames failed — leaving the registry pointing at a root that has no .constellation.

**Scenario:** Universe at C:\Name\Name activates while Windows Defender / the search indexer / a sync client holds an open handle on any file inside .constellation (search.db is a prime target) → the directory rename fails silently (Windows cannot move a dir with open handles) → other entries move, .constellation stays nested → registry is repointed to C:\Name anyway and saved. Every subsequent boot activates C:\Name: settings.json, collections.json, workspaces.json, session.json, review-pulse.json all resolve to a nonexistent C:\Name\.constellation → the app silently presents an empty universe state (no collections, no workspaces, no session tabs, default settings) while the real state sits stranded in the nested folder; notes are also scattered across two levels (the partial move). No error is ever surfaced — the consolidation guard (parent has no .constellation) even re-arms and repeats the partial

## [MED] `src-tauri/src/sources/mod.rs:468` (cece-sources-derived)

rewrite_frontmatter_sources (and rewrite_frontmatter_content_type at line 1043) silently returns the content UNCHANGED when the note starts with '---' but has no closing '\n---', yet every caller still reports success, mirrors the new values into note_meta, and clears the suggestion row — DB says classified, disk says nothing, and the value silently disappears on the next reindex.

**Scenario:** A note whose first line is '---' used as a markdown horizontal rule (or frontmatter whose closing fence was lost) is classified. User accepts the suggestion or sets sources manually: rewrite_note_sources_on_disk's gate_rmw closure gets rewritten==content (the 'Malformed frontmatter — leave alone' early return at mod.rs:468-471), still sets effective=ids and returns Ok; sources_set_manual (mod.rs:667-679) then writes effective to note_meta.sources and clears the suggestion; bulk accept_one (bulk_ops.rs:317-333) does the same via Ok(None). UI shows the classification applied; Sight/provenance surfaces read it from note_meta. On the next reindex of that file (boot reconcile, any editor save), extract_sources finds nothing on disk and overwrites the mirror — the classification silently evaporates and the suggestion the user acted on is gone. No error at any point.

## [MED] `src/lib/lens/store.ts:132` (frontend-write-callers)

updateNoteProperty() returns void (indistinguishable from success) when flushOpenTabOrAbort aborts, and its caller BaseTab.commitEdit (BaseTab.svelte:257) then applies the optimistic update `row.dimensions[dim] = next` with saveError left null — the table displays a property value that was never written to disk or index.

**Scenario:** The edited row's note is open with unsaved edits and its flush is not durable (writer-lock contention / transient FS error). flushOpenTabOrAbort returns false → updateNoteProperty silently returns before invoke('update_note_property') → BaseTab's try block completes without throwing → the cell shows the new value, saveError stays null. The property edit itself is dropped forever (it lived only in the local `next` variable — save-health retries the FLUSH, not the property edit); the next table refresh silently reverts the cell to the old value with no error ever surfaced. Same silent-abort shape exists in toggleTaskReconciled (store.ts:889) and resolveStructuralConflict (store.ts:4219), but only the BaseTab caller converts it into displayed false state.

## [MED] `src/routes/+layout.svelte:3246` (frontend-write-callers)

The watcher reconcile's reindex of externally-changed paths is swallowed on both branches: `try { await invoke('reindex_changed_paths', …) } catch {}` (≤250 paths) and `.catch(() => {})` (line 3250, large burst) — a failed reconcile is never retried and the changed paths are discarded.

**Scenario:** External edit (Syncthing/Obsidian/git pull) lands while the DB writer is busy (e.g. a large embed/reindex holds the lock) and reindex_changed_paths errors. The catch swallows it, reindexPaths are dropped, loadAllStats/refreshLibraryCaches proceed reading the STALE note_meta rows. The externally-changed notes remain absent/stale in FTS, Quick Switcher, backlinks and counts indefinitely — the very watcher-index-freshness surface (v3.32) silently fails open, with no retry queue and no user-visible signal.

## [MED] `src/lib/libraries/store.ts:432` (frontend-write-callers)

Every post-durable-save reindex is fire-and-forget with a fully swallowed error — navFlushEnv here, plus the same `invoke('constellation_search_reindex',…).catch(() => {})` at store.ts:414/1142, NoteEditor.svelte:229/271/296/355/370, +layout.svelte:1556 — and because internal writes call markRecentWrite (watcher echo suppressed), NO other path re-indexes the note when this call fails.

**Scenario:** A save lands durably on disk, then constellation_search_reindex fails (DB busy/locked at that instant). The error is swallowed; the watcher won't reconcile (own-write suppression); nothing retries. note_meta/notes_fts/note_links/sky_links silently diverge from the .md source until the user happens to edit and successfully re-save that same note — search, backlinks, Index panel, and counts all serve stale data for it with zero surfaced signal. Ironically navFlushEnv's own comment names stale-index divergence as the app-killer it exists to prevent, while swallowing its own reindex failure.

## [MED] `src-tauri/src/search.rs:8709` (boot-init-ordering)

Version-bump rebuild swallows the search.db remove_file failure (`let _ = std::fs::remove_file(&path)`), then unconditionally stamps the .version file as current at :8721 — a rebuild that never happened is recorded as done, permanently.

**Scenario:** App update bumps current_version 7→8 (a tokenizer/schema change that init_db's in-DB migrations do NOT cover — that is the only reason the file-level rebuild mechanism exists). At first boot on Windows, Defender/OneDrive/Syncthing holds a handle on search.db → remove_file fails silently → init_db opens the OLD un-rebuilt database → :8721 writes version '8' to the stamp file. From now on needs_rebuild is false on every boot: the index runs forever on the old tokenization/schema (wrong/missing search results, derived surfaces diverged from .md truth) with no error, no retry, and no way to detect it short of manually deleting search.version.

## [MED] `src-tauri/src/review.rs:1052` (boot-init-ordering)

sync_action_to_row silently skips syncing an explicit ✓-reviewed/snooze/dismiss into review_schedule when state.db is None (boot-init window / mid universe-switch), and additionally swallows the write error with `let _ = f(conn)` when the conn exists — and nothing ever re-derives the row afterward.

**Scenario:** Cold boot on a large universe: init_db takes 20-40s while state.db is still None; auto-restored tabs are already interactive. User opens a note's Review tab and clicks ✓ Reviewed (or snooze/dismiss) inside that window. mark_reviewed writes review-pulse.json (the history source of truth) successfully and returns success, but the review_schedule row-sync is silently skipped. Per the module's own design comment (:1041-1045), upsert_schedule_row thereafter reads last_reviewed/interval from THE ROW, not from review-pulse.json, and review_backfill is a stamped one-shot — so the stale row is re-perpetuated on every future save: the note stays in the due queue forever at its old due date despite being reviewed, with zero error anywhere. Same permanent divergence if the UPDATE fails under lock contention (`let _ = f(conn)` at :1056).

## [MED] `src-tauri/src/canonical.rs:951` (reactivity-concurrency)

`let _ = fs::remove_file(file_path)` after copying a note to its restored filename swallows the delete failure with zero retries (contrast gate_delete/atomic_write which retry the Windows AV/indexer sharing-violation class 5×), yet `restored += 1` still reports success — the note silently persists at BOTH paths with identical content (twin dead-code instance in canonicalize_execute at canonical.rs:561).

**Scenario:** During the boot repair sweep, Defender/an indexer holds note X's old canonical file open at the instant of remove_file (the documented #1 real-world transient on Windows — the reason write_gate's atomic replace and gate_delete both retry 5×). The remove fails, the error is discarded, the file is counted as restored. The library now contains TWO notes with the same title/content (old canonical + new human-named); the next reconcile indexes both, wikilinks by title resolve ambiguously, backlinks and counts double, and there is no error, no journal line, no UI surface — the user discovers a phantom duplicate days later with no trail to explain it.

## [MED] `src-tauri/src/libraries.rs:5281` (freeze-and-leaks)

collect_library_notes is a SYNC command (no (async)) that recursively walks an entire library AND full-read_to_string()s every canonical .md for its frontmatter title, all on the WebView2 IPC dispatch thread — and the second screen re-triggers it for EVERY library 3s after EVERY library-changed/note-created event.

**Scenario:** Second screen open on the 7,600-note universe (E:\Cognitive Knowledge). User types in the main window; the debounced save fires library-changed; 3s later SecondScreenPage.svelte:686 calls loadAllData() → :431 invokes collect_library_notes per library. Being sync, each call runs inline on the main/dispatch thread (the project's own annotations at libraries.rs:536-542 and :1879-1881 document that sync bodies starve every other IPC on the UI thread) — the recursive walk plus ~7,600 fs::read_to_string calls (extract_frontmatter_title_quick at :5322 reads the WHOLE file despite the 'first 1KB' comment) stall the thread for hundreds of ms to seconds. During the stall every queued IPC — including the main window's write_note debounced save and reindex — parks behind it and both windows' event loops hang. No error is ever surfaced; the user just gets a recurring whole-app micro-freeze after ever

## [MED] `src/lib/editor/livePreview.ts:242` (freeze-and-leaks)

_imageCache (and _embedCache at :261) are module-level Maps with ZERO eviction — no size cap, no clear on universe switch, nothing anywhere in the codebase deletes entries — and _imageCache stores full base64 data-URLs keyed per (libraryPath|notePath|filename), so the same image is cached once PER EMBEDDING NOTE.

**Scenario:** A long session browsing image-heavy notes: each ImageWidget.toDOM cache-miss invokes resolve_embed_image and stores the complete data-URL string (a 2 MB photo ≈ 2.7 MB string) forever; a screenshot embedded in 10 notes is retained 10 times under 10 keys. _embedCache additionally retains the full note_body of every transcluded note ever rendered. Over hours of use the webview silently accumulates hundreds of MB, degrading typing latency and eventually pressuring the WebView2 process toward OOM — no error, no signal, just gradual slowdown until restart. (grep confirms only .get/.set on both maps; no .delete/.clear exists.)

## [MED] `src/lib/editor/livePreview.ts:383` (freeze-and-leaks)

_embedCache/_imageCache keys contain no content hash or mtime and are never invalidated, so a transcluded note or replaced image renders its FIRST-SEEN content for the rest of the session — on-screen embed silently diverges from the .md on disk.

**Scenario:** Note A contains ![[B]]. User opens A (B's body cached in _embedCache under 'lib|A.md|B'), edits and saves B, returns to A. UniversalEmbedWidget.toDOM hits the cache at :383-386 and renders B's STALE body; widget eq() compares only target/paths so no rebuild ever refreshes it. The user reads outdated knowledge presented as current — for the entire app session, across tab switches and reloads of A — with no indicator and no error. Same for a replaced image file (cache hit at :310). Only an app restart resyncs the display with disk.

## [LOW] `src/routes/+layout.svelte:8023` (editor-lifecycle)

Latent rollback landmine: with SINGLE_OWNERSHIP flipped to false (the advertised one-keystroke rollback in ownershipFlag.ts), FocusPane's onflush handler still calls commitFocusSave() unconditionally — commitFocusSave composes from the note MODEL, which under the legacy flag is never updated during a Focus session (the onchange legacy branch writes disk directly at +layout.svelte:8014 and skips editNoteBody), so the exit flush overwrites the file with the stale open-time model content, reverting the entire Focus session's edits on disk.

**Scenario:** Ops rollback scenario the flag exists for: a Boss test fails, SINGLE_OWNERSHIP is set to false and the app rebuilt ('instant, clean rollback with NO half-state' per ownershipFlag.ts:7). User enters Focus on note A (model holds open-time content T0, maintained by NoteEditor's ensure effect), types 500 words — each keystroke writeNote()s T0+edits to disk via the legacy branch. User presses Esc (or hides the window: beforeunload/visibilitychange/idle all funnel to flushNow -> onflush): the guard 'if (SINGLE_OWNERSHIP && focusSessionId)' skips the model push, but commitFocusSave() runs anyway, composes the model (still T0 — NoteEditor with its onDocChange feed is unmounted during Focus), compose succeeds (path matches), and saveNoteSession durably writes T0 over the 500 words. markRecentWrite suppresses the watcher; markSaved marks the model clean — no banner, no conflict, total silent rever

## [LOW] `src/routes/+layout.svelte:6318` (rename-cascade-integrity)

CascadeResult.failed / failed_truncated (store.ts:4191-4195) have ZERO consumers — handleRenameComplete reads only result.rewritten (:6322), no toast/banner exists (flushAllTabsInLibrary's docstring at store.ts:1019-1020 claims 'the user sees cascade results in the toast' — no such toast is wired), so per-file walker rewrite failures are completely invisible to the user.

**Scenario:** During a rename cascade, one backlink .md fails its gated write (AV lock, read-only attribute). The Rust walker records it in result.failed, the IPC resolves Ok, and the frontend discards the field — no toast, no console line, no journal marker. That note's body keeps [[old title]] on disk indefinitely; the rename-stamped alias masks resolution, so the divergence (body text and note_links.target_name out of step with the user's rename intent) never surfaces until the alias net is the only thing holding the link together.

## [LOW] `src/lib/libraries/store.ts:994` (cross-window-integrity)

addLinkToNote's CLOSED-note branch writes the note via writeNote('reviewer_connect') with markRecentWrite (watcher-suppressed via the Rust write_gate) but emits NO screen:note-saved broadcast — unlike every other main-window content write path (saveTabContent:1139, navFlushEnv:431, task toggle:4262, NoteEditor saves) — so a second-screen view of that note is never told the disk changed.

**Scenario:** SS is displaying note X read-only (peek / dashboard recent / index-term view — all independent of main's open tabs). In the main window the user connects X to Y from the Reviewer while X has no open tab: the closed branch rewrites X's frontmatter on disk. No screen:note-saved is emitted, the watcher event is suppressed as an app-own write, and SS's library-changed listener only reloads the note-name list, never open-view content. The SS keeps showing X's pre-connect frontmatter/content indefinitely (until some later save of X elsewhere), silently diverged from the .md on disk.

## [LOW] `src/lib/libraries/store.ts:994` (frontmatter-property-writes)

The CLOSED-note branches of addLinkToNote (and addTagToNote at +layout.svelte:6033) do frontend read-modify-write (readNote -> composeUpdatedContent -> writeNote) with the per-path lock held only for the final write, unlike the Rust writers which run the whole cycle under gate_rmw — a concurrent gated writer (sources_set_manual, content_type_set_manual, a batch op) landing between the read and the write is silently clobbered by the stale full-content write.

**Scenario:** User multi-selects notes and batch-adds a tag while the CECE Cataloger's accept flow writes `sources:` to one of the same (closed) notes: addTagToNote read the note before the sources rewrite landed, then writes its stale read + tag back — the freshly-accepted `sources:` list vanishes from disk. note_meta.sources (already mirrored to the accepted set) now diverges from disk until the tag-add's own reindex re-extracts and quietly reverts it; the accept appears to have succeeded but left no trace.

## [LOW] `src-tauri/src/review.rs:1056` (persisted-json-state)

sync_action_to_row runs the review_schedule row-write for ✓ Reviewed / snooze / dismiss as `let _ = f(conn);` — a failed SQL write (SQLITE_BUSY beyond timeout, I/O error, mid-teardown conn) is discarded while mark_reviewed/snooze_note/dismiss_note still return Ok(()).

**Scenario:** User dismisses a noisy note from the Reviewer → dismiss_note updates review-pulse.json, then review_row_dismiss fails (e.g. the writer connection errors during a universe-switch teardown race) → swallowed → command returns Ok, frontend removes the note from the queue. The review_schedule row still says due, so on the next get_due_notes the 'dismissed' note resurfaces in the queue — pulse JSON and the derived table silently diverge until the next periodic reconcile (which may be next boot) recomputes from the pulse. Mitigated by the reconcile self-heal, hence LOW, but the divergence window is real and fully silent.

## [LOW] `src-tauri/src/universe.rs:239` (persisted-json-state)

Legacy-migration moves/copies of the user's saved base definitions swallow errors: migrate_to_constellation moves bases/*.json with `fs::rename(...).ok()` (line 239) and migrate_legacy_data copies them with `fs::copy(...).ok()` (line 1683) — a failed transfer leaves a base out of the new .constellation/bases with no error and no retry (the migration is gated to run once).

**Scenario:** During the one-time flat→.constellation migration, one bases/*.json is locked by AV/sync at rename time → .ok() swallows → migration completes 'successfully' and never re-runs (cdir now exists, so the `!old_meta.exists() || cdir.exists()` gate blocks re-entry). The user's saved table view silently never appears in the migrated universe; the orphan file sits in the old location the app no longer reads. Legacy one-shot path, low reachability today — hence LOW.

## [LOW] `src-tauri/src/search.rs:8365` (boot-init-ordering)

spawn_wal_checkpoint_daemon spawns an immortal `loop {}` thread on EVERY successful init (once per universe switch, since invalidate_search_state forces a re-init) with no generation check and no exit condition — threads and periodic stale-path connections accumulate unboundedly across a long session.

**Scenario:** User switches universes A→B→A→B over a long session: each switch's ensure_search_db_ready (:8823) spawns a new daemon bound to that universe's search.db path; old daemons never die. After N switches, N threads each wake every 300s and Connection::open the (possibly stale) path — reopening handles on the OLD universe's search.db (on Windows those transient handles can also make a later external rebuild/delete of that file fail), and duplicate daemons TRUNCATE-checkpoint the active DB concurrently. If a user resets a universe by deleting its search.db while its stale daemon lives, the daemon's Connection::open silently re-creates an empty search.db in the residual .constellation dir. No error surfaces anywhere; growth is unbounded for the process lifetime.

## [LOW] `src-tauri/src/lens/query.rs:389` (reactivity-concurrency)

User-authored .base lens definitions are rewritten with truncate-then-write std::fs::write (update_base_columns lens/query.rs:389, update_base_order :417, save_workspace_base bases.rs:630) — no same-dir tmp+rename, unlike the project's own established pattern for universe.json (universe.rs atomic_write, whose comment names plain fs::write a torn-file hazard) and libraries.json (libraries.rs:197 tmp+rename).

**Scenario:** The user reorders columns in a BaseTab (update_base_columns fires); the OS crashes / power cuts / the process is killed after fs::write truncated the .base but before the new YAML flushed. On next boot the .base file is zero-length or torn YAML: parse_lens_yaml fails and the user's hand-built lens (scope, filters, columns, sort) is gone. Nothing logs the loss at write time — the write appeared to succeed to the session that made it; the corruption is only discovered when the base is next opened, with no recovery path (no tmp survivor, no journal entry — .base writes bypass the write gate entirely).

## [LOW] `src/lib/components/NotePane.svelte:777` (reactivity-concurrency)

The 30s idle-save interval queues `requestIdleCallback(() => doSave())` whose id is never stored or cancelled; onDestroy clears the interval but a pending idle callback survives the {#key} teardown, and since the teardown doFlush does NOT clear `dirty` (and handleFlush is cascade-gated to a no-op during a rename cascade), the zombie doSave can later push the destroyed editor's pre-cascade text back into the freshly-reloaded same-id/same-path model.

**Scenario:** User types in note A (which links [[B]]); within the 1.5s debounce window they commit a title rename of B. handleRenameComplete marks cascading, flushes A's model, the cascade rewrites A on disk ([[B]]→[[B2]]), reloadTabsFromDisk bumps reloadVersion → the old NotePane is destroyed with local dirty=true (its doFlush was cascade-gated). If the 30s idle tick fired in the last frame before teardown, its uncancelled requestIdleCallback is still queued; the browser defers it through the cascade's awaits and runs it AFTER clearCascading. doSave (dirty→false, view null) calls onsave(latestText = pre-cascade snapshot with [[B]], mountedFilePath = A) → handleSave passes every guard (filePath === tab.path — same tab; cascade/reseed gates already lifted) → editBody dirties the reloaded model with the stale body → the durability gate writes it: the cascade's rewrite of A silently reverts on disk, lea

## [LOW] `src-tauri/src/perf_trace.rs:37` (freeze-and-leaks)

TRACE_LOG is a static append-only Vec<(String,u64)> that records EVERY IPC command dispatch (wired at lib.rs:656) for the process lifetime; clear_perf_trace_log is 'not currently called from the frontend', so it grows unbounded in release builds.

**Scenario:** Constellation left running for days (the normal PKM pattern): every keystroke-driven save, watcher event, search, boot fan-out and panel refresh appends a heap-allocated command-name string. High-IPC stretches (bulk imports, rename cascades, backfills emitting thousands of commands) push the Vec to tens of MB of never-freed allocations plus a mutex lock+push tax on every dispatch. Nothing surfaces it; memory just creeps up until restart. A Round-6 boot diagnostic left permanently on in production.

