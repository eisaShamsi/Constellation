# Handover — 2026-09-15 (PJ-466 CLOSED)

**Read `docs/Constellation Orientation & Onboarding v4.35.md` first, then this.**
Supersedes `HANDOVER-2026-09-14.md`. Branch `main`, clean. **No untested code on `main`** — Stage 1
was Boss-passed on his screen before the commit.

---

## 1. What closed, and the one sentence that explains it
**PJ-466 + PJ-469 + PJ-470 + PJ-471**, all in `5e3e6857`. The Mold Repair Door's receipt made false
statements — and it was **not four wording bugs, it was one missing field**. `MoldRepairOutcome`
carried only `ok: bool`, and `failed` was computed by subtraction, so nothing could separate *"left
alone, nothing owed"* from *"still broken"*. A file that needed nothing halted the whole cascade; a
real write failure was called **"skipped"** in fifteen languages.

Now: `MoldOutcomeKind` (`repaired` | `kept` | `failed`) + a stable `reason` code + `backed_up`,
counted by class. **The shape was not invented** — `phantom_prune.rs`'s `PruneReceipt` already ships
it, rendered in the same Settings page as this door's own entry point.

## 2. Four of the defects fixed were MINE — read v4.35 §"FOUR of the defects"
A boot regression I measured at **7,768 ms** on his 2.03 GB universe · a safety guard wired to the
**wrong loader** (a rule `index_repair.rs` already enforces as a test; `mold_repair.rs` was missing
from its list) · PJ-469 fixed at the **wrong layer** with a false receipt line in it · and a
placeholder mismatch **two translators** caught that neither inspection round did.

**The fifth was in the TEST:** the wrap panel found Step 4 presented as optional when it was the only
thing that made the test test anything. Every count would have read identically. A false PASS.

## 3. NEW STANDING ORDER (2026-09-15)
> *"Bring the tutorial in detailed steps here, and consider it a standing order for my future tests
> and examinations."*

`CLAUDE.md` Testing Instructions Rule **point 5**. The complete tutorial goes **in the chat**; the
`lab/reports/PJ-NNN-BOSS-TEST-*.md` copy is the archive. Write both.

## 4. METHOD — two things that will save the next session real time
- **`%APPDATA%` is NOT readable from this session.** `universes.json` there returns a frozen
  7-August copy naming a universe that is not the current set; a byte-identical shadow sits under the
  Claude package's `LocalCache\Roaming\`. A wrap-panel lens moved to **BLOCK a whole test** on that
  file. Use `E:`-resident witnesses instead: each universe's own `owner.info.json` and
  `boot-perf.latest.json`.
- **Before inventing a shape, grep for the one the house already renders.** Three of four design
  lenses proposed inventing an outcome taxonomy *while telling the panel no precedent existed*. An
  attacker found it in `phantom_prune.rs`.

## 5. State of his data
Scratch is **clean**: this round's staging and the residue from 2026-09-11 / 09-13 (two folders, 10
backups) all removed on his word. **Disclosed residual:** the 8 deleted files still have rows in
Scratch's `search.db`, so Scratch will show a drift/phantom notice on its next boot — the app's own
machinery working as designed. Filed **PJ-495**. The four real universes return **0 molds**.

## 5b. Docs — 10 updated, and the probe lesson
The help topic and the User Manual both described this door and both carried the sentence PJ-466 had
just falsified. **8** translated manuals carry the passage (ar de es fa ja ko ru zh) and were
corrected + extended; **6** do not (fr he hi pt tr ur — **PJ-496**). I found the 8/6 split only after
a first grep with an **English** label returned a confident 0/14. **Probe a translated file with that
locale's own shipped string, never with English.**

## 6. Where to look
Ledger **v2.16** · orientation **v4.35** · `SESSION-LOG-2026-09-14.md` §21–§22 and `-15.md` ·
`PJ-466-PANEL-VERDICT-2026-09-14.md` · `PJ-466-BOSS-TEST-2026-09-14.md` (the approved tutorial,
with the staging and cleanup sections) · `docs/MoCh/MoCh-2026-09-14-1730.md`.

## 7. Ready-to-paste prompt for the next session
```
Read docs/Constellation Orientation & Onboarding v4.35.md first, then
lab/reports/HANDOVER-2026-09-15.md. DRAIN cycle — fix the backlog, run NO new whole-app hunt.

STANDING ORDERS (binding): consult the panel before any action — anything that writes, commits,
builds or touches his data goes to the panel first, with its verdict shown to him before
proceeding; reads and measurements are free, but any finding reaches him only after an independent
check that could contradict it; he tests every build before its commit. CHECK WHAT IS ALREADY BUILT
BEFORE YOU ASK — open the surface and read its controls before putting any question to him. AND
(2026-09-15): DELIVER EVERY TEST TUTORIAL IN THE CHAT, in full detailed steps — the file in
lab/reports is the archive, not the delivery.

KNOW THIS ABOUT HIM: the Style Setter IS the theme control and Saved Styles ARE the themes. He runs
colorScheme: light in all nine universes and calls the built-in dark theme unsuitable — do not design
tests inside it.

STATE: main clean after 5e3e6857. PJ-466 + PJ-469 + PJ-470 + PJ-471 CLOSED, Boss-passed on his
screen, all 8 items of Stage 1. Scratch cleaned of all three rounds' test residue.

NEXT ACTION: PJ-475 — HIS request, which he timed for after PJ-461: switch a Saved Style from the
command palette. loadStylePresets() and applyPreset() already exist and are standalone; only
app-level wiring is missing. Three open questions are in the ledger entry — under the 2026-09-14 law,
check each against what the Style Setter already offers before any of them reaches him.

METHOD: %APPDATA%\world.uconstellation.app is NOT readable from this session — it returns a frozen
7-August copy, and a panel lens once moved to BLOCK a whole test on it. Use E:-resident witnesses
(each universe's own owner.info.json / boot-perf.latest.json). Before inventing a shape, grep for
the one the house already renders — three of four design lenses proposed inventing a taxonomy that
phantom_prune.rs already ships. And a check that cannot disagree is worthless: the i18n gate was RED
before the merge, which is why its GREEN means something.
```
