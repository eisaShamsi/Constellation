# Handover — next session (MIG-081 rich calendar continues; + MIG-080 §B–§F)

**Prepared:** end of a marathon session, 2026-06-17. **Branch:** `main` (all pushed, through `9e6ae75f`). **Active universe:** "Eisa Cognitive Knowledge" (7,651–7,653 notes, 1.97 GB). **Binary rebuild:** frontend change → `npm run build` THEN `cargo build --release`; close the app first (it locks the .exe).

**Read first:** the latest orientation (`docs/Constellation Orientation & Onboarding v2.90.md`), then `lab/reports/SESSION-LOG-2026-06-17.md`, then this file. Design docs: `docs/MIG-081-Architect-Cultural-Calendars.md`, `docs/MIG-081-Rich-Calendar-Design.md`, `docs/MIG-080-Architect-Right-Sidebar-Note-Context.md` + `docs/MIG-080-Plan.md`.

## SHIPPED + Boss-validated this session (on `main`)
- **MIG-079 §C.2d** — deferred the Sky read off boot (cold `graph_ready` ~11 s → ~1.5 s; async-ify + lazy-on-open + after-idle warm-up + `_skyEpoch`/`_skyGeneration` guards). + the ×15 i18n cleanup for the §C.2c summary toggles.
- **MIG-080 §A / §A.2** — Calendar removed from the right rail; Daily-Note **launcher** + a full-page **Calendar** view on the left dock; file-tree-refresh fix. (Right-rail §B–§F still pending — see below.)
- **MIG-081 §A/§B/§C/§C.2a/§C.2b** — vendored Eisa's astronomical Hijri engine (`src/lib/calendar/hijri.js`, pinned to `eisaShamsi/hijri-calendar@a06be50e`) + `@js-temporal/polyfill`; `src/lib/calendar/calendarMath.ts` (incl. `buildRichMonthGrid`); a **Calendar** Settings category (primary/secondary system, week-start, week-numbers toggle, daily-note format/folder); a **rich `CalendarPanel`** ported from Eisa's app (ornate header, gold AH/sacred pill, Gregorian-range subtitle, dual-date cells, moon glyphs, event dots, Wk column) — **Boss said "Perfect!"** Cultural calendars: Hijri = Eisa's engine; Persian/Hebrew = Temporal; filenames stay Gregorian ISO.

## NEXT — Boss's 4 new requirements (2026-06-17, captured verbatim-intent)
1. **Month Correction** — a ±1-day-per-Hijri-month control + a corrections list ("Dhul-Hijjah 1447: -1") + Clear All (Eisa's app screenshot). **Engine already supports it:** `HijriCalendar.setCorrection / getCorrection / clearCorrections / getAllCorrections` (vendored, in the public API). Decide placement: in the Calendar **page** (correct the currently-viewed Hijri month — most usable) and/or the Calendar **settings**. Persistence: hijri.js writes localStorage `hijri-corrections` (works in the WebView); consider mirroring into `appSettings` for cross-device sync (Boss call).
2. **Calculation Mode** — Astronomical (Lunar Conjunction) ⟷ **Tabular (al-Tawfīqāt al-Ilhāmiyyah)** selector. Engine default `currentMode='astronomical'`; it reads localStorage `hijri-mode`. **VERIFY the public mode setter** in the vendored `hijri.js` (there's an internal `currentMode = mode` at ~line 3152 + a localStorage load; confirm an exported `setMode`/`setCalculationMode` — if none, add a thin setter or set via the exposed path). Put the selector in Calendar settings (+ maybe the page).
3. **Style Setter → a "Calendar" tab** — wire the `--cal-*` CSS variables already in `CalendarPanel.svelte` (`--cal-header-from/to`, `--cal-pill-bg/-sacred-*/-border/-text`, `--cal-today-from/to/-text`, `--cal-primary-color`, `--cal-sub-color`, `--cal-weekday-color`, `--cal-grid-border`, `--cal-event-holiday/-observance/-special`, `--cal-moon-color`, `--cal-wk-color`, `--cal-note-dot`, `--cal-task-dot`, fonts) into the Style Setter catalog + apply path, as a new **Calendar** category. **Follow the full-center-zone preview rule** (Boss-dictated 2026-06-11; see CLAUDE.md "Style Setter Preview Rule"). This was the planned §C.2d.
4. **Retire the Daily-Note dock button** — the Calendar fully serves daily notes (day-click → daily note already works). Remove the §A launcher dock button + its popover (`+layout.svelte` `showDailyLauncher`/`.daily-launcher-*`); KEEP `handleOpenDailyNote`/`openDailyNote` (the command-palette "Daily Note" command + the Calendar page both use `openDailyNote`). The left dock keeps the **Calendar** button only.

## Then — the rest of MIG-081 + MIG-080
- **MIG-081 §C.2e:** vendor Amiri/Cairo fonts for the calendar (the repo ships the woff2) or map to Constellation's script-font system; **×15 i18n** for the new `settings.calendar.*` + `calendarPanel.weekAbbrev` keys (EN added; the en-fallback chain shows English elsewhere now); 3-agent audit.
- **MIG-080 §B–§F** (the right-rail note-context cascade, Plan approved): §B Tags "All tags"→Dashboard; §C Tasks split (contextual right rail + left agenda + `toggle_task` reindex fix); §D Source Review split (note-scoped + Cataloger); §E Knowledge Health split; §F Review Pulse split (+ full-page reviewer + `record_note_visit` fix); §G reconcile + audit + the deferred inspector360 Settings-UI bug + ×15.
- **Deferred from earlier:** §C.2b §C.2b's §D-1 stays; the §C.2b/§C.2c MIG-079 cleanups (retire `cache_full_links` etc.) — low priority.

## Standing reminders that bit this session
- **Measure, don't guess** (boot-history tool). **WA#5 cross-check before any inventive fix** (the Hijri-engine + Temporal approach was WA#5-confirmed; Eisa then said use HIS engine — always check his repos/intent first). **SO #8 cross-check before tackling a deferred item** (the right-sidebar decision was missed → the §D-Calendar wrong-target; the `sight.calendarSystems` vocabulary was found by cross-checking). **Stop-On-Correction** (the "no Calendar Settings"/"two calendars doesn't work"/"rich like my app" redirects). **i18n gotcha:** `$t` returns the key for a missing key (so `|| fallback` shows the raw key) BUT falls back to the EN translation for non-EN locales → always add EN keys. **Whitespace:** Edit tool mismatches on tab-indented files were frequent — Python string-replace was the reliable fallback. **Test instructions LITERAL.** Close the app before `cargo build --release`; frontend change → `npm run build` THEN cargo, grep `build/` for a new string.
