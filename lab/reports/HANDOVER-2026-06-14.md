# HANDOVER — 2026-06-14 (morning)

Pickup state for the next session. Read the highest orientation (**v2.79**) first, then this.

## Where we are in one line
**MIG-077 (App-Wide Right-Click Context Menus) is mid-build** — Architect + Plan Boss-approved (Option A), Phase A steps **A0/A1/A2 shipped + Boss-validated**, **resume at A3 (OrgChart)**. Everything is committed + pushed; the tree is clean.

## (a) Verified-shipped + protected this session (8 commits, all pushed to `main`)
| Commit | What | State |
|---|---|---|
| `7a2bc03b` | MIG-076 §E-1 universe-wide collision dialog + trash de-collide | Boss-validated |
| `6bc7beb7` | MIG-076 §E-2 write-integrity diagnostics line (§E CLOSED) | Boss-validated |
| `c2648a75` | Delete-path: "Deleted files" setting now governs deletion; always recoverable; dropped "Permanently delete" | Boss-validated |
| `1a220187` | Styled batch-delete dialog (#2) + MIG-077 Architect + Plan docs | #2 validated; design approved |
| `eb3a246f` | MIG-077 A0 (ContextMenu separator/disabled) + A1 (Tabs menu) | Boss gate PASS |
| `381a471e` | MIG-077 A1 follow-up: instant pin icon (reactive `openTabs.update`) | Boss gate PASS |
| `a999c165` | MIG-077 A2 (Index term menu → shared ContextMenu) | Boss gate PASS |
| `8aab9db2` | MIG-077 A2 follow-up: localize Index "hidden terms" tooltip | Boss gate PASS |

(Prior to this session, `122ac0de` MIG-076 §D was the last commit.)

## (b) RESUME HERE — MIG-077 Phase A, step A3
Plan: `lab/reports/MIG-077-RIGHTCLICK-CONTEXT-MENUS-PLAN.md`. Architecture = **Option A** (shared `ContextMenu` for action-menus + a separate `<ConfidencePicker>`).

**The proven idiom (followed in A1 + A2):** per surface, write `getXContextMenuItems(payload): MenuItem[]` (labels via `$t()`), replace the inline `{#if menu}…{/if}` render with `<ContextMenu x y items onClose>`, delete the inline menu's dead CSS, add any new i18n keys ×15 via a throwaway `_tmp_*.mjs` script (anchor on a namespace key, JSON-validate). `ContextMenu` now supports `{ label?, icon?, action?, danger?, disabled?, separator? }`.

- **A3 — OrgChart** (`src/lib/components/OrgChart.svelte`): inline `.oc-fs-ctx` menu at `~:714` (handler `handleCtxAction`) + render `~:978-986`. 1–2 items (Open for a note; Expand/Collapse for a container). Reuse existing keys (`contextMenu.open`, `sidebar.expandAll`-family) where present. **[Boss GATE]**
- **A4 — extract `<ConfidencePicker>`** (NEW component): the IDENTICAL inline confidence popovers in `BacklinksPanel.svelte` (`:260-277`) and `OutgoingLinksPanel.svelte` (`:208-225`) — header "Set confidence" + 4 swatch radios with active-state + separator + Archive. This is the ONE non-command menu (a radio group) → its own widget, NOT `ContextMenu`. Reuse `linkConfidence.*` keys. Write once, use in both panels. **[Boss GATE]**
- **A5 — GraphMind** (`GraphMindView.svelte:1154`, `.gm-context-menu`): optional, already i18n'd + RTL-aware. Decide at the A4 gate (default: consolidate). Then `/simplify` on the Phase-A diff.

**Then Phase B** (extend to missing surfaces, in order): **B1 List-mode (#1 — the Boss's original ask)** → B2 Search → B3 Tags → B4 Calendar → B5 Sky View → B6 in-editor wikilinks → B7 lower-value. B1 detail: `NavFileItem.svelte` needs `data-path` + a single delegated `oncontextmenu` on `NavFileList`'s scroll container (NOT per-row — the list is non-virtualized; use delegation). Item set is a Boss decision at the B1 gate (proposed: Open · Open in new tab · Rename · Move · Add tag · Delete). **Then Phase 4: 3-agent audit + close.**

## (c) Known-broken
None. svelte-check 0 errors; all builds green; every Boss gate PASS.

## (d) Pending / not started (the backlog)
- **MIG-076 §F** (the only remaining MIG-076 phase): flip `WRITE_GATE_ENFORCE=true` after a clean soak (the §E-2 diagnostic is the instrument — watch "Anomalies" stay at the 2 historical) + the refusal/quarantine/freshness recovery UX + permanent regression suite + 3-agent audit + close. WriteGate is in **shadow mode** until then.
- **Deferred from the delete/right-click work:** the **unified Trash view** (Boss-wanted; lists every `.trash` note with its source library); an **explicit "Delete permanently"** action (engine already supports the `permanent` mode in `delete_path`); the **two trash-setting gaps** (normal `delete_item` ignores `trashDestination`; collision-Overwrite always uses per-library `.trash` — both belong with §F's recovery UX).
- **From the §D arc:** session-restore (reopen tabs on relaunch — Boss wants it); the Focus cursor-to-beginning quirk; residual ~1.4s embed CPU contention.

## (e) Documentation drift
None new. Help/User Manual were updated per-feature (Notes Management → collision + delete; Security & Privacy → Write integrity). MIG-077's menu consolidations are i18n/consistency refinements — no new help topics. The 14 translated help mirrors ride the standing batched-translation debt (unchanged).

## Build discipline (do not skip)
ANY frontend change → `npm run build` THEN `cargo build --release`, then **grep `build/` for the new string** to confirm it's bundled (binary-newer-than-source is necessary but NOT sufficient — see memory `feedback_frontend_build_before_cargo`). Every UI string ×15 locales.
