# Handover — 2026-09-13 session CLOSE (the drain cycle's second session)

**Read `docs/Constellation Orientation & Onboarding v4.33.md` first, then this.**
Supersedes `HANDOVER-2026-09-01-close.md`. Branch `main`, pushed; code commits `7d79daba` +
`d58955c8`, records `b0d8a5d1`, this close in the commit after it. **No untested code is on `main`.**

---

## 1. The standing order — unchanged, and it worked
> *"consult the panel before taking any action."* — Eisa, 2026-09-01

Applied this session: the finding that the door would have failed on Eisa Universe went through
a findings-verifier before it reached him; the fix went to a panel before design; every build had
a diff-scoped inspection and its one confirmed finding was fixed and re-inspected before commit;
the tutorial went auditor → inspector → panel wrap → inspector; every "done" was checked by
something that could disagree (the shipping harness vs the port; the disk vs his screenshots;
the healer's own log line). Both commits landed AFTER his pass.

## 2. What shipped — Boss-tested
| commit | item | Boss test |
|---|---|---|
| `7d79daba` | the mold scan lists each mold ONCE under its owner (top-level own roots + the oracle second pass for roots the parent's walk cannot enter); 3 tests + an `#[ignore]`d real-registry harness | Stage 1 rehearsal + Stage 2 (39) + relaunch ✅ |
| `d58955c8` | banner dismiss flag + count reset on universe switch | Stage 1 Steps 1–4 ✅ |

**PJ-454 is CLOSED: 43 of 43 molds repaired.** The deliberate-failure rehearsal is DONE (the halt
and the ordinary-batch continue both witnessed on his screen). Scratch keeps `mold-rehearsal-2\`
(four repaired copies) — his scratch ground; leave it unless he says otherwise.

## 3. What a fresh session must know to touch the door
- Everything in `HANDOVER-2026-09-01-close.md` §3 still holds (own-libraries only; the
  running-state invariant; six inspection rounds built it).
- **The scan** (`scan_stamped_molds_in`): walk top-level own roots; drop foreign after collection;
  attribute by `library_name_for_path`; then walk any nested own root no collected path fell under
  (dot-folder / junction roots). **No dedupe** — the RED→GREEN tests are the guard. The harness:
  `PJ454_LIBS="<universe>/.constellation/libraries.json" cargo test --lib
  pj454_scan_real_registry_read_only -- --ignored --nocapture` prints rows / distinct / duplicated.
- **Known-wrong halt wording** (filed PJ-466): "Templates fixed" on 0 fixed; "skipped" for a write
  failure; "every file was saved" when batch 2 never ran; nothing about untouched files. One
  concern across three exits; fix as one job before PJ-456's wave.
- **The write journal** is `%APPDATA%\world.uconstellation.app\write-journal.jsonl` (epoch-ms
  `ts`), not in the universe. A blocked repair writes NO line (filed PJ-468).
- **`LNK1104`** on the test exe is transient (seconds); wrap cargo runs in a wait-and-retry loop.

## 4. Next actions (ledger v2.13 ► NEXT ACTION)
1. **PJ-461** — its own build and sitting: 75 usage sites / 21 files (synthesis in the 09-12
   session log §3b/§3c; the scratchpad copy is gone with the session — re-derive from the ledger
   entry, which carries the table). Three decisions in the entry; Boss test is visual, light + dark.
2. **PJ-466** — the halt-screen wording (panel: before PJ-456).
3. Then PJ-456 (by enumeration in folder batches) · PJ-457 (now with stronger evidence) · PJ-458 ·
   PJ-459 · PJ-462 (after the drain) · PJ-463 · the multi-folder templates setting · PJ-264/PJ-378
   unpack · PJ-434 · PJ-438.

## 5. Method lessons paid for this session
- Measure the thing the next click depends on BEFORE the click, with the same rule the app uses.
- An oracle (what the collector actually reached) beats a second copy of its skip rules.
- The panel wrap after the inspector caught three blocking test-logic errors string-matching cannot.
- "31 distinct" was arithmetic error; "same shape in index_repair" was a wrong reading; the
  register sent to a panel must carry my own errors so the panel can correct me in front of him.
- The date in a session-log filename is the session's, not the wall clock's — say which.

## 6. Ready-to-paste prompt for the next session
```
Read docs/Constellation Orientation & Onboarding v4.33.md first, then
lab/reports/HANDOVER-2026-09-13.md. DRAIN cycle — fix the backlog, run NO new whole-app hunt.

STANDING ORDER (2026-09-01, binding): consult the panel before any action. Anything that writes,
commits, builds or touches his data goes to the panel first, with its verdict shown to him
before proceeding. Reads and measurements are free, but any finding reaches him only after an
independent check that could contradict it. Boss tests every build before its commit.

STATE: main clean after `b0d8a5d1` and the session-close commit. PJ-454 CLOSED — 43 of 43 molds repaired
(4 موسوعة عيسى 09-11, 39 Eisa Universe 09-13); the failure rehearsal DONE. The door's scan now
lists each mold once under its owner and walks nested roots the parent cannot enter.

NEXT ACTION: PJ-461 — 75 CSS-token sites across 21 files (the ledger v2.13 entry carries the
table and three open decisions); its own build and its own Boss sitting (visual, light + dark).
Then PJ-466 (halt-screen wording, one concern across three exits) before PJ-456's 107-file wave.

METHOD: measure before the click with the app's own rule; verify a compiled fix by READING the
emitted code around a literal unique to it; wrap cargo runs against transient LNK1104; the write
journal is in %APPDATA%; the mount is the tab-store write; an empty findings list is a pass only
if the agents completed.
```
