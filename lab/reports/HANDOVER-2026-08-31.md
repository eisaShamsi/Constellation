# Handover — 2026-08-31 (after midnight)

**Read `docs/Constellation Orientation & Onboarding v4.25.md` first, then this.**

Branch `main`, pushed. The PJ-435 close landed at **`e48ff80e`**; this session-close PCS (doc
rename + records) lands as the commit on top of it.

---

## 1. Where things stand in one paragraph

**PJ-435 is CLOSED — Boss-tested twice.** A moved universe now detects itself (persisted
`relocation.json`; a copy never arms, a second move chains, moving back home disarms), tells the
truth (the honest banner ×15 replaced the destructive drift alarm), and repairs on one click
(mig108 engine: verified backup, one transaction, conditional destination purge, post-repair app
reload). "Keeps everything" was MEASURED, not asserted: 1,000/1,000 link `created` dates
byte-identical to the pre-repair backup; 501/501 review rows re-addressed. Between his two passes
the mandatory diff-scoped safety inspection found **8 findings — all fixed RED-first** (suite
**1,613/0**, binary 2026-08-30 21:23:37). At session close, on the Boss's order, the **Linked
Universe naming ruling was applied to every living user doc** (English manual, 11 help topics,
14 translated manuals) — prose says "Linked Universe"; verbatim quotes of still-unrenamed
on-screen labels stay letter-true to the screen. **Next: PJ-433** (the silent boot fallback
persisted as the user's choice), then PJ-438 / PJ-434; PJ-437 (identity-relative addressing)
remains the direction that outlives them all.

## 2. The single most important thing to absorb

**A gate is not an oracle — in either direction — and this session proved it three times with
primary evidence.**

- The safety inspection (a gate I convened) found 8 real defects AFTER the Boss's pass — my
  "CLOSED" records were premature and had to be re-trued. The gate was right; I was early.
- A re-inspection finding's premise ("a read-only attribute defeats `remove_file`") was REFUTED
  by a standalone Rust probe: **std 1.94 deletes read-only files natively.** The structural half
  (an unconditional "removed" log) was real and got fixed; the premise did not ship as fact.
- The review panel ordered "Eisa Cognitive Knowledge" renamed to "كون عيسى" in the re-test after
  reading `%APPDATA%\world.uconstellation.app\universes.json` — a file whose mtime (2026-08-07,
  three weeks stale against a night of registry-writing actions) exposed it as the
  **MSIX-virtualization ghost** this session had already documented. The Boss's own passed
  Step 12, on screen, was the authoritative evidence. Edit refused, with the receipts.

Corollary for the next session: **sandbox reads of `%APPDATA%` are NOT trustworthy for this
app's live registry.** Rely on on-screen evidence or the fsutil hardlink route.

## 3. What is verified-shipped and protected

- PJ-435, all four phases + 8 inspection fixes, at `e48ff80e`. Two Boss passes (three-stage full
  test on the 17:39 build; 8/8 smoke re-test on the 21:23 build). Ten PJ-435 behavioural/pin
  tests in `universe.rs` / `relocate.rs` / `link_life_restore.rs`.
- CLAUDE.md storage section amended per its own closing instruction: a MOVE no longer costs
  `created` dates or the review schedule; only a deliberate REBUILD still does (owned by PJ-437).
- Manuals ×15: the false "repaired automatically" promise corrected everywhere (per-locale
  verified, button strings byte-matched to each locale's `indexDrift.movedRepairNow`), and the
  Linked Universe naming applied to all living docs (26-file workflow, per-file verification).

## 4. Known-broken / at-risk / open

- **PJ-331 (UI half)**: the RUNNING APP still shows "Child Universes" / "Add Child Universe" /
  "cUniverse" (en.json keys: `universe.setup.addChildUniverse`, `universe.manager.children`,
  `secondScreen.dashboard.childUniverses`, `federation.cuniverseLabel`, `federation.warningBadge`,
  `constellationMap.childUniverse`, `styleSetter.labels.cuniverse` — ×15 locales). The docs
  deliberately keep verbatim quotes of those labels until the UI rename ships (needs build +
  Boss test). This is now the most visible naming drift left.
- **PJ-439** (cosmetic): the relocation backup's db file is named `search.db.pre-mig108`
  (mig108.rs:564 hardcodes it regardless of the PJ-435 `backup_dir_name` parameter).
- **PJ-438**: two racing first-index boot routes; no new evidence from either live test (the Boss
  did not report whether a progress strip appeared).
- Filed, not started: PJ-419 / 420–427 / 429 / 430 / 432 / 433 / 434 / 436 / 437.

## 5. Measured facts worth not re-deriving

- The 91.6 s the Boss saw at the big repair was `mig003_step3_soft_rebackfill` injecting identity
  into 500 generator-made stamp-less notes (~183 ms each) — NOT the path rewrite (<1 s). His
  daily universe has 27/8,033 empty `cid_cn` (14 exempt templates + 13 candidates ≈ seconds, paid
  at an ordinary boot anyway).
- The duplicate-fold prediction held twice live: pre-repair count 3 → post-repair 2, both
  fixtures. The variable pre-repair shape (2/3/4 possible) with an invariant ending of 2 is
  traced in the panel's false-failure lens (workflow `wf_c8e4639a-f7e`).
- Rust 1.94 `std::fs::remove_file` deletes read-only files (probe in scratchpad `rsprobe`).

## 6. Ready-to-paste prompt for the next session

```
Read docs/Constellation Orientation & Onboarding v4.25.md first, then
lab/reports/HANDOVER-2026-08-31.md. PJ-435 is CLOSED (Boss-tested twice, committed at e48ff80e).
The ledger is docs/Constellation Pending Jobs v2.08.md — its ► Next action is PJ-433: the silent
boot fallback (when the last-active universe is unreachable at boot, the app quietly opens a
different universe AND persists that fallback as if the user chose it). Cross-check PJ-433 per
SO#8 before starting (orientation §4.x BODY + session logs), state the function in hand, and
follow the standing orders: panel before any ruling request, tutorial-auditor → ui-inspector →
panel before any test reaches the Boss, findings-verifier before any factual claim lands, Boss
tests every build before commit. Warning carried from 2026-08-31: sandbox reads of %APPDATA% hit
MSIX virtualization for this app's registry — trust on-screen evidence, never that file.
```
