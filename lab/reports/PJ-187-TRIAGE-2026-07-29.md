# PJ-187 — the register, RE-VERIFIED against HEAD (2026-07-29, after the day's three commits)

All 48 still-open findings were re-read against the current tree. **32 STILL-VALID at the
original line, 16 LINE-MOVED, 0 already-fixed, 0 invalid.** Today's commits (PJ-182, PJ-181,
PJ-187 headline) rewrote `parseFrontmatter`, `yamlDoc.ts`, `noteModel.ts`, `propsCommit.ts`,
`PropertyEditor.svelte`, `store.ts` and six Rust writers — and resolved none of these.

**Fix cost: 29 are S (small, local, under ~30 min) · 19 are M. None is L.**

---

### [APP-KILLER] `src/lib/editor/yamlDoc.ts:311` — cost **M**

**User impact.** If a note's property block contains a typo, editing that note's properties looks like it works and reports a successful save, but nothing is actually written — every change silently vanishes the next time the note is opened.

**Fix.** Have composeFrontmatter/composeContent return a discriminated result carrying a `yaml_parse_error` flag instead of a bare string, and make noteSession.saveUnchained return `{ok:false, reason:'yaml_parse_error'}` so the existing save-failure banner surfaces it and the model stays dirty rather than being marked clean.

<details><summary>evidence</summary>

Line 311 is verbatim the described H1 branch: `if (parseDocument(rawYaml).errors.length) return `---${eol}${rawYaml}---${eol}${body}`; // H1`. It returns the ORIGINAL rawYaml and never touches `newProps`. Traced the caller chain: noteModel.ts:136 `composeModel` -> `composeFrontmatter(m.base.rawYaml, m.base.hadFence, m.base.props, m.props, ...)`, and noteSession.ts:247-262 `saveUnchained` does `const r = M.compose(id, expectPath); if (!r.ok) return r;` — a passthrough compose is `ok`, so the write proceeds, then line 258 `M.markSaved(...)`, line 259 `M.noteDiskSynced(...)`, line 261 `e.onSuccess?.(...)` all fire. The model is marked clean and the save-health entry is cleared (store.ts:453 `clearSaveFailure(path)`). Nothing anywhere returns or surfaces the parse-error condition. PJ-182 rewrote the SET/ADD diff below it (samePropRow at line 379) but left line 311 untouched.

</details>

### [APP-KILLER] `src-tauri/src/universe.rs:85` — cost **M**

**User impact.** If the file listing your universes is unreadable for even a moment — an antivirus scan, a sync client, a half-written file — Constellation acts as if you have no universes at all, and the very next thing you do quietly rewrites that list, so every universe you had registered vanishes from the app with no error message.

**Fix.** Make `load_registry` return `Result<UniverseRegistry, String>` that distinguishes "file absent" (empty is a fact) from "unreadable/corrupt" (propagate + rename the file aside as a backup), and make all six registry-write paths (661/666, 718/740, 936/941, 1015/1021, 1054/1082, 1210/1213, 1765) abort with a surfaced error rather than saving a registry built on an empty value.

<details><summary>evidence</summary>

universe.rs is untouched by today's three commits (last change 042802c5, 2026-07-27), and line 85 is still `fn load_registry(app: &tauri::AppHandle) -> UniverseRegistry {`. Read error → `eprintln!("[universe] Failed to read registry ({}): {}", ...); return UniverseRegistry { entries: vec![], active_id: None };` (l.96-99); parse error → `serde_json::from_str(&data).unwrap_or_else(|e| { eprintln!("[universe] Corrupt registry JSON..."); UniverseRegistry { entries: vec![], active_id: None } })` (l.101-104). The write chain is intact: create_universe `let mut registry = load_registry(&app);` (661) → `registry.entries.push(entry.clone());` (662) → `save_registry(&app, &registry)?;` (666), and remove_universe_from_registry 936→941; `save_registry` (156-160) calls `atomic_write` which renames over `universes.json`. Note the SAME file already carries the correct pattern for libraries.json at l.380-401 ("Absent is a fact; unreadable is an unknown") — load_registry was never given it.

</details>

### [APP-KILLER] `src/routes/+layout.svelte:3407` — cost **M**

**User impact.** Right after you accept a suggested source or content type on a note you're currently editing, that new information can silently vanish from the file moments later — the note saves over it with no warning and no way to tell it happened.

**Fix.** Give the Rust announce its own signal — a dedicated event name or an `announce: true` field on the `library-changed` payload — and route those paths straight to `adoptExternalChangeIntoTabs` bypassing the `wasRecentlyWritten` recency filter.

<details><summary>evidence</summary>

Line 3407 is verbatim `if (!wasRecentlyWritten(p)) {` inside the `library-changed` listener (3403-3413); `+layout.svelte` is untouched by all three of today's commits (git diff 11a199e5 HEAD lists only search.rs and store.ts among the finding files). The Rust side is unchanged too: sources/mod.rs:597-606 `announce_frontmatter_writes` still emits plain `library-changed` with `{"libraryId": "", "paths": note_paths}` — no marker distinguishing it from a watcher echo. store.ts:1594-1598 `wasRecentlyWritten` is still the bare 2000ms `recentWrites` window. So an announce arriving within 2s of the note's own autosave is dropped before it reaches `adoptExternalChangeIntoTabs` (3369), the only route from disk back into the model. PJ-181 (8101e918) touched only the write-ahead net's `snapshot` flag, not this path.

</details>

### [APP-KILLER] `src-tauri/src/universe.rs:117` — cost **S**

**User impact.** If two Constellation windows happen to save at the same instant, your settings, your open-tab session, your collections or your list of libraries can be written as a scrambled mix of both saves — and because a scrambled file is read as empty, they silently come back blank.

**Fix.** Give universe.rs::atomic_write the same pid+counter unique temp name write_gate::atomic_write already uses, so two concurrent writers of the same file can never name the same temp.

<details><summary>evidence</summary>

universe.rs is untouched by today's three commits (`git diff --stat 32fcface~1 HEAD -- src-tauri/src/universe.rs` is empty), and line 117 still reads `let tmp = path.with_file_name(format!("{}.tmp", path.file_name().and_then(|n| n.to_str()).unwrap_or("state.json")))` — a temp name derived ONLY from the target, so every concurrent writer of the same file computes the identical path. The contrast the finding names is intact: write_gate.rs:239-244 builds `.{stem}.{pid}-{counter}.cnstmp` from `std::process::id()` + a `TMP_COUNTER.fetch_add`, and link_life.rs:498 states the rule outright — "write them to a UNIQUE temp in the same dir (PJ-087: never a fixed `<name>.tmp`)" — with link_life.rs:1264-1274 carrying a regression test whose assertion message is "a fixed temp name is the PJ-087 collision". Concurrency is real, not theoretical: the callers of this helper are `#[tauri::command(async)]` (verified on save_universe_settings at universe.rs:1459 and save_universe_workspaces at 1503), so they execute on Tauri's async runtime pool, and the second screen is a separate window issuing the same commands against the same `settings.json` / `session.json` / `collections.json` / `property-types.

</details>

### [HIGH] `src/lib/libraries/store.ts:6057` *(moved from :6008)* — cost **M**

**User impact.** If your settings file is briefly unreadable or damaged, the app silently starts up on factory defaults — and the first preference you change afterwards saves those defaults over your real settings, so all your customisation is permanently gone.

**Fix.** Have boot_bundle return an explicit settings-load-failure marker instead of `.unwrap_or({})`, and have the frontend set a `settingsLoadFailed` flag that blocks every `saveSettings()` write and shows a banner until the user resolves it.

<details><summary>evidence</summary>

PJ-182/PJ-181 shifted store.ts by ~117 lines: `export function applyParsedSettings` was at 5939 in the pre-PJ-182 tree (32fcface~1) and is now at 6056, with the defect line `if (!parsed || Object.keys(parsed).length === 0) return;` now at 6057 — unchanged text. The whole chain still holds at HEAD: universe.rs:1446-1448 returns Err on read/parse failure → boot_bundle.rs:100-101 `crate::universe::read_universe_settings(app.clone()).unwrap_or(serde_json::Value::Object(serde_json::Map::new()))` → +layout.svelte:2571-2572 `const parsed = (bundle.settings as Record<string, unknown>) || {}; applyParsedSettings(parsed);` (the only load path — loadSettings at 6263 has no callers) → early return, `appSettings` stays DEFAULT_SETTINGS → `saveSettings` (now 6272-6277) does `invoke('save_universe_settings', { settings: get(appSettings) }).catch(e => console.error(...))`, writing defaults over the file.

</details>

### [HIGH] `src/lib/libraries/store.ts:2649` *(moved from :2600)* — cost **M**

**User impact.** If you click toward a note whose unsaved text is being held in the app's emergency backup, and that click gets cancelled or you click somewhere else first, the emergency copy is thrown away and those unsaved words are gone for good.

**Fix.** Make `resolveNoteContent` never clear the net itself — pass `preserveNet: true` from both nav paths and call `clearWriteAhead(filePath)` only after the tab+model have actually adopted the content, the same only-adopters-clear rule `renameItem` already follows at store.ts:4098-4101.

<details><summary>evidence</summary>

The defect is intact at both nav paths; PJ-181's edits to `resolveNoteContent` changed WHICH buffers are accepted, not WHEN the buffer is consumed. (1) `openNoteTab` (now declared at :2605) calls `const resolved = await resolveNoteContent(filePath, { preserveNet: preserveNet ?? displayOnlyWindow })` at :2649 — in the main window `displayOnlyWindow` is false, so `resolveNoteContent` reaches `if (!opts.preserveNet) clearWriteAhead(filePath);` at :2561 (and :2551 on the reject branch) and the incoming note's net is GONE. The abort/supersede early-returns still sit AFTER it, at :2743 `if (!f.ok) { _traceNav('openNoteTab:flushAbort', ...); return; }` and :2744 `if (_navTokens.get(currentTab.id) !== navToken) { ... return; }` — both before the first `openNoteModel(...)` at :2776, so no model is ever created to hold the consumed content. (2) `loadTabHistoryEntry` consumes at :1697 and then has `if (_navTokens.get(tabId) !== myToken) return;` at :1700, again before `openNoteModel(tabId, ...)` at :1727. NOTE: the batch prompt paraphrased this finding as "openNoteTab calls ensure_cid_cn_cmd unguarded on a display-only window"; that is a DIFFERENT (and separately real — :2668-2684 has no `dis

</details>

### [HIGH] `src-tauri/src/libraries.rs:1270` — cost **M**

**User impact.** Renaming a folder that holds a lot of notes makes the app look frozen — the rename seems to never finish, and typing or saving elsewhere stalls until it does.

**Fix.** Detach the folder branch's descendant DB cascade + per-note reindex into `tauri::async_runtime::spawn_blocking` — a folder twin of the existing `rename_item_db_tail` — so the invoke settles the moment the filesystem rename is final.

<details><summary>evidence</summary>

Verified the register's libraries.rs line numbers are HEAD-relative (its cross-reference ":1371" lands exactly on `tauri::async_runtime::spawn_blocking(move || { rename_item_db_tail(...) });` at HEAD 1371). The folder branch of `rename_item` (`#[tauri::command(async)]`, HEAD 1239-1240) is unchanged and still fully inline: 1269 `collect_md_paths(new_p, &mut md_paths);` → 1270 `if let Ok(guard) = search_state.db.lock() {` held across the whole `for new_desc in &md_paths { ... migrate_note_db_paths(conn, ...) }` loop (1270-1279) → then 1283-1288 `for new_desc in &md_paths { ... crate::search::reindex_single_note(&search_state, &nd, &name) }` → `return Ok(new_path);` at 1290. Nothing is detached. The .md branch's own comment at 1349-1359 states the rule this branch breaks: "an awaited IPC surface must never include an unbounded writer-lock wait". PJ-182 (32fcface) touched libraries.rs only at 899, 1754-1998 and 7128 — never this block.

</details>

### [HIGH] `src/lib/components/PropertyEditor.svelte:448` *(moved from :429)* — cost **M**

**User impact.** Type a property value and switch to another note or tab within about a second, and the edit silently disappears — the note keeps its old value and nothing warns you.

**Fix.** On `tabChanged`, flush the pending timer against the OUTGOING tabId/filePath (the snapshot pair the onDestroy path already keeps) before re-seeding, and add an `onfocusout` flush on the row inputs so an edit is never held only in local rows.

<details><summary>evidence</summary>

The seeding $effect moved from 414-462 to 433-480; the cited predicate is now line 448: `if ((!saving && !localEditPending) || tabChanged) {`, with `const localEditPending = saveTimeout !== undefined;` at 447 — byte-identical to the register base (verified against `git show e4e793be`). PJ-187 (450123d1) added only `seededForPath = filePath;` at 449 and the `rowsBelongToTarget` refusal in commitAndSave (939-965); neither touches the `|| tabChanged` bypass. Its own comment at 958-959 concedes the gap: 'Saving it to its own note instead requires flushing pending PANEL edits at navigation time … — filed.' The debounce is the only route to the model (debouncedSave 996-1025, 800 ms, `commitAndSave(tabId, filePath)` at 1019 reading LIVE identity), all ~16 mutation sites only assign `editableProps` then call `debouncedSave()`, and a grep for `onblur|onfocusout` in the file returns nothing. The right-sidebar instance is mounted at +layout.svelte:9056 with no `{#key}`, so a tab switch does NOT tear it down and the onDestroy flush (578-613) never runs.

</details>

### [HIGH] `src/lib/libraries/store.ts:490` *(moved from :457)* — cost **S**

**User impact.** When a note is locked and you rescue it with 'Save a copy', a copy whose title contains a colon, a leading dash or bracket, or a '#' comes out with a broken title line — the copy shows the wrong name, or loses its name entirely.

**Fix.** Build the new title line with the YAML serializer (the same `yamlStringify({ title: value }, { lineWidth: 0 })` yamlDoc.ts:248 already uses) instead of string concatenation, so quoting is re-applied whenever the value needs it.

<details><summary>evidence</summary>

`rebrandCopyFrontmatter` now begins at line 486 (PJ-181/PJ-182 pushed it down ~29 lines); the defective statement is line 490: `fm = fm.replace(/^title:[ \t]*"?([^"\r\n]*?)"?[ \t]*$/m, (_, title: string) => `title: ${title} (${copySuffix})`);` — the regex consumes the optional surrounding quotes into the capture-free positions and re-emits the bare value. I ran the actual function over four titles with the repo's own `yaml` parser: `title: "Plan: phase two"` -> `title: Plan: phase two (recovered copy)` = 1 parse error ("Nested mappings are not allowed in compact mappings") and the title decodes as the object `{"Plan":"phase two (recovered copy)"}`; `"#hashtag start"` -> parses clean but the value becomes NULL (the `#` starts a comment); `"- dash lead"` -> 2 parse errors; `"[bracket]"` -> 1 parse error, title decodes as the array `["bracket"]`. Call site unchanged at line 518 `const copyContent = rebrandCopyFrontmatter(content, copySuffix);`.

</details>

### [HIGH] `src-tauri/src/sources/bulk_ops.rs:401` — cost **S**

**User impact.** (not stated)

**Fix.** In `accept_one`, capture the step-4 mirror errors instead of `?`-returning them so `Ok(wrote)` still fires the announce, and carry the failure count into the emitted `done` event so SourceReviewPanel can surface it.

<details><summary>evidence</summary>

Line 401 is verbatim `write_sources_to_db(conn, note_path, &merged_h)?;`, immediately after the disk write already committed at 383-389 (`gate_rmw(path, "bulk_accept", …)?`) and after `let wrote = matches!(outcome, WriteOutcome::Ok);` at 393. The `?` on 401/402/403 returns Err before `Ok(wrote)` at 405, so the caller's `Err(e) =>` arm (243-250) never calls `announce_pending.record(&note_path, wrote)` — the note is neither announced nor mirrored. The error goes only to `state.last_error` (247-249); the loop continues and the run still ends with `phase: "done", error: None` (275-283). The single frontend listener, SourceReviewPanel.svelte:1049-1070, destructures only `{phase,total,completed}` and never calls `sources_bulk_accept_status`, so `last_error` is genuinely unread by any UI.

</details>

### [HIGH] `src/routes/+layout.svelte:3382` — cost **S**

**User impact.** When a note is changed outside Constellation and the re-index quietly fails, the note reads correctly on screen but search, the quick switcher and backlinks keep showing the old text until you restart the app — and nothing tells you.

**Fix.** On a rejected `reindex_changed_paths`, re-add the paths to `pendingReindex` and re-arm the flush (bounded retry) instead of swallowing the rejection with an empty catch.

<details><summary>evidence</summary>

+layout.svelte was not touched by today's commits (empty `git diff --stat 32fcface~1 HEAD` for this path), so the line is unmoved. Line 3382 reads `try { await invoke('reindex_changed_paths', { paths: reindexPaths }); } catch {}` — an empty catch — and the large-burst branch at 3384-3386 swallows identically with `.catch(() => {})`. The ordering that makes this a divergence is right above it: line 3369 `await adoptExternalChangeIntoTabs(tabPaths, adoptExternalHooks())` has ALREADY re-pointed the open tabs at the new disk content before the reindex is attempted. The reindex can fail as a whole batch: search.rs:10424 `reindex_changed_paths` opens with `ensure_search_db_ready(&app)?`, which the comment there says exists for "a watcher event arriving mid universe-switch". Nothing retries — `pendingReindex` was already cleared at 3351, and reconcile.rs only removes rows for files that are gone (reconcile.rs:14: "a stale row is just an index entry pointing at a file that is gone"), so nothing repairs a content divergence until the next boot's mtime-gated index walk.

</details>

### [HIGH] `src-tauri/src/write_gate.rs:249` — cost **S**

**User impact.** Every time the app saves a note it also wakes itself up thinking the folder changed, and re-scans the whole folder — so saving inside a large library makes the app stutter or briefly hang for no reason.

**Fix.** Swap the `mark(&tmp)`/`mark(path)` pairs at write_gate.rs:249-250, :598-599, :725-726 and :763 for `mark_with_parent`, so the containing directory is suppressed too.

<details><summary>evidence</summary>

write_gate.rs:246-250 is unchanged: comment "Suppress watcher events for BOTH paths involved in the swap" then `crate::watcher_suppress::mark(&tmp); crate::watcher_suppress::mark(path);` — two keys, not three. watcher_suppress.rs:56-70 documents the contract: "Callers doing an atomic replace must mark THREE keys: the temp path, the final path, and (via this helper) the containing directory" and defines `pub fn mark_with_parent`. `grep -rn "mark_with_parent" src-tauri/src` returns ONLY its own definition (:65) and its unit test (:134,:138,:147) — zero production callers. The leak path is intact: watcher.rs:111-119 passes bare-directory events (`Ok(m) => m.is_dir() || ...` → true) and then filters on `!watcher_suppress::was_recent(p)`, which is exact-path keyed (`HashMap<PathBuf, Instant>`, :36) so the never-marked directory key returns false and the event is emitted. Same two-key-only shape at :598-599, :725-726, :763.

</details>

### [HIGH] `src/routes/+layout.svelte:4394` — cost **S**

**User impact.** Creating a note whose name contains a colon, #, or similar punctuation inside a folder that applies a template leaves the note's hidden info block malformed, so its title and any later property edits silently stop sticking.

**Fix.** Export `quoteIfNeeded` from store.ts and emit `${p.key}: ${quoteIfNeeded(p.value)}` at +layout.svelte:4394 (the same quoter `reconstructFrontmatter` already uses).

<details><summary>evidence</summary>

+layout.svelte was not touched by any of today's three commits (last commit on it is 11a199e5), so 4394 is byte-identical to the register's line. HEAD 4392-4394 reads `const canonicalFields: string[] = parsed.properties .filter(p => canonicalKeys.has(p.key.toLowerCase())) .map(p => `${p.key}: ${p.value}`);` — bare, unquoted re-emission, then joined into `mergedFM` (4398-4401) and written as `---\n${mergedFM}\n---\n` at 4402-4403. The round-trip is confirmed lossy at both ends: Rust `create_note` always writes the title QUOTED (`fm_lines.push(format!("title: \"{}\"", ...))`, libraries.rs:954), and `parseFrontmatter` strips those quotes (`value = unquote(value);`, store.ts:2171). A canonical quoter exists but is not used here — `quoteIfNeeded` (store.ts:2206-2214, module-private) covers exactly `[:{}\[\],&*?|>!%@`#]`.

</details>

### [HIGH] `src/lib/libraries/store.ts:1867` *(moved from :1834)* — cost **S**

**User impact.** If Constellation cannot read your saved collections at startup or when you switch universes, the panel quietly shows an empty list — and the next item you star or add writes that emptiness over the real file, permanently losing every collection and starred note.

**Fix.** Set a module-level `collectionsLoaded` flag only when `read_universe_collections` actually succeeds, have `saveCollections()` refuse to write while it is false, and surface the read failure instead of `catch { /* ignore */ }`.

<details><summary>evidence</summary>

loadCollections is now at store.ts:1855-1868, unchanged in substance; line 1867 is still `} catch { /* ignore — empty until first save */ }` wrapping `const data = await invoke<unknown[]>('read_universe_collections')` (1857) and `collectionSets.set(r.list)` (1865). saveCollections (the writer named at the register's store.ts:1722) is now store.ts:1755-1759 and still does `invoke('save_universe_collections', { collections: get(collectionSets) }).catch(e => console.error(...))` with no loaded-gate. Every mutator (createCollection 1765, addToCollection 1791, removeFromCollection 1797, toggleStarred via 1821/1822) calls it unconditionally. +layout.svelte:2663 confirms `loadCollections().catch(() => {});` runs on boot AND 'every universe switch' (comment at 2662). The three commits today shifted the region +33 lines; nothing in it changed.

</details>

### [MED] `src/lib/editor/livePreview.ts:285` — cost **M**

**User impact.** Previews of notes embedded inside other notes pile up in memory the longer you browse, and an embedded note you have since edited keeps showing its old text until you restart the app.

**Fix.** Replace the bare Map with a size-capped LRU and add an invalidation hook that drops entries whose target note path was just saved or externally changed.

<details><summary>evidence</summary>

Line 285 is `const _embedCache = new Map<string, EmbedResolution>();`. Grepping the whole of src/ returns exactly three references — the declaration (285), `const cached = _embedCache.get(cacheKey);` (407) and `_embedCache.set(cacheKey, res);` (419) inside `UniversalEmbedWidget.toDOM`. There is no `.clear()`, no `.delete()`, no size cap, no TTL, and no invalidation hook anywhere. The `EmbedResolution` interface at 269-284 confirms the payload weight: `note_body?: string | null`, `attachment_folder_listing?: string[]`, `similar_files?: string[]`, `tried_paths?: string[]`, plus `url`. Keys are `${libraryPath}|${notePath}|${target}`, so the same embedded note cached from N different host notes stores N copies of its body. (Its sibling `_imageCache` at line 266 has the same never-cleared shape.)

</details>

### [MED] `src-tauri/src/search.rs:9925` — cost **M**

**User impact.** If the app is force-closed while it's rebuilding its index, some notes' link counts stay wrong permanently — restarting never repairs them, only a manual index rebuild does.

**Fix.** Stamp a `reconcile_outgoing_dropped` row in `schema_versions` before line 9925 and clear it after the recompute at 9943, then have `init_db` (or the first read) see a surviving stamp and run `recompute_all_outgoing` once.

<details><summary>evidence</summary>

Line 9925 is verbatim `let _ = drop_outgoing_link_triggers(&walk_conn);` inside `reconcile_filesystem` (starts 9895); the library walk runs at 9934-9938 and only afterwards does 9942 `create_outgoing_link_triggers` + 9943 `recompute_all_outgoing` run. `init_db` recreates the triggers at 4399 but there is no `recompute_all_outgoing` anywhere in init_db — the only other caller is links_backfill's own once-stamped backfill. And reconcile is NOT a boot step: cache.rs:1511-1541 `cache_reconcile` is the only production caller, and +layout.svelte:2765-2769 only fires it when `totalIndexed === 0` (MIG-067's explicit ZERO BOOT-TIME WALKS rule, cache.rs:1544-1549). So a kill mid-walk leaves note_meta's outgoing aggregates stale until the user manually rebuilds the index.

</details>

### [MED] `src/lib/libraries/store.ts:6440` *(moved from :6391)* — cost **M**

**User impact.** If the file holding your saved workspaces can't be read when the app starts, it quietly behaves as though you never saved any — and the moment you save a new one, every old workspace is wiped permanently.

**Fix.** Stop the Rust `unwrap_or` degrading a read/parse failure to an empty array, and have the frontend hold a `workspacesLoadFailed` flag that makes `persistWorkspaces` refuse to write (with a surfaced warning) until a load succeeds.

<details><summary>evidence</summary>

`git show e3b165cc:store.ts` puts `function persistWorkspaces() {` at exactly 6391; at HEAD it is 6440, with the write on 6441: `invoke('save_universe_workspaces', { workspaces: get(workspaces) }).catch(e => console.error('[save] workspaces failed:', e));`. Both halves of the premise are intact at HEAD: boot_bundle.rs:104-107 still does `read_universe_workspaces(app.clone()).unwrap_or(serde_json::Value::Array(vec![]))`, and +layout.svelte:2578 still gates with `if (Array.isArray(bundle.workspaces) && bundle.workspaces.length > 0)`. The store's own loader (store.ts:6433-6438) repeats the same `length > 0` gate and swallows the failure with `catch { /* ignore */ }`. So an unreadable file and a genuinely empty one are indistinguishable, the store stays `[]`, and the next `saveWorkspace` (6469) writes that `[]` over the real file.

</details>

### [MED] `src/lib/components/PropertyEditor.svelte:603` *(moved from :583)* — cost **M**

**User impact.** A property you just typed can vanish with no trace if you click away to another note, or the note reloads from disk, within the brief moment before the panel saves — no error appears, the value is simply gone.

**Fix.** Flush pending panel rows at navigation/teardown time against the SNAPSHOT identity (mountedTabId + mountedFilePath + seededForPath) so the edit lands on the note it was typed into, instead of being cleared and dropped.

<details><summary>evidence</summary>

At the register's base (32fcface~1) line 583 was the identity gate `if (!readOnly && mountedTabId && mountedFilePath && tabId === mountedTabId && filePath === mountedFilePath) {`; PJ-182/181/187 added 20 lines above it (19 for the `seededForPath` doc block at ~197, 1 at ~446), so it is now line 603. The defect is unchanged: inside `onDestroy` (578), `clearTimeout(saveTimeout)` at 586 runs unconditionally, and then TWO paths discard the pending edit without writing it anywhere — 602 `if (isReseeding(filePath) || isCascading(filePath)) return;` and the 603 gate falling through. Nothing catches the dropped rows on the way out. PJ-187 did not fix it and in fact added a third drop at 960 (`if (!rowsBelongToTarget(seededForPath, path)) { ...; return; }`); its own comment at 955-959 confirms the state of play: "The pending edit is DROPPED, not redirected... That matches what the teardown path already does (its identity gate skips for the same reason). Saving it to its own note instead requires flushing pending PANEL edits at navigation time... — filed."

</details>

### [MED] `src-tauri/src/sky_backfill.rs:154` — cost **M**

**User impact.** If the app is closed or hits an error while it is rebuilding the Sky View in the background, the notes it had not reached yet lose their computed level and maturity for the rest of that session — they show as unclassified until the next restart.

**Fix.** Move the stratum/maturity wipe into process_batch, scoped to that batch's own `(after_path, last_path]` range, so an interrupt never leaves a wiped-but-unrecomputed span (and make is_needed check for a live cursor row as its doc already claims).

<details><summary>evidence</summary>

sky_backfill.rs is untouched by today's commits (empty diff for the path); line 154 still reads `finalize(&state.db)?;`. It is the only production path that ends a run — `finalize` (456-472) stamps `schema_versions.sky` and deletes the cursor in one transaction, and the only other 'sky' stamp in the tree (cache.rs:1650) sits inside `#[cfg(test)] mod tests` at cache.rs:1592. The divergence mechanism is the up-front wipe at 140-144: `UPDATE sky_nodes SET stratum = NULL, maturity = NULL WHERE path > ?1` NULLs the ENTIRE remaining range in one statement before anything is recomputed, while Phase D (382-403) refills only `(after_path, last_path]` per batch. So any interrupt after the wipe — a process kill, or any `?` error return such as the 30s busy_timeout at 136/318/348 or a lock poisoning — leaves every not-yet-processed row at NULL. `run()` is fire-and-forget on a bare `thread::spawn` (74-83) whose only failure record is `diag(&app_bg, &format!("[sky_backfill] FAILED: {}", e))` to the diagnostics file, with no retry; the values only return on the next boot. Related drift found while reading: is_needed's doc (86-88) claims it also returns true when "there's a cursor row indicating a

</details>

### [MED] `src/lib/libraries/propertyTypeRegistry.ts:33` — cost **M**

**User impact.** If the file holding your property-type choices is damaged or unreadable at startup, the app quietly behaves as if you never set any — and the next type you assign saves over it, erasing all of them permanently.

**Fix.** Have boot_bundle pass through a null (not `{}`) when `read_universe_property_types` errors, and make `seedFromBundle(null)`/`loadPropertyTypes`'s catch leave `loaded = false` with `persistPropertyTypes` refusing to write while unloaded.

<details><summary>evidence</summary>

Every link in the chain is present and unchanged. universe.rs:1660-1670 `read_universe_property_types` returns `Err` on both read failure and parse failure (`.map_err(|e| format!("Failed to parse property types: {}", e))`). boot_bundle.rs:128-132 degrades that to empty: `crate::universe::read_universe_property_types(app.clone()).unwrap_or(serde_json::Value::Object(serde_json::Map::new()))`. propertyTypeRegistry.ts:33-38 `seedFromBundle` then does `if (data && typeof data === 'object') { cache = data ... } loaded = true;` — `{}` is a truthy object, so a corrupt file is indistinguishable from an empty one and `loaded` is set true regardless. Its only caller is +layout.svelte:2590 `reg.seedFromBundle(bundle.property_types)`. `setRegisteredType` (:65-69) then writes into that empty cache and calls `persistPropertyTypes` (:41-50), whose timer fires `invoke('save_universe_property_types', { types: cache })` → universe.rs:1678-1683 `atomic_write(&dir.join("property-types.json"), ...)` over the original. `loadPropertyTypes`'s catch (:24-27) has the identical blind `cache = {}; loaded = true;`.

</details>

### [MED] `src-tauri/src/links_backfill.rs:184` — cost **M**

**User impact.** Switching to a different Universe while background indexing is still running can mark the new Universe as fully indexed when it isn't, leaving parts of its link counts permanently blank.

**Fix.** Capture a universe/db generation token at `run()` start and re-check it inside `process_batch`, `write_cursor` and `finalize` (the `federation_generation` pattern), abandoning the run on mismatch — applied to both this and sky_backfill.

<details><summary>evidence</summary>

links_backfill.rs has not been touched since b4056671 (well before the register), so 184 is exact: it is `finalize(&state.db, run_fp)?;` inside `run()`'s drained branch. `run()` holds its resume position in a Rust local — 176 `let mut last_path = read_cursor(&state.db)?;` then the loop at 179-191 calls `process_batch(&state.db, &last_path)` / `write_cursor(&state.db, &last_path)` / `finalize(&state.db, run_fp)` against whatever connection currently sits behind the shared `state.db` mutex. That connection is swapped on a universe switch: `invalidate_search_state` (search.rs:9188) does `*db = None` at 9208 and the next `ensure_search_db_ready` publishes the new universe's connection into the same mutex. There is no generation/epoch check anywhere in links_backfill.rs, even though the proven pattern sits 12 lines above the swap — `state.federation_generation.fetch_add(...)` (search.rs:9196) with the documented contract "The thread captures the pre-switch generation at start and checks before writing — mismatch → abandon stale work." `finalize` is unconditional. Sibling sky_backfill.rs:154 carries the identical shape (separately confirmed in the same register).

</details>

### [MED] `src-tauri/src/search.rs:6559` — cost **M**

**User impact.** A link you wrote before creating the note it points to stays only half-connected: that note never appears in the features that follow links by identity (like review dependencies), and if you later rename it the link quietly breaks.

**Fix.** In the same post-index step that recomputes incoming aggregates, UPDATE note_links SET target_cid_cn = the indexed note's cid_cn WHERE target_cid_cn IS NULL AND target_name matches its folded name or any alias.

<details><summary>evidence</summary>

Line 6559 is still the sole resolution site: `let target_cid_cn: Option<String> = conn.query_row("SELECT cid_cn FROM note_meta WHERE name_lower = ?1 UNION …", params![tl.target], …).ok();` inside the per-source-note edge build (6544-6575), stored into new_edges (6574) and written by the INSERTs at 6702/6714/6720. A repo-wide grep for `target_cid_cn` in src-tauri finds no other writer: the only UPDATEs are mig003_step2_backfill (search.rs:3124-3131, a one-time cid_cn migration that joins on `note_links.target_path`, not on a later-created target) and review_rehearse.rs:353 (hard-scoped to `LOWER(target_name)='evidence'`). The post-save maintenance that DOES run on the target side, maintain_incoming_after_save (1629+) via resolve_incoming_target_paths (1585-1617), only recomputes incoming AGGREGATES — it never writes target_cid_cn. So a forward link written before its target exists keeps NULL forever.

</details>

### [MED] `src-tauri/src/universe.rs:1196` — cost **M**

**User impact.** If you re-link a folder whose one manifest file went missing or got damaged, Constellation silently erases that folder's library list, settings, saved workspaces, property types and starred items back to empty — with no error message and no backup.

**Fix.** Gate on the `.constellation/` directory (or any of its manifests) rather than universe.json alone, write each data file only when it is absent, and propagate the write errors instead of `.ok()`.

<details><summary>evidence</summary>

File untouched by today's commits. link_library_as_universe (1140) still gates only on one file — `if cdir.join("universe.json").exists() { return open_existing_universe(app, path); }` (1150-1152) — then unconditionally `fs::write(cdir.join("libraries.json"), &libs_json)` with a single-entry vec (1191-1193) and, at the cited lines, `fs::write(cdir.join("bookmarks.json"), "[]").ok(); fs::write(cdir.join("settings.json"), "{}").ok(); fs::write(cdir.join("workspaces.json"), "[]").ok(); fs::write(cdir.join("property-types.json"), "{}").ok();` (1196-1199, plus collections.json at 1200). Every one of those five uses `.ok()` — the error is discarded — and none checks whether the file already exists.

</details>

### [MED] `src/lib/editor/yamlDoc.ts:383` *(moved from :362)* — cost **M**

**User impact.** The `#` notes-to-self you write inside a note's properties block quietly disappear — add a tag and the comment above it is gone, and that property jumps to the bottom of the list.

**Fix.** Preserve comment tokens across the write: carry the spliced item's `start` comment tokens onto the appended line (or edit a block seq's items in place instead of splice+append), and stop discarding the parser's pre-`document` tokens in the loop at 313-316.

<details><summary>evidence</summary>

PJ-182 (32fcface) rewrote this loop, so the anchor moved: at the register's base (e4e793be) line 362 was the `continue; // unchanged` inside composeFrontmatter's SET/ADD loop, whose branch test sat at base-365. At HEAD the same test is line 383: `if (item && item.value && 'type' in item.value && item.value.type === 'scalar' && np.type !== 'list')` — `np.type !== 'list'` still excludes EVERY list prop, so a tag edit always falls to the else at 386-388 (`findItemIndex` → `items.splice(idx,1)` → `addLines.push(serializeLine(...))`). The REMOVE splice is now line 362 (`if (idx !== -1) coll.items.splice(idx, 1)`). I ran composeFrontmatter at HEAD on `title: Alpha / # which tags matter / tags: / - a / # trailing thought / status: draft`: adding a tag emitted frontmatter with `# which tags matter` DELETED and the whole `tags` block relocated to the bottom; removing `status` deleted its attached comment as well. A third, broader instance surfaced in the same run: a comment before the FIRST key (`# identity`) is present in `rawYaml` but is dropped even on a pure `CST.setScalarValue` edit, because the token loop at 313-316 keeps only the `document` token and discards everything emitted befor

</details>

### [MED] `src-tauri/src/search.rs:6609` *(moved from :6560)* — cost **M**

**User impact.** The links and graph views can keep showing a connection a note no longer has — or miss one it does — until something forces a full re-index.

**Fix.** Close it at the unchanged code — index_note's note_links rebuild (search.rs:6609-6726): make the DB-vs-file equality that gates the rebuild cover every column the rebuild writes, behind one shared predicate (the discipline PJ-182 applied with samePropRow), so a stored edge can never survive a file that no longer describes it.

<details><summary>evidence</summary>

PJ-182 (32fcface) touched search.rs only above line 5760 (hunk headers: @@4925, @@4949, @@5639, @@5656, @@5719), shifting everything below by +49 lines. I diffed the register base's search.rs lines 6480-6700 against HEAD's 6529-6749: byte-identical, so nothing in today's commits fixed or altered this code. The cited line is inside `index_note` (6078) — specifically its SECOND write path, the note_links rebuild: after the note_meta/note_body/FTS upsert at 6301-6331, the stored-edge read runs at 6587-6634 (HEAD 6609 = `}).map_err(|e| e.to_string())?;` closing that query_map) and the rebuild at 6652-6726 is gated by two subset-equality predicates — `unchanged` (6639-6650) and per-edge `identical` (6678-6686) — either of which skips the write and leaves the stored row in place. Also read the schema (UNIQUE(source_path,target_name,link_type) at 3820) to rule out the `INSERT OR IGNORE`s at 6702/6714/6720 silently dropping a row: each is preceded by a DELETE on that exact triple, so they are not the divergence.

</details>

### [MED] `src-tauri/src/canonical.rs:1311` — cost **S**

**User impact.** The first time you open a note that came from outside the app, its permanent identity is written into the file but the app's own catalogue doesn't learn about it — so anything that matches notes by identity can quietly miss that note until you edit and save it.

**Fix.** In store.ts openNoteTab, when ensure_cid_cn_cmd returns changed content, fire the existing `invoke('constellation_search_reindex', { notePath: filePath, libraryName })` (it already has both arguments there) so the index learns the new identity in the same pass.

<details><summary>evidence</summary>

Line 1311 is exactly `pub fn ensure_cid_cn_cmd(file_path: String) -> Result<String, String> {`; its body (1312-1316) reads the file and calls `ensure_cid_cn`, with no index call. `ensure_cid_cn` writes at canonical.rs:1303 via `crate::write_gate::gate_write(file_path, &updated, None, "ensure_cid_cn")`, and write_gate.rs:249-250 inside `atomic_write` does `crate::watcher_suppress::mark(&tmp); crate::watcher_suppress::mark(path);` — while watcher.rs:119 filters events with `.filter(|p| !watcher_suppress::was_recent(p))`, so the watcher provably never sees this write. On the frontend, both call sites are reindex-free: store.ts:2670 in openNoteTab (`const updated = await invoke<string>('ensure_cid_cn_cmd', { filePath });`) and store.ts:2976 in drainCidEnsure — I grepped every `constellation_search_reindex` call in src/ (11 sites, all in NoteEditor save handlers and store save/rename paths) and none is on the cid-injection path. search.rs:6140-6148 confirms the divergence is real: `index_note` takes cid_cn straight from frontmatter and its own comment says externally-created notes' "cid is injected lazily at first OPEN (ensure_cid_cn_cmd in openNoteTab)", leaving note_meta.cid_cn as the

</details>

### [MED] `src/lib/components/SecondScreenPage.svelte:333` — cost **S**

**User impact.** (not stated)

**Fix.** Call `closeNoteModel(peekTab.id)` before clearing/replacing peekTab in closePeek and loadPeekPreview, and loop `closeNoteModel` over `get(openTabs)` before the `openTabs.set([])` in onWorkspaceRestore.

<details><summary>evidence</summary>

Line 332-335 is `function closePeek() { peekNote = null; peekTab = null; }` — line 333 is `peekNote = null;`. No `closeNoteModel` import or call exists anywhere in the file (grepped: 0 hits). The model IS created: line 1129 renders `<NoteEditor tab={peekTab} .../>` and NoteEditor.svelte:186 calls `ensureModel(tab.id, tab.path, tab.content ?? '')`, which at noteSession.ts:67-72 does `M.openModel(id, path, diskContent)` — a permanent entry in the module-level `models` map keyed by the peek id. loadPeekPreview (308-330) mints a fresh `id: `peek-${note.path}`` per note (line 316) and simply overwrites `peekTab` on replacement, orphaning the previous id. onWorkspaceRestore at line 718 does `openTabs.set([]);` with no disposal loop, unlike store.ts:2840 (`for (const t of departing) closeNoteModel(t.id);`) and store.ts:3147 which do dispose.

</details>

### [MED] `src/lib/components/GlobalTasksView.svelte:139` — cost **S**

**User impact.** (not stated)

**Fix.** In all three catch blocks, re-run the surface's refresh (or explicitly restore `task.completed`) and raise a visible failure notice instead of only logging.

<details><summary>evidence</summary>

Line 139 is `await toggleTaskReconciled(filePath, lineNumber);` inside handleToggle (135-145); the refresh `await loadAllTasks();` sits at line 141 INSIDE the same try, and the handler ends `catch (e) { console.error('Failed to toggle task:', e); }` — so a failure skips the refresh entirely and tells nobody. The checkbox at lines 284-288 is `<input type="checkbox" checked={task.completed} onchange={() => handleToggle(...)} />` — an unbound `checked` attribute, so the browser's own visual flip stands and Svelte has no changed value to re-render from. Both sibling surfaces are identical in shape: +layout.svelte:2015-2018 (`await toggleTaskReconciled(...); if (showCalendarPage) await refreshCalendarData();` then `catch (e) { console.error('Calendar task toggle failed:', e); }`) and +layout.svelte:9174-9182 (the TasksPanel onToggle, refresh + broadcast inside the try, `catch (e) { console.error('Toggle task failed:', e); }`).

</details>

### [MED] `src-tauri/src/write_gate.rs:250` — cost **S**

**User impact.** Every note save also wakes the folder-watcher into a full re-scan of the library, so on a large library typing and saving can stutter or briefly hang for no reason the user can see.

**Fix.** Change write_gate.rs:250 from `watcher_suppress::mark(path)` to `watcher_suppress::mark_with_parent(path)` so the containing directory — the third documented key — is suppressed on every gated note write.

<details><summary>evidence</summary>

write_gate.rs is untouched since 332f566d; lines 249-250 read exactly `crate::watcher_suppress::mark(&tmp);` / `crate::watcher_suppress::mark(path);` — two keys only, no parent-directory mark. watcher_suppress.rs:63-64 states the contract: "Callers doing an atomic replace must mark THREE keys: the temp path, the final path, and (via this helper) the containing directory." A repo-wide grep for `mark_with_parent` returns only its definition (watcher_suppress.rs:65) and its own test module (l.134/138/147) — zero production callers, exactly as described.

</details>

### [MED] `src/lib/libraries/store.ts:1756` *(moved from :1723)* — cost **S**

**User impact.** Starring a note or adding it to a collection can silently fail to save — the app shows it as starred, but it is gone the next time you open Constellation, with no warning that anything went wrong.

**Fix.** Make `saveCollections()` return its promise, retry once on failure, and on final failure raise a visible error state instead of a console.error that release builds never show.

<details><summary>evidence</summary>

Shifted +33 lines by PJ-182/PJ-181: at 32fcface~1 `function saveCollections()` was line 1722 with the invoke at 1723; at HEAD it is line 1755 with the identical body at 1756-1758: `invoke('save_universe_collections', { collections: get(collectionSets) }).catch(e => console.error('[save] collections failed:', e));`. Still the sole write path — every mutator funnels into it (createCollection 1765, renameCollection 1771, 1777, 1791, 1797, 1835, 1848, and the Starred seed-migration at 1866). No await, no retry, no dirty flag, no user-visible surface.

</details>

### [MED] `src-tauri/src/review.rs:1122` — cost **S**

**User impact.** You mark a note as reviewed, snooze it, or dismiss it, the app looks like it accepted the action — but the review queue never hears about it, so the same note keeps reappearing as due.

**Fix.** Change `sync_action_to_row` to return `Result<(), String>` — propagating `f(conn)`'s error and returning Err (not silent success) when the DB is unavailable — and have the ✓ Reviewed / Snooze / Dismiss commands surface it.

<details><summary>evidence</summary>

review.rs is untouched since c8e76f6b (MIG-104 Slice 4); line 1122 is still `let _ = f(conn);` inside `fn sync_action_to_row` (declared 1114), nested inside `if let Some(state) = app.try_state::<SearchState>() { if let Ok(db) = state.db.lock() { if let Some(conn) = db.as_ref() { if is_stamped(conn) {` — four guards with no else arm, so a missing DB, a poisoned lock or an unstamped schema all silently no-op, and a real SQL failure from `f(conn)` (e.g. `review_row_mark`'s upsert at 1148-1156, which does return Err) is discarded. The function returns `()`, so no caller can observe any of it.

</details>

### [MED] `src/lib/libraries/store.ts:1727` *(moved from :1694)* — cost **S**

**User impact.** If you press Back or Forward while you're still typing, the last few characters you typed can disappear for good — no error, no recovery.

**Fix.** Reorder `loadTabHistoryEntry` to match openNoteTab — await `resolveNoteContent` first, then flush, then do the tab update and `openNoteModel` in one synchronous block.

<details><summary>evidence</summary>

`git show e3b165cc:store.ts` puts `openNoteModel(tabId, filePath, content)` at exactly 1694; at HEAD the identical line is 1727 (function now starts at 1663). Body is unchanged: `flushOutgoing(tabId,'nav_flush')` at 1689, then the AWAITED `const resolved = await resolveNoteContent(...)` at 1697, then the force re-seed at 1727. `noteSession.open` (noteSession.ts:55-58) calls `M.openModel` unconditionally — no dirty check — so anything typed during the await is destroyed. The `_navTokens` checks at 1691/1700 test supersession only, never dirtiness. openNoteTab does it the other way round: its `resolveNoteContent` await happens at 2600, and the flush at 2742 is followed by the synchronous `openTabs.update` + model re-seed with no await between them.

</details>

### [MED] `src-tauri/src/libraries.rs:2234` *(moved from :2194)* — cost **S**

**User impact.** Opening the "Move to…" folder chooser on a large library freezes the whole app for as long as it takes to scan every folder on disk — the window stops responding, with no progress shown.

**Fix.** Change the attribute to `#[tauri::command(async)]` so the whole-universe folder walk runs off the IPC dispatch thread.

<details><summary>evidence</summary>

PJ-182 added 143 lines to libraries.rs, shifting this region by +40. At the base, line 2194 was `let p = entry.path();` and 2195 `if !p.is_dir() {` inside `collect_folders`; at HEAD that exact pair is 2234/2235, and `collect_folders` now begins at 2213. Every element of the finding holds: the command is still declared `#[tauri::command]` with NO `(async)` at HEAD line 2194, `pub fn list_universe_folders` at 2195; 2196-2209 loops `load_all_libraries(&app)` (which resolves federated cUniverse libraries) and calls `collect_folders` per library; and the walker recurses to depth 30 (2221) doing a `p.is_dir()` metadata syscall per directory entry (2235). This codebase treats sync as the dispatch thread by its own convention — universe.rs:1455-1458 "a sync command would park the WebView2 dispatch thread for the fsync", libraries.rs:2085-2087 "(async) — off the IPC dispatch thread". The caller does not cache: +layout.svelte:6397 `all = await invoke('list_universe_folders')` runs fresh on every Move-picker and template-destination open.

</details>

### [MED] `src/lib/libraries/store.ts:4113` *(moved from :4064)* — cost **S**

**User impact.** Moving a note into another folder while you are still typing in it can leave a second, stray copy behind in the old folder carrying your newest words — two notes, no sign which one is real.

**Fix.** Wrap `moveItem`'s `invoke('move_item')` in the same markCascading + flush-if-dirty + `finally { clearCascading }` envelope `renameItem` already uses.

<details><summary>evidence</summary>

`git show 32fcface:...store.ts | grep -n "export async function moveItem"` → 4064; at HEAD it is 4113 (PJ-181 inserted 49 lines above it). The body is unchanged and still bare: :4113-4116 is `export async function moveItem(...) { const newPath = await invoke<string>('move_item', { sourcePath, targetFolder }); migratePathKeyedAuxStateOnRename(sourcePath, newPath); ...` then the `repathNoteModel` loop. No `markCascading`, no flush. Its sibling `renameItem` (:3954) carries BOTH — :3966 `if (renamedTab) markCascading(renamedTab.path);` and :3971-3975 the `saveNoteSession(... 'rename_flush')` flush-before-mutate, with the rationale comment at :3955-3964. Neither call site compensates: +layout.svelte:6476 and :6486 (`handleMoveConfirm`) call `moveItem` directly with no flush. The duplicate-creation mechanism is live: `WRITE_GATE_ENFORCE: bool = false` (write_gate.rs:41) so the gate is shadow-only, and `atomic_write` (:264-269) takes the `else { fs::rename(&tmp, path) }` branch when the target is absent — it creates.

</details>

### [MED] `src/routes/+layout.svelte:6802` — cost **S**

**User impact.** If two renames overlap, the "please wait" lock that stops you typing into notes the app is busy rewriting can lift early — so you can type into a note mid-rewrite and lose what you just typed.

**Fix.** Replace the two bare `cascadeFreeze.set(...)` calls with refcounted `markFreeze(lib.path)` / `clearFreeze(lib.path)` helpers in store.ts that maintain a `Map<string, number>` and publish its keys, mirroring `markCascadingLibrary`.

<details><summary>evidence</summary>

Line 6802 reads exactly `if (willCascade) cascadeFreeze.set(new Set([lib.path]));` and its pair at 6937 reads `if (willCascade) cascadeFreeze.set(new Set()); // §D1 — lift the freeze`. Both are bare whole-set assignments. `grep -n cascadeFreeze` shows these are the only two writers app-wide. The two maps it mirrors ARE refcounted for exactly this reason: `cascadingPaths` (store.ts:766) with the comment at :757-760 "Refcounted (Map<path, count>) so overlapping cascades — e.g. the user spam-renames two notes in the same library — don't pop each other's marks", and `cascadingLibraries` (store.ts:821) with `markCascadingLibrary`/`clearCascadingLibrary` doing increment/decrement at :822-832. store.ts:846-848 asserts the freeze and the gate are "now ONE concept with one boundary rule instead of two representations that can disagree" — yet only the gate is refcounted. The `renamesInFlight` guard does not save it: +layout.svelte:6695 is `if (renamesInFlight.has(oldPath)) return;` — keyed on oldPath, so it serialises re-entry for the SAME path only, never two different notes or two libraries.

</details>

### [MED] `src/lib/libraries/store.ts:592` *(moved from :559)* — cost **S**

**User impact.** If you switch away from or close a note while the app is still updating links after a rename, that note's freshly-corrected links can be silently reverted to the old name.

**Fix.** Add `if (isCascading(tab.path)) return Promise.resolve({ ok: true });` immediately after the tab lookup in `flushOutgoing` (store.ts:608), matching the gate NoteEditor already applies at :207/:283/:344/:512.

<details><summary>evidence</summary>

PJ-181 (8101e918) rewrote large parts of store.ts; I read the register-base file (`git show 32fcface~1`) and its line 559 is `function flushOutgoing(tabId: string, origin = 'nav_flush'): Promise<FlushResult> {`. That same function now starts at HEAD 592 and its guard set is unchanged: 606 `if (displayOnlyWindow) return Promise.resolve({ ok: true });`, 607 `const tab = get(openTabs).find((t) => t.id === tabId);`, 608 `if (!tab || !isNoteDirty(tabId)) return Promise.resolve({ ok: true });`, 609 `markRecentWrite(tab.path);`, 610 `return flushOutgoingModel(tabId, navFlushEnv(tab, origin));`. No `isCascading` anywhere in the function, and none downstream either — `flushOutgoingModel` is `flushIfDirty` from `$lib/editor/noteSession` (imported at store.ts:17), whose body (noteSession.ts:282-297) only checks model existence and dirtiness. A repo-wide grep shows `isCascading` gating NoteEditor.svelte:207/283/344/512, PropertyEditor.svelte:602 and +layout.svelte:1638 — but none of flushOutgoing's four call sites (store.ts 1689, 2742, 2857, 3132).

</details>

### [MED] `src-tauri/src/libraries.rs:2160` — cost **S**

**User impact.** Notes you deleted can reappear in search results and link suggestions after you rename or move a folder, or the first time a library is indexed.

**Fix.** Add the sibling walkers' `if name.starts_with('.') { continue; }` entry skip to `collect_md_paths` (2161-2176) so no dot-directory note is ever handed to `reindex_single_note`.

<details><summary>evidence</summary>

HEAD 2160 is `fn collect_md_paths(dir: &Path, out: &mut Vec<std::path::PathBuf>)`, and its walk (2161-2176) contains exactly one skip — the symlink guard at 2166 — with no `.`-segment skip. Every sibling walker in the same file has one: 2247 (`// skip hidden / system folders (.trash, .constellation, .git, .obsidian…)` + `if name.starts_with('.')`), 2565, 2602, 3056, 3297, 3574, 3625, 4314, 4685, 5815, 6142, plus reconcile.rs:396. The invariant is written down at 2732-2735: "The filesystem walk skips these (`name.starts_with('.')`); the index does not, so index results MUST be filtered" — and 2740-2743 records that this exact class was already fixed once (MIG-104 Slice 2b, `reindex_changed_paths` Pass 1, "the verified producer of the live `note_meta` rows sitting at `.trash` paths"). collect_md_paths feeds `reindex_single_note` at three sites: the folder-rename cascade (1269→1286), `move_item` (2122→2150), and the cold-start library walk `collect_md_paths(Path::new(&library_path), ...)` (3263→3279).

</details>

### [MED] `src/routes/+layout.svelte:6537` — cost **S**

**User impact.** Adding a tag to a note that isn't currently open, while a rename is still updating links across your notes, can silently write an older copy of that note back over the rename's changes — and the tag write also makes the app think the file was edited by something outside Constellation.

**Fix.** Add `if (isCascading(path)) return;` before the readNote and `markRecentWrite(path);` before the writeNote in addTagToNote's closed-note branch, mirroring addLinkToNote.

<details><summary>evidence</summary>

File untouched by today's three commits (`git diff e4e793be HEAD` lists no +layout.svelte). Line 6537 is still `const content = await readNote(path);` inside addTagToNote's else-branch, followed by `parseFrontmatter(content)` (6538) and `await writeNote(path, composeUpdatedContent(...), 'add_tag')` (6541) — no `isCascading(path)` gate and no `markRecentWrite(path)` anywhere in the function (6526-6549). Both siblings do have them: addLinkToNote's closed branch has `if (isCascading(sourcePath)) return;` (store.ts:1270) then `markRecentWrite(sourcePath)` (1275); linkMentionInNote has `if (!openTab && isCascading(mentionPath)) return false;` (1351) then `markRecentWrite(mentionPath)` (1358). writeNote itself (store.ts:2311-2316) only forwards to the IPC — it does not mark.

</details>

### [MED] `src/lib/components/FocusPane.svelte:155` — cost **S**

**User impact.** If you retype a note's title in Focus mode and press Esc to get back to writing, the new title is thrown away without a word — the note keeps its old name.

**Fix.** In `handleTitleKeydown`'s Escape branch, run the same commit `handleTitleBlur` does (`if (titleValue !== title) ontitlechange?.(titleValue)`) before `reportCaret()` / `onexit?.()` — ideally by calling `handleTitleBlur()` directly so there is one commit path.

<details><summary>evidence</summary>

Read FocusPane.svelte:150-159 at HEAD: `function handleTitleKeydown(e){ if (e.key==='Enter'){e.preventDefault(); view?.focus();} if (e.key==='Escape'){ reportCaret(); onexit?.(); } }` — the Escape branch never calls `ontitlechange`. The ONLY commit path is `handleTitleBlur` (lines 137-144: `if (titleValue !== title) ontitlechange?.(titleValue)`), and `titleValue` is a one-shot `$state(title)` seeded at mount (line 112), so the typed text lives nowhere else. The host's `onexit` is `+layout.svelte:8626 onexit={() => { focusMode = false; }}`, which drops the whole `{#if focusMode}` branch (and the focused input) from the DOM; the title commit that IS wired (`ontitlechange` → `handleRenameComplete`, +layout.svelte:8584-8594) is therefore never reached. FocusPane.svelte was not touched by any of today's three commits (its last change is ca6ce954).

</details>

### [MED] `src/lib/components/ReviewStatusPanel.svelte:97` — cost **S**

**User impact.** You drag a note's review priority and the number stays where you put it, but nothing was actually saved — you only discover it later when the note is back at its old priority.

**Fix.** Replace `catch {}` with a handler that clears `priorityDraft` (so the slider snaps back to the stored value) and surfaces the failure the way the panel's other error states are surfaced.

<details><summary>evidence</summary>

Read ReviewStatusPanel.svelte:95-98 at HEAD: `async function commitPriority(value){ if(!notePath) return; try { await invoke('set_review_priority', { notePath, priority: value }); await load(); onRefresh?.(); } catch {} }` — the empty `catch {}` is line 97's tail. On failure `load()` never runs, so `priorityDraft` (set by the slider's `oninput`, line 148) keeps displaying the dragged number at line 150, making a failed write look identical to a successful one. The file appears in none of today's three commits (name-only lists: only NoteEditor.svelte, store.ts, PropertyEditor.svelte, propsCommit.ts and the Rust/YAML set changed).

</details>

### [MED] `src-tauri/src/libraries.rs:560` — cost **S**

**User impact.** When a save has to wait — a slow or synced drive, antivirus scanning the file, another write to the same note — the whole window can lock up instead of just that one save taking longer.

**Fix.** Change the attribute at libraries.rs:559 to `#[tauri::command(async)]` (the body has no `.await`; gate_write already serialises per path) and run the write_gate concurrency proofs before shipping.

<details><summary>evidence</summary>

libraries.rs:559 is `#[tauri::command]` and 560 is `pub fn write_note(` — plain sync, unchanged by today's commits. Its body ends at `crate::write_gate::gate_write(path, &content, expect.as_ref(), surface)`, which takes a per-path mutex and does an fsync'd atomic write (write_gate.rs:387-397; the module's own bench at 1035-1046 measures "avg gated write (8KB, fsync incl.)"). Every other file-mutating command in the same file IS `#[tauri::command(async)]`: create_note:927, rename_item:1240, resolve_structural_conflict:2039, move_item:2089, update_links_on_rename:6018, move_to_trash:6568, delete_path:6649 (write_conflict_sidecar:619 is the one other sync straggler). The repo documents why this matters, in its own words at search.rs:11238-11240: "`(async)` moves this off the WebView2 IPC dispatch thread so a writer-lock wait … can never freeze the app." write_gate.rs:1049-1053 also notes the concurrency proofs "MUST stay green before any note-file command is converted to #[tauri::command(async)]" — the conversion is anticipated and the proofs exist.

</details>

### [LOW] `src-tauri/src/cece/orchestrator.rs:153` — cost **M**

**User impact.** If one of the note-classification analysers ever gets stuck, that note's suggestions just spin forever instead of giving up after their time limit.

**Fix.** Give the worker owned `'static` data (clone the context / box the cataloger behind Arc) and use a detached `std::thread::spawn` so a hung cataloger can be abandoned after the budget instead of joined by the scope.

<details><summary>evidence</summary>

orchestrator.rs:153 is `std::thread::scope(|scope| {` inside `run_one_safe` (142), unchanged today (no CECE file is in the three commits). `std::thread::scope` joins every thread spawned in it before it returns, so when `rx.recv_timeout(timeout)` at 160 hits `Err(Timeout)` at 172 and the closure returns the abstain trail, the scope still blocks joining the worker at 154 that is running `c.classify(ctx)` — i.e. the timeout bounds only the WAIT for a result, not the call, which is exactly the hang the doc comment at 130-141 claims to have closed ("the original `run_one_safe` only caught panics, not hangs … `recv_timeout` enforces the per-cataloger budget"). Reached from `run_one_safe` call sites 74 and 87, via cece::wiring::build_orchestrator ← classifier/mod.rs:139, inside `#[tauri::command(async)] classifier_suggest_for_note` (classifier/mod.rs:91-95) — an async command, so a hang wedges a runtime worker thread rather than the UI thread, which matches the LOW rating.

</details>

### [LOW] `src/lib/components/StyleSetter.svelte:962` — cost **S**

**User impact.** If saving a style fails, the gallery still shows your new, renamed, or deleted style as though it worked — but the change is gone the next time you open the app.

**Fix.** Wrap each `await saveStylePresets(...)` in StyleSetter (962, 985, 989, 1001) in a try/catch that rolls back the in-memory `savedStyles` change and shows a visible error.

<details><summary>evidence</summary>

StyleSetter.svelte is untouched since 332f566d; line 962 is still `await saveStylePresets($state.snapshot(savedStyles) as StylePreset[]);` inside `async function saveAsStyle()` (958-963), preceded at 961 by the in-memory mutation `savedStyles = [...savedStyles, newPresetFromCurrent(draftName, keys)];` and with no try/catch. The same unguarded shape repeats at 985 (confirmRename), 989 (removeStyle) and 1001 (updateStyle); only importStyle (972) sits inside a try that itself swallows (`catch { /* invalid style file — ignore */ }`). `saveStylePresets` (stylePresets.ts:133-134) is a bare `await invoke('save_style_presets', { presets })` that rejects on failure.

</details>

### [LOW] `src-tauri/src/cece/reliability.rs:199` — cost **S**

**User impact.** Very occasionally, correcting a suggested source at the same moment another part of the app reads its accuracy stats makes your correction silently not stick.

**Fix.** Skip `.tmp` files whose mtime is within the last ~60s in `sweep_tmp_orphans` (a plain re-lock would deadlock, since `update_reliability_from_correction` already holds the non-reentrant `RELIABILITY_LOCK` when it reaches `load_or_default`).

<details><summary>evidence</summary>

Line 199 is verbatim `let _ = fs::remove_file(entry.path());` inside `sweep_tmp_orphans` (185-202), which deletes any `.constellation/` entry matching `.cataloger_reliability.*.tmp` — the exact prefix/suffix `save` gives its NamedTempFile at 155-158. `sweep_tmp_orphans` is called from `load_or_default` at line 96 with no lock. `RELIABILITY_LOCK` (declared 311) is taken only by `update_reliability_from_correction` at 319. Two unlocked callers of `load_or_default` exist and can run concurrently with a locked save: `cece_get_reliability_for_active_library` (line 257) and `classifier/mod.rs:164`. Either can unlink the in-flight temp file between `tmp.write_all`/`sync_all` (167-174) and `tmp.persist(&path)` (175), whose failure is only `eprintln!`'d.

</details>

### [LOW] `src/lib/components/PropertyEditor.svelte:974` *(moved from :929)* — cost **S**

**User impact.** If a property edit is quietly rejected inside the app, the panel finishes saving exactly as if it had worked — no warning at all — so the change simply never reaches the file.

**Fix.** Have `apply` return per-op outcomes (applied / no-op / refused) and make commitAndSave treat a refusal as an error to log and surface, rather than folding it into `changed === false`.

<details><summary>evidence</summary>

At the base, line 929 was `const ops = planPropOps(withAutoUpdatedDate(editableProps), model.props, seededKeysOf(seededRows), touched);`; PJ-187's 22-line guard plus the earlier hunks shift commitAndSave by +45 (base `if (!PROPS_SINGLE_OWNERSHIP) {` 922 → HEAD 967), putting that statement at HEAD 974. The false-success is intact end to end: propsCommit.apply (propsCommit.ts:198-216) ORs every intent's boolean into one `changed` flag, and noteModel.setPropValue (noteModel.ts:258-291) returns `false` for four distinct reasons — no model (266), an `expectPath` REFUSAL (267), the key not present (269), and a genuine no-op (278) — which apply cannot tell apart. commitAndSave then exits at 988 (`if (!changed && !isNoteDirty(id)) return;`) resolving successfully, and its caller debouncedSave (1008-1024) wraps the await in a try/catch that only fires on a throw, then clears `saving`. A refused write and a nothing-to-do write are therefore indistinguishable to the caller and produce the identical UI outcome.

</details>

### [LOW] `src-tauri/src/search.rs:10247` — cost **S**

**User impact.** On a fresh install, or after the search database is rebuilt, the Sky View's picture of your notes can stop updating as you edit — it keeps showing stale sizes and levels until a full re-scan runs.

**Fix.** Compute the sky pre-save signature on its own (independent of `incoming_links_backfill::is_stamped`) and hoist `maintain_sky_after_save` out of the `if let Some(inc_old)` block.

<details><summary>evidence</summary>

Line 10247 is `if let Some((old_t, old_n, old_a)) = inc_old {` and the sky call is nested inside it at :10255 `if let Err(e) = maintain_sky_after_save(conn, note_path, &old_t, &old_n, &old_a)`. `inc_old` is computed at :10207-10211 as `if crate::incoming_links_backfill::is_stamped(conn) { incoming_signature(conn, note_path).ok() } else { None }` — so when that unrelated stamp is absent, `inc_old` is None and the sky branch never runs. `is_stamped` (incoming_links_backfill.rs:36-44) is `SELECT version FROM schema_versions WHERE module = 'incoming_links' ... .unwrap_or(0) >= SCHEMA_VERSION`, i.e. false on a fresh/rebuilt DB and false after a version bump until the backfill completes. The fallback does not exist: init_db unconditionally executes `DROP TRIGGER IF EXISTS note_links_sky_stratum_ai/ad/au` at search.rs:4312-4314 on every boot and the following `execute_batch` recreates only `note_meta_sky_stratum_au`, with the comment at :4343-4348 "the per-edge note_links_sky_stratum_ai/ad/au triggers are REMOVED … The unconditional DROP statements above shed these triggers from existing DBs on next boot."

</details>

### [LOW] `src-tauri/src/libraries.rs:6753` — cost **S**

**User impact.** Deleting two files with the same name in quick succession can overwrite the first one already sitting in the trash, so it can no longer be recovered.

**Fix.** Discriminate the gate's error at 6753 — on an "already exists" refusal re-run the de-collide loop for a fresh trash name (and log the swallowed cause) instead of falling through to copy+remove.

<details><summary>evidence</summary>

HEAD 6753 is `if crate::write_gate::gate_rename(source, &dest, "delete_trash").is_err() {` in `move_into_trash_folder` — the Err value is discarded with no logging and no cause discrimination, and the fallback body runs `fs::copy(source, &dest)` at 6760 (or `copy_dir_recursive` at 6756), both of which overwrite an existing destination. The premise holds on the other side too: `write_gate::gate_rename` returns `Err("An item with this name already exists at the destination.")` for a pre-existing destination at write_gate.rs:593-596, and its own comment at 589-590 says "delete_trash's `.is_err()` fallback then completes the move via copy+remove" — i.e. the never-clobber refusal is routed straight into a clobbering copy. The outside-the-lock de-collide loop at 6730-6745 is exactly the pre-check that gate_rename:582-584 documents as racy.

</details>

### [LOW] `src/lib/libraries/store.ts:4187` *(moved from :4138)* — cost **S**

**User impact.** Notes you delete while they are open stay fully loaded in memory for the rest of the session, so a long working session with many deletions gradually uses more memory and feels heavier.

**Fix.** Collect the tabs the filter drops and call `closeNoteModel(t.id)` for each, exactly as closeTab (3147) and the departure sweep (2840) do.

<details><summary>evidence</summary>

deleteWithSetting is now at store.ts:4165-4192 (it was 4048 at commit e4e793be; the register's 4138 matches the working tree that carried PJ-182's uncommitted parseFrontmatter rewrite). The body is unchanged: after `await deletePath(...)` (4184) and `clearPathKeyedAuxStateOnDelete(path)` (4186) it does a bare `openTabs.update(tabs => tabs.filter(t => { … }))` (4187-4191) with no `closeNoteModel` anywhere in the function. The sibling departure primitives do call it: `closeNoteModel(tabId);` in closeTab (store.ts:3147, commented 'MIG-076 §C — dispose this tab's content model') and `for (const t of departing) closeNoteModel(t.id);` (store.ts:2840).

</details>
