# Constellation Pending Jobs

**Version 1.47 | 2026-07-24**

> **What changed in v1.47** (**The owed inspection actually RUN and triaged — 2 APP-KILLERs + 6 more fixed. Destination propose+show BUILT, awaiting the Boss test. SO#9. Ultracode**):
>
> ### ★ THE RE-PRIORITIZED BACKLOG
>
> **► NEXT ACTION — (1) BOSS TEST the destination propose+show** for new-note-from-template (built this job; binary rebuilt; tutorial written per the Testing Instructions Rule — commit is gated on the pass). **(2) A RULING on PJ-140**, the 54 whole-app inspection findings left unfixed. **Then** §1 use-side remainder (mixing heads-up) · D4 · §2 · §3 visual gallery · §5 · §6 · §7.
>
> **CLOSED THIS JOB:**
> - **The in-flight inspection `wf_acc3ca2c-4f6` — TRIAGED, and it was EMPTY.** Killed ~2 min in by the Boss-directed PCS: 14 finder agents started, **0 produced any output**. Nothing was hidden and nothing was salvageable — but the Slice-2 per-build inspection was therefore still *owed*, so it was **re-run in full** as `wf_8aff685b-b8b` (87 agents, 14 scopes, every candidate adversarially refuted before confirmation). **62 confirmed: 2 APP-KILLER, 14 HIGH, 27 MED, 19 LOW.** Register: `lab/reports/INSPECTION-2026-07-24-wf_8aff685b.md`.
> - **APP-KILLER #1 — a property edit could silently delete frontmatter children** (`store.ts` + `yamlDoc.ts`). `parseFrontmatter`'s list branch consumed only consecutive `- item` lines, so a **seq-of-maps** (`authors:` / `- name: X` / `role: Y`) and a **block list with an interior blank line** reached the panel TRUNCATED — drawn as an editable chip list with no hint the hidden fields existed. Editing one chip made compose splice the whole block and rewrite it from the truncated projection: the children were gone from the `.md`, no error, and the result re-parsed cleanly so nothing ever noticed. **This is the THIRD shape of the class PJ-136 and MIG-101 already closed twice** — more evidence for PJ-137. Fixed on both sides: the parser takes the block's FULL extent and projects what it cannot round-trip as READ-ONLY (the Boss's 2026-07-22 rule), and `immutableBlockKeys` now refuses any seq holding a non-scalar. `ikhtilāf` is exempted **by key** (it has a real lossless serializer), so MIG-101's editable structured list is untouched — and the key set now lives in ONE place beside that serializer. 7 regression tests incl. two guards proving ordinary `tags:` lists stay editable.
> - **APP-KILLER #2 — Approve All could silently erase the whole batch it just accepted** (`sources/bulk_ops.rs`). The bulk path rewrote frontmatter through the watcher-SUPPRESSED gate but never announced it — the missed sibling of the 2026-07-22 fix that added the announce to all four per-card seams. An open note kept its open-time base, and one keystroke later its save wiped the accepted `sources:`/`content_type:` blocks off disk. Now announced, **batched** (accumulated and flushed on the progress boundary, never one event per note) and drained on cancel.
>   - **Boss test attempted and NOT runnable — recorded honestly.** The Cataloger reported *"Apply suggestions to 0 notes"*: `skipSplit` is hardcoded `true` and **all 7,179 queued cards are "catalogers split"** (`Catalogers agreed (0)`), so the backend filtered out every one and `accept_one` never ran. The app behaved correctly and said so plainly; the fixed path simply has no live trigger on this Universe until a scan produces an agreed card. **Verified instead by construction:** the announce bookkeeping was extracted into a pure `AnnounceBuffer` and **6 Rust tests** now prove the property that can actually break — *every written path is announced exactly once* — swept across queue lengths 0–40, plus cancel-inside-the-unflushed-window, no-double-announce, and no-announce-for-idempotent-no-ops. The emit itself is byte-identical to the four per-card seams the Boss validated on 07-22. **Residual gap stated, not hidden:** no end-to-end run of the bulk path against a live open note. **PJ-142.**
> - **The library-resolver class — 4 sites** (`libraries.rs:918/1203/1807`, `universe.rs:2119`). All used a raw first-match `starts_with`; because `ensure_universe_notes_folder` puts universe_notes (path == the Universe **root**) at index 0, first-match **always** returned the root, so every note created, renamed, or cascade-rewritten inside a nested sub-library was indexed under the wrong `library_name` — silently and durably. The canonical longest-root-wins resolver `library_name_for_path` already existed; the defective sites simply never called it. Frontend twin fixed too (`handleRenameComplete` → the shared boundary-checked `libraryForPath`), where the consequence was worse: the cascade could walk, flush and freeze the **wrong library**, leaving every `[[OldTitle]]` un-rewritten while the success path fired normally.
> - **The three Slice-2-batch findings** this per-build inspection was owed for: `merge_fields_into_template`'s unguarded read-modify-write → `gate_rmw` (one critical section) **+ announce**, so a merge can no longer be silently overwritten by an open template tab; `props_reparse_backfill` now **verifies completeness before stamping** (the mature `note_body_backfill::finalize` pattern) so a universe switch mid-run can no longer mark an unconverted universe done forever; the Template Studio detail column is **keyed on the selected kind**, so one kind's naming evidence can no longer head another kind's panel.
> - **Destination propose + show for new-note-from-template — BUILT** (the Boss's 07-24 catch: *"it didn't ask for a location… we need to fix this"*). The title prompt now **shows** where the note will be created (`Library / folder`, read the way the sidebar reads) with a **Change…** door onto the existing MoveDialog folder tree — reuse, not rebuild. With nothing open there is nothing to propose, so the tree opens **first**. The silent fallback (focused folder, else the FIRST library = the universe root) is **deleted**: the create path now requires an explicit destination. Extracted `libraryForPath` + `buildUniverseFolderEntries` as shared helpers rather than copying the walk. i18n ×15; EN help topic + User Manual updated. **NOT YET BOSS-TESTED — the commit is gated on it.**
>
> **NEWLY FILED:**
> - **PJ-140** *(safety · needs a Boss ruling — 54 items)* — the whole-app inspection findings **left unfixed** this job: 14 HIGH, 26 MED, 18 LOW (the 8 above are closed). They are unrelated to MIG-103 and several cross subsystem boundaries — fixing them as drive-bys inside a feature build is exactly the unproven cross-cutting change WA#4 forbids. They are recorded in full, per WA#6's *"never silently park it"*, in `lab/reports/INSPECTION-2026-07-24-wf_8aff685b.md`. **The ruling needed: which get their own `/migration`, which get a batched remediation job, and in what order.** Standouts: folder rename has **no index cascade for descendants**; `delete_path` on a folder purges **nothing** from the index; `review-pulse.json` is a non-atomic write whose loader silently falls back to defaults (**a corrupt file loses all review history with no error**); the schema-version rebuild **deletes `search.db`, the only store of user-earned Living-Link properties and review overrides**, on every version bump.
> - **PJ-142** *(test-coverage gap · small)* — **the bulk Approve-All path has no end-to-end exercise.** Its one live trigger requires a card where the catalogers AGREED, and this Universe has 7,179 split cards and zero agreed, so the path cannot be reached from the UI at all. Two things worth deciding: (a) a way to exercise it — a scratch Universe fixture, or a Tauri mock-app harness (**the repo has none today**, which is why this fix is unit-proven rather than integration-proven); (b) whether *"Approve all"* should say plainly that it will apply to **0 of 7,179** cards *before* the user commits to it — the confirm banner does say so, but the button reads as though it will clear the queue. Not a defect; a legibility question.
> - **PJ-141** *(hygiene · the rest of the resolver class)* — `+layout.svelte` still holds **~10 more** first-match `$libraryStats.find(l => path.startsWith(l.path))` sites. `libraryForPath` (extracted this job) is the correct shared resolver; each site needs its consequence reasoned individually rather than swapped blind, so it is a deliberate sweep, not a drive-by. **Solve-the-class**, properly.
>
> **STILL OPEN:** **PJ-135** (English-only noise list — Boss ruling pending) · **PJ-137** (one YAML authority — **now SIX app-killers** traced to the two-parser split, this job's being the third of the *same* nested-shape family; the case is now overwhelming) · **PJ-130** backlog (54) · **PJ-133** · **PJ-124** (inspection ignores `args.files` — **8th+ confirmation**, this job's diff-scoped request ran whole-app again) · **PJ-132** (Sight perf flake — fired again under parallel load, passes isolated 31/31) · PJ-125/126/127/128/129/131/138/139.
>
> **MIG-103 remaining phases:** §1 use-side remainder (mixing heads-up) · §2 request path · §3 the Studio as a core-plugin app-within-app + the visual gallery · D4 · §5 the manuscript builder (proposer, NOT a wizard) · §6 the Studio's own style setter · §7 tending.
>
> ---

**Version 1.46 | 2026-07-24**

> **What changed in v1.46** (**MIG-103 §4 Slice 2 KEEP shipped & Boss-validated (`9ab033be`). The freeze saga closed. SO#9. Ultracode**):
>
> ### ★ THE RE-PRIORITIZED BACKLOG
>
> **► NEXT ACTION — (1) TRIAGE the in-flight inspection** `wf_acc3ca2c-4f6` (was still running at Boss-directed PCS; findings unseen — fix per WA#6 before anything else). **(2) Destination propose+show for new-note-from-template** (Boss, 2026-07-24: "it didn't ask for a location... saved it in the root library. We need to fix this." Already ruled in the locked interaction model; design settled: reuse MoveDialog's folder tree; NOT built). **Then** §1 use-side remainder (mixing heads-up) · D4 · §2 · §3 visual gallery · §5 · §6 · §7.
>
> **CLOSED THIS JOB:**
> - **MIG-103 §4 Slice 2 — KEEP + the Studio's memory + the honest index** — Boss ALL PASS. Create-exclusive Keep with typed clash + additive merge + byte-guarded Undo; `from_kind:` memory (restart-proven); templates excluded from kinds; phantom `- "…"` properties fixed at the parser and cleared by the new resumable `props_reparse_backfill` (12 corrected, 0 remain, DB-verified); NUL-byte `kindSignature` separator caught pre-Boss; the 11-hour `i += 1` infinite loop caught and one-lined; casts no longer inherit `template_kind:`; three inspection app-killers (PropertyEditor teardown gate · parent block-list · classifier announce) Stage-2 validated.
> - **The Studio hang** — root cause was a reactive-map WRITE during render (Rule 2 in different clothes), fixed and compiler-enforced (`ReadonlySet`). Two earlier wrong diagnoses recorded in the session log as the canonical reproduce-first violation.
>
> **STILL OPEN:** **PJ-135** (English-only noise list — Boss ruling pending) · **PJ-137** (one YAML authority — now FIVE app-killers from line-parsers: rename-title, parent, template-merge, classifier-adjacent, the indexer's list-item colon) · **PJ-130** backlog (54) · **PJ-133** · **PJ-124** (inspection ignores args.files — every Slice-2 run whole-app again) · **PJ-132** (Sight flake, fired twice more) · PJ-125/126/127/128/129/131/138/139.
>
> ---

*(Versions 1.45 and earlier: see `Constellation Pending Jobs v1.46.md` and its predecessors — the trail is durable, never overwritten.)*
