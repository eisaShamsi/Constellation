# Handover — Constellation (new session)

**Generated 2026-05-22, end of the help-docs + MIG-041 session.**

**Project**: Constellation — Tauri v2 (Rust + SvelteKit / Svelte 5), a Personal Knowledge Formulation system.
**Working location**: `E:\مشاريع كلاود\Constellation`, branch `main`.
**State**: everything is committed + pushed; working tree is clean; **nothing is in flight.**

---

## First steps (per CLAUDE.md)

1. `git pull origin main` — origin/main HEAD = `67f6ef83`.
2. Read **`docs/Constellation Orientation & Onboarding v2.24.md`** — the canonical onboarding doc. Read ONLY the highest version (v2.24).
3. Skim `docs/LESSONS-LEARNED.md`.
4. `git log --oneline -10` and `git status` to confirm state.

---

## What shipped last session (2026-05-21 → 05-22)

- **Help docs (orientation v2.23, commit `bae5ee11`)**: two new help topics — **"The Cataloger"** (the MIG-039 left-dock classification view) and **"Note Summaries"** (the NSC) — in English + all 14 other languages, with feature/button names anchored to each locale's shipped i18n strings. Source Review + User Manual cross-linked.

- **MIG-041 — term_vocab bigram shrink (orientation v2.24; commits `83a8453c`, `0d8c0cd9`, `e1daf018`, `be24388a`, `5ac2c08a`)**: removed ~5.19M dead "bigram" rows (90.6%) from the `term_vocab` CTSE shadow table → DB **2.35 → 1.75 GB**. Stops writing them (`ctse::hooks::token_counts` filter) + a one-time chunked background DELETE purge + a one-time VACUUM. Schema gates `term_vocab_bridge=3` + `term_vocab_vacuum=1`. **Boss-validated end-to-end on the live DB.**

  > ⚠ **Critical lesson**: it passed an isolated copy-test but **STALLED on the live DB** — the chunked purge worker died on a `SQLITE_BUSY` collision with the WAL checkpoint daemon. Fixed by: pausing the daemon during the migration (`MIGRATION_ACTIVE` flag) + retry-on-busy + worker self-checkpoint + a `diagnostics.log` trail. **→ Test DB migrations under live app concurrency, not just an isolated copy.**

---

## Detail if needed

- `lab/reports/SESSION-LOG-2026-05-21.md` — what shipped, blow-by-blow.
- `docs/MoCh/MoCh-2026-05-21-1900.md` — the conversational trace (decisions/steers).
- `docs/MIG-041-term-vocab-bigram-shrink-ARCHITECT.md` — design + build-outcome addendum.

---

## Open / deferred (none urgent)

- MIG-041 left the dead `bridge_concept_id` column in `term_vocab` (harmless; dropping it needs its own DROP COLUMN migration — deferred).
- The "test under live concurrency" lesson is in orientation v2.24 + the session log but is **not yet a numbered LESSONS-LEARNED entry** — worth formalizing.
- Eisa's stated direction: grow the **NSC into a standalone "core plug-in"** serving every Constellation function (future feature, not started).
- For the broader backlog, consult the latest **Pending Jobs** doc — do NOT assume backlog state from memory.

---

## Standing rules that mattered this session (full set in CLAUDE.md)

- One location (`E:\مشاريع كلاود\Constellation`, `main`) — no worktrees / alt paths.
- **NEVER commit or push unless Eisa explicitly asks.**
- **SO #6**: an orientation version bump lands in the SAME commit as its trigger (a shipped MIG, a new/restructured help topic, an LL entry, a BUG open/close).
- Test instructions are **tutorials** (define the feature, then click-by-click); split tests into stages; send Stage 1 first.
- **Stage 0** before any Boss test: verify the running binary's mtime post-dates the code change.
- **BASIC RULE**: if you don't know, say "I don't know" — never fabricate file paths, names, or facts.
- For schema / cross-subsystem changes, use the four-phase `/migration` workflow (Architect → Plan → Build → Audit); plan approval = build approval.

---

## Verified state

Working tree clean; `origin/main` @ `67f6ef83`; live `search.db` = 1.75 GB, integrity ok; all search features (cross-language `via`, ≈similar, Arabic, Index, phrase) confirmed working post-migration.

**Await Eisa's direction for the next task. State the function-in-hand (one line, naming the surface as the orientation doc names it) before starting any work.**
