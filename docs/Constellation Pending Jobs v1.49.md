# Constellation Pending Jobs

**Version 1.49 | 2026-07-25**

> **What changed in v1.49** (**A 14+5 PJ-140 Rust remediation shipped & Boss-validated · the Whole-Ecosystem file-tree fix (PJ-141 closed) + self-heal backfill · the library-icon system · TWO new top-principal LAWS. Per-build inspection: no regression. Ultracode**):
>
> ### ★ THE RE-PRIORITIZED BACKLOG
>
> **► NEXT ACTION — (1) FIX the PJ-140 HIGH content-loss** (`BacklinksPanel.svelte:181` `linkMention` — a "link it" click writes a `[[wikilink]]` via raw `invoke('write_note')` bypassing the open model, error swallowed, no reindex: open-model overwrite + false-success + index divergence, all silent). It is the single HIGH left in the register and is fixable now — own build + Boss test. **(2) MIG-105 Architect** (root library vs flat Universe — the data-model root cause behind the resolver class this job patched at the surface; Boss-directed, ready). **(3) MIG-104 remaining open questions** (location = `.constellation`, re-type-keeps-history = YES; questions left on review-state + coalesced counts). Then §1 use-side remainder · D4.
>
> **CLOSED THIS JOB (Boss-validated on the running binary, 18:32):**
> - **The Whole-Ecosystem file-tree fix** (`libraries.rs` / `search.rs` / `tasks.rs` + 33 frontend sites) — one concern ("enumerate the file tree, honoring Library ≠ Folder"), **every** surface. Two shared helpers `nested_library_paths` / `is_nested_library`; 13 walkers (sidebar tree, **Move picker**, links, tags, notes-by-tag, stages, index, tasks, note-dates, `index_library_recursive`, `reconcile_filesystem`) now honor the exclude set; per-file library attribution (longest-root-wins) replaces fixed-name stamping so a nested library no longer reports **0 notes**. Frontend `libraryForPath` (longest-root-wins) at 33 sites → **PJ-141 CLOSED**. Move-picker filter no longer hides a library whose note lives at its own root. Born of the nested-library bug + **THE WHOLE-ECOSYSTEM FIX LAW** (Boss-dictated; CLAUDE.md top-principal).
> - **Self-heal `library_attribution_backfill.rs`** (new) — a one-off, versioned (`LIB_ATTR_VERSION=1`), batched (500/40 ms, keyset), off-main-thread column re-write that corrects `note_meta.library_name` rows a *prior* reconcile mis-attributed; **stamps only after a completeness check** (interrupt-safe, the props_reparse pattern). Wired in `ensure_search_db_ready` after the two existing backfills. Pure Write-Time-Derivation — no body re-tokenized.
> - **PJ-140 Rust remediation — 14 numbered + 5 unnumbered findings** (per-build inspection `wf_ae5d4d18`: **ZERO new silent-failures**). Path-integrity via the shared `migrate_note_db_paths` (migrates `note_meta`/`note_links`/`note_aliases`/`note_embeddings`/`review_schedule`, pre-deletes stale PK rows, skips the dead `target_path` scan): **#2** folder-rename cascade · **#16** move-migrates-not-reinsert (earned review history survives a move) · **#3** folder-delete purges descendants · **#17** delete purges aliases+embedding · **#18** `gate_rename` dest-guard under the lock. Freeze/leak: **#27/#28** `name_lower` seek (kills the PJ-066 full-scan-per-link) · **#42/#60** async link-backfill + term-mentions/cooccurrence · **#57** WAL-daemon newest-wins (leaked immortal daemons) · **#61** perf_trace bounded · **#43** copy skips junctions. Silent-failure: **#53** propagate DB `Err` · **#33** nested-universe consolidation gated on the `.constellation` move landing. Durability (atomic_write / corrupt-aside): `libraries.json`, `review-pulse.json` (**was a silent `default()` that discarded ALL review history** — now backed aside), `collections.json`. +9 Rust tests.
> - **The library-icon system** (planet → **library building**, Boss-chosen "D"). One shared `LibraryIcon.svelte` (kinds library/cuniverse/**root=no icon**) at every surface (sidebar, toolbar, Move, Dashboard, picker, Style-Setter preview); cUniverse keeps a planet-and-orbit mark; folders unchanged. New `Style Setter → Library → Icon size` control (`--ft-library-icon-size`); toolbar `:global(svg)` scoping fix (the "dead icon"); Move icons carry each library's colour; glyph enlarged (drawing only, no size/control change). Boss-validated end-to-end.
> - **TWO new top-principal LAWS** — **The Whole-Ecosystem Fix Law** and **No Guessing — Investigate to Build Awareness** (CLAUDE.md + memory). Both born this session from the same failure shape (a fix / a diagnosis stopping at the first surface seen).
>
> **NEWLY FILED / GROWN:**
> - **PJ-140 now ~40** (was ~58; 14+5 closed this job). It gains the entries `wf_ae5d4d18` surfaced: **[0] HIGH `BacklinksPanel.svelte:181`** open-model overwrite + false-success + no-reindex (the NEXT ACTION); **ExpressionForge / SenseMakingCanvas** createNote-then-write-with-no-reindex (index divergence); **NotePane** editor-init fallback; the editor-lifecycle cascade-freeze cluster (its own migration). Register: `lab/reports/INSPECTION-2026-07-25-wf_ae5d4d18.md`.
> - **PJ-146** *(docs · small)* — the translated help dirs (`help.ar`, `help.de`, … ×14) are a **partial subset**, not a 1:1 mirror of `help.uConstellation.World` (most have no "Libraries" topic). The library-icon note landed in the English canonical help + User Manual only. A full 14-language help sync (and a decision on which topics translations must carry) is its own job.
>
> **STILL OPEN:** **PJ-140** (~40; **[0] is a fixable HIGH — the NEXT ACTION**) · **PJ-145 / MIG-105** (root library vs flat Universe — Architect next) · **MIG-104** (durable earned-link home — 3 questions) · **PJ-142** (bulk-accept end-to-end — needs a Tauri mock harness) · **PJ-143** (`note_links.target_path` empty on all rows) · **PJ-144** (per-note scan reload) · **PJ-137** (one YAML authority) · **PJ-135** · **PJ-124** (inspection ignores `args.files` — struck again this job) · **PJ-132** (Sight flake) · PJ-125/126/127/128/129/131/133/138/139/141✔.
>
> ---

**Version 1.48 | 2026-07-25**

> **What changed in v1.48** (**Boss-directed batch: search.db made un-deletable · an archive-reversal APP-KILLER fixed · the hour-long scan diagnosed + de-frozen · sidebar library duplication fixed · CLAUDE.md link-durability corrected · MIG-104 + MIG-105 logged. Per-build inspection: no regression. Ultracode**):
>
> ### ★ THE RE-PRIORITIZED BACKLOG
>
> **► NEXT ACTION — (1) BOSS RULING on PJ-140 sequencing** (the deferred whole-app safety findings; four lose data — the search.db deletion is now hardened, but folder-rename-no-cascade, folder-delete-purges-nothing, and review-pulse.json-silent-default remain). **(2) MIG-105 Architect** (root library vs flat Universe — Boss-directed, logged, ready to run). **(3) MIG-104 remaining open questions** (Boss settled location=`.constellation`, re-type-keeps-history=YES; 3 questions left). Then §1 use-side remainder · D4.
>
> **CLOSED THIS JOB (all Boss-validated on the running binary):**
> - **search.db is no longer deletable by the schema gate** (`search.rs`) — RENAMES aside, never deletes; a MISSING version marker no longer authorises destruction ("absent is not stale"); stamp only on proven set-aside; stamp failure surfaced. Pure `schema_gate()` + 5 tests. Closes the catastrophic half of PJ-140's #15/#40/#56.
> - **APP-KILLER — archiving a link was reversed by the next save** (`search.rs` `index_note`). Archival keeps the wikilink in the note, so an archived edge failed the unchanged-edge fast path (needs `status=='active'`) and was re-inserted hardcoded `'active'` with weight 0.0 — one ordinary save un-retired it, silently. Found independently by the safety inspection AND the MIG-104 architect pass. Status now preserved + restored; `status != "active"` qualifies on its own. 5 tests. **0 archived links in the live Universe, so nothing was damaged — but Retire did not hold.**
> - **The hour-long Cataloger scan — diagnosed (9-agent adversarial) + de-frozen.** Both neighbour lookups took the WRITER lock for pure reads → an hour of blocked saves; now on the read-only connection (`cece/wiring.rs`). The scan was the ONLY background job with no inter-item yield; added 30 ms (`scan_job.rs`). Lying "~3.5 min" header comment corrected to the measured >1 h. Structural per-scan-snapshot win deferred (PJ-144); the dead-query "fast fix" REFUSED because it would cement a correctness bug (PJ-143).
> - **Sidebar library duplication** (`libraries.rs`) — a library created at the Universe root appeared twice (as itself + as a folder inside the root library). `read_library_tree` now excludes any subdir that is itself a registered library. Blast-radius verified SAFE (indexing/watcher/reveal all resolve per-library, never via this tree). Display-only; 3 tests. Data-model fix is MIG-105.
> - **Approve/Reject scope labels** (`SourceReviewPanel.svelte`) — the two buttons read as a symmetrical pair but Approve respects the split filter (0 of 7,179) while Reject ignores it (all 7,179). Each now shows its true count; Approve disabled at 0; the confirm names the asymmetry. i18n ×15. (Boss hit the trap live.)
> - **CLAUDE.md corrected** — the "dual-layer storage / LINK file kind" claim was asserted as built; NEITHER exists. Section now states search.db is the current system-of-record for earned link data, lists recomputable-vs-lost fields, and forbids citing it as durability evidence. The doc error was the bigger hazard.
>
> **NEWLY FILED:**
> - **PJ-143** *(correctness · P-med)* — `note_links.target_path` is empty on all 234,192 rows in the live Universe, so the Graph Cataloger abstains on every note. `resolve_incoming_target_paths` never writes it (misleading name); only a one-time MIG-003 migration does. The scan-perf report's "make the dead query fast" was REFUSED because it would cement this. Needs its own investigation into where target_path should be populated.
> - **PJ-144** *(perf · `/migration`)* — the scan reloads the whole classified-neighbour set once PER NOTE (7,339×) instead of once per scan; ~9 min of the hour. Crosses the scan/orchestrator boundary + needs a ruling on mid-scan acceptance (snapshot staleness).
> - **PJ-145** *(`/migration` · MIG-105 — Boss-directed)* — root library vs flat Universe. `docs/migrations/MIG-105-*.md` logged (concept + six-patch evidence trail + 3 options). Architect workflow next.
> - **MIG-104** *(`/migration` · Boss-directed)* — durable home for earned link data. `docs/migrations/MIG-104-Architect-*.md` written (Earned-Life Ledger: snapshot+tail). Boss settled: lives in `.constellation`; re-type keeps history. 3 open questions remain.
> - **PJ-140 grows by ~4** net-new whole-app inspection entries (`wf_1b68be62`): NoteEditor handleSave drops trailing save · reindex_single_note Ok-on-None · yamlDoc scalar-SET no-quote · BacklinksPanel swallowed write. Register: `lab/reports/INSPECTION-2026-07-25-wf_1b68be62.md`.
>
> **STILL OPEN:** **PJ-140** (deferred safety backlog, now ~58; **Boss ruling requested**) · **PJ-142** (bulk-accept end-to-end untested — needs a Tauri mock-app harness; Approve/Reject legibility addressed this job) · **PJ-137** (one YAML authority — the archive fix is the 7th defect adjacent to the two-parser/preserve-map split) · **PJ-135** · **PJ-124** (inspection ignores args.files — 9th confirmation) · **PJ-132** (Sight flake) · PJ-125/126/127/128/129/131/133/138/139/141.
>
> ---

*(Versions 1.47 and earlier: see `Constellation Pending Jobs v1.48.md` and its predecessors — the trail is durable, never overwritten.)*
