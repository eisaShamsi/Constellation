# Handover — 2026-08-07 (PJ-207 §8)

**Branch:** `main`, clean, pushed. **Head:** `57144760`.
**Read first:** `docs/Constellation Orientation & Onboarding v3.84.md`, then
`docs/Constellation Pending Jobs v1.68.md`, then `docs/PJ-207-Index-Repair-Plan.md`.

---

## 1 · Where the work stands in one paragraph

**PJ-207 §8 shipped and was Boss-validated on a genuinely federated universe.** §1–§8 of a
15-step `/migration` are committed and Boss-passed; §9–§15 are unstarted and their plan is
unchanged. **§11 is still the door.** §8's own testing surfaced two live findings that matter
more than §8 did — one of which **blocks §13** — and they are the first thing to read below.

## 2 · What §8 actually did

Charter **W2-9 is half-closed.** Six *automatic* index-write passes now scope to the active
universe's OWN libraries via `libraries::try_load_libraries`:

| Surface | Note |
|---|---|
| `search.rs` `reconcile_filesystem` | the bulk walk's roots + a foreign-root skip set |
| `reconcile.rs` `run` | roots **and** the orphan re-adopt — the half that made removal oscillate |
| `index_repair.rs` `submit`/`run_cold_start` | refused **at the door** with a typed `Foreign` outcome |
| `search.rs` `reindex_changed_paths` | the watcher — re-adopted immediately, no boot needed |
| the boot cold-start fan-out | **not in the plan** — one `ColdStart` per RECURSIVE entry, every boot |
| `library_attribution_backfill.rs` | **not in the plan** — a boot pass that UPDATEs `library_name` |

**No new loader was written.** `try_load_libraries` already had the contract ("absent is a FACT;
unreadable is an unknown"), and `load_libraries`' own comment says its empty-on-failure return is
safe *only because no write path uses it*. Six write paths now take the strict twin.

**Also fixed here** (safety inspection, HIGH): `reconcile::run` had **no guard of any kind** — a
universe switch mid-pass made its re-adopt tail index the *departed* universe's notes into the
*newly-active* index. It now takes §7's shared `walk_may_proceed` decision per-iteration.

## 3 · OPEN, in order — read §3.1 before touching §13

1. **PJ-224 — the ordinary search box does NOT reach linked universes. THIS BLOCKS §13.**
   A plain word routes `SearchHub.triggerSearch` → `universalSearch` → `execute_universal_search(conn, …)`,
   which queries the active universe's connection alone. `federated_lexical_search_or_fallback` is
   reachable **only** from `constellation_search`'s advanced-syntax branches. So §8's stated premise
   — *"foreign notes stay searchable via the MIG-056 federated scatter-gather path"* — is true of that
   command and **false of the box the user types into**. §8 is unaffected (it removes nothing).
   **§13 offers to DELETE the duplicated copies on that premise. Do not start §13 until this is ruled on.**
2. **PJ-223 — one stray row disarms cold-start recovery for a whole library.** Live on `Eisa Universe`:
   2,096 `.md` on disk vs 1,890 indexed; **798 missing notes in `Constellation PKM`, a registered
   library**. The `only_if_unindexed` gate asks `COUNT(*) WHERE library_name = ?`, gets **1**, and
   concludes "already indexed". Proven pre-existing from MIG-108's own pre-unification snapshot.
   **This is the strongest existing argument for §9 (which would report it) and §11 (which would fix it).**
3. **§9** the drift check · **§10** one progress strip · **§11 THE DOOR** · **§12** the docs ·
   **§13** foreign-copy removal (**gated on PJ-224**) · **§14** Full re-read (flag-off) · **§15** the close.
4. **PJ-219** — the user-action write class (~18 surfaces). Boss-ruled 2026-08-07 to file, not fix.
   `validate_path_in_any_library`'s first branch is the recursive set **by design**, so editing a linked
   universe's note from the parent still writes a row here. **§13's removal can be undone one note at a
   time through this door.**
5. The 2026-08-07 whole-app inspection's **38 confirmed findings** (`wbxz23bdr`), of which **§8
   introduced none** — every one was cross-referenced against this commit's diff hunks. Two APP-KILLERs:
   `bases.rs:796` (→ PJ-221) and the already-known `store.ts:7221` `loadWorkspaces`.

## 4 · What to internalise before touching this code

**Behaviour tests can pin a MECHANISM but not a WIRING here.** Every index entry point takes an
`AppHandle` and this crate has **no Tauri test harness** (`Cargo.toml` has no `tauri`/`test` feature),
so reverting `try_load_libraries` → `load_all_libraries` at any call site would leave the whole suite
green. §6's `ConvergeKey` token does not fit — the parameter is a plain `&[LibraryInfo]` shared with a
dozen legitimate read paths. §8 closed it with a **source-level guard**
(`index_repair.rs::tests_pj207_s8_write_scope_guard`). RED-proven twice, both observed.

**Two mirrors were removed rather than written.** `walk_exclusions` and `foreign_roots_of` are shared
by production and test, so a test cannot keep passing after production changes — the `search.rs:338`
mirror trap §1 deleted, not re-introduced in a test.

**And the §8 lesson about my own reporting:** three times I stated a conclusion drawn from one
artefact without checking it against the running system — PJ-166's cause, the MIG-108 attribution,
and which universe the Boss was actually in. All three were wrong. **Check the running system, not
just the file that ought to describe it.**

## 5 · Testing PJ-207 — read this before writing any test

- **The federated test universe is `Eisa Universe`** (two live children: `كون عيسى`,
  `Eisa Cognitive Knowledge`). **NOT `كون عيسى`** — its declared child directory no longer exists, so it
  has no federation in effect and can only prove non-regression.
- **The registry file is NOT a reliable indicator of what the app is running.** Verify from the
  universe's own `.constellation/boot-perf.latest.json` and `diagnostics.log`. (Unexplained — §17.)
- **An in-app save cannot test the watcher path.** `write_gate::atomic_write` calls
  `watcher_suppress::mark` *before* writing and `watcher.rs` filters suppressed paths, so an in-app
  save is invisible to the watcher **by design**. Only an **external** edit travels `reindex_changed_paths`.
- Marker words: `blorptide`/`zarquon` exist in `Eisa Cognitive Knowledge` only; `vandrasil` is now used.
  A word whose stem is an English stopword is unfindable by construction, and a trailing digit ends a word.
- **Pin the repo path in every agent brief.** An agent sent to verify §8 read the stale
  `.claude/worktrees/…` copy and reported the fix missing. It is not.

## 6 · Known-broken / unverified — do not assume these are fine

- **The safety inspection still ignores `args.files` — TWELFTH strike, cause narrowed (PJ-220).** My
  first diagnosis (the skill template stringifies `args`) is **WRONG**: a real array passed via
  `scriptPath` still ran whole-app (14 agents = exactly the 14 `WHOLE_APP` groups). The script's own
  gate `Array.isArray(args.files)` is correct code, so **`args` are not reaching the workflow at all**.
  Separately, `Workflow({name:'safety-inspection'})` is **blocked outright** by a permission handler
  ("script contains control characters") while the file contains **none** — only `{scriptPath}` launches.
- The 2026-07-30 inspection's **25 candidates lost to server errors** — never triaged, still owed.
- **Sight v6 perf tests are flaky under parallel load** (PJ-172). 5 failed while 14 audit agents ran;
  **31/31 in isolation**. Assertions are bare `elapsed < N`.
- **Help/User-Manual corrections have NOT shipped** — still §12, deliberately. §8 adds no user-facing
  surface, so it required none.
- **UNSAVED WORK IS SITTING IN TWO STALE WORKTREES — do not delete them.** Found while pruning
  on 2026-08-07. Neither has any commit that isn't on `main`, so `git log main..branch` says they
  are empty — but the working trees are not:
  - `.claude/worktrees/eager-turing-493c70` — **23 modified Sight v6 tradition files**
    (`+139 / −111` across `akan-wiredu`, `dewey`, `husserl`, `masadir`, `pramana`,
    `shatibi-maqasid`, `talmudic-middot`, `wang-yangming`, …) **plus two untracked concept papers**:
    `docs/Constellation-Sight-Concept-Paper-v4.1.md` and
    `docs/Constellation-Sight-Subsystem-Concept-Paper-v1.0.md`.
  - `.claude/worktrees/sweet-jackson-2fbff3` — two untracked design mockups,
    `docs/Sight-vNext-MockA-Dashboard.svg` and `docs/Sight-vNext-MockB-Metaphor.svg`.
  **These need a Boss ruling: land them, or discard them deliberately.** Until then they are one
  `git worktree remove --force` away from being gone, with nothing in git to recover them from.
  Four genuinely-empty worktrees (`angry-knuth`, `crazy-pascal`, `frosty-stonebraker`,
  `suspicious-wright` — untracked `dev/null` artifacts only) were pruned. The session's own
  `pj207-section8-index-adoption-464823` was left in place because the running session's shell
  resolves to it. `E:\مشاريع كلاود\Constellation-wtSC` (detached HEAD, clean) was not touched.
- **`موسوعة عيسى` (979 notes) left `Eisa Universe`'s `libraries.json`** during the Boss's unification.
  Asked; unanswered.

## 7 · Gates at close

Rust **1364 / 0** (13 ignored) · vitest **895/900** (the 5 are PJ-172) · svelte-check **0 errors** ·
i18n parity **15/15 ✓**. Release binary built **13:48** from source last touched **13:40**.

## 8 · Start here

`docs/NEXT-SESSION-PROMPT-2026-08-08.md` — the opening message, ready to paste verbatim.
