# Handover — 2026-07-05 (end of the MIG-091 session)

**Branch:** `main` · **All work committed + pushed.** · **Binary:** last Boss-validated release build 2026-07-05 17:51.

---

## Where we stand (one screen)

**Shipped + Boss-validated this session:**
- **F2′** — app-created notes now surface in the file tree (`note-created` event; cross-window).
- **Shift+digit shortcut class fix** — every `Ctrl+Shift+<digit>` was dead since birth (the matcher normalized letters but not `Digit*` codes); `Ctrl+Shift+2` (second screen) now works.
- **Batch W** — 11 panel fs-walk/writer commands moved off the WebView2 dispatch thread to `(async)`, with stale-result seq guards + two Rust single-flight locks (embeds `BUILD_LOCK`, cece `RELIABILITY_LOCK`).
- **One-tag-authority fix** — tag chip counting 127 but listing 0; three tag definitions collapsed to one (`search::parse_frontmatter` is now THE authority; `notes_by_tag`/`scan_library_tags` delegate). Independent tag-column scrolling shipped.
- **Cross-library embeds + the ONE-universe ruling** — `resolve_embed` is `(async)` with a cross-library fallback spanning the whole universe **including federated child-universes**. Standing law: every name resolver spans the whole universe, never one library.
- **Dataview removed** (1,090 lines) — a week-3 vestige with no concept/toggle/manual mention; pasted ```` ```dataview ```` blocks now render as ordinary code.
- **cid_cn partial-index fix** — externally-created `cid_cn=''` notes were colliding on a FULL unique index and going invisible; the index is now **partial** (`WHERE cid_cn != ''`), self-migrating. Rule: any parameterized cid lookup needs `AND cid_cn != ''` to use it.
- **MIG-091 — File Explorer empowered** (the resolution of the "restructure the Notes Navigator" directive): name **filter** across all libraries (reveals+loads collapsed ones, restores on clear; names only), **sort** += created + size (8-state), **multi-select** (Ctrl/⌘ toggle · Shift range · Esc clears), **batch bar** (Move / Add-tag / Delete — each loops the existing gated single-item handler, no YAML splice, federated read-only members skipped). Old two-pane Navigator **retired**.

**Docs:** English + 14 translated User Manuals & affected help topics updated; Orientation **v3.25**; this handover; MoCh `2026-07-05-1200`.

---

## What's queued (ranked — start here next session)

1. **Workbench → Collections on Search Hub** *(approved).* The MIG-090 Workbench was rejected as "another search engine" but its durable-basket idea was approved for the Search Hub. **Do:** delete the Intent-Bar-as-search + the standalone Workbench page/overlay/dock/palette; **repurpose** the machinery already built and kept — `workbench.json` (universe.rs read/save), `workbench_hydrate` (workbench.rs, the batched membership→facts read), the `workbenchSets` store + `NoteRow`/`NoteList` primitive — into a Search-Hub "Collections" feature: run a search, hand-pick results into a persistent, task-scoped basket with live state, come back to it later. Concept-first (state the horse), then `/migration` (it crosses Search Hub ↔ store ↔ Rust).
2. **Quick Switcher speed** *(reproduce-first).* Boss wants Ctrl+O faster at title retrieval — NOT a new engine (Ctrl+O already IS the title search). It already reads an in-memory cache; **measure where the delay actually is** before touching anything (Reproduce-First rule). No fix ships before the delay is reproduced under instrumentation.
3. **PJ-069** — the whole-entity deduplication pass (map found: tags×6, folders×4, recents×3, orphan/fragile finders×5, hub-panel overlap, note-lists hand-rolled ×9). Concept-paper-first.
4. **PJ-068** *(parked)* — Second-Screen contextual-companion rework (SS must be contextual, never replicate the main screen). Concept paper exists; per-mode retire/redesign rulings pending at reopen.

**Older backlog (unchanged):** the freeze queue L′/F1/F3 (incl. FileTree virtualization = the deferred MIG-091 §D), `get_360_view` index-read `/migration`, `init_db` profiling, MIG-088 Phases 6–10, the Arabic-callout End/Home caret.

---

## Watch-outs
- **Never scope a name resolver to one library.** ONE-universe ruling — span the whole universe including federated child-universes (`resolve_universe_libraries`).
- **cid_cn `''` is a sentinel, not an identity.** Any new cid lookup needs `AND cid_cn != ''`.
- **Batch/tree writes go through the gated single-item handlers** (`moveItem`/`deleteWithSetting`/`addTagToNote`) — never YAML-splice. Content-integrity harness (`tests/mig-076/`) is the gate; run it after any editor/save/lifecycle touch.
- **`cargo build --release` alone re-embeds stale frontend** — run `npm run build` first, then grep `build/` for the new UI string before any Boss test.
- **Structural doc-lag:** several locales have no dedicated List-mode section and no localized `Notes Management` topic; MIG-091 coverage was *added* there, not rewritten. If that topic is ever translated, mirror the English MIG-091 changes.

---

## Ready-to-paste next-session prompt

```
Resume Constellation. Branch main; git pull first, then git log --oneline -8.
Read docs/Constellation Orientation & Onboarding v3.25.md and docs/HANDOVER-2026-07-05.md.
Ultracode is on — use the Workflow tool for substantive multi-step work + adversarial verify.

We just shipped MIG-091 (File Explorer empowered) and closed the session. Start the top queued item:

**Workbench → Collections on Search Hub** (Boss-approved).
1. State the concept first (the horse): what "Collections" is FOR — one clear sentence — before any design.
2. Confirm the whole-entity fit (no duplication with Bases / File Explorer / the Search Hub itself).
3. Predecessor Lookup + Function-in-Hand lines before any edit.
4. Then /migration: delete the Intent-Bar-as-search + standalone Workbench page/overlay/dock/palette; repurpose workbench.json + workbench_hydrate + workbenchSets + NoteRow/NoteList into a Search-Hub Collections basket (search → hand-pick → persistent task-scoped basket with live state).
Stop at the plan for my approval before building.
```
