# SS Three-Zone Cockpit /migration — Phase 1 ARCHITECT

**Date:** 2026-07-13 · **Workflow:** `wf_a130430f-19a` (3 territory mappers + architect synthesis) · **Status:** Boss ruled 2026-07-13 — **Option C** (Cut + full HEALTH board + WHERE-lite; contradiction pairs + orphan/fragile counts added to the existing background recompute, no new tables/backfill) with the **conservative cut** (only the ruled targets: RO note copies, 9 stub facets + tab bar, dead clones; the split-companion + Index/dashboard list halves stay until the zones replace them). → Phase 2 Plan.
**Grounding:** `docs/concept-papers/PJ-068-v3-SS-Honest-Audit-2026-07-13.md` + PJ-068/PJ-068-v2 concept papers + Boss rulings 2026-07-13 (cut the toggle; full three-zone cockpit).

---

**MIG — Second-Screen Three-Zone Cockpit — Phase 1 Architect**

**Concept (the horse):** The SS is a read-only Presenter Display answering three questions about the moment of work: where am I (WHERE), how healthy is the corpus's link fabric (HEALTH), what to engage next (DECISION).

**1. Territory**
1. LIVE-KEEP: cockpit chassis, Pin/Follow dial, 3 lenses, Rule-8-clean per-note fetch (SecondScreenPage.svelte:1552-1562; SecondScreenCockpit.svelte:73-111), loading/idle states.
2. LIVE-CUT: the RO note copies (dashboard note/tag, index term/compare), split-companion Tasks tab, 9 stub facets + tab bar (SecondScreenCockpit.svelte:123-139).
3. ALREADY-DEAD: !COCKPIT_ENABLED clone (1563-1690), OrgChart clone, Map companion, unpopulatable SS-tab branches, dead-letter screen:open-note, dead receivers universe-switched/workspace-restore; Sky View companion order-masked (1692-1838).
4. HEALTH data ~80% exists: link_stats_cache's 12 background-maintained aggregates — lifecycle, ccs_cooling/contested/tiers, by_type/by_confidence — one async IPC each (search.rs:3593/8080/8126), push via kh-snapshot-ready. Gaps: contradiction pair-list, orphan/fragile counts (uncached), tasks (no write-time index — fs-walk only).
5. WHERE = O(1) reads (focus event + path derivation + note_meta/sky_nodes PK); DECISION already shipped. Fix-in-pass defects: SS collect_library_notes walk (SecondScreenPage.svelte:620), Index note-click dead-end (+layout.svelte:6466-6479), false help-file editing claims ×15.

**Open rulings Move 1 doesn't name (one Boss line each):** Split companion (live), Dashboard/tree companion (WHERE's natural successor), Sky View companion + the handleSkyNodeClick short-circuit (+layout.svelte:5051-5061), fate of screen:open-note.

**2. Options**

| Opt | Scope | Speed | Effort | Risk / failure mode |
|---|---|---|---|---|
| A | Cut only; zones later | 1 session | S-M | Lowest. Traps: workspace-save 2s stall (cut both exchange ends together); grep-by-name killing the editor-panels lifeline. Fails soft: cockpit+empty until Move 2. |
| B | Cut + HEALTH from existing caches only (snapshot, CCS, due-count; contradictions = count only; tasks absent) | 2-3 sessions | M | A's risks + walk temptation. Fails as a half-board: a tension board that can't name one tension. |
| C | B + two new keys in the existing background recompute (contradiction pairs, orphan/fragile counts — no new tables/triggers) + WHERE-lite (focus event + O(1) lookups) | 3-4 sessions | L | Touches recompute (writer-adjacent; PJ-066 canon). Failure = stale/slow key → degraded tiles, no data loss. |
| D | C + tasks write-time index (new derived view + triggers + resumable backfill) + Estimation Map | 5-6 sessions | XL | Schema+backfill+trigger burden in one migration; mid-backfill-interrupt / index-drift class. Breaches the finale-out-of-scope ruling. |

**3. Invariants (Audit checklist)**
- INV-1 SS never writes; mutations forward via screen:request-note-action; settings single-writer via screen:set-lens.
- INV-2 Cockpit lifeline intact: screen:editor-panels channel, editorPanelsActive/Data, +layout.svelte:572 sender, cockpitReload bumps (841/853).
- INV-3 Main-window right sidebar, settings.panels.* keys, panel choreography untouched.
- INV-4 Rust window lifecycle byte-identical (lib.rs:660-693; two-monitor gate; handshake).
- INV-5 Zero SS IPC per main-window keystroke (path+nonce guard, SecondScreenCockpit.svelte:97-110).
- INV-6 G3 freshness: lenses repaint on note-saved + cascade:rewrote; echo guard kept.
- INV-7 Rule 8: zones read write-time-derived data; no whole-universe walk on SS open; measure boot/typing/IPC on the 7,600-note Universe pre-commit.
- INV-8 Legacy workspace secondScreen blobs load silently forever; saves never stall.
- INV-9 i18n ×15 + RTL; locale deletions consumer-checked per key (secondScreen.dashboard.*, settings.panels.* are main-window-shared).
- INV-10 Lens toggle (Boss-validated 2026-07-11) re-homed, never lost with the tab bar.
- INV-11 New/surviving IPC: async + read-conn; no COALESCE-in-WHERE on note_meta.

**4. Migration path / rollback**
Workspace: restore already ignores SS state (+layout.svelte:8764-8769); keep Workspace.secondScreen tolerated-optional; delete requester+responder in one commit. COCKPIT_ENABLED (cockpitFlag.ts:9) guards nothing post-cut — repoint as the three-zone rollback toggle, retire at Move-2 close. Rollback: Move 1 is pure deletion (one revertable commit); Move 2 sits behind the repointed flag. Backfill: A/B/C need none — link_stats_cache self-heals (boot-if-empty cache.rs:1559); new keys fill on the next recompute (tiles show honest "computing…" until then). Only D's tasks index needs a resumable background backfill. Scope statement required: link_stats_cache is active-universe-only (search.rs:7994) — the HEALTH board must label that scope or a later migration federates the cache per "It is ONE universe."

**5. Recommendation: C.** B's board cannot show a single actual contradiction — the verified gap this zone exists to close — while C closes it by adding two keys to an already-running background job; D's tasks index is the only genuinely new risky surface and tasks are only an urgency signal, so it (and the Estimation Map) belongs in its own migration.

---

## Appendix — Territory mapper detail

### Mapper A — SS code territory

Mapped the full SS territory for the Move-1 cut. SecondScreenPage.svelte (2,490 lines) dispatches 13 template branches: LIVE-and-keep = loading, cockpit mount (1552-1562), empty idle; LIVE-cut-targets = dashboard note/tag, index term/compare (their RO NoteEditor halves = the 'read-only note copies'); LIVE-unruled = split companion (only its Tasks tab is explicitly cut); DEAD = the !COCKPIT_ENABLED ep clone (1563-1690, flag-dead), OrgChart clone (1840-1855, 'skyview' sidebar-mode never emitted), DashboardView-in-SS + fallback note companion (1857-1895, SS's own openTabs is unpopulatable), Map companion (1339-1425, feature-flag off since MIG-038); MASKED = Sky View companion (1692-1838, shadowed by editorPanelsActive which never resets). The cockpit chassis keeps the Pin/Follow dial, the gen-guarded per-note fetch (get_backlink_rows/get_outgoing_rows/get_note_review_status — the Rule-8 pattern HEALTH must copy) and 3 lenses; its 9 stub facets + tab bar (SecondScreenCockpit:123-139,174-205) are the explicit facet cut. Event vocabulary fully mapped sender→receiver: 8 keep-channels (editor-panels, open-in-main, set-lens, settings-changed, note-saved, cascade:rewrote, request-note-action, closed/ready/link-types), 1 dead-letter (screen:open-note — live senders, no-op receiver), 2 dead receivers (universe-switched, workspace-restore — zero senders), 3 masked (context-changed/skyview-hover/skyview-click), plus per-branch feeds. Dead-after-cut inventory: loadEditorPanelsData + all ep* state (cockpit never reads them), the tasks halves of both loaders + scanNoteTasks/TasksPanel imports, and two pre-existing free wins — the never-rendered SS-local dashboard state with its 2s poll + per-change tag scans, and six unused write-path imports. adoptFreshDiskIntoSS survives but shrinks to the surviving RO views + cockpitReload bumps.

- **SS entry point — SecondScreenPage is NOT a SvelteKit route** @ `E:\مشاريع كلاود\Constellation\src\screen-entry.ts:8` [FACT] — static/screen.html loads src/screen-entry.ts which mounts SecondScreenPage directly (vite.screen.config.js standalone build). The SS window never runs +layout.svelte — the cut is contained to the SecondScreenPage component tree + secondScreen.ts vocabulary + main-window sender sites.
- **Dispatch #1 — loading spinner** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:1172-1176` [LIVE — KEEP] — First-boot only (initialLoadDone guard at 607-611).
- **Dispatch #2 — dashboardMode==='note' (RO note copy)** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:1177-1188` [LIVE — CUT (read-only note copy)] — Sender: emitDashboardOpenNote at +layout.svelte:8311 (main home dashboard, SS open). RO NoteEditor at 1186. This is one of Move-1's 'read-only note copies'. State killed with it: dashboardNoteTab (86), dashboardMode.
- **Dispatch #3 — dashboardMode==='tag' (tag list + RO note copy)** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:1190-1234` [LIVE — editor half is a CUT target] — Sender: emitDashboardTagSelected at +layout.svelte:8317. RO NoteEditor at 1226 = read-only note copy. Whether the tag LIST half survives (as a WHERE/DECISION feed) is an Architect ruling — Move-1 names only the note copies.
- **Dispatch #4 — indexMode==='term' (term list + RO note copy)** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:1236-1281` [LIVE — editor half is a CUT target] — Sender: emitIndexTermSelected at +layout.svelte:7449 (Index panel onTermClick when SS open). RO NoteEditor at 1273. State: indexTermData, indexSelectedNote.
- **Dispatch #5 — indexMode==='compare' (multi-term columns + RO note copy)** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:1283-1337` [LIVE — editor half is a CUT target] — Sender: emitIndexCompare at +layout.svelte:7460 (onTermSelect multi-select). RO NoteEditor at 1329. State: indexCompareData, indexActiveCompareIdx.
- **Dispatch #6 — Map companion** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:1339-1425` [LEGACY-REACHABLE — Move-1 EXPLICIT CUT] — Reachable only if $appSettings.enabledFeatures.constellationMap===true (default off since MIG-038; gates at +layout.svelte:2306 and 6691). Senders: emitMapCompanion at +layout.svelte:7522/7528/7534/7541 inside the mapEverOpened overlay. Contains RO NoteEditor (1346) + 3 ConstellationMap mounts (1350/1399/1412). Dies with it: mapCompanion* state (99-102), u17 listener (1036-1067), ConstellationMap import (38), MapCompanionData/emitMapCompanion/onMapCompanion in secondScreen.ts:361-376.
- **Dispatch #7 — Split companion (comparison panels)** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:1427-1550` [LIVE — NOT named in Move-1; Tasks tab IS cut] — Senders: emitSplitModeChanged $effect at +layout.svelte:602-620 on $splitActive (split view is live, cycleSplit +layout:7247). Its Tasks tab (1529-1541, TasksPanel readOnly — the PJ-090 resolved-by-cut surface) falls under Move-1's 'read-only Tasks facet'. Whether the whole branch survives into DECISION needs a ruling. Note its loader does whole-library scanLibraryLinks per split note (569) — Rule-8 hostile; if kept, re-point at the MIG-079 per-note seeks.
- **Dispatch #8a — SecondScreenCockpit mount (the chassis)** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:1552-1562` [LIVE — THE KEEP] — Branch condition editorPanelsActive && editorPanelsData; COCKPIT_ENABLED=true (cockpitFlag.ts:9). Props: focus built from editorPanelsData fields, dialMode=cockpitDial ($state 183, session-only, not persisted), onNavigate=cockpitNavigate (187-195 → sendNoteToMain), reloadNonce=cockpitReload (186; bumped at 841 on note-saved and 853 on cascade:rewrote).
- **Dispatch #8b — !COCKPIT_ENABLED editor-panels clone** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:1563-1690` [DEAD-BY-FLAG — Move-1 EXPLICIT CUT] — The migrated-right-sidebar tabbed clone (properties/backlinks/tags/star/tasks; TasksPanel RO at 1675). Sole renderer of ALL ep* state. Deleting it kills loadEditorPanelsData entirely (see loader entry). KEEP editorPanelsActive/editorPanelsData — they are the cockpit's focus feed.
- **Dispatch #9 — Sky View companion** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:1692-1838` [LIVE-BUT-ORDER-MASKED] — Branch sits AFTER editorPanels (1552), and editorPanelsActive never resets to false within an SS lifetime (the only active:false senders are on SS close: +layout.svelte:3400, 4966) — so once any note was active with SS open, this branch is unreachable. Senders still live: emitContextChanged +layout:548, emitSkyViewHover +layout:7772 (100ms debounce), emitSkyViewClick +layout:5053. Contains peek RO NoteEditor (1709 — a 'read-only note copy') + LocalSkyView (1808). Dies with it (if cut): loadSkyViewCompanionData (351-416, a whole-library scanLibraryLinks+buildSkyData walk), peek loaders (418-445), skyview history (316-349), skyview* state (296-313).
- **Dispatch #10 — OrgChart clone** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:1840-1855` [DEAD — Move-1 EXPLICIT CUT] — Requires mainSidebarMode==='skyview', but emitSidebarModeChanged is only ever called with 'tree' (+layout.svelte:5848, 6848) and 'digest' (6856) — 'skyview' is never emitted (the Sky View is a full-page overlay, not a sidebar mode, post-MIG-091). Dies with it: OrgChart import (40), its openNoteTab call (1851).
- **Dispatch #11 — DashboardView-in-SS** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:1857-1868` [DEAD-IN-PRACTICE] — Condition mainSidebarMode==='tree' && $activeTab — but $activeTab is the SS's OWN tab store (separate JS realm), and its only populators are dead: u7 workspace-restore (never fires — see event entry), the dead OrgChart branch (1851), and this branch's own onNoteClick (1863 — circular: the branch requires a tab to already exist). SS openTabs can never gain a first tab.
- **Dispatch #12 — fallback note companion (SS-own tabs + RO editor)** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:1870-1895` [DEAD-IN-PRACTICE — the 'read-only note copies' archetype] — Same $activeTab impossibility as #11. Dies with it: handleTabSwitch (1132), closeTab/switchTab usage (1885/1133), the noteWidth slider (77, 1154-1161 — only shows when mainSidebarMode!=='tree', i.e. digest), detail-tabs strip.
- **Dispatch #13 — empty idle state** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:1897-1904` [LIVE — KEEP] — The actual render when SS opens before any note is active on main.
- **Cockpit — Pin/Follow dial** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenCockpit.svelte:56-62,116-119,152-160` [LIVE — KEEP] — pinned captured by $effect at dial-flip to 'locked'; shown = locked ? pinned : focus (62). DialMode='normal'|'locked' (cockpitFlag.ts:15; 'live' removed 2026-07-10). Dial state lives in SecondScreenPage.cockpitDial — not persisted across SS restarts.
- **Cockpit — link/review fetch (the Rule-8-clean read path)** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenCockpit.svelte:73-111` [LIVE — KEEP] — get_backlink_rows + get_outgoing_rows (MIG-079 index seeks) + get_note_review_status, gen-guarded, path+reloadNonce-keyed (zero IPC on main-window keystrokes; 450ms delayed refetch on same-note save). This is the pattern HEALTH must copy.
- **Cockpit — lens toggle + 4 lens mounts** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenCockpit.svelte:143-147,179-199` [LIVE — KEEP] — lens = normalizeGraphStyle($appSettings.noteGraphStyle); buttons call requestLensChange (Display-not-Domain). Mounts NoteButterflyGraph/NoteLedgerGraph/NoteOrreryGraph (+NoteRadialGraph fallback for unbuilt heartwood). Shared helpers src/lib/cockpitGraphData.ts are ALSO imported by StyleSetter.svelte:20 and NoteGaugeDeck — must survive any cleanup.
- **Cockpit — the 9 stub mirror facets + facet tab bar** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenCockpit.svelte:123-139,174-188,200-205` [STUBS — Move-1 EXPLICIT CUT] — FACETS = links + 9 stubs (properties, structure, tags, skyview, tasks, health, provenance, review, sources); every non-links tab renders 'this facet is wired in the next pass' (201-204). Cut FACETS/activeTab/facetLabel + the .ck-tabs bar (keep the .ck-lens group, relocating it since it currently nests inside the tab bar at 179-187).
- **EVENT screen:editor-panels (the cockpit's lifeline)** @ `E:\مشاريع كلاود\Constellation\src\lib\secondScreen.ts:392-398` [LIVE — KEEP] — Senders: +layout.svelte:572 (tab-switch $effect, fires per active-tab change when SS open & !showSkyView), 4977 (toggle open), 5008 (send-to-screen), 3400+4966 (active:false on SS close). Receiver: SS u18 at SecondScreenPage:1084-1101 (also calls loadEditorPanelsData — that call dies with the ep clone).
- **EVENT screen:open-note — DEAD-LETTER CHANNEL** @ `E:\مشاريع كلاود\Constellation\src\lib\secondScreen.ts:91-93,190-192` [DEAD (receiver is a no-op)] — Senders live at +layout.svelte:551, 564, 5000 (handleSendToSecondScreen), 6471 (index note click) — but SS receiver u1 (SecondScreenPage:806-809) is an explicit no-op. Every sendNoteToScreen emit lands nowhere. Cut candidate: retire senders + channel; the 'send-to-screen' palette command (+layout:2281) then reduces to its emitEditorPanels half — needs a Predecessor→Replacement entry.
- **EVENT screen:open-in-main (SS→main navigate)** @ `E:\مشاريع كلاود\Constellation\src\lib\secondScreen.ts:110-112,194-196` [LIVE — KEEP] — SS senders: cockpitNavigate (189-195), link buttons at 1479/1494/1522/1617/1632/1663, map 1358, DashboardView prop 1866. Main receiver +layout.svelte:3393 → openNoteTab. The cockpit's navigate-back loop: click node → main opens tab → main's :562 $effect emits fresh editor-panels → cockpit follows.
- **EVENT screen:set-lens (lens request flow)** @ `E:\مشاريع كلاود\Constellation\src\lib\secondScreen.ts:133-138` [LIVE — KEEP] — Cockpit:183 requestLensChange → main onLensChangeRequest +layout.svelte:3404 → updateSettings({noteGraphStyle: normalizeGraphStyle(id)}) → store emits screen:settings-changed (store.ts:5456+) → SS u4 merges appSettings → cockpit lens $derived re-renders. Single-writer settings loop, zero SS writes.
- **EVENT screen:settings-changed** @ `E:\مشاريع كلاود\Constellation\src\lib\secondScreen.ts:101-103,206-208` [LIVE — KEEP] — Senders: libraries/store.ts:5456/5464/5476/5491/5500/5511/5518 (updateSettings family) + SettingsModal.svelte:291/306 (locale/fonts). Receiver: SS u4 (SecondScreenPage:880-890, incl. setLocale).
- **EVENT screen:note-saved** @ `E:\مشاريع كلاود\Constellation\src\lib\secondScreen.ts:156-158,198-200` [LIVE — KEEP] — Senders: NoteEditor.svelte:228/266/295/351/369, +layout.svelte:1555 (focus save) + 8576 (sidebar task toggle), store.ts:431/1139/4262. Receivers: SS u2 (SecondScreenPage:812-843 → adoptFreshDiskIntoSS + cockpitReload++ + the two legacy panel-reload blocks that shrink after the cut), main +layout:3343 (tension) + 3413 (adopt), CollectionsPanel.svelte:79.
- **EVENT cascade:rewrote (Rust→both windows)** @ `E:\مشاريع كلاود\Constellation\src-tauri\src\libraries.rs:1687,5602` [LIVE — KEEP] — SS receiver uCascade (SecondScreenPage:850-856): adoptFreshDiskIntoSS per rewritten path + cockpitReload++. The G3 rename-cascade freshness path.
- **EVENT screen:request-note-action (SS right-click forward)** @ `E:\مشاريع كلاود\Constellation\src\lib\secondScreen.ts:120-122` [LIVE — KEEP] — SS showSSNoteMenu (124-143) → main +layout.svelte:3425 → handleOrgNodeMenuAction (rename/move/delete dialogs open on main). Display-not-Domain exemplar. Post-cut consumers: only surviving link lists.
- **EVENT screen:closed / screen-hidden / screen:ready** @ `E:\مشاريع كلاود\Constellation\src\lib\secondScreen.ts:125-127,405-420` [LIVE — KEEP] — Rust lib.rs:667 emits screen-hidden → SS:786 notifyScreenClosed → main +layout:3396 (restores right sidebar, emits editor-panels active:false). screen:ready: SS:1107 → main waitForScreenReady 4974/4998 (handshake before first data send).
- **EVENT screen:state-request/state-response (workspace save)** @ `E:\مشاريع كلاود\Constellation\src\lib\secondScreen.ts:172-179,214-220` [LIVE but VESTIGIAL] — Main WorkspaceManager getLayoutState (+layout.svelte:8720-8731) → SS u6 (910-924) replies with SS-own openTabs (which can no longer be populated) + hardcoded mode:'detail'. Saves permanently-empty SS state. ScreenMode 'grid'|'star' and ContextMode 'browser' are vestigial type values.
- **EVENT screen:workspace-restore — DEAD RECEIVER** @ `E:\مشاريع كلاود\Constellation\src\lib\secondScreen.ts:182-184,222-224` [DEAD] — sendWorkspaceRestore has ZERO call sites; workspace onRestore explicitly closes the SS instead ('SS always starts closed', +layout.svelte:8764-8769). SS u7 (927-941) never fires — and it was the last legitimate populator of SS openTabs (root of why dispatch #11/#12 are dead).
- **EVENT screen:universe-switched — DEAD RECEIVER** @ `E:\مشاريع كلاود\Constellation\src\lib\secondScreen.ts:96-98,202-204` [DEAD] — notifyUniverseSwitch has ZERO call sites (imported at +layout:150, never invoked). SS u3 (859-872) never fires — the SS currently tracks universe switches only via the 3s-debounced library-changed reload. A Move-2 consideration: WHERE zone needs a real universe-switch signal or this stays a hole.
- **EVENT link-types:changed** @ `E:\مشاريع كلاود\Constellation\src\lib\secondScreen.ts:144-149` [LIVE — KEEP] — Sender LinkTypesEditor.svelte:96; SS uLT (876-877) re-pulls loadLinkTypes so lens colours/labels stay current (per-window registry cache).
- **EVENTS screen:context-changed / skyview-hover / skyview-click** @ `E:\مشاريع كلاود\Constellation\src\lib\secondScreen.ts:231-255` [LIVE senders, MASKED consumers] — Senders: +layout.svelte:548 ($effect on showSkyView), 7772 (node hover, 100ms debounce), 5053 (handleSkyNodeClick when SS open — returns early so main STAYS in Sky View). Consumers u8/u9/u10 feed only the masked dispatch #9. If #9 is cut, all three channels + skyviewHoverTimer die; +layout:5051-5061's early-return then changes main-window UX (node click would need to open on main).
- **EVENT screen:sidebar-mode-changed** @ `E:\مشاريع كلاود\Constellation\src\lib\secondScreen.ts:262-271` [LIVE but only 'tree'/'digest' ever sent] — Senders: +layout.svelte:5848, 6848 ('tree'), 6856 ('digest'). 'skyview' never emitted → dispatch #10 dead. SS u11 (980-984) also calls loadDashboardData on 'tree' — dead-feeding (see dead dashboard entry). mainSidebarMode also gates the noteWidth slider (1154) and dispatch #11.
- **EVENTS screen:split-mode-changed / dashboard-open-note / dashboard-tag-selected / index-term-selected / index-compare / map-companion** @ `E:\مشاريع كلاود\Constellation\src\lib\secondScreen.ts:291-297,309-324,340-355,370-376` [LIVE (map: flag-gated)] — Senders → receivers: +layout:607/618→u12(1069); 8311→u13(986); 8317→u14(1008); 7449→u15(1017); 7460→u16(1026); 7522-7541→u17(1036). Each dies with its branch if cut; each carries full note content over IPC (SplitNoteEntry.content, EditorPanelsData.content).
- **EVENTS library-changed / note-created (SS refresh)** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:893-907` [LIVE — KEEP (halve the work)] — Rust watcher.rs:91 + store createNote emit. Both funnel into a 3s-debounced loadAllData()+loadDashboardData(). loadAllData rebuilds allNotes via collect_library_notes per library (614-623) — the SS's own whole-universe walk; loadDashboardData is dead-feeding and should die in the cut.
- **LOADER loadEditorPanelsData + ALL ep* state — fully dead after cut** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:106-115,472-542` [DEAD AFTER MOVE-1] — epBacklinks/epForwardLinks/epTags/epProperties/epLocalSkyNodes/epLocalSkyLinks/epTasks + editorPanelsTab are rendered ONLY inside the !COCKPIT_ENABLED clone (1563-1690); the cockpit fetches its own links. Unwire 3 call sites: u18 (1099), u2's reload block (816-824), onNoteMutation (155). Removes a whole-library scanLibraryLinks (489) + buildSkyData over allNotes (518) + scanNoteTasks (530) per tab-switch. KEEP editorPanelsActive/editorPanelsData + the cockpitReload nonce.
- **LOADER loadSplitCompanionPanelData — tasks half dead; whole fate = split ruling** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:545-604` [PARTIAL CUT (tasks) / RULING (rest)] — scanNoteTasks per note (597-598) + SplitPanelEntry.tasks (172) + the tasks tab (1529-1541) die with the Tasks-facet cut. If the split companion branch is cut entirely, the whole loader + scPanels + scGeneration + onNoteMutation call (154) + u2 reload block (825-835) die. Also imports killed: scanNoteTasks/TaskItem/TasksPanel (SecondScreenPage:28-30; components stay — main window uses them).
- **ALREADY-DEAD — SS-local dashboard state + loadDashboardData + recent-lists poll** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:204-293,252-263,792-803` [DEAD CODE (renders nowhere) — free cut] — childUniverses/childUniverseLibs/dashboardTags/selectedTag/selectedTagNotes/selectTag/loadDashboardData + deriveds (totalNotes/totalFolders/cuLibNames/topLevelStats/universeNotesStats at 239-249) + recentOpened/Edited lists + refreshRecentLists + storage listener + a 2-SECOND setInterval poll (802) — all populated, none rendered (the markup was replaced by the DashboardView component mount in dead dispatch #11; only orphan CSS .dashboard-* remains at 1921+). loadDashboardData still runs at mount, universe-switch, library-changed, note-created, sidebar-'tree' — wasted IPC incl. scanLibraryTags over every library every debounce.
- **ALREADY-DEAD — write-path imports in a read-only window** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:13-15` [DEAD IMPORTS — free cut] — buildFullContent, writeNote, markRecentWrite, setWriteAhead, clearWriteAhead, renameItem imported, zero uses in file (ssReadOnly=true since the Boss ruling). wasRecentlyWritten (814) IS used — keep.
- **KEEP-BUT-SHRINK — adoptFreshDiskIntoSS + adoptCompanionTab (G3 freshness)** @ `E:\مشاريع كلاود\Constellation\src\lib\components\SecondScreenPage.svelte:734-775` [KEEP (shrinks with the cut)] — The freshness-gated adopt for up to 7 NoteEditor mounts. Companion list at 740/760-764 (dashboardNoteTab, dashboardSelectedNote, indexSelectedNote, mapCompanionNoteTab, peekTab) shrinks to whichever RO copies survive; the openTabs half (751-758) serves only dead dispatch #11/#12. If ALL note copies are cut, the whole helper + externalChangeNoteModel/SINGLE_OWNERSHIP imports (26-27) reduce to nothing — but u2/uCascade must keep bumping cockpitReload.
- **COCKPIT_ENABLED flag — retire or repoint** @ `E:\مشاريع كلاود\Constellation\src\lib\cockpitFlag.ts:9` [DECISION AT CUT] — After Move-1 deletes the else-half (1563-1690) the flag guards nothing; either retire it (P5-style close) or repoint it as the three-zone rollback toggle for Move-2. DialMode/NOTE_GRAPH_STYLES/normalizeGraphStyle in the same file are KEEP (main window imports normalizeGraphStyle at +layout:151).

**Risks:** The cut must sever the ep clone WITHOUT touching the cockpit's lifeline: editorPanelsActive + editorPanelsData + the screen:editor-panels channel + the +layout:562 $effect sender + the cockpitReload bumps in u2/uCascade all feed the cockpit. Deleting 'editor panels' wholesale (grep-by-name) would kill the KEEP. | Branch-order mask: the Sky View companion (dispatch #9) is unreachable once editorPanelsActive is true, and nothing resets it during an SS lifetime. Any Move-1/Move-2 design must explicitly rule the Sky View context's fate — cutting it also changes MAIN-window UX at +layout:5051-5061 (handleSkyNodeClick early-returns to 'pin on SS' instead of opening the note). | sendNoteToScreen ('screen:open-note') is a dead-letter channel — 4 live main-window senders into a no-op receiver. Retiring it touches main-window code (the 'send-to-screen' palette command +layout:2281 and handleSendToSecondScreen reduce to their emitEditorPanels half) — Predecessor→Replacement entries required per the Predecessor Lookup Rule. | SS openTabs is unpopulatable (u7 dead, u1 no-op, OrgChart branch dead) yet the workspace save path (screen:state-request/response) still round-trips it and NoteEditor/store save plumbing still exists in the SS bundle; the cut should also simplify sendScreenState or workspaces persist permanently-empty SS state. | Rule-8 debt the cut removes vs keeps: cut removes whole-library scanLibraryLinks+buildSkyData walks in loadEditorPanelsData/loadSkyViewCompanionData and the 2s recent-lists poll + dead loadDashboardData tag scans; but loadAllData's collect_library_notes walk (allNotes) SURVIVES because the cockpit's resolveTarget and the index/dashboard list-joins use it — HEALTH must not add another walk on top (7,600+ note universes). | Shared components — TasksPanel, DashboardView, OrgChart, ConstellationMap, LocalSkyView, NoteEditor, ContextMenu and cockpitGraphData.ts (imported by StyleSetter:20) — are main-window citizens; the cut deletes SS mounts and imports only, never the components. | If the split companion is cut too (not named in Move-1), the +layout:602-620 $effect sender and SplitCompanionData vocabulary must go in the same commit, or main keeps emitting full note contents of every open tab into a void on every split change. | Two receiver-orphans predate the cut and touch Move-2's WHERE zone: screen:universe-switched (never sent) and screen:workspace-restore (never sent). WHERE needs a real universe/context signal — do not build it on the dead channel assuming it fires. | i18n: deleting branches removes usages of secondScreen.* / panels.* keys shared with the main window — do not prune locale keys by grep of this file alone; conversely the cockpit's cockpit.* keys x15 locales stay.

### Mapper B — zone data availability

Derived-data availability for the three SS cockpit zones, verified against the code. HEALTH is the best-covered zone: the MIG-073/074 link_stats_cache (search.rs:3593, recompute :7919-7977) already persists 12 whole-corpus aggregates — link stats incl. by_type/by_confidence, the 6-stage lifecycle with derived dormancy (idle>90d), and the CCS registers living/load_bearing/cooling/contested/tiers/retired — read by ONE async IPC each (constellation_knowledge_health_snapshot :8080, constellation_ccs_snapshot :8126) with stale-while-revalidate background refresh (2-min freshness :7666; refresh at reconcile-settle cache.rs:1528 and boot-if-empty cache.rs:1559; 'kh-snapshot-ready' emitted app-wide :8011 so the SS can subscribe). The whole-corpus decaying/dormant question is answered: YES, cached — ccs_cooling (:7721) + ccs_tiers.stale (:7777) on the 90-day idle predicate (:7694); weight decay itself is display-only read-time math (store.ts:3575 effectiveLinkWeight; the DB write-decay loop was removed 2026-06-10 per :7542-7548). HEALTH gaps: whole-corpus contradiction pair-list (today only per-library read-time detect_tensions, main-window-cached at +layout.svelte:462/4021 — cheap to add as a 13th cache key), orphan/fragile corpus counts (write-time columns + covering index exist, count not cached), tasks urgency (NO write-time index — fs walk only, the zone's one real Rule-8 hazard), and the cache's active-universe-only scope (no federation). WHERE needs almost nothing new: the focus event carries identity (secondScreen.ts:13-19), the folder chain is a path-string derivation, libraries/cUniverses come from cheap JSON reads (universe.rs:1257/1312), and per-note facts (counts, word_count, timestamps write-time per MIG-066/079) plus sky_nodes stratum/maturity (WTD triggers search.rs:3711-4029) are O(1) PK lookups; the only gap is folder-level aggregates (cheap prefix-range counts suffice), and the two temptations to refuse are constellation_map_universe (whole-universe read-time assembly, map.rs:463) and the full sky snapshot (cache.rs:849, ~11-17 s). DECISION is already fully served by the shipped chassis: get_backlink_rows/get_outgoing_rows index seeks with all Living-Link properties (cache.rs:597/648), get_note_review_status O(1) (review.rs:522), note_tension_status O(1) ungated verdicts (tension.rs:131), and the frontend deriveStats layer (cockpitGraphData.ts:124). One pre-existing SS defect to fix in-pass: SecondScreenPage.svelte:620's per-library collect_library_notes filesystem walk (libraries.rs:5281/5291) feeding the cockpit's name resolver.

- **WHERE — focus-note identity feed (path/name/library/libraryPath/libraryColor pushed to SS)** @ `src/lib/secondScreen.ts:13-19, 91-93 (ScreenNote + screen:open-note)` [EXISTS — event push, zero IPC] — The locator's anchor is already delivered. The folder chain is a pure string derivation from `path` (no IPC needed). Event vocabulary also has screen:universe-switched (96-98) and screen:settings-changed (101-103).
- **WHERE — library/universe/cUniverse structure list** @ `src-tauri/src/universe.rs:1257 (resolve_universe_libraries), :409 (resolve_libraries_recursive), :1312 (get_child_universes), :894 (get_active_universe_path)` [EXISTS — cheap JSON reads] — Reads libraries.json + universe.json recursively; small files, no note walk. Federation-aware per the 'It is ONE universe' ruling. get_child_universes is already async.
- **WHERE — per-note canonical facts (library_name, word_count, created_at, modified, incoming/outgoing counts + type breakdowns)** @ `src-tauri/src/search.rs:2949-2963 (note_meta schema); outgoing aggregates write-time via triggers :1256-1287 (assignments :1212-1240); incoming aggregates write-time via save-path diff :1314-1351 + maintain_incoming_after_save :1437` [EXISTS — write-time (Rule 8), O(1) PK read] — incoming_count is alias-aware DISTINCT-source; structural (PJ-065) lane excluded from cognitive counts. This is the canonical per-note fact source every locator chip should read.
- **WHERE — per-note graph enrichment (stratum, maturity, link_count, outgoing_count)** @ `src-tauri/src/search.rs:3540-3566 (sky_nodes); WTD triggers :3711/3719/3728 (note_links→sky_links), :3814/3825/3854 (note_meta→sky_nodes), :3985 (stratum AU), :4029 (maturity AU)` [EXISTS — write-time; per-node PK lookup is O(1)] — A locator wanting stratum/maturity for the focus note should do a single sky_nodes PK lookup — NOT the whole-graph snapshot (see risk on cache_boot_snapshot_sky).
- **WHERE — folder-level aggregates (note counts per folder / sibling context)** @ `no table exists; note_meta.path is PK (search.rs:2950) so prefix-range seeks are indexed` [GAP — small] — No materialized folder view. For ONE breadcrumb chain (depth ≤ ~6), per-prefix COUNT range-seeks on the path PK are cheap reads and Rule-8 acceptable. A full folder-tree aggregate would need a new derived view — not required for a you-are-here chip.
- **WHERE — whole-universe tree (Map/OrgChart source)** @ `src-tauri/src/map.rs:463-491 (constellation_map_universe), :184 (MAP_TREE_FROM_INDEX=true), :75-124 (load_note_records — ALL note_meta rows), :458-462 (documented Rule-8 follow-up: tree NOT persisted)` [EXISTS but READ-TIME — whole-universe assembly per call] — No fs walk since MIG-078 §A′, but it still loads every note_meta row + rebuilds inbound maps in memory (historically 17.2 s inline on 7,600 notes; now async). Documented in-code as a pending Rule-8 item. Do NOT base the locator on it.
- **WHERE — SS's current allNotes source (cockpit resolveTarget)** @ `src/lib/components/SecondScreenPage.svelte:620 → src-tauri/src/libraries.rs:5281 (collect_library_notes) → :5291 (collect_notes_names_recursive — fs::read_dir walk + per-canonical-file 1KB read)` [RISK — an EXISTING read-time filesystem walk inside the SS today] — Called per library to feed SecondScreenCockpit's resolveTarget (SecondScreenCockpit.svelte:47-52). Any cockpit expansion should replace this with a note_meta-backed read (name/path/library_name are all indexed: idx_note_name_lower search.rs:3400) or on-demand name resolution.
- **HEALTH — the corpus link-health snapshot (12 cached aggregates: stats, lifecycle, 4 formulation lenses, ccs_living/load_bearing/cooling/contested/tiers/retired)** @ `src-tauri/src/search.rs:3593-3597 (link_stats_cache table), :7919-7977 (recompute_link_stats_cache), :8080 (constellation_knowledge_health_snapshot), :8126 (constellation_ccs_snapshot)` [EXISTS — background-maintained snapshot (Rule-8 conformant), read is tiny rows via ONE async IPC] — This is the HEALTH zone's ready-made backbone. Recompute runs ONLY off the open path: on reconcile completion (cache.rs:1528), boot-if-empty (cache.rs:1559), and stale-while-revalidate reads (search.rs:8094/8137; freshness 2 min, :7666). Refresh completion emits 'kh-snapshot-ready' app-wide (:8011) — the SS window can listen directly.
- **HEALTH — decaying/dormant links, whole-corpus: DOES a query exist? YES, cached** @ `src-tauri/src/search.rs:7694-7697 (CCS_STALE/WARM predicates — 90-day idle boundary), :7721-7727 (ccs_cooling register: active+traversed+idle>90d, coldest-first top-20 + total), :7777-7780 (ccs_tiers.stale count), :7559 compute_lifecycle_distribution (derived dormancy: idle>90d + historical 'dormant')` [EXISTS — cached in link_stats_cache; indexed (idx_link_last_traversed search.rs:3482)] — The 'decaying/dormant' corpus board is fully served by ccs_cooling + ccs_tiers + lifecycle. All predicates are index-driven and run only in the background recompute.
- **HEALTH — weight decay: where computed** @ `src/lib/libraries/store.ts:3563-3595 (effectiveLinkWeight — read-time exp half-life, default 60d); DB write-decay REMOVED 2026-06-10 (search.rs:7542-7548 comment; constellation_link_decay :7548 is now read-only census); raw earned weight = 1+ln(1+traversal_count) (search.rs:7167-7169)` [EXISTS — display-only, on the fly; DB weight column is the immutable traversal integral] — Consequence: a corpus query for 'effective weight below threshold' does NOT exist and would need per-row math; the shipped whole-corpus proxy is the 90-day idle predicate (ccs_cooling/stale). The HEALTH board should reuse the proxy, not invent an effective-weight scan.
- **HEALTH — contested-confidence links, whole-corpus** @ `src-tauri/src/search.rs:7729-7732 (ccs_contested register: confidence='contested', top-20 by weight + total), :7130-7135 (stats.by_confidence map)` [EXISTS — cached in link_stats_cache; indexed (idx_link_confidence search.rs:3480)] — Ready as-is for the contested tile.
- **HEALTH — unresolved contradictions, whole-corpus** @ `count EXISTS: stats.by_type['contradicts'] (search.rs:7123-7128, cached). Pair LIST is per-LIBRARY read-time: detect_tensions (tension.rs:82-105; DB loads :239-326; detection :331-518), cached client-side in the MAIN window only (+layout.svelte:462-465, invoke at :4021-4027)` [PARTIAL — corpus count cached; corpus pair-list is a GAP] — detect_tensions re-runs a per-library in-memory detection (all note_meta rows + note_links + json_each(tags_json) for that library) and is gated at 50 linked notes. For the SS board: either (a) add a 13th link_stats_cache key (contradiction pairs are a pure indexed note_links query — link_type='contradicts' AND status='active' via idx_link_type search.rs:3478, joined to note_meta) inside the SAME background recompute — the Rule-8 path; or (b) hand off the main window's cached tensionReportFull via an SS event. Do NOT invoke detect_tensions per-library from the SS.
- **HEALTH — per-note tension verdicts (orphan/fragile/contested + contested_with)** @ `src-tauri/src/tension.rs:131-141 (note_tension_status), pure core :144-226 — O(1) note_meta PK read + 2 indexed contradicts lookups; UNGATED (MIG-095)` [EXISTS — write-time facts, O(1) read] — Ideal for the focus-note strip of the HEALTH zone (and already the pattern the main window's note Health tab uses).
- **HEALTH — orphan/fragile corpus counts** @ `note_meta.incoming_count + covering index idx_note_meta_incoming_wc (search.rs:3446-3448, MIG-084 §F.2: orphan =0, fragile >=5, index-only scans)` [EXISTS as write-time columns — corpus COUNT is an index-only O(N) scan, NOT cached] — Cheap-ish (narrow index) but on a 7,600-note universe should join the link_stats_cache recompute rather than run on SS open, to honor the no-walks-on-open law.
- **HEALTH — review urgency (due/overdue counts)** @ `review_schedule table WTD (search.rs:2998-3018; maintained in index_note gated on schema_versions.review; back-fill review_backfill.rs); indexed read get_due_notes (review.rs:68-103, <100 ms; idx_review_due :3011, partial idx_review_last_reviewed :3017); per-note get_note_review_status (review.rs:522-624, O(1))` [EXISTS — write-time (Rule 8), indexed reads] — Empty library_path = whole-universe scope is already supported (review.rs:113-125 scope_clause). A due-COUNT variant for an urgency badge would be a trivial indexed COUNT.
- **HEALTH — tasks as urgency signal** @ `src-tauri/src/tasks.rs:382-398 (scan_library_tasks) → :240-246 (scan_tasks_recursive — fs::read_dir + read every .md)` [GAP — NO write-time task index exists; the only source is a whole-library filesystem walk] — The single biggest walk-temptation in the ruled design. Feeding 'overdue task count' into HEALTH per Rule 8 needs either (a) a derived tasks view maintained in index_note (new trigger/hook + backfill — a real migration step), or (b) a count-only hand-off from the main window's existing scan results via an SS event (zero backend work, but stale between scans). PJ-090 already cut the SS Tasks facet; the urgency-signal fold-in must not resurrect the walk.
- **DECISION — per-note backlinks/outgoing with all Living-Link properties (type, annotation, weight, confidence, last_traversed, traversal_count)** @ `src-tauri/src/cache.rs:597-639 (get_backlink_rows), :648-677 (get_outgoing_rows), row shape :571-586; consumed today by SecondScreenCockpit.svelte:79-85` [EXISTS — MIG-079 index seeks, federated-aware, async, read-conn (PJ-066 §C3)] — The lens chassis is already fed Rule-8-clean: path+nonce-keyed, gen-guarded, 450 ms settle on same-note saves (SecondScreenCockpit.svelte:96-110). The DECISION zone's connection space inherits this for free.
- **DECISION — per-note stats derivation (stage/stratum/maturity/typeMix/confMix/reviewState)** @ `src/lib/cockpitGraphData.ts:124-166 (deriveStats), :79-92 (recency shells), :96-100 (earnedWeight); review feed via get_note_review_status (SecondScreenCockpit.svelte:87)` [EXISTS — frontend derivation from data already in hand (documented Rule-8 clean)] — Shared chassis for Butterfly/Ledger/Orrery; registry-base palette (relColor :32). Nothing new needed for the kept lenses.
- **Cross-cutting — tag context (tag_counts)** @ `src-tauri/src/search.rs:2985-2989 (tag_counts, ±delta write-time in index_note per MIG-079 §C.1); read path cache.rs:1391-1399 (stamped-gated ~ms lookup)` [EXISTS — write-time] — Available if the WHERE locator or HEALTH board wants tag-level context chips at zero cost.
- **Cross-cutting — whole-sky graph snapshot (if any zone is tempted to draw the universe graph)** @ `src-tauri/src/cache.rs:711-721, 849+ (cache_boot_snapshot_sky; ~11 s cold scan of 234k sky_links documented :839-847); tables WTD (search.rs triggers above)` [EXISTS but EXPENSIVE READ — deferred/lazy by design (MIG-079 §C.2d)] — Write-time maintained, but the READ is the measured ~17 s boot cost on the reference universe. Any SS zone must consume sky data per-node (PK lookups), never the full snapshot.

**Risks:** Tasks urgency signal: no write-time task index exists anywhere — scan_library_tasks (tasks.rs:382) is a recursive fs walk reading every .md. Folding tasks into HEALTH naively would put a whole-library disk walk on SS open. Needs a Boss-visible design choice: derived tasks view (new migration surface) vs count-hand-off event from main. | Whole-corpus contradiction LIST does not exist as a cheap read: detect_tensions (tension.rs:82) is per-library, read-time (in-memory detection over all of a library's note_meta+note_links rows), 50-linked-note gated, and its cache lives only in the MAIN window (+layout.svelte:462/4021). Calling it per-library from the SS multiplies the compute per window. Cheap Rule-8 fix: a new link_stats_cache key (indexed note_links query) in the existing background recompute. | link_stats_cache is ACTIVE-UNIVERSE-ONLY: recompute opens the main search.db directly (search.rs:7994-7995) and never attaches cUniverse schemas — so a HEALTH board reading it shows the active universe's corpus, not the federation. Given the 2026-07-05 'It is ONE universe' ruling, this scope must be stated explicitly in the Architect doc (accepted scope or extended cache). | The SS already contains a read-time filesystem walk TODAY: SecondScreenPage.svelte:620 calls collect_library_notes (libraries.rs:5281 → recursive read_dir + 1KB reads) per library to build the cockpit's allNotes resolver. The cockpit rework should replace this with a note_meta-backed read, not inherit it into the new chassis. | constellation_map_universe (map.rs:463) is the obvious-but-wrong WHERE source: it assembles the whole universe tree from ALL note_meta rows per call (historically 17.2 s inline; the in-code Rule-8 follow-up at map.rs:458-462 to persist the tree was never done). A locator must be built from the focus note's path string + O(1) lookups, never this command. | cache_boot_snapshot_sky (cache.rs:849) is the ~11-17 s / 234k-row read the MIG-079 memo names as the remaining boot cost — any locator/decision visual that pulls the whole sky graph into the SS window re-pays it per window. Per-node sky_nodes PK lookups are the safe alternative. | Any NEW derived-data command for the zones must follow the PJ-066 canon: async + with_read_conn (never sync on the writer lock), and never COALESCE(col,expr) in a WHERE on note_meta (the 22 s full-scan landmine, fixed form at search.rs:1400-1411). | Decay is display-only (store.ts:3575); there is NO corpus query for 'effective weight below X' and building one would be per-row math over 234k links — the HEALTH board should present the shipped 90-day idle proxy (ccs_cooling/ccs_tiers.stale), which is already cached, rather than invent an effective-weight scan.

### Mapper C — blast radius + invariants

Mapper C — blast radius of Move 1 (the SS cut) + the migration's invariant list. The cut's external surface is almost entirely inside two files: +layout.svelte (all 11 main→SS senders and their UI triggers) and secondScreen.ts (the event vocabulary). Clean CUTS: emitIndexTermSelected/emitIndexCompare (IndexPanel callbacks 7447-7462), emitDashboardOpenNote/emitDashboardTagSelected (home DashboardView 8309-8318), emitMapCompanion ×4 (already unreachable — Map disabled since MIG-038), the SS OrgChart-clone branch (provably dead: 'skyview' is never emitted on screen:sidebar-mode-changed), the three read-only TasksPanel/tasks-facet surfaces (PJ-090's write toggle is already gone), and the 9 stub facets + tab bar (no external references except shared i18n keys and the nested lens toggle, both of which must survive). MUST PRESERVE: emitEditorPanels — misleading legacy name, but it is THE focus-follow wire feeding the cockpit (and the WHERE locator); broadcastNoteSaved + cascade:rewrote + note-created/library-changed + link-types:changed — the G3 freshness spine the lenses and allNotes-based target resolution depend on; screen:set-lens and screen:request-note-action (the Display-not-Domain vocabulary); the entire Rust window lifecycle (untouched). DECISIONS the Architect doc must state explicitly (not in the ruled parenthetical): the Split Comparison companion, the Universe Dashboard companion (WHERE-zone successor), and the Sky View companion trio incl. the handleSkyNodeClick main-window short-circuit at 5051-5061. Workspace persistence DOES serialize cut-mode state (SS read-only tabs, mode 'detail'), but restore already ignores it and the field is optional — the only trap is a 2s save-stall if the responder is cut without the main-side exchange. Two pre-existing silent defects surfaced: the Index note-click dead-end with SS open (6466-6479 → no-op listener) and the help file's false full-editing claims. Help topic exists only in the English tree; User Manual §9 + Index §'Second Screen Integration' are stale in all 15 manuals."

- **SENDER: emitEditorPanels (screen:editor-panels) — THE cockpit focus-follow channel** @ `src/routes/+layout.svelte:572, 3400, 4966, 4977, 5008; src/lib/secondScreen.ts:382-398` [PRESERVE (rename optional)] — Despite its legacy name ('mirrors right sidebar data'), this is the ONLY channel that feeds SecondScreenCockpit's focus: SS listener u18 (SecondScreenPage.svelte:1084-1101) sets editorPanelsData → cockpit mount at SecondScreenPage.svelte:1552-1562. The 5 sites: tab-switch $effect (572), SS-closed active:false (3400), toggle-close active:false (4966), toggle-open initial send (4977), send-to-SS (5008). The payload's `content` field is consumed by the lenses (SecondScreenCockpit.svelte:192-196 passes content to Butterfly/Ledger/Orrery). Cutting the legacy !COCKPIT_ENABLED clone (SecondScreenPage.svelte:1564-1690) kills only the RECEIVER's fallback branch + loadEditorPanelsData (474-542), NOT this channel. WHERE-zone locator rides this same wire.
- **SENDER: emitSplitModeChanged (screen:split-mode-changed) — feeds the Split Comparison companion** @ `src/routes/+layout.svelte:602-620 (sites 607, 618); receiver SecondScreenPage.svelte:1069-1081, render 1427-1550` [DECISION-NEEDED (likely CUT)] — Reachable code (fires whenever $splitActive && SS open), NOT dead. The companion it feeds is a per-note mirror of the 5 sidebar panels incl. a read-only TasksPanel (1534-1538) — squarely in the 'mirror' class the audit condemned, but the ruled Move-1 parenthetical does not name it explicitly. Architect must rule; if cut, delete the $effect at 602-620, the SplitCompanionData/SplitNoteEntry types (secondScreen.ts:277-297), and SS state/loader (79-82, 160-174, 547-604).
- **SENDER: emitIndexTermSelected / emitIndexCompare (screen:index-term-selected / screen:index-compare)** @ `src/routes/+layout.svelte:7447-7462 (IndexPanel onTermClick/onTermSelect, gated secondScreenOpen); receivers SecondScreenPage.svelte:1017-1034, render 1236-1337` [CUT (read-only note copies)] — Feeds the Index term/compare companions whose right pane is a read-only NoteEditor copy (1273, 1329) — in the ruled cut. IndexPanel itself and its main-window behavior are untouched; only the `if (secondScreenOpen)` branches in the two callbacks die. Types IndexTermData/IndexCompareData (secondScreen.ts:330-355) die with them.
- **SENDER: emitDashboardOpenNote / emitDashboardTagSelected (screen:dashboard-open-note / screen:dashboard-tag-selected)** @ `src/routes/+layout.svelte:8309-8318 (main-window home DashboardView onNoteToScreen/onTagSelect); receivers SecondScreenPage.svelte:986-1015, render 1177-1234` [CUT (read-only note copies)] — Feeds dashboardMode 'note'/'tag' — read-only NoteEditor copies (1186, 1226). After cut, the main DashboardView's onNoteToScreen must fall through to its else branch (openNoteTab in main, 8313) and onTagSelect goes back to undefined. Main-window home dashboard itself is unaffected (shared component).
- **SENDER: emitMapCompanion (screen:map-companion) — the disabled-Map companion** @ `src/routes/+layout.svelte:7522, 7528, 7534, 7541 (ConstellationMap callbacks); receiver SecondScreenPage.svelte:1036-1067, render 1339-1425` [CUT (effectively dead already)] — Constellation Map is disabled in core (MIG-038, re-categorized to Wings; flags off), so these sites are unreachable in shipped config. Cut the 4 `if (secondScreenOpen)` branches + MapCompanionData type (secondScreen.ts:361-376) + SS receiver/branch + mapCompanionNoteTab (a read-only note copy).
- **SENDER: emitSidebarModeChanged (screen:sidebar-mode-changed) — mode dispatcher for Dashboard/OrgChart companions** @ `src/routes/+layout.svelte:5848, 6848, 6856 (only 'tree' and 'digest' ever sent); receiver SecondScreenPage.svelte:980-984; branches 1840-1855 (OrgChart) and 1857-1868 (DashboardView)` [PARTIAL: OrgChart branch ALREADY-DEAD; channel DECISION-NEEDED] — 'skyview' is NEVER emitted (SidebarMode sites are only tree/digest) → the SS OrgChart clone branch (mainSidebarMode==='skyview', 1840-1855) is unreachable dead code — cut is zero-risk. The 'tree'→Universe Dashboard branch (1857-1868) IS live; the ruled cut list doesn't name the Dashboard companion — the WHERE zone is its natural replacement. Architect must rule; if Dashboard companion goes, this whole channel dies.
- **SENDER: emitContextChanged / emitSkyViewHover / emitSkyViewClick — the Sky View companion trio** @ `src/routes/+layout.svelte:548 ($effect), 7769-7781 (hover, gated secondScreenOpen), 5051-5061 (handleSkyNodeClick short-circuit); receivers SecondScreenPage.svelte:944-977, render 1692-1838` [DECISION-NEEDED (not in ruled cut list); its peek editor IS in the cut] — SecondScreenPage:1693 comment says 'kept as-is', but the peek NoteEditor (1698-1712, peekTab) is a read-only note copy → in the cut class. NOTE the main-window behavior coupling: handleSkyNodeClick (5051-5061) does NOT open the note in main when SS is open — it pins to SS and returns. If the companion is cut without touching this, Sky-View clicks with SS open would do nothing. Architect must rule the companion's fate (fold into WHERE/DECISION zones vs keep).
- **SENDER: sendNoteToScreen (screen:open-note)** @ `src/routes/+layout.svelte:551, 564, 5000, 6471; receiver SecondScreenPage.svelte:806-809` [ALREADY-DEAD (receiver is a no-op)] — The SS listener u1 is literally '// No-op for tracking'. All four sends today have zero visible effect on the SS. Candidate to become the WHERE-zone focus channel, or cut. See the Index dead-end risk (layout:6466-6479 returns after this no-op send, so Index note-clicks with SS open currently do NOTHING anywhere).
- **UI TRIGGERS for SS lifecycle: dock button, command palette, shortcut, toggle/send handlers** @ `src/routes/+layout.svelte:6803-6805 (dock, gated hasMultipleDisplays && enabledFeatures.secondScreen !== false), 2279-2281 (palette: 'second-screen' Ctrl+Shift+2, 'send-to-screen'), 4958-4987 (handleToggleSecondScreen incl. right-sidebar hide/restore + waitForScreenReady), 4989-5016 (handleSendToSecondScreen)` [PRESERVE] — Lifecycle unchanged by Move 1. handleSendToSecondScreen's emitEditorPanels tail (5008) is the cockpit hand-off — keep.
- **SETTINGS: enabledFeatures.secondScreen plugin toggle** @ `src/lib/libraries/store.ts:4766 (type), 5095 (default true); src/lib/components/SettingsModal.svelte:251 (plugin row, keys settings.plugins.secondScreen/secondScreenDesc)` [PRESERVE] — Gates the whole SS feature, not the cut modes. No other SS-specific settings entries exist; noteGraphStyle (the lens choice) also PRESERVE.
- **i18n: 'settings.panels.panel*' keys — SHARED, must NOT be deleted** @ `Consumers: src/lib/components/SettingsModal.svelte:2118-2129 (main-window right-sidebar panel visibility rows) AND SecondScreenCockpit.svelte:129,133,135 (FACETS labelKeys)` [PRESERVE keys; only the cockpit reuse dies] — The 9 stub facets borrow these keys (panelProperties/panelTasks/panelProvenance etc.). Deleting the FACETS array removes the reuse; the keys themselves belong to the main-window Settings → Panels section and stay in all 15 locales.
- **i18n: 'secondScreen.*' key block (20 keys × 15 locales)** @ `src/lib/i18n/en.json ('secondScreen'); consumers SecondScreenPage.svelte + DashboardView.svelte + +layout.svelte:2280-2281, 6804` [PARTIAL: prune with care] — Already-dead pre-cut (no src refs): detail, grid, graph, graphView, linkedOn, linkedOff, skyviewMode, backlinksFor. Die with cut: splitCompanion, editorPanels, forwardLinks, noProperties (+ the MISSING key secondScreen.comparingTerms — SecondScreenPage:1290 references it, it's absent from en.json, renders the English fallback today). MUST SURVIVE: title, sendToScreen (palette/dock), loading, linked (status bar), selectNote/detailEmpty (empty states), and the entire secondScreen.dashboard.* subtree — DashboardView is ALSO the MAIN-window home dashboard (+layout.svelte:8304), deleting those breaks the main window. cockpit.* block: PRESERVE (dial/lens/deck; estimationMap/controlDashboard/operationMap keys already exist for the three-zone naming, used by NoteGaugeDeck.svelte).
- **G3 CONTRACT: broadcastNoteSaved (screen:note-saved) senders + main-side adopt** @ `Senders: src/lib/components/NoteEditor.svelte:228,266,295,351,369; +layout.svelte:1555, 8576. Main listener: +layout.svelte:3413-3416 (wasRecentlyWritten guard → adoptExternalChangeIntoTabs)` [PRESERVE — untouched by the cut] — Both windows' NoteEditors broadcast every save; main adopts via the ONE shared freshness-gated helper (PJ-070). The cut removes SS NoteEditor mounts (SS stops SENDING since it never saves — it's read-only anyway), but the channel must stay: it drives the SS cockpitReload.
- **G3 CONTRACT: SS onNoteSaved listener u2 — the lenses' live-link freshness** @ `SecondScreenPage.svelte:812-843; cockpitReload++ at 841; cockpit re-fetch debounce SecondScreenCockpit.svelte:99-110 (450ms)` [PARTIAL-PRESERVE] — KEEP: the wasRecentlyWritten echo guard (814), the editorPanelsData content re-read (816-824 — refreshes the cockpit focus content), and cockpitReload++ (841). DIES: the splitCompanionData reload arm (825-835). refreshRecentLists (813) lives only if the Dashboard companion survives.
- **G3 CONTRACT: adoptFreshDiskIntoSS + adoptCompanionTab** @ `SecondScreenPage.svelte:734-775; consumers of its companion-tab list: dashboardNoteTab, dashboardSelectedNote, indexSelectedNote, mapCompanionNoteTab, peekTab (740, 760-764)` [SHRINKS (possibly to zero)] — All 5 companion $state tabs it services are read-only note copies in the cut. If Move 1 also removes the fallback note companion ($activeTab branch, SecondScreenPage:1870-1895) and the SS openTabs usage, the whole primitive + the externalChangeNoteModel import (line 26) + SINGLE_OWNERSHIP import can go. If ANY NoteEditor mount survives (e.g. skyview peek kept), it must stay for that mount.
- **G3 CONTRACT: cascade:rewrote listener (SS side)** @ `SecondScreenPage.svelte:850-856; Rust emitters src-tauri/src/libraries.rs:1687, 5596-5602` [PRESERVE (the cockpitReload arm)] — After a main-window rename cascade the cockpit must re-read its link zones (853: cockpitReload++). The adoptFreshDiskIntoSS arm (852) shrinks with the companion cuts. The app-wide Rust emit is untouched.
- **G3 CONTRACT: note-created (u5b) + library-changed (u5) listeners — allNotes freshness** @ `SecondScreenPage.svelte:893-907; consumer: allNotes → SecondScreenCockpit resolveTarget (SecondScreenCockpit.svelte:48-52) via prop at 1560` [PRESERVE (allNotes maintenance); Dashboard arm conditional] — The lenses resolve outgoing wikilink targets through allNotes — this must stay fresh. loadDashboardData() in both handlers dies if the Dashboard companion is cut (a Rule-8 win: it scans tags across all libraries on every debounced change). Note loadAllData (608-628) itself is a per-library collect_library_notes walk — pre-existing, survives for resolveTarget; flag for the HEALTH zone design to NOT add more read-time walks.
- **G3 CONTRACT: link-types:changed + loadLinkTypes (per-window registry)** @ `SecondScreenPage.svelte:784 (boot), 868 (universe switch), 876-877 (listener); sender src/lib/components/LinkTypesEditor.svelte:25 (notifyLinkTypesChanged)` [PRESERVE] — Without it every lens wedge renders neutral grey (cockpitGraphData.ts:17). Untouched by the cut.
- **CONTRACT: onLensChangeRequest (screen:set-lens) — single-settings-writer lens toggle** @ `SS requester SecondScreenCockpit.svelte:183 (requestLensChange); main listener +layout.svelte:3404-3406 (updateSettings noteGraphStyle); secondScreen.ts:129-138` [PRESERVE — but the toggle's HOME moves] — The lens toggle currently renders INSIDE the facet tab bar (.ck-tabs, SecondScreenCockpit.svelte:174-188, shown only when activeTab==='links'). Cutting the tab bar must re-home the lens toggle into the DECISION zone chrome — do not lose it with the bar. The request→main-writes→broadcast shape is the template for any new zone preference.
- **CONTRACT: screen:request-note-action (SS right-click → main performs)** @ `secondScreen.ts:114-122; main listener +layout.svelte:3421-3428 (→ handleOrgNodeMenuAction); SS builder SecondScreenPage.svelte:117-143; all current call sites in CUT branches (1479, 1494, 1617, 1632, 1853)` [PRESERVE the channel; call sites die] — After Move 1 zero SS call sites remain, but the channel + shared buildContextMenu pattern is the Display-not-Domain vocabulary the new zones' note lists will need. Keep listener + helper; do not delete from secondScreen.ts.
- **WORKSPACE PERSISTENCE: WorkspaceSecondScreen / ScreenState exchange** @ `Types: src/lib/libraries/store.ts:5584-5601; save: 5617-5643; restore: 5645-5674; main exchange +layout.svelte:8716-8744 (requestScreenState + onStateResponse, 2s timeout, fallback 8741); restore handler 8746-8769 (IGNORES screen state — 'SS always starts closed', 8764-8768); SS responder SecondScreenPage.svelte:910-924 (replies mode:'detail' + SS openTabs), restore listener 926-941` [CUT the exchange OR keep a stub responder — pick one] — YES, it serializes cut-mode state: the SS reply's tabs are the read-only note-copy openTabs; onWorkspaceRestore re-opens them. Restore side is already safe (ignored + field optional → old workspaces with a secondScreen blob stay loadable, no crash). Save side is the trap: remove the SS responder without removing the main-side exchange and EVERY workspace save with SS open stalls 2 seconds then writes the {open:true, mode:'grid'} fallback. Clean cut = remove requestScreenState/onStateResponse/sendScreenState/onStateRequest/onWorkspaceRestore + ScreenState + the saveWorkspace secondScreenState param + WorkspaceManager.svelte:11-30 plumbing, keep Workspace.secondScreen as a tolerated-optional legacy field.
- **RUST LIFECYCLE: second-screen window, hide-not-destroy, monitor gating** @ `src-tauri/src/lib.rs:100-108 (open), 217 (open_on_monitor), 264 (close), 272 (is_open), 660-693 (CloseRequested: SS hides + emits screen-hidden; main close also closes SS); src-tauri/tauri.conf.json:22-24 (label 'second-screen', url screen.html); mount src/screen-entry.ts:8-19; SS screen-hidden listener SecondScreenPage.svelte:786-789; ready handshake emitScreenReady 1107 / waitForScreenReady +layout:4974,4998` [PRESERVE — zero changes] — The cut is entirely inside the SS webview content + main-window senders. Window creation, monitor placement (openSecondScreenSmart), hide/show cycle, ready handshake, and main-close-closes-SS all stay byte-identical.
- **PJ-090 read-only Tasks surfaces (resolved-by-cut state)** @ `SecondScreenPage.svelte:1529-1541 (split companion TasksPanel readOnly), 1670-1683 (legacy editor-panels TasksPanel readOnly); cockpit 'tasks' stub facet SecondScreenCockpit.svelte:133` [CUT all three; write-toggle already gone] — The PJ-090 write toggle was already removed (commit 4196a9d2 — TasksPanel mounted readOnly). Move 1 removes the remaining read-only Tasks mounts + the stub facet. TasksPanel component itself is shared with the main window — only the SS mounts go. HEALTH zone re-admits tasks ONLY as an urgency signal, never a checklist.
- **THE 9 STUB FACETS + facet tab bar (the in-scope deletion, for completeness)** @ `SecondScreenCockpit.svelte:123-139 (FACETS array — 10 entries, only 'links' real), 174-188 (.ck-tabs), 200-205 ('this facet is wired in the next pass' stub)` [CUT (in-scope)] — Outside references: none — the FACETS ids are local. Only external coupling is the shared labelKeys (see settings.panels.panel* entry) and the lens toggle nested in the bar (see onLensChangeRequest entry). KEEP: DIALS (116-119), the dial pin logic (56-62), fetchLinks + path-guard (73-110), the four lens mounts (190-199), get_backlink_rows/get_outgoing_rows/get_note_review_status IPCs.
- **HELP FILE: Second Screen.md — stale sections inventory** @ `docs/help.uConstellation.World/Second Screen/Second Screen.md (291 lines; no translated Second Screen topic exists in any of the 14 help.{lang} trees — verified)` [STALE-DOC (rewrite with Move 2)] — Stale/dying: §Editor Panels Companion (37-51); §Mode-Based Companion table (54-66 — lists retired Navigator rows ×2, MIG-091); §Note Editing in Second Screen (147-156 — claims FULL EDITING incl. save/rename: contradicts read-only-always TODAY, pre-cut); §Dashboard Interaction 'All edits… saved' (161-176); §Navigator Companion (180-183 — retired); §Split View Comparison (208-218); §Index Companion (221-227); §Map Companion (230-240); §Workspace Integration (243-245 — restore never re-opens SS, +layout:8764); facet-tabs line 266 ('the rest are being wired in'); frontmatter description line 9. KEEP+UPDATE: two-monitor gate, opening methods, Settings Sync, RTL, the Knowledge Cockpit / lenses / gauge-deck sections (255-291).
- **USER MANUAL: §9 Second Screen + Index SS-integration — ×15 languages** @ `docs/User Manual.md:22, 854-858, 871, 1022-1056+; all 14 translations carry it (e.g. docs/help.ar/User Manual.md — 23 'الشاشة الثانية' mentions)` [STALE-DOC (update in the same PCS)] — §9's mode table (1035-1047) lists the Index/Map/Split/Dashboard companions; §'Second Screen Integration' under Index (854-858) describes term-click/compare → SS, which dies with the cut. 15 files to touch at ship.
- **INVARIANT 1 — SS never writes (Display-not-Domain)** @ `Anchor: ssReadOnly const SecondScreenPage.svelte:197-200; forward channel secondScreen.ts:114-122; PJ-090 comments 1531-1533` [INVARIANT] — Zero write IPC from the SS realm, ever. Any surviving note view mounts readOnly; every mutating right-click forwards to main via screen:request-note-action; the lens switch writes settings only via main (screen:set-lens). The HEALTH/WHERE/DECISION zones are reads + navigation requests only.
- **INVARIANT 2 — main-window right sidebar unaffected** @ `SettingsModal.svelte:2118-2129 (panel rows); +layout.svelte:8760-8761 (rightSidebarTab valid list); hide/restore pair +layout.svelte:4969-4970 / 3398-3399 / 4964-4965` [INVARIANT] — The cut deletes SS mirrors, not the originals. settings.panels.panel* keys, panelPlacements, and the SS-open→sidebar-hide / SS-close→sidebar-restore choreography all stay.
- **INVARIANT 3 — SS open/close lifecycle + monitor gating unchanged** @ `+layout.svelte:4958-4960, 4990, 6803, 2279 (hasMultipleDisplays gates); lib.rs:660-693; SecondScreenPage.svelte:786-789, 1107` [INVARIANT] — Two-monitor requirement, smart placement, hide-not-destroy, screen-hidden notification, ready handshake, main-close-closes-SS, SS-starts-closed-on-workspace-restore (+layout:8764-8768) — all byte-identical after Move 1.
- **INVARIANT 4 — focus-follow latency & zero keystroke IPC** @ `+layout.svelte:562-581 (fires on activeTab change only); SecondScreenCockpit.svelte:97-110 (path+nonce guard, 450ms save debounce)` [INVARIANT] — No new invoke() on the typing hot path in either window; the WHERE locator must key off the same activeTab-change event, not polling; a same-note keystroke must cause ZERO SS IPC (the existing path-guard proves the pattern).
- **INVARIANT 5 — G3 freshness-gated adopt both directions + echo guards** @ `Main: +layout.svelte:3413-3416 (wasRecentlyWritten → adoptExternalChangeIntoTabs); SS: SecondScreenPage.svelte:814 (echo guard), 841/853 (cockpitReload on save/cascade)` [INVARIANT] — After the cut the SS must still repaint its lenses on main-window save AND on rename cascade; the main window's adopt path is untouched. A dirty main-window model is never clobbered (freshness gate lives in the shared primitive).
- **INVARIANT 6 — single settings writer (main) for every SS preference** @ `secondScreen.ts:129-138; +layout.svelte:3404-3406` [INVARIANT] — Any new zone preference (e.g. HEALTH board sort) follows request→main-persists→broadcast, or stays per-session $state like cockpitDial (SecondScreenPage.svelte:183). Never a settings write from the SS (the PJ-089/PJ-090 clobber class).
- **INVARIANT 7 — no boot / typing / IPC regression; zones are Rule-8 cheap reads** @ `CLAUDE.md Rule 8; precedent: get_backlink_rows/get_outgoing_rows/get_note_review_status (SecondScreenCockpit.svelte:80-88 — index seeks)` [INVARIANT] — WHERE/HEALTH/DECISION read write-time-maintained derived data; NO whole-universe walk on SS open; measure on the 7,600+ note Universe before commit. The cut itself only REMOVES walks (loadDashboardData tag scans, split/editor-panels scanLibraryLinks) — a regression here is impossible unless Move 2 adds one.
- **INVARIANT 8 — workspace restore never crashes on a removed mode; saves never stall** @ `store.ts:5601 (optional field), +layout.svelte:8746-8769 (restore ignores SS state), 8720-8742 (the 2s exchange)` [INVARIANT] — Old workspaces carrying secondScreen blobs (mode 'detail'/'grid', tab lists) must load silently forever. AND: the save path must not inherit a 2s dead-wait once the SS responder is cut — remove both ends of the exchange in the same step.
- **INVARIANT 9 — i18n ×15 + RTL for every new string; shared keys survive the prune** @ `Consumers proving sharedness: DashboardView.svelte:156-353 (secondScreen.dashboard.* in the MAIN window), SettingsModal.svelte:2118-2129, +layout.svelte:2280-2281/6804` [INVARIANT] — New zone strings land in all 15 locale files with dir-aware layout (detectDir pattern already used at SecondScreenPage.svelte:161, 1179). Key deletion is consumer-checked per key, never per block.

**Risks:** WORKSPACE-SAVE STALL: cutting the SS onStateRequest responder (SecondScreenPage.svelte:910-924) without also removing the main-side requestScreenState exchange (+layout.svelte:8720-8742) makes every workspace save with SS open block 2 seconds and persist the bogus {open:true, mode:'grid'} fallback. Both ends must go in one step. | EXISTING DEAD-END BUG exposed by the cut: with SS open, clicking a note in the Index does NOTHING anywhere — +layout.svelte:6466-6479 sends screen:open-note and returns, but the SS listener is a no-op (SecondScreenPage.svelte:806-809). The cut must either delete that branch (note opens in main) or wire it to the new zones; leaving it is a silent-failure class item for the per-build safety-inspection. | SKY-VIEW CLICK BEHAVIOR COUPLING: handleSkyNodeClick (+layout.svelte:5051-5061) short-circuits main-window navigation when SS is open (pins to SS instead). If the Sky View companion is cut/changed without touching this, Sky-View node clicks with SS open become no-ops. The companion's fate is NOT in the ruled cut list — needs an explicit Architect line. | SPLIT COMPARISON COMPANION not named in the ruled Move-1 parenthetical but is a live, reachable panel-mirror (with a read-only Tasks tab). Cutting or keeping it must be an explicit ruling, not an implicit casualty. | UNIVERSE DASHBOARD COMPANION (SS tree-mode branch) is likewise unruled; the WHERE zone is its natural successor. If cut, emitSidebarModeChanged dies entirely; if kept, the loadDashboardData read-time tag walk survives — a Rule-8 tension either way. | LENS TOGGLE LIVES INSIDE THE CONDEMNED TAB BAR (SecondScreenCockpit.svelte:174-188): deleting .ck-tabs without re-homing the Butterfly/Ledger/Orrery toggle silently loses a Boss-validated shipped feature (2026-07-11). | i18n COLLATERAL: secondScreen.dashboard.* keys are consumed by the MAIN window's home dashboard (DashboardView is dual-mounted, +layout.svelte:8304); settings.panels.panel* keys are the main Settings panel rows. Block-deleting the 'secondScreen' locale namespace or the facet labelKeys breaks main-window surfaces in all 15 languages. | emitEditorPanels PAYLOAD is load-bearing: the lenses receive `content` through it (unlinked-mentions/gauge inputs). Any rename/slimming of the channel that drops content or the active:false close-signal (3400/4966) breaks the cockpit or leaves it showing a ghost note after SS close. | adoptFreshDiskIntoSS teardown ordering: if any SS NoteEditor mount survives (e.g. a kept skyview peek), removing the adopt primitive or the externalChangeNoteModel import while a mount remains reopens the G3 HIGH #1/#2 staleness bugs; conversely removing all mounts but keeping SINGLE_OWNERSHIP plumbing leaves dead model registrations. Cut must be all-or-consistent. | DOC DEBT ALREADY LIVE: the help file's 'Note Editing in Second Screen' section (147-156) promises full editing/save on the SS — false since the read-only ruling (2026-07-09). Ship the doc rewrite in the same PCS as Move 1 or the tutorial-grade help contradicts the app in 15 languages.

