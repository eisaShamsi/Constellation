# Session Handover — 2026-06-26

**For the next fresh session.** Read the orientation FIRST (always the highest version — **v3.07** as of
this handover); this file is the short bridge, not its replacement. Three open tasks below (§4–§6), each
with a paste-verbatim kickoff prompt (§7). None blocks the others — pick per priority.

---

## 1. State of play — shipped, verified, protected (all on `main`, pushed through `b29df058`)

- **MIG-086 — COMPLETE + Boss-validated end-to-end.** The "Suggested Connections" feature: when on a note,
  Constellation surfaces related notes (BM25 over the live FTS index, §A) and lets you make a **typed** link
  in one click (frontmatter-fold, §C) — in **all 5 hosts** (Reviewer, NotePane Backlinks tab, 360° Inspector ×2,
  Health/TensionPanel, Sky View per-node menu; §D), **localized ×15 + RTL-correct + documented** (help topic +
  User-Manual section in all 15 languages; §E). Direction is host-set (diagnostic→inbound, general→outbound;
  the user In/Out/Both toggle is PJ-067).
- **§E specifics (this session):** i18n ×15 (`panels.suggestedConnections`, `graphView.suggestConnections`,
  `backlinksPanel.filter`); a latent **NUL byte** in `RelatedCandidates.svelte` removed; **right-sidebar
  direction root fix** — `.rs-inner dir={$dir}` (UI direction, was `noteDir`) + 8-panel `dir="auto"` content
  audit; **"Sky View" localized everywhere** (7 keys → ar **منظر السماء**, etc.). Commits `19448e1c`,
  `29199057`, `1d5712b0`, `37bfbabb`, `51fc8c85`, docs `b29df058`.
- **PJ-066 — CLOSED (2026-06-25).** The ~2-min link-dense-note reindex freeze → **~3 s (≈15×)**, data correct.
  Four layers: §A1 sky COUNT `OR`→`IN(UNION)` (215×); §B per-edge sky triggers → save-path recompute diff +
  reconcile self-heal; §C1 reindex `#[tauri::command(async)]` (off UI thread); §C2 incremental diff-edges; §C3
  **lock-free `db_ready` readiness flag + a second READ-ONLY WAL connection `read_db`** (the real freeze fix).
  Phase-4 audited (1 P1 fixed: publish db+read_db+db_ready under one `federation_generation` check). Commits
  `8bd9039b`→`a0454fb9` + `4a1a290e`.

### Canonical architecture facts established this session (update your mental model)
- **Search-engine connections:** `SearchState` has **two** SQLite connections — `db` (single writer, WAL) +
  **`read_db`** (READ-ONLY WAL reader). Readiness is a **lock-free `db_ready` AtomicBool**; `with_read_conn()`
  routes reads to `read_db` (falls back to `db` only pre-init). `get_backlink_rows`/`get_outgoing_rows`
  (single-schema) use it.
- **Sky aggregates** (stratum/maturity) are **maintained write-time by a save-path diff** (`maintain_sky_after_save`
  + `recompute_all_sky` reconcile) — same shape as incoming/outgoing — NOT per-edge triggers.
- **`index_note`** rebuilds links **incrementally** (diff-edges; unchanged edges keep rowid + earned
  weight/confidence/traversal).
- **RTL model for the right sidebar:** `.rs-inner` follows the **UI** direction (it's chrome — panels ABOUT the
  note); note-content elements within (names, tags, task text, property values, outline headings) carry
  `dir="auto"`. Self-contained components (RelatedCandidates, Inspector360, TagsPanel, BacklinksPanel,
  OutgoingLinksPanel) set their own `dir`.

## 2. Repo / build facts
- One location: `E:\مشاريع كلاود\Constellation`, branch `main`, in sync with `origin/main` (`b29df058`).
- Binary `src-tauri/target/release/constellation.exe` built **2026-06-26 11:00** — carries all of MIG-086 +
  PJ-066. **Stage 0 of any test: check the binary mtime vs the commits under test** (and remember `npm run build`
  BEFORE `cargo build --release` for any frontend change — cargo alone re-embeds stale `build/`).
- **i18n edit pattern (reuse it):** a Python `json.load` → mutate → `json.dumps(ensure_ascii=False, indent=2)+"\n"`
  round-trip is **byte-identical** to these locale files (verified), so it produces clean minimal diffs. en.json/
  ar.json may be CRLF; verify per-file. ASCII commit messages only (heredocs choke on non-ASCII / control chars;
  use `git commit -m` ASCII or a `-F file`).
- Live universe for measurements: `E:/Constellation Universes/Eisa Cognitive Knowledge/` — ~234k links,
  ~7,661 notes, 18 libraries; `search.db` ~2 GB (large; every cold read is slow).

## 3. Open follow-ups (filed; none blocking)
- **Broader i18n pass** (Task A) — ~44 keys still English in the Arabic UI.
- **PJ-066 follow-up** (Task B) — residual ~3 s on mega-notes during the background reindex + complete the
  read-connection split (route search/stats reads off the writer lock; make sibling read commands async).
- **PJ-067** (Task C) — Living Link relationship typology v2 (Concept-Paper-first; the directional/symmetric +
  n-ary work deferred from MIG-086). Research banked: `docs/Living-Link-Relationship-Typology-Research-2026-06-24.md`.
- **PJ-065** — frontmatter parent/TOC link type. **StyleSetter** category labels hardcoded English.

## 4. Task A — Broader i18n pass (the localization completeness sweep)
A proactive audit (`ar` value == `en` value AND contains Latin letters) found **~44 keys still English** in the
Arabic UI, three buckets: **(1) ~20 clear UI labels** to translate ×14 — `commands.quickCapture`,
`settings.plugins.commandPalette`, `tabs.newTab`, `nav.home`/`libraries`/`skills`, the `universe.setup.*` Import
flow (~10), `libraries.linkLibrary`/`linkLibraryDesc`/`newLibrary`/`newLibraryDesc`, `orgChart.reset`, and a few
plugin **descriptions**; **(2) brand-name decisions (need a Boss ruling)** — `app.name` "Constellation",
`Constellation Sight`, `Constellation Map`, `Constellation Nervous System (CNS)`, `Dataview`, `lens.returnToLens`
"Return to CNS"; **(3) keep English** — `importer.formats.html`/`csv`, `fontSets.cjk`. Also the StyleSetter's
hardcoded category labels (a separate component surface). Governing principle: **feedback_full_localization_everything**
(top-principal — everything adapts to the UI language; use the right NATIVE equivalent, not transliteration).

## 5. Task B — PJ-066 follow-up (chase the last 3 s + finish the read split)
MIG-086/PJ-066 got the connect freeze to ~3 s on the single heaviest note (533 links). The residual ~3 s happens
DURING the now-background reindex; cause unconfirmed (needs ONE instrumented measurement — likely a remaining
write-connection read OR a frontend re-render). The §C3 `read_db`/`with_read_conn` infra is in place; the
"full read-connection split" the Boss chose was only partly applied (the connect's two panel reads). Complete it:
route the remaining pure-read commands (search/stats) to `read_db`, and apply `#[tauri::command(async)]` to the
sibling long-running read commands (`/simplify` flagged `constellation_search`/`link_stats` as inconsistent).

## 6. Task C — PJ-067 (Living Link relationship typology v2 — Concept-Paper FIRST)
Per the MIG-086 exploration, a typed link varies on DIMENSIONS (symmetry · transitivity · inverse/converse ·
arity binary↔n-ary · cardinality · taxonomic↔thematic) and FAMILIES (7 covered; uncharted: thematic/functional,
analogy/structure-mapping, n-ary synthesis, undercuts/undermines, problematizes/answers, qualifies). Two flags:
rename proposed `complements`→`co-completes`; "together form an idea" is genuinely **n-ary** (a synthesis node).
The thinnest Five Acts are **Tension** + **Synthesis**. **Boss ruling: Concept-Paper this FIRST** (state the
horse before any function) — do not jump to schema.

## 7. Kickoff prompts (paste verbatim, one per fresh session)

### Prompt A — Broader i18n pass
```
We're continuing Constellation at E:\مشاريع كلاود\Constellation on branch main.

First: git pull origin main. Then read "docs/Constellation Orientation & Onboarding v3.07.md" IN FULL
(the canonical orientation — read no older version), and skim docs/handover/Handover-2026-06-26.md (§1 state,
§4 is your brief).

Task: the broader i18n localization pass. Re-run the audit that found the gap: a Python script that flattens
src/lib/i18n/en.json and each locale, and lists keys where the locale value == the en value AND contains Latin
letters (likely untranslated). Reconcile against the §4 buckets in the handover.

Before translating anything, present the BRAND-NAME ruling decision to me (app.name "Constellation",
Constellation Sight, Constellation Map, Constellation Nervous System / CNS, Dataview, "Return to CNS") —
translate to native or keep English? Use AskUserQuestion. The full-localization top-principal
(feedback_full_localization_everything) leans translate, but these are product/feature brands — my call.

Then translate the ~20 clear UI labels ×14 locales (native equivalents, NOT transliteration; reuse each
locale's existing related vocabulary — fan out native-speaker agents per locale via a Workflow). Apply with the
byte-identical Python json round-trip (handover §2), verify all 15 parse + the keys are present, svelte-check 0,
rebuild (npm run build THEN cargo), and give me a staged Arabic test. Keep HTML/CSV/CJK English. Then SO close-out.
```

### Prompt B — PJ-066 follow-up
```
We're continuing Constellation at E:\مشاريع كلاود\Constellation on branch main.

First: git pull origin main. Then read "docs/Constellation Orientation & Onboarding v3.07.md" IN FULL, and skim
docs/handover/Handover-2026-06-26.md (§1 state + the "search-engine connections" architecture facts; §5 is your brief).

Task: PJ-066 follow-up. MIG-086/PJ-066 got the link-dense-note connect freeze to ~3 s; the residual happens during
the background reindex. SO #8 first: cross-check the brief against the CURRENT search.rs (db_ready / read_db /
with_read_conn shipped 2026-06-25, commits a0454fb9 + 4a1a290e — re-read them).

Reproduce-First: take ONE instrumented split-measurement of the connect on the heaviest note (Ancient history,
533 links) to pin whether the residual ~3 s is a remaining write-connection read or a frontend re-render — do not
guess (the layer was mis-diagnosed twice before). Then, per the Boss's "full read-connection split" choice:
route the remaining pure-read commands (constellation_search, link_stats, etc.) through with_read_conn/read_db,
and apply #[tauri::command(async)] to the sibling long-running read commands. State the function-in-hand + the
WA#4 wires-it-touches before code; verify on the live universe (no boot/typing regression); pause for my connect re-test.
```

### Prompt C — PJ-067 (Concept Paper)
```
We're continuing Constellation at E:\مشاريع كلاود\Constellation on branch main.

First: git pull origin main. Then read "docs/Constellation Orientation & Onboarding v3.07.md" IN FULL, and skim
docs/handover/Handover-2026-06-26.md (§6 is your brief).

Task: PJ-067 — Living Link relationship typology v2, CONCEPT PAPER FIRST (Concept-Before-Function: state the
horse before any function — do NOT touch schema/code). Read
docs/Living-Link-Relationship-Typology-Research-2026-06-24.md in full + the Living Link sections of CLAUDE.md +
the orientation. Cross-check proven prior art (WA#5): RDF/OWL property characteristics (symmetric/transitive/
inverse), SKOS, argumentation frameworks (undercut/undermine/rebut), structure-mapping (analogy), n-ary relation
modeling. Produce docs/concept-papers/PJ-067-Living-Link-Relationship-Model-v2-Concept-Paper.md: the concept
(the horse) for each proposed dimension/family, what it answers in the Five Acts (esp. Tension + Synthesis),
the complements→co-completes rename, the n-ary synthesis-node question. STOP at the concept paper for my ratification.
```

---

*End of handover. Written 2026-06-26 at session close. Companion records: orientation v3.07 (canonical),
SESSION-LOG-2026-06-26.md (everything shipped) + SESSION-LOG-2026-06-25.md (PJ-066), MoCh-2026-06-26-0500 +
MoCh-2026-06-25-1700. Latest commit b29df058; binary built 2026-06-26 11:00.*
