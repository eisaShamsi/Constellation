# Session Handover — 2026-06-26 (updated at afternoon close)

**For the next fresh session.** Read the orientation FIRST (highest version — **v3.14** as of this handover);
this file is the short bridge. Note: it's ~635K tokens (Arabic-dense) — read the **body §0–17** (canonical
current state, SO #8) + the recent version preambles; the older preamble is retained-for-diff version history.

---

## 1. State of play — shipped + Boss-validated (all on `main`)

- **MIG-086 — COMPLETE** (morning): the one-click typed "Suggested Connections" feature, all 5 hosts,
  localized ×15 + RTL-correct + documented. (See the earlier part of `SESSION-LOG-2026-06-26.md`.)
- **MIG-087 §E — SHIPPED + Boss-validated** (item 2): **the live PROPERTIES (خصائص) count** — a one-way,
  display-only `onLiveProps` observer (mirrors §C; never writes content; `noteModel` non-reactive), wired on both
  PropertyEditor instances + both NoteEditor mounts; emits the non-empty-key count in `debouncedSave()` (==
  settled count via `parseFrontmatter if(key)`). MIG-076 harness 28/28. **PLUS Boss-ruled all-noun Arabic
  singular-"one":** all 27 `ar plurals.*.one` → "{noun} واحد/واحدة" (gender-correct; خاصية واحدة …), which also
  improved §D's count-1 sentences. Orientation v3.10.
- **MIG-087 §D — SHIPPED + Boss Stage-1 PASS** (evening; backlog item 1): **the filed i18n sentence-embed
  remainder.** The 3 full-sentence count strings (`dialogs.batchDelete*`, `settings.arabicOverrides.reindexed`,
  `importer.andMore`) now use a `{noun}` placeholder filled by `$tn('plurals.<noun>', count)` (reused `plurals.*`,
  no new noun data); rewritten ×15 in native word order by a 15-rewriter→15-verifier Workflow (Arabic passive
  framing `ستُحذف {noun}` → grammatical dual ملاحظتان). svelte-check 0, 60/60 byte-identical, simulation-proven.
  Orientation v3.09. **(Doc-truth corrected: the note list has NO Constellation right-click; Note Navigator
  multi-select = per-row checkbox; File Explorer tree has no multi-select.)**
- **MIG-087 — COMPLETE + Boss-validated** (afternoon; earlier this day): **the broader i18n localization pass +
  a plural-aware count system + a live word/char counter.**
  - **§A** — 34 all-14-English keys localized ×14 (22 UI labels + 12 brand keys). **Boss brand ruling:**
    `app.name` "Constellation" → keep English; **Sight/Map/CNS/Dataview/"Return to X" → native**;
    HTML/CSV/CJK/360.3D → keep English.
  - **§B** — **plural-aware counts** via the browser-native **`Intl.PluralRules`** (CLDR). New `tn(key,count)` /
    `tnIn(loc,…)` in `src/lib/i18n/index.ts`; a canonical **`plurals.<noun>`** namespace (27 nouns ×15). Boss
    ruling: **Arabic 1 & 2 are word-only** (مفرد/مثنى, no number); 3–10 plural; 11+ singular (تمييز). Wired to
    **~50 count call-sites app-wide**. Fixes the wrong status bar ("19 مكتبات" → "19 مكتبة").
  - **§C** — **live word/char counter**: a one-way, display-only `onLiveStats` observer from `NoteEditor` →
    `+layout` (debounced); **never writes back into editor content** (outside the BUG-015/§C-2 vector;
    `noteModel` stays non-reactive). Now counts **body only** (frontmatter no longer inflates it).

### Canonical facts established this session (update your mental model)
- **i18n is now plural-aware.** For any counted noun use **`$tn('plurals.<noun>', count)`** — NOT
  `{count} {$t('noun')}`. The 27 `plurals.*` nouns live in every locale; add a new noun there (with each
  locale's CLDR categories) if you need one. `Intl.PluralRules(locale).select(n)` is the category source.
- **`noteModel` is deliberately non-reactive** (a plain Map) — do NOT make it reactive (re-introduces §C-2).
  Live editor signals to the host are **one-way display-only observers** (the `onLiveStats` pattern), never
  write-backs.
- **Brand policy:** `app.name` "Constellation" stays English everywhere; feature names render native (reuse the
  shipped `lens.title`/`orgChart.title`/`panels.skyView` etc.).

## 2. Repo / build facts
- One location: `E:\مشاريع كلاود\Constellation`, branch `main`. Binary `src-tauri/target/release/constellation.exe`
  rebuilt this session (carries MIG-087). **Stage 0 of any test: check the binary mtime.** `npm run build` BEFORE
  `cargo build --release` for any frontend change.
- **i18n byte-identical edit pattern (reuse it):** Python `json.load` → mutate → `json.dumps(ensure_ascii=False,
  indent=2)+"\n"` is byte-identical to these locale files (CRLF — verify per file). ASCII commit messages only.
- Live universe for measurements: `E:/Constellation Universes/Eisa Cognitive Knowledge/`.

## 3. Open follow-ups — Boss-prioritized order (2026-06-26; do one at a time, top to bottom)
1. ~~**i18n sentence-embeds**~~ — ✅ **DONE (Item 1, evening) + Boss Stage-1 PASS.** All 3 sentences use a
   `{noun}` placeholder filled by `$tn('plurals.<noun>', count)` (notes ← `plurals.notes`, andMore ← `plurals.more`;
   no new noun data). See MIG-087 §D above + orientation v3.09.
2. ~~**Live "properties" count**~~ — ✅ **DONE (Item 2, MIG-087 §E) + Boss-validated.** One-way `onLiveProps`
   observer mirroring §C; + all-noun Arabic singular-"one" (واحد/واحدة). See above + orientation v3.10.
3. ~~**PJ-065**~~ — ✅ **CONCEPT RATIFIED (Item 3) + Boss "Approved."** Concept paper
   `docs/concept-papers/PJ-065-Parent-TOC-Link-Type-Concept-Paper.md` — the structural link = the compositional
   spine (distinct KIND from the 8 cognitive acts: ordered/tree/authored/no-living-apparatus; serves Externalization).
   **Build deferred to a future `/migration`** (name + final design decisions ruled at the Architect phase). See
   orientation v3.11.
4. ~~**PJ-067**~~ — ✅ **CONCEPT RATIFIED (Item 4) + Boss "Family, it is."** Concept paper
   `docs/concept-papers/PJ-067-Living-Link-Relationship-Model-v2-Concept-Paper.md` (dimensions = declarable algebra;
   families by the Five Acts; co-completes rename; the n-ary SYNTH node shared with analogy). **R4 wargamed** (3
   steelman defences + findings: `PJ-067-R4-Wargame-Question-Relation.md`) → **R4 = FAMILY** (`problematizes/answers`
   = its own interrogative family; the 8 + §7 stay frozen). Hierarchy cross-ref added to PJ-065 §2.1 + PJ-067 §2.
   **Build deferred to a sequence of `/migration`s (Tension-first).** See orientation v3.12.
5. ~~**i18n broader remainder**~~ — ✅ **DONE (Item 5) + Boss "Pass."** Triaged (≈75% intentional: disabled Sight,
   CECE en+ar taxonomy [PJ-043], brand names, CLDR plurals, Badge-Taxonomy codes, `_comment`; styleSetter→Item 6).
   Boss ruled the thorough sweep → **854 translations** across the 13 non-Arabic locales (174 keys) via a
   14-locale native Workflow. svelte-check 0, byte-identical, Boss Stage-1 (non-Arabic) PASS. Orientation v3.13.
   **Don't re-flag the intentional buckets.**
6. ~~**StyleSetter category labels**~~ — ✅ **DONE (Item 6) + Boss "Pass."** Data-only (labels already route through
   `L()`→`$t` with English fallback; "hardcoded" = untranslated). 36 English-left keys → 6 keep-English (brand/codes)
   + 24 auto-reuse established native terms + 41 native-translated; 65 `styleSetter.labels` ×13 locales. `sky_view`
   now native (Vue du ciel / Himmelsansicht …). svelte-check 0; Boss Stage-1 (fr/de) PASS. Orientation v3.14.
7. **PJ-066** — speed: residual ~3 s on mega-notes during the background reindex + finish the read-connection
   split (route remaining pure-read commands through `with_read_conn`/`read_db`; sibling read commands → async).
   Reproduce-First (one instrumented measurement; mis-diagnosed twice before).

## 4. Kickoff prompt (paste verbatim — covers the whole §3 list in priority order)
```
We're continuing Constellation at E:\مشاريع كلاود\Constellation on branch main.

First: git pull origin main. Then read "docs/Constellation Orientation & Onboarding v3.08.md" — the body
§0-17 (canonical current state, SO #8) + the v3.08 preamble (the file is ~635K tokens, Arabic-dense; read
the body, not the whole file). Skim docs/handover/Handover-2026-06-26.md. Ultracode is on — use Workflows
for substantive work. SO #8: cross-check each PJ against the orientation body + session logs before starting it.

Work the backlog below IN THIS ORDER, ONE AT A TIME. After each shippable item: staged Boss test where
user-facing, then full SO close-out (commit/push + session log + orientation v-bump + MoCh + handover) before
moving to the next. Don't batch.

1. i18n sentence-embeds (finish MIG-087's remainder). Three full-sentence count strings still ignore plural
   grammar: dialogs.batchDeleteTrash / dialogs.batchDeleteSystem ("Delete {count} notes? ..."),
   settings.arabicOverrides.reindexed ("Reindexed {count} note(s)"), importer.andMore ("and {count} more...").
   Approach: rewrite each sentence x15 with a "{noun}" placeholder (native word order -> native-speaker agents
   via a Workflow), fill it at the call site with get(tn)('plurals.notes', count). REUSE the existing plurals.*
   namespace; add no new noun data. Byte-identical Python round-trip; svelte-check 0; staged Arabic test at
   counts 1/2/3/11.

2. Live "properties" count. The right-sidebar/status-bar خصائص count still refreshes only on tab-switch/save,
   not as you edit properties. Mirror MIG-087 §C exactly: a one-way, display-only observer from the Property
   editor -> +layout (debounced); NEVER write back into editor content; noteModel stays non-reactive. Verify
   with the MIG-076 harness.

3. PJ-065 - frontmatter "parent / table-of-contents" link type. Cross-check the orientation first; design the
   concept before any code.

4. PJ-067 - Living Link relationship typology v2. CONCEPT-PAPER FIRST (Concept-before-Function: state the horse
   before any function - do NOT touch schema/code). Read docs/Living-Link-Relationship-Typology-Research-2026-06-24.md
   in full + the Living Link sections of CLAUDE.md + the orientation. Cross-check proven prior art (WA#5):
   RDF/OWL property characteristics, SKOS, argumentation frameworks, structure-mapping, n-ary relation modeling.
   Produce docs/concept-papers/PJ-067-Living-Link-Relationship-Model-v2-Concept-Paper.md (the horse for each
   dimension/family; what it answers in the Five Acts, esp. Tension + Synthesis; the complements->co-completes
   rename; the n-ary synthesis-node question). STOP at the concept paper for Boss ratification - build nothing.

5. Broader i18n remainder (~382 scattered partial-locale gaps). TRIAGE FIRST, then get a Boss scope ruling
   before any sweep: much is intentional (CECE bilingual taxonomy = en+ar by design per PJ-043, disabled
   Sight v3/v6 strings, _comment dev keys, philosopher proper-names). Present the categorized buckets + a
   recommendation; only translate what the Boss approves.

6. StyleSetter category labels - still hardcoded English in the StyleSetter component. Localize x15 via $t
   (reuse existing vocab); svelte-check 0; staged test.

7. PJ-066 - speed: the residual ~3s on very large notes during the now-background reindex + finish the read-
   connection split. Reproduce-First: take ONE instrumented split-measurement on the heaviest note (Ancient
   history, 533 links) to pin whether the residual is a remaining writer-lock read or a frontend re-render -
   do NOT guess (mis-diagnosed twice before). Then route remaining pure-read commands through with_read_conn/
   read_db and make sibling long-running read commands #[tauri::command(async)]. Re-read search.rs first
   (db_ready/read_db/with_read_conn shipped 2026-06-25). Verify on the live universe: no boot/typing regression.
```

---

*End of handover. Updated 2026-06-26 afternoon close (Boss-prioritized backlog 1-2-5-4-7-6-3). Records:
orientation v3.08 (canonical), SESSION-LOG-2026-06-26.md (MIG-086 §E + MIG-087), MoCh-2026-06-26-1300 (+ -0500).*
