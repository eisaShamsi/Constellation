# Session Handover — 2026-06-10

**For the next fresh session(s).** Two prepared tasks, each with its own kickoff prompt (§4 and §5) —
Eisa runs them in separate sessions. Read the orientation FIRST (always the highest version file —
**v2.64** as of this handover); this file is the short bridge, not the orientation's replacement.

---

## 1. State of play — shipped, verified, protected (all on `main`, pushed)

Everything below is Boss-validated and committed through `74bb7648` + the close-out commit carrying this file.

- **MIG-073 CLOSED** (`milestone/mig-073-kh-cache` tag + ZIP in `E:/Backups/Constellation/`). The Knowledge
  Health overlay reads ONE snapshot IPC (`constellation_knowledge_health_snapshot`) off the general
  **`link_stats_cache`** table (Perf-Rule-8; 0.17 ms vs the old ~11s cold scan); stale-while-revalidate
  (2-min window, in-place refresh via `kh-snapshot-ready`); first-time population one-off on boot;
  unconditional refresh post-reconcile. Audit: invariants 8/8, drift 10/10, Scenario-2 self-healing shipped.
  **This cache is the data layer CCS §8 assumes — KH is its FIRST consumer, CCS is the intended second.**
- **Decay-write corruption killed** (`aa9941ee`): `constellation_link_decay` is strictly READ-ONLY now;
  the boot + 24h decay jobs are deleted; decay is display-only (`effectiveLinkWeight`).
  **Consequence: nothing writes `status='dormant'` anymore** — dormancy rows are historical-only until a
  read-time derivation ships (a CCS-era item).
- **The right-sidebar "Knowledge Health" tab fixed** (`b2a23d4e`): it renders **TensionPanel** (NOT the KH
  overlay); its loader was disconnected from boot → eternal "Loading…". Now lazy on tab activation, scoped
  to the active note's library, cached per library, honest analyzing/unavailable states ×15 locales.
  `detect_tensions` itself is an fs-walk (reads every .md per run) — **its Rule-8 modernization is queued
  for the CNS/CCS reorganization**, not done.
- **Right-sidebar polish**: tension/review-pulse/provenance panel roots self-scroll (`fcd43b3c`);
  contradictions deduped to one row per pair with ×N + stable sort (`1325c5e7`); KHD card bleed fixed.
- **KHD fully localized ×15** (`20e333b0` + `848c3049`): every overlay string through `$t()`;
  `linkTypes.relates` added ×15 (it was missing everywhere — 301 live links carry that legacy type);
  **LinkTypePill app-wide** now falls back locale → registry label → raw id (custom types show their
  user-given label, e.g. "Inspires"); RTL row alignment fixed via `text-align: match-parent`.
- **Docs current**: orientation **v2.64** (canonical); Pending Jobs **v1.13**; session log
  `lab/reports/SESSION-LOG-2026-06-09.md` (covers 06-09 → 06-10); MoCh ×2 for 06-10.

## 2. Repo / build facts

- One location: `E:\مشاريع كلاود\Constellation`, branch `main` (in sync with `origin/main`).
  (`git status -sb` shows an odd "ahead of origin/ConstellationMain" — main's upstream points at the
  RELEASE branch; cosmetic. Verify against `origin/main` if in doubt.)
- Binary `src-tauri/target/release/constellation.exe` built 2026-06-10 13:48 — carries everything above.
  **Stage 0 of any test: check the binary mtime vs the commits under test.**
- en.json + ar.json are CRLF; the other 13 locales LF. Preserve per-file endings (the session's Python
  merge-script pattern with the flat-diff verification is in the session log — reuse it).
- The live universe for measurements: `E:/Constellation Universes/Eisa Cognitive Knowledge/` —
  **234,061 links / 7,661 notes / 18 libraries; search.db is 1.7 GB** (suspicious for the size — likely
  FTS bloat; flagged, uninvestigated; makes every cold read slow).

## 3. Open follow-ups (noted, not blocking either task)

- **The 1.7 GB search.db** — investigate why (VACUUM? FTS bloat?). Global cold-read win if fixed.
- **Translation debt**: the 14 `docs/help.{lang}/` manuals are stale snapshots missing whole chapters
  (e.g. the Tension Detector). Needs a dedicated sync pass — do NOT patch sentences into missing chapters.
- **Custom link-type per-locale labels** (registry schema extension, MIG-066-family) — Boss aware, not asked.
- **PJ-063** — `note_links.link_type` 'relates' bug: re-verify under MIG-067 (KHD by_type data now looks
  healthy — supports 104,719 / derives-from 97,958 / relates only 301 — so the entry may be stale).
- Sight wrapper cluster in `store.ts` (~1605-1709) is dead code pending the Wings decision (simplify P2).

## 4. Task A — CCS `/migration` Architect (Phase 1 ONLY)

**What CCS is:** the **Constellation Circulatory System (الجهاز الدوري)** — the ratified circulatory
complement to CNS, answering the Cognitive-Engine Connection question from the flow side (weight · decay ·
traversal · lifecycle · confidence). Concept **RATIFIED 2026-06-09**:
`docs/Constellation-Circulatory-System-Concept-Paper-v1.1.md`. Seven cognition-named registers (§6);
contracts in §8 (reads `note_links` + the Link-Type Registry; writes ONLY via existing lifecycle commands;
**`link_stats_cache` from MIG-073 is the assumed aggregate layer**); rulings in §15 (left-dock Core
Plug-in, peer of CNS/Cataloger; **fully retire** the right-sidebar Link Dashboard panel; **coordinate —
not subsume** — Knowledge Health; re-point the MIG-007 hub button when CCS ships).

**Architect-scope items to rule in/out explicitly:** (a) `detect_tensions` fs-walk → Rule-8 modernization
(its registers are CNS-topology: orphans/gaps/single-points; tension/contradictions sit at the boundary);
(b) read-time dormancy derivation (see §1 — dormancy is write-orphaned); (c) CCS §15 open items: exact
left-dock placement + icon, per-register trends, the Knowledge-Health coordination surface (link vs card).

**The kickoff prompt is at the END of this file (§6, Prompt A) — paste it verbatim into a fresh session.**

## 5. Task B — PJ-060 (`index_note` cache-hit short-circuit)

**The entry** (Pending Jobs v1.13, ~line 1113; top-of-queue #1; P1 "highest-leverage open fix"; mini-MIG):
`index_note`'s cache-hit short-circuit (`search.rs:3004` as of 2026-05-19 — **re-locate it**, search.rs
has shifted since) returns early when the file mtime matches, so the write-time refresh of `note_meta`
never fires on an unchanged-mtime re-save — a write-time-derivation blocker (traced as the MIG-029 root
cause). **Acceptance:** an unchanged-mtime re-save still refreshes `note_meta` + every write-time-derived
surface; no boot/typing regression on the big universe.

**Mandatory first step (SO #8):** cross-check the entry against the CURRENT code — MIG-066/067 and
MIG-073 (2026-06-10) all touched `search.rs`. Confirm the short-circuit still misbehaves AND that no
consumer now depends on the early return. If stale/fixed → STOP and mark it in the next Pending Jobs
version before any other work.

**The kickoff prompt is at the END of this file (§6, Prompt B).**

## 6. Kickoff prompts (paste verbatim, one per fresh session)

### Prompt A — CCS Architect

```
We're continuing Constellation at E:\مشاريع كلاود\Constellation on branch main.

First: git pull origin main. Then read docs/Constellation Orientation & Onboarding v2.64.md IN FULL
(the canonical orientation — read no older version), and skim docs/handover/Handover-2026-06-10.md
(§1 state, §4 is your brief).

Task: open the CCS /migration — Phase 1 (Architect) ONLY. Read the RATIFIED concept
docs/Constellation-Circulatory-System-Concept-Paper-v1.1.md in full (especially §6 registers, §8
contracts, §13 migration shape, §15 rulings), plus docs/Living-Link-Concept-Paper-v1.0.md (the link
canon) and lab/reports/MIG-073-KH-CACHE-ARCHITECT.md + MIG-073-KH-CACHE-PLAN.md (the link_stats_cache
aggregate layer CCS §8 assumes — shipped 2026-06-10, Knowledge Health is its first consumer).

Fold into the Architect's scope-mapping with explicit in/out rulings: (a) detect_tensions' fs-walk
Rule-8 modernization (queued to the CNS/CCS reorganization — SESSION-LOG-2026-06-09 §HEALTH-TAB FIX);
(b) read-time dormancy derivation (nothing writes status='dormant' since the decay fix — same log,
§MIG-073 P4); (c) the CCS §15 open items: left-dock placement + icon, per-register trends, the
Knowledge-Health coordination surface.

Allocate the next free MIG number from orientation §8 (verify the highest used — don't assume). State
the function-in-hand in one line, cross-check this brief against the orientation §4.x BODY + the recent
session logs (SO #8), then produce lab/reports/MIG-0XX-CCS-ARCHITECT.md: territory map, design options
with speed/effort/risk, invariants that must not break. STOP after the Architect — present it for my
ratification before any Plan.
```

### Prompt B — PJ-060

```
We're continuing Constellation at E:\مشاريع كلاود\Constellation on branch main.

First: git pull origin main. Then read docs/Constellation Orientation & Onboarding v2.64.md IN FULL
(the canonical orientation — read no older version), and skim docs/handover/Handover-2026-06-10.md
(§1 state, §5 is your brief).

Task: PJ-060 — the index_note cache-hit short-circuit fix (Pending Jobs v1.13 top-of-queue #1, P1
"highest-leverage open fix", mini-MIG). Read the full PJ-060 entry in
"docs/Constellation Pending Jobs v1.13.md" (~line 1113) and its source section in
lab/reports/SESSION-LOG-2026-05-19.md before anything else.

MANDATORY first step (SO #8 — cross-check before tackling): the entry claims index_note's cache-hit
short-circuit (search.rs:3004 as of 2026-05-19 — re-locate it; the file has shifted since) returns
early on matching mtime so the write-time refresh of note_meta never fires on an unchanged-mtime
re-save. Verify this still holds under the CURRENT code (MIG-066/067 + MIG-073 all touched search.rs),
and check whether any consumer now depends on the early return. If the entry is stale or already fixed,
STOP and mark it accordingly in the next Pending Jobs version before any other work.

If confirmed: state the function-in-hand in one line, then propose the fix approach (the wires it
touches — WA#4) and the verification plan (acceptance: an unchanged-mtime re-save refreshes note_meta +
every write-time-derived surface; no boot/typing regression on the 7,600-note universe) BEFORE writing
any code, and wait for my approval.
```

---

*End of handover. Written 2026-06-10 ~14:15 at session close; companion records: orientation v2.64
(canonical), MoCh-2026-06-10-1240 + -1245 (the day's chat), SESSION-LOG-2026-06-09.md (everything shipped).*
