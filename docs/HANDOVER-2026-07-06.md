# Handover — 2026-07-06 (end of the MIG-092 session)

**Branch:** `main` · **All work committed** (push at session close). · **Binary:** final release build 2026-07-06 (localized, "done" removed, efficiency fix).

---

## Where we stand (one screen)

**MIG-092 — Collections on the Search Hub — SHIPPED + Boss-validated (Stage 1 + Stage 2) + closed out.**

The parked MIG-090 Workbench's durable-basket machinery was repurposed into a **Collections** feature: search → hand-pick results → a named, persistent, task-scoped basket. **Boss rulings honored:** UNIFY the former Bookmarks in (Starred = a pinned, undeletable collection); a **Results | Collections** header tab inside the Search Hub; the **"done"/sweep concept dropped** (a note is in a collection or you ✕ it out).

**What shipped (commits `3e6aba1b` → `f0f7ddbc`):**
- **§1** `collections.rs` (`collections_hydrate`, note-only) · `universe.rs` (`read/save_universe_collections`; a legacy `workbench.json` is migrated-then-retained as `.migrated`; `read_universe_bookmarks` kept READ-ONLY for the one-time migration).
- **§2** pure `collectionsLogic.ts` reducers (unit-tested) + a thin `store.ts` shell (`collectionSets`; `loadCollections` runs the idempotent **Bookmarks→Starred** migration; `hydrateCollectionNotes`). The Workbench **surface** was retired here.
- **§3** mixed-member hydration (notes carry live facts; folder/search render inline).
- **§4** `CollectionsPanel.svelte` — the Collections tab (switch/create/rename/delete; remove members).
- **§5** pick-up: **"Add to collection ▸"** submenu on file-tree notes AND search results (shared `contextMenuBuilder`).
- **§6** the sidebar "Bookmarks" section **is** the Starred collection now — flat list, a **cUniverse / library / folder** breadcrumb on the reading-END side, per-title RTL rows, a right-click note menu; ⭐ → `toggleStarred`.
- **§7** four state chips (due/unlinked/contested/forming — narrow, never expand; zero IPC). **"done" dropped** per Boss ruling.
- **§8** debounced liveness (re-hydrate on existing mutation events; unlisten on destroy).
- **§9b** retired the `enabledFeatures.workbench` flag + Settings toggle, the vestigial `bookmarks` store, `save_universe_bookmarks`, and all dead `showWorkbench`/`.workbench-overlay`.
- **§10** i18n `collections.*` across **all 15 locales** (native translations) − `workbench.*`; User Manual + a help topic; Orientation **v3.26** (new file); `/simplify`; the **clean audit trio**.

**Verification:** svelte-check 0 errors throughout · `cargo test collections::` + `tests/mig-092` + the content-integrity harness (`tests/mig-076`) + the pinned chips subset test all green (full suite **224/224**) · **audit trio CLEAN** (invariants: all 8 design rules; drift: none; migration-path: all 6 data-safety paths safe).

**Docs:** English User Manual + help topic; Orientation v3.26; this handover; MoCh `2026-07-06`. *(The 14 translated User-Manual prose sections are a documented follow-up — the app UI is fully localized in all 15 languages, which is the user-facing requirement.)*

---

## What's queued (ranked — start here next session)

1. **Quick Switcher speed** *(reproduce-first).* Boss wants Ctrl+O faster at title retrieval — NOT a new engine (Ctrl+O already IS the title search, reading an in-memory cache). **Measure where the delay actually is** under instrumentation before touching anything. No fix ships before the delay is reproduced.
2. **PJ-069** — the whole-entity deduplication pass (tags×6, folders×4, recents×3, orphan/fragile finders×5, hub-panel overlap, note-lists hand-rolled ×9). Concept-paper-first. *(MIG-092 seeded the shared `NoteRow`/`NoteList` primitive — the adoption is PJ-069.)*
3. **PJ-068** *(parked)* — Second-Screen contextual-companion rework (SS must be contextual, never replicate the main screen). Concept paper exists; per-mode rulings pending at reopen.

**Older backlog (unchanged):** the freeze queue L′/F1/F3, `get_360_view` index-read `/migration`, `init_db` profiling, MIG-088 Phases 6–10, the Arabic-callout End/Home caret.

**Trivial cleanups noted (not blocking):**
- The boot bundle (`boot_bundle.rs`) still fetches `bookmarks` the frontend no longer reads — negligible; drop it in a future pass (not touched at close-out per WA#4).
- 14 translated User-Manual prose sections for the Collections chapter (app i18n is done).

---

## Watch-outs
- **Collections is membership-only — it NEVER writes note content.** Adding/removing a note touches only `collections.json`. (Editor-Surface Gate not triggered; the harness was still run after §6/§9b.)
- **Starred is pinned** (id `starred`, undeletable). The Bookmarks→Starred migration is **idempotent + reversible** — `bookmarks.json` and `workbench.json.migrated` are both retained as backups. `collections.json` is the source of truth.
- **ONE-universe still stands.** `bookmarkLocation()` uses a longest-prefix library match + `childUniverseLibPaths` so federated members resolve correctly.
- **cid_cn `''` is a sentinel** — `collections_hydrate` guards `AND cid_cn != ''`.
- **Chips NARROW, never expand** — pinned by `tests/mig-090/chips.test.ts`; hydration `$effect` depends on `keySig`/`activeId` only, never on chips (zero IPC on toggle).
- **`cargo build --release` alone re-embeds stale frontend** — run `npm run build` first, then grep `build/_app/immutable/` for a new UI string before any Boss test.

---

## Ready-to-paste next-session prompt

```
Resume Constellation. Branch main; git pull first, then git log --oneline -8.
Read docs/Constellation Orientation & Onboarding v3.26.md and docs/HANDOVER-2026-07-06.md.
Ultracode is on — use the Workflow tool for substantive multi-step work + adversarial verify.

MIG-092 (Collections on the Search Hub, unifying Bookmarks as Starred) is shipped,
Boss-validated, and closed out. Start the top queued item:

**Quick Switcher speed** (reproduce-first). The Boss wants Ctrl+O faster at title
retrieval — it is NOT a new engine (Ctrl+O already IS the title search and already
reads an in-memory cache). Per the Reproduce-First rule: instrument and MEASURE where
the delay actually is before designing or shipping anything. State the function-in-hand
first; if a fix crosses subsystem boundaries, run /migration. Stop at the plan for approval.
```
