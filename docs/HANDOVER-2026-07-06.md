# Handover — 2026-07-06 (end of the MIG-092 + MIG-093 day)

**Branch:** `main` · **All work committed + pushed.** · **Binary:** final release build after §E (localized, instrumentation-free).

---

## Where we stand (one screen)

**Two migrations shipped + Boss-validated today:**

**MIG-092 — Collections on the Search Hub** *(closed this morning; see HANDOVER section in SESSION-LOG-2026-07-05.md)*: the parked Workbench machinery repurposed into hand-picked, persistent, named baskets on a Results | Collections tab; the former **Bookmarks unified in as the pinned "Starred" collection** (sidebar mount, location breadcrumbs, per-title RTL, right-click menu); "done" concept dropped; Workbench fully retired; i18n ×15; audit trio clean.

**MIG-093 — the Quick Switcher, the pure title jumper** *(closed now)*:
- **Reproduce-First delivered the diagnosis:** Boss traces on an instrumented build showed Ctrl+O's embedded content search at 20-29s per keystroke-pause (FTS5 native `snippet()` re-tokenizing EVERY matching row before LIMIT; stale runs piling on the db lock) and exact-title hits buried by an unranked cap-20 local filter. **Boss ruling: "Ctrl+O should focus only on the titles" + a research directive** (Obsidian/VS Code/fzf/peers — unanimous: names only, banded ranking, content demoted/elsewhere).
- **Shipped:** §D/§D-2 two-phase rank-then-fetch in BOTH search commands (`lexical_search_in_schema` + the universal `search_contents`) — native `snippet()` gone from every production path, `include_snippet` honored · §A `read_notes` federated → `allNotes` spans the ONE universe (~9.1k titles) · §B `$lib/searchFold.ts` (shared Arabic/Latin fold; IndexPanel re-pointed) · §C `$lib/switcherRank.ts` (banded: exact>prefix>word-boundary>fuzzy + tie-breaks; pinned test: `islam` → exact title #1) + the `QuickSwitcher.svelte` rewrite (titles+aliases only, ZERO IPC per keystroke, empty=recents, pinned **Create note** + **Search in Search Hub** rows) · §E instrumentation removed, i18n ×15, User Manual §5 section, Orientation **v3.27**, /simplify (collator hoist).
- **Boss-measured:** `islam` 25.0s+heavy-thrash → **2.7ms over 9,161 candidates**; `knowledge` 29.3s → **1.6ms**; Search Hub content ~25s → "Much better. Pass."

**Docs:** `MIG-093-Architect-Plan-Quick-Switcher.md` · Orientation v3.27 · SESSION-LOG-2026-07-05/06 · MoCh ×2 · this handover.

---

## What's queued (ranked — start here next session)

1. **PJ-069 — the whole-entity deduplication pass.** The MIG-090 map found: tags×6, folders×4, recents×3, orphan/fragile finders×5, hub-panel overlap, note-lists hand-rolled ×9, two confidence-menu copies. Concept-paper-first. *(Seeds already planted: the shared `NoteRow`/`NoteList` primitive (MIG-090/092), `$lib/searchFold.ts` (MIG-093 — the Index filter already re-pointed), `$lib/switcherRank.ts`.)*
2. **PJ-068** *(parked)* — Second-Screen contextual-companion rework; concept paper exists; per-mode rulings at reopen.
3. **Older backlog (unchanged):** the freeze queue L′/F1/F3 (incl. FileTree virtualization), `get_360_view` index-read `/migration`, `init_db` profiling, MIG-088 Phases 6–10, the Arabic-callout End/Home caret.

**Trivial cleanups noted (not blocking):** the boot bundle still fetches a `bookmarks` list nothing reads (drop in a future pass) · the 14 translated User-Manual prose sections for the Collections + Quick Switcher chapters (app UI is fully localized ×15; the English manual is current).

---

## Watch-outs
- **The two-phase search shape is now load-bearing:** rank on the index FIRST (no joins, no aux functions), fetch details for ONLY the returned rows. Never reintroduce FTS5 native `snippet()` (external-content tables re-tokenize the full body per matching row, pre-LIMIT) — synthesize snippets in Rust (`synth_snippet_for_body`) for the winners only.
- **Ctrl+O is titles+aliases ONLY** (Boss ruling + the whole-entity law: the switcher owns "jump by name"; content = the Search Hub). Don't add content search back; the escape row is the hand-off.
- **`$lib/searchFold.ts` is THE frontend fold** — keep in parity with Rust `normalize_stripped`/`fold_letters` (documented in its header); never re-inline a copy.
- **`allNotes` spans federated schemas** (§A) — anything consuming it now sees cUniverse titles; the `federation:ready` handler re-fetches it (shrink-overwrite guarded).
- **`cargo build --release` alone re-embeds stale frontend** — `npm run build` first; grep `build/_app/immutable/` for a new string before any Boss test.
- **Collections/Starred rules** (MIG-092): membership-only, never writes note content; Starred pinned; migration idempotent + reversible (backups retained).

---

## Ready-to-paste next-session prompt

```
Resume Constellation. Branch main; git pull first, then git log --oneline -8.
Read docs/Constellation Orientation & Onboarding v3.27.md and docs/HANDOVER-2026-07-06.md.
Ultracode is on — use the Workflow tool for substantive multi-step work + adversarial verify.

MIG-092 (Collections) and MIG-093 (Quick Switcher = pure title jumper, 25s→2ms) are
shipped, Boss-validated, and closed out. Start the top queued item:

**PJ-069 — the whole-entity deduplication pass** (concept-paper-first, per Standing
Order #8 cross-check the PJ against orientation §4.x bodies + session logs before
starting). The MIG-090 map found: tags×6, folders×4, recents×3, orphan/fragile
finders×5, hubs 3-way, note-lists hand-rolled ×9, confidence-menu ×2. Seeds already
shipped: NoteRow/NoteList, $lib/searchFold, $lib/switcherRank. State the concept (the
horse) first; stop at the concept paper / plan for my approval before building.
```
