# Minutes of Chating — 2026-07-16, block starting ~12:15

*(Direct Boss ↔ Claude interaction only. What shipped is in `lab/reports/SESSION-LOG-2026-07-16.md`.)*

## Block scope
The PJ-108 live crash-recovery test — reroutes, the run, the verdict.

## The exchanges

1. **Stage-2 reroute #1.** Claude's original path (click the wikilink in `PJ108 Linker`) hit the
   save-protection: **Boss: "I cannot open the linker note while the red banner is on."** Claude
   confirmed that blocking is CORRECT behavior (the PJ-102 arc's protection) and rerouted the test to
   the second screen's own doors.

2. **Stage-2 reroute #2.** **Boss sent a screenshot:** the SS showed the Knowledge Cockpit (Orrery) —
   "I can only click the node." Claude confirmed the Cockpit is the SAFE surface (node clicks delegate
   to main) and, tracing the reachable doors in code, found the workspace-restore trigger is DEAD in
   the shipped app (zero callers). Released the lock so the Boss's EDIT TWO saved; honestly restated
   where the proof stood (red→green + review) and offered: commit-on-proof / live edge-mode test /
   walk-through-first. **Boss: "Set up the live edge-mode test."**

3. **Stage A (the door).** Claude reset the target note (+ a task line), mapped the exact path
   (main-window Split View → Ctrl+Shift+2 → companion → Tasks tab). **Boss: screenshot + "Pass" —**
   noting the panel is labeled **"Panels Companion"**, not "Split Comparison" (locale string vs code
   fallback — recorded).

4. **Stage B (the run).** Lock re-armed. Boss typed ` — EDIT THREE` → red banner → clicked the task's
   file-link in the SS (the vulnerable call) → **Boss: "done"** → Claude force-killed the app (PID
   44100), verified disk held NO trace of the edit, released the lock.

5. **Stage C (the verdict).** Boss relaunched and read the line:
   **Boss: "original line — do not lose me — EDIT THREE"** — the recovery net SURVIVED the
   second-screen open and the crash. Claude explained the recovered edit was still unsaved (born-dirty
   contract) and asked for one keystroke; **Boss: "I typed 'Testing' on a new line"** → disk verified
   holding EDIT THREE. **PJ-108 proven fixed live, end to end.** Commit followed with ledger v1.31 +
   orientation v3.52 in the same commit (SO#6/SO#9).
