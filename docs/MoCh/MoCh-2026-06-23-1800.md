# Minutes of Chatting — 2026-06-23 — 18:00 block (ran into 2026-06-24)

Boss ↔ Claude conversational trace for the MIG-086 build block. (What shipped: the session log.)

## Opening
After MIG-085 §B closed out, the session continued (per the resume) into MIG-086 — Tier-2 link
suggestions for orphan/fragile notes.

## The arc
1. **Architecture + decisions.** Claude ran a workflow → recommended **BM25 "More Like This"** (the
   proven Lucene pattern) over the FTS index. Boss made the 5 §9 decisions; the two that shaped it:
   **"Always ask the type"** (every connect opens the 8-type picker — the keystone) and **"everywhere
   now"** (build the shared component into every link-dealing surface, not Reviewer-only).
2. **"Don't forget to check and validate the Concept."** Boss invoked his own Concept-Before-Function
   principle. Claude wrote a rigorous validation: the horse is *"reveal a note's unconnected relatives
   and let the user FORMULATE each as a typed connection"*; the serious objection ("this outsources
   thinking") survives because the suggester does the lookup, never the formulation — and Boss's
   "always ask the type" is precisely the safeguard. Yielded 5 concept-derived constraints (no bulk
   accept; show the why; hypothesis confidence; invitational; honest empty state).
3. **"Validate the left-dock core plugins first, like 360.3D — then check back."** Boss caught that the
   surface list was thin. Claude concept-tested all ~21 left-dock plugins → final hosts: Reviewer,
   NotePane sidebar, 360.3D (both mounts), the note's Health tab, Sky View's node menu (canvas
   excluded). Boss ruled IN the Health tab (TensionPanel — the strongest fit) + the Sky menu item.
4. **Plan approved → "Approved" → cascade.** Claude built **§A** (the backend), fighting a real
   60-second perf bug down to bounded (the JOIN-defeats-FTS-fast-path discovery), quality verified
   excellent across English/Arabic/science. Committed.
5. **The two forensic detours (Boss pasted the spawned-task prompts).** Boss directed Claude to
   investigate term_vocab drift + FTS fragmentation inline. Findings: term_vocab is ~92% EMPTY (a
   queued-and-abandoned bootstrap), not drift; the index is NOT fragmented (the 1.9 GB size + live
   access is the slowness). Report saved. "And then push on into §B."
6. **§B + four Boss-caught fixes.** Claude built the read-only component. Boss tested and found, in
   order: stuck "Finding…" forever (a $effect loop — Claude's Rule-2 violation), the title should be
   "Reviewer" not "Review Pulse", and TWO RTL rounds (content-RTL, then UI-RTL after Boss switched to
   Arabic UI: *"the Arabic 'Connect To:' has to be on the right"*). Each fixed; final RTL rule covers
   all UI×content combos. **Boss: "Pass."**

## Boss's steers (the through-line)
Every steer this block was a *discipline* steer, not a feature steer: validate the concept; validate
the surfaces; check term_vocab/FTS; fix the RTL properly. Claude's own bugs (the $effect loop, the
content-only RTL) were caught by the Boss test — the gate worked.

## Net
MIG-086 **§A + §B shipped + Boss-validated**. **§C (save-path) deferred to a fresh session** — Claude
recommended it (the BUG-015 class deserves a rested build), Boss agreed via the close.
