# Minutes of Chating — 2026-06-17 (block ~18:00; the §C.2d → MIG-080 → MIG-081 marathon)

> Direct Eisa ↔ Claude trace — the questions, steers, decisions. Not the commit log (that's `lab/reports/SESSION-LOG-2026-06-17.md`). Continues from MoCh-2026-06-17-0930.

## MIG-079 §C.2d — defer the Sky read off boot
- Picked up the §C.2d kickoff. Architect (4 agents) measured the real bottleneck off the IPC trace (an 11.4 s gap = the synchronous Sky read monopolising the IPC thread; warm = 250 ms). Boss approved **Option B+** (defer + async + after-idle warm-up). Built; a 4-agent audit caught + fixed a P1 race + a P2 (Lens-on-empty) before the test. **Boss test: graph_ready ~11 s → 1.5 s, Sky opens instantly — PASS.** Stage-2 (notes untouched, universe-switch) PASS.

## Cleanups — then the stop-on-correction
- Boss chose "§C.2c/§C.2d cleanups." I cross-checked (SO #8): did the ×15 i18n (real debt); recommended KEEPING the §C.2b rollback flag (not hygiene to retire a 1-day-old safety net) — Boss-aligned.
- Boss chose "start §D bring-up." I fixed the Calendar day-click defect **in place** — and Boss **stopped me**: the Calendar shouldn't be in the right sidebar at all. Boss directed me to **read the past-3-days MoCh** → I found the 06-16 **Right-Sidebar-Note-Context decision** I'd missed (Calendar/Tasks/Review relocate; defects fixed BY relocation, not patched). Owned the SO #8 / Predecessor-Lookup miss.

## MIG-080 — Right Sidebar → Note-Context-Only
- Familiarized (Boss: "first familiarize what stays/removed, then redesign"), confirmed the disposition (STAY ×6, SPLIT ×4 incl. **Tasks contextual**, RELOCATE ×2). Architect (4 agents) → Plan (§A–§G) → Boss approved, "cascade as far as we can."
- **§A** (Calendar → left launcher, wrong-library fix) — Boss test PASS. **§A.2** (full-page Calendar view, separate dock function — Boss: "separate Daily Note from Calendar; both on the left dock") — PASS. **#1 file-tree fix** (daily note didn't appear in the tree) — fixed.

## MIG-081 — Cultural Calendars (the big pivot)
- Boss: Calendar settings don't exist + want cultural calendars (Hijri/Persian/Hebrew), integrated or standalone. WA#5 research (Intl + Temporal). Boss decisions: the 4 systems, primary+secondary, default Gregorian, write a cultural frontmatter field.
- **★ Boss: "I built an accurate Hijri calendar — use the same one" (github.com/eisaShamsi/hijri-calendar).** Studied it: an astronomical engine (new-moon based) + the ديرة الدرور heritage data. Vendored hijri.js; built calendarMath + the Calendar settings category + a §C panel rendering the chosen system.
- Boss tested §C: wanted the **RICH design from his app** (image) — ornate header, dual dates, moon phases, week numbers, Style-Setter-styleable. "Two calendars doesn't work" → the rich cell shows both inline. Boss chose **"study your app's UI, then design."** Studied (3 agents) → his engine exposes the WHOLE rich grid (getMonthData + getMoonPhase + getEvent + isSacredMonth). Built §C.2a (rich grid, Node-verified) + §C.2b (rich CalendarPanel). **Boss: "Perfect!"**
- **Boss's 4 NEW asks (captured for next session):** Month Correction (±/month + list); Calculation Mode (Astronomical/Tabular); a Style-Setter "Calendar" tab; **retire the Daily-Note dock button** (the Calendar fully serves daily notes).

## Eisa's steers that shaped the session
- **Use his own engines** (the Hijri calendar) — check his repos/intent before reaching for a standard library. **Rich, styled UI** (not plain) — and **restylable via the Style Setter**. **Full-center-zone** for previews. **Separate concerns** (Daily Note vs Calendar) — then **integrate** (retire the dock once the Calendar subsumes it). The right-sidebar = note-context only.

## Close
- Marathon banked at §C.2b (Boss-validated). The 4 new Calendar functions + §C.2e fonts/i18n + the MIG-080 §B–§F cascade are next session. Full PCS done (orientation v2.90, this MoCh, handover, next-prompt, session log, help/manual note).
