# Minutes of Chatting — 2026-06-05 08:48

Direct Boss (Eisa) ↔ Claude interaction trace. Covers the language-smart fonts arc (Phase 4.1/4.2) through the handover. Not a commit log — that's `lab/reports/SESSION-LOG-2026-06-*.md`.

## Context entering this block
Mid MIG-070 §C (unify all styling into the Style Setter). Phases 0/1/3 + swatches + focused previews already passed. Working on fonts.

## The font arc
- **Eisa:** Latin font pickers passed. Then the steer: make fonts **language-smart** — the interface language is selectable; if an **Arabic note is opened in an English interface**, the interface keeps the chosen Latin fonts while the **note shows the original Arabic font** it was typed in.
- **Claude:** Surfaced the technical finding — a single `--font-text-theme` override bypasses the per-script `@font-face` engine, so per-script *choice* needs to feed that engine, not replace it. Asked scope.
- **Eisa (decisions):** (1) support **all major scripts** (Latin/Arabic/Hebrew/CJK/Devanagari/Cyrillic); (2) **also add the pickers to the Style Setter**.
- **Eisa:** "Build it now." → Built Phase 4.2: `perScriptFonts` + `setPerScriptFont`, the `+layout` font `$effect` composition via distinct virtual families `CnSetterText`/`CnSetterUI`, and the Setter pickers + a multilingual preview.
- **Boss test → Pass** ✅ (an Arabic note picked up Dubai; per-script independent of UI language).

## Two follow-up decisions
- **Eisa (image 1):** "We have to avoid duplication. Your technical opinion — where should the app language live? As-is (Settings → Language) or in the Style Setter?"
  - **Claude's recommendation:** keep it in **Settings → Language** — it's a *locale* setting, not styling; the per-script *fonts* belong in the Setter; standard UX; avoids duplication.
  - **Eisa confirmed.** → Removed the language dropdown from the Setter (commit `b49658e1`); the element is now "Per-script fonts" (5 script pickers).
- **Eisa (image 2):** "The Style Setter should adapt to the interface language." (TOP PRINCIPAL — everything localizes.)
  - **Decision:** the Setter UI is English-only today; localize to **all 15 languages AFTER the Setter content is complete** (so the ~100-string set is translated once, not re-translated each phase). Queued as the penultimate migration step, before Phase 9 retire. **Eisa confirmed.**

## Handover
- **Eisa:** "Prepare the handover files & prompt."
- **Claude:** Prepared the handover set at commit `b49658e1` (clean tree): the self-contained `docs/MIG-070C-HANDOVER.md` (with copy-paste resume prompt), orientation bump **v2.51 → v2.52**, this MoCh, and the state-of-standing in `SESSION-LOG-2026-06-05.md`. Next phase on resume = **Phase 5 — link colours**.

## Decisions ratified this block
1. App/interface language stays in **Settings → Language** (not the Setter).
2. Per-script fonts (all major scripts) live in the Setter, feeding the `@font-face` engine — never clobbering `--font-text-theme`.
3. Setter UI localization (15 langs) = penultimate step, after content is final.
4. Handover prepared; resume at Phase 5.
