# Minutes of Chating — 2026-06-19, 21:00 block (into 2026-06-20)

**Participants:** Eisa (Boss) ↔ Claude. **Topic:** MIG-082 — the clickable Calendar (opened after the MIG-081 close), §A.1 → §A.3.

## What Eisa asked for (the mandate)
After "What a milestone" (MIG-081 done), Eisa raised three things in one message:
1. **"We made Gregorian and Hijri — what about the OTHER calendars?"**
2. **"I want to click the dots — task, note, or others — to open / add / edit it. And click empty space within a date to create the daily note."**
3. **A question:** "If a date already has a note/task and I click it, will the app create a new one? Will we have duplicates?"

## What Claude did first (verify, don't guess)
- Ran a recon workflow → **answered the duplicate question definitively: NO duplicates** — clicking an existing date OPENS the daily note (deterministic Gregorian-ISO filename + open-if-exists gate + tab reuse). Reported the dot data flow + calendar-system state.
- Surfaced two genuine scope decisions; **Eisa decided:** (1) add **all four** new calendars — Indian + Buddhist **and** the hard lunisolar Chinese + Korean; (2) the note dot = **any note edited that day, but distinguish the daily note** from the others **by colour**.
- Opened **MIG-082**, ran a 4-agent Architect + Plan. Eisa: **"Approved."** (Key finding shared: the Temporal polyfill is broken for Chinese/Korean in the WebView → they'll use a self-contained Intl branch.)

## The build cascade + Eisa's steers
- **§A.1** (clickable cells) → Eisa: **"All pass"**, then two fixes he caught: the Hijri year showed a comma ("1,448" → fixed to "1448"); and "Secondary calendar = None" was being ignored (still showed two calendars) → Claude **wired the secondary setting** (None = one calendar). Then a follow-up: the header subtitle should also vanish when None → fixed. Eisa: **Pass**.
- **§A.2** (open at the task's line) → Eisa ran the **full Editor-Surface Gate** himself: **"All pass."** Two observations: the task dot is **blue not red** (→ explained: it's his saved Style-Setter theme/accent, not a code swap; pinned the note-dot default; he can set Task dot → red); and a task dot **disappeared with one task still unchecked** (→ explained: only tasks with their own `📅` date are calendar tasks; his undated sibling was never one — working as designed). Eisa accepted.
- **§A.3** (toggle a task from the calendar) → Eisa said **"Proceed to §A.3"**, then **"Pass"** — including the data-safety gate (toggling a task in a note with unsaved edits keeps both the toggle and the edits). *(Behind the scenes: the adversarial review caught a real P1 — the toggle would revert on a dirty open note via the teardown-flush stomp — and it was fixed with the `markCascading` guard before Eisa ever tested. The review earned its keep.)*

## Process notes
- Eisa corrected a scope assumption at the end: when he said **"PCS + Orientation"** he meant the close-out **without** the handover / next-session prompt (the session isn't being handed off yet).
- Eisa tests each step as a tutorial, validates, and steers immediately; corrections were taken without power-through. Three sub-steps (§A.1–§A.3) shipped + validated + pushed in the block; §A.4/§B/§C remain.
