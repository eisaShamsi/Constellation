# MoCh — 2026-05-25 07:00 local time

Session block covering MIG-048 Phase 1 cascade from Architect approval through PCS gate.

## Boss steers

**0700 — MIG-048 §9 decisions locked.** Eisa replied to the Architect's
four open decisions:
> 1. Chat surface mount: It shall be on the left Dock.
> 2. Citation validator: C1.
> 3. History compaction when context overflows: E2.
> 4. Boss-test Stage 1 universe: Eisa Cognitive Knowledge.

Override on E2: the original Architect recommendation was E1 (Fanar
self-summarizes). Eisa chose E2 (sliding window) — "visible-but-dropped
beats invisible lossy."

**Mid-cascade (around §J):** Eisa preferred NSIS build over `npm run tauri
dev` for the Boss test. Aligns with the established preference
(memory `feedback_prefer_exe_over_msi.md`).

**Boss-test §I — bug surface.** Eisa ran the first NSIS installer (137 MB,
contained §A–§J) and sent a screenshot showing:
> provider error: runtime error: runtime error:
> batch.add prompt[512]: Insufficient Space of 512

Root cause: §F's system prompt + 6 tool descriptions exceeded 512
tokens; `LlamaBatch::new(512, 1)` overflowed. Fix shipped inline
(commit `82d2cf91`): n_ctx 4096 → 8192, batch capacity to 8192.
Rebuilt installer surfaced for re-test.

## Decisions reached during the block

- **Sidebar mount** = D1 (Eisa).
- **Citation retry budget** = 1 (Eisa).
- **History compaction strategy** = E2 sliding window (Eisa — overrode
  original Architect recommendation E1).
- **Brand-naming policy for 13 locales** = deferred to Phase 1.x.
  Translation agent chose a "hybrid" pattern (Constellation +
  localized "Mind"); Eisa's full-localization standing order would
  push toward the Arabic-aligned "Mind of the Constellation" pattern.
  Flagged in §L commit + audit report; Phase 1.x decides.
- **`tauri::AppHandle` in struct fields = forbidden in test-reached code.**
  Discovered via STATUS_ENTRYPOINT_NOT_FOUND on test-binary load when
  `app: Option<tauri::AppHandle>` was added as a ChatOrchestrator
  field. Switched to closure-based hook. Worth memorializing as a
  pattern.

## Outputs delivered

- 14 commits between Architect (`9cd41e4a`) and PCS (this commit).
- 12 step commits (§A–§N) + 1 mid-cascade fix + 1 Architect doc.
- Bench: `lab/reports/MIG-048-D-bench-tool-use-2026-05-25.md` (7/10).
- Audit: `lab/reports/MIG-048-M-audit-consolidated-2026-05-25.md`
  + three per-agent reports.
- Session log: `lab/reports/SESSION-LOG-2026-05-25.md`.
- Orientation: `docs/Constellation Orientation & Onboarding v2.33.md`.
- Two NSIS installers built (first with the §F prompt bug, second
  with the fix).

## What Eisa is testing now

The rebuilt NSIS installer at
`E:\مشاريع كلاود\Constellation\src-tauri\target\release\bundle\nsis\Constellation_0.1.0_x64-setup.exe`.
Stage 1 of the Boss test: chat tab visible → chat pane mounts → first
turn EN streams tokens + tool call + citations. Then Arabic. Then
pre-warm latency check.

Stage 1 Phase 1 ship gate (50-turn ≥90% citation faithfulness) runs
once Eisa confirms basic functionality works.

## Open thread for next block

If Stage 1 passes: Phase 2 (MIG-049 — write tools) is the natural
next migration. If not, iterate on §F system prompt + §G validator
heuristics before re-running.
