# MoCh — 2026-05-25 15:00 local time

Second block of the day. Block 1 (0700) was the MIG-048 §A–§N cascade
through PCS. Block 2 (1500) is the Boss-test verdict + full revert of
Constellation Mind.

## Boss steers

**1500-ish — Boss-test results.** Eisa ran the rebuilt NSIS installer
(the one with the n_ctx + batch fix and tool-result caps). Screenshot
showed end-to-end success:
- Chat tab visible ✓
- Tool call dispatched ✓ (`▸ Tool: search_notes {"query":"Canopus"}` ✓)
- Tokens streamed into assistant bubble ✓
- Tool result rendered inside the bubble (verbatim — Fanar showed its
  work including the `<tool_result>` framing tag, which is a
  prompt-engineering issue not a plumbing issue)
- App didn't crash on the SECOND interaction. Crashed on the THIRD.

But Eisa's observations:
1. Response is slow (~1 min for tool-call entry; ~2 min full response).
2. App crashed after the third question.
3. **Verdict:** *"If the LLM is using the search_note tool, then what
   is special about it? I can search if I want to. It is NOT worth it."*

**1530 — revert decision.** *"Revert back to the prior LLM design and
build. Constellation will not have it at this stage. Clean and do the
necessary housekeeping."* Plus a follow-up: *"Delete those as well"* —
referring to the local Fanar GGUF file (5.13 GB) and the GitHub Release
artifacts.

## Decisions reached

- **Mind paused.** All of MIG-046 + MIG-047 + MIG-048 reverted in one
  commit (`a9cf4d62`).
- **Local model file deleted** from `%APPDATA%\world.uconstellation.app\models\`.
- **GitHub Release `models-fanar-1-9b-q4km-v1` deleted** (+ git tag,
  + the 4 split-chunk artifacts).
- **History KEPT** — session logs, MoChs, audit reports, bench reports,
  orientation v2.30–v2.33 all stay as the trail. The revert is reversible
  if Mind ever comes back.
- **Orientation v2.34 written** documenting the revert + 6 lessons
  preserved for any future re-attempt.

## My honest read I gave Eisa before he revert-called

Spelled out three options:
- **A** — accept Phase 1 as "plumbing works", invest Phase 1.x in
  cross-language and synthesis demos.
- **B** — pause and reconsider the LLM's role (cloud-only via Phase 5
  Anthropic key, or reframe Fanar as embedding source not chat).
- **C** — narrow Phase 1's value claim to one tool combination that
  genuinely demos value.

Eisa chose effectively **Option B** with the strongest framing: not
just pause, but full revert. That's the right call for now — running
local-LLM chat at 5 tok/s CPU was never going to win against
millisecond-latency SearchHub for the demo cases.

## What proved itself even though the feature didn't

The Architect → Plan → Build → Audit → Boss-test discipline ran cleanly
through 14 commits in MIG-048 alone, caught real issues at each gate
(mistral.rs gemma2 panic → Path A pivot; GBNF eager grammar crash →
grammar_lazy; AppHandle struct-field DLL gotcha → closure-based hook;
LlamaBatch capacity overflow → caught by Boss test; tool-result balloon
→ caught by Boss test; validator fail-CLOSED → caught by audit). The
meta-takeaway: the machinery for shipping safely is solid, the feature
just wasn't the right feature.

## Outputs delivered this block

- `a9cf4d62 revert: Constellation Mind (MIG-046 + MIG-047 + MIG-048)`
  — single commit, −13,442 / +5,194 lines.
- `docs/Constellation Orientation & Onboarding v2.34.md` — the
  authoritative record of why Mind was paused and what was learned.
- This MoCh.

## Open thread for next block

Mind is shelved. The Architect-Plan-Build-Audit discipline is intact.
Next migration target pending Eisa's direction.
