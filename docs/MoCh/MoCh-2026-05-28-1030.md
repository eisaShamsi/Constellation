# Minutes of Chating — 2026-05-28, 10:30 block

*Covers an extended session block (≈10:30 → night) spanning MIG-060 close, the federation audit, and the full MIG-061 federation fix. Records the Boss ↔ Claude conversational trace — what was said, the steers given, the decisions reached. The commit-by-commit record lives in `lab/reports/SESSION-LOG-2026-05-28.md` (Blocks 2-4).*

---

## 1. MIG-060 — threading gestures (continuation → close)

The block opened mid-cascade on MIG-060 §A (i18n for the lens-row threading gestures). Eisa drove with "Proceed." Build cascaded §A → §F.

**"Where is the build?"** — Eisa's question after §F exposed that I'd walked the whole cascade without producing a testable binary. The honest answer was that no current build had MIG-060 yet — the last release exe predated the §A commit. Lesson reinforced: a feature isn't testable until there's a binary whose mtime post-dates the code. I rebuilt and gave exact paths + mtimes from then on.

## 2. The Stage-3 double correction

Stage 1 (icons appear) and Stage 2 (360.3D gesture) passed clean. Stage 3 (CNS gesture) is where I drifted twice in one exchange:

- I wrote a test step pointing at "**Settings → Features → Constellation Sight**." Eisa: *"there are no 'Features' in the Settings. Second, there is no more Constellation Sight."* I had **fabricated a settings path** from a variable name (`enabledFeatures`) and **conflated CNS with the retired Constellation Sight dome**. BASIC-RULE violation (invented path) + Stop-On-Correction fired.
- I over-corrected — started proposing to rip CNS out of MIG-060. Eisa: *"CNS exist. Don't mix things out!"* A second correction. I stopped, read the codebase instead of my assumptions, and confirmed: **CNS (Constellation Nervous System) is a live core surface** (internal `lensActive`/`toggleLens`, user-label "CNS"), **distinct from the retired Constellation Sight dome** (future plugin per MIG-038). Fixed the Boss-test doc; no code revert was needed — the §B/§C wiring was already correct. The actual settings path is **Settings → Core Plug-Ins → "Constellation Nervous System (CNS)"**.

The takeaway Eisa drove home: don't paper over a knowledge gap with a plausible path; and don't "mix things out" — verify the distinction before pruning.

## 3. Per-surface focus — three sub-fixes

Boss-test then surfaced that "open in surface X" isn't uniform: 360.3D reads the active tab for free, but CNS holds its own `selectedNode` (§C-fix: `focusNoteId` prop + pan-to-center) and the Cataloger holds its own classify-target (§C-fix-3: dispatch `constellation:classify-and-show`). Orphan notes aren't in the CNS graph, so the CNS gesture is hidden for them (§C-fix-2). Eisa verified CNS focus on the single-universe "Eisa Cognitive Knowledge" universe (Stage 3 pass), and the Cataloger gesture on a linked note (Stage 4 pass).

## 4. The trigger for the federation audit

Stage 4's Cataloger gesture **passed** on a linked note but **failed** on a federated note with `FOREIGN KEY constraint failed: sources_suggestions(note_path) → note_meta(path)`. I called it a pre-existing federation gap, not a MIG-060 bug. Eisa widened the lens:

> *"I also want to check how the remaining functions/core plugins are handling Universes with cUniverse(s) included, like mine."*

That became a 4-agent parallel audit of every core surface. Result: **4 federated · 5 N/A · 1 partial · 14 broken**, collapsing to 4 root-cause patterns (P1 boot-snapshot chokepoint, P2 bare-conn reads, P3 filesystem walks, P4 FK constraints). Eisa picked **Option B** — four pattern-grouped MIGs (061→064) rather than one mega-MIG.

## 5. MIG-061 — the federation fix, and Eisa's principle

Architect locked Q1-Q4. The build cascade (§A-§J) shipped, but **Stage 2 (the headline "CNS should show 8 751 nodes") failed three times** — each time still 987.

Before the fourth attempt, Eisa stated the federation principle in full — the structural anchor for the whole effort:

> *"When a user creates a universe (A)… the app would create a database… When this user adds an existing universe (A) to their new universe (B) as a cUniverse, the process for the cUniverse shouldn't repeat itself, because the data is already there… the data of Universe A shouldn't be merged/integrated with Universe B, and vice versa: if the user decides to detach Universe A from Universe B, each should keep its data intact. Meaning, the Federation should be simple. The app shouldn't reinvent the wheel; the wheel is already there!"*

This corrected the Architect's Q3 lock (Option B, cross-universe link resolution) to **Option A (per-schema isolation)** — §L. Each cUniverse resolves only within itself; standalone-A behaves identically to A-attached-to-B; detach changes nothing. The "wheel is already there" reading was exactly right: each universe already has its own self-contained `search.db`; federation is read-side concatenation via ATTACH, never a merge.

## 6. The diagnostic that found the real bug

After three Stage-2 failures, I stopped patching the (already-working) federation chain and added `diag_log_line` tracing. The trace pinpointed it: `cache_boot_snapshot_sky` was **crashing on `Invalid column type Text at index: 4, name: stratum`** — a **pre-existing latent bug** (column declared TEXT, read as `Option<i64>`; some rows store INTEGER, some TEXT). Production had been silently falling back to the legacy `buildSkyData` path the whole time. §K fixed the read to handle both storage classes. Stage 2 then passed: **8 751 nodes · 233 286 links**.

Stages 4-5 (Backlinks/Outgoing) then failed once — the audit had mis-attributed their data source; they read from `cache_boot_snapshot_graph`, not `_sky`. §M+§N federated the graph payload too. Result: **104 backlinks · 101 outgoing** on the federated test note. All 6 stages pass.

## 7. Polish flagged + close

Eisa flagged two visual items on the now-federated views: **Sky View bubbles look too large** at 8 751-node scale, and **the CNS gravity well doesn't fill the canvas** when the window is maximized. Logged as PJ-10 / PJ-11 (polish, not blockers). The 3-agent Audit returned 8/8 invariants upheld; §P/§Q closed the two follow-ups. §R PCS shipped: orientation v2.41, milestone tag, ZIP backup.

## 8. Session handoff

Model switched to `claude-opus-4-8`. Eisa set the forward agenda: MoCh (this file) → PJ-10/11 → MIG-062 (P3) → MIG-063 (P2 read) → MIG-064 (P2+P4 write) → 15-locale help-doc updates.

---

### Decisions ratified this block

1. **CNS ≠ Constellation Sight.** CNS is a live core surface; Sight is retired (future plugin). Never conflate.
2. **Federation = per-schema isolation (Q3 Option A).** No cross-universe merge. Each universe's data stays its own; detach is lossless. "Don't reinvent the wheel."
3. **Option B for the federation fixes** — four pattern-grouped MIGs (061-064), not one mega-MIG.
4. **5 of 14 broken surfaces closed by MIG-061** (CNS, Sky View, Backlinks, Outgoing, + Tag Browser as a §M side effect).

### Corrections Eisa issued (for the record)

- Fabricated "Settings → Features" path → BASIC-RULE violation.
- Conflating CNS with retired Sight → "Don't mix things out."
- Over-pruning on first correction → had to verify-then-act, not pivot-and-power-through.
