# MoCh — 2026-05-27, 09:00 block

## What this block covers

Continuation of the MIG-056 §K Boss-test cascade from 2026-05-26's overnight handoff. Eisa returned this morning with "Do v2 scatter-gather now. And BTW, it's the next day now." That single sentence opened the §K.3 implementation block and closed it ~5 hours later with the federation foundation shipped.

## Conversation arc

**Morning (09:00-11:00) — §K.3 implementation.**

Eisa's opening line was "Do v2 scatter-gather now." with the date update. I followed the §K Standing Order: `git pull` first (clean — no overnight changes), state the function in hand ("MIG-056 §K.3 — v2 scatter-gather federated search"), open today's session log. Then implemented:

- New `SearchState.federated_search_conns: Mutex<Vec<(PathBuf, Connection)>>` — one Connection per cUniverse, opened in the background-attach thread alongside the existing `federated_conn`.
- Per-Connection PRAGMA setup matching `init_db` (WAL, busy_timeout, cache_size, recursive_triggers) + `register_fts5_tokenizer`.
- `federated_lexical_search_or_fallback` rewritten as scatter-gather: runs single-schema `lexical_search` once per Connection. Each runs with full BM25 + FTS5 snippet because there's only one `notes_fts` in scope per Connection (the FTS5 aux-function constraint that broke §G doesn't apply).
- **Reciprocal Rank Fusion (RRF, k=60)** merges per-branch ranked lists in Rust. Combined score(d) = Σ over branches: 1 / (60 + rank_in_branch(d)). The Lucene/Elasticsearch/Vespa standard for cross-shard merging. Avoids cross-corpus BM25 incomparability.
- Deleted dead `build_federated_lexical_sql` + obsolete `federated_lexical_search` (now the single function handles everything).
- 7 RRF unit tests added: empty branches, single-branch passthrough, two-branch interleave, three-branch fairness, overlapping-path accumulation, LIMIT truncation, k=60 head-softening math. All pass.

**11:00-11:30 — First NSIS build failed with `rustc-LLVM ERROR: out of memory`.** Known Tauri-on-Windows release-build issue with `opt-level=3` + LTO. Retried with `CARGO_BUILD_JOBS=1` to serialize rustc invocations. Build completed at 11:01.

**11:30-12:30 — §K.3 Boss-test (first round).** Eisa installed `Constellation_0.1.0_x64-setup.MIG056-K3-scatter.exe`. Status bar 8751 notes ✓. Searched `الرباط` → got "irrelevant result" at top. Reported with screenshots.

I asked for the diag log + clarification on success criterion. Eisa: "By getting the right term I am searching for, not only the semantic terms." Critical clarification — exact-term match should win.

**12:30-14:00 — §K.3.A diagnostic round.** Built `Constellation_0.1.0_x64-setup.MIG056-K3-probes.exe` with two diagnostic additions: (1) per-branch top-5 rank logging, (2) targeted probes for "الرباط" — `name_like_الرباط`, `exact_match_الرباط`, `prefix_AND_name_الرباط` — to isolate which layer was dropping the note. Eisa ran the probes and pasted the log.

**The diag log told a clear story** I'd missed:
- `name_like_الرباط=1` (note in note_meta ✓)
- `exact_match_الرباط=41` (FTS5 has the tokens ✓)
- `prefix_AND_name_الرباط=1` (prefix `الربا*` MATCHES the row ✓)

Yet the per-branch top-5 for query `الربا` had `عدد رباعي مركب` at rank 1 with `الرباط` not in top 30. **The data + tokenizer + index were all fine.** What didn't match was the actual MATCH expression `lexical_search` was running.

**Root cause realization:** `expanded_match_query("الربا")` was firing the lexicon expansion (because `الربا` = Arabic for "interest/usury", a corpus lemma). Expansion replaced the prefix wildcard `الربا*` with multi-language exact-phrase OR (`"الربا" OR "interest" OR "usury" OR ...`). That OR doesn't match `الرباط` (different word). The note disappeared.

This was a lexicon-expansion-vs-prefix collision — **pre-existing in single-schema mode**. Federation made it visible by putting Eisa's queries through more notes.

**14:30 — Eisa's correction: "Enough patching."**

She also flagged a **second** pre-existing bug: the QuickSwitcher truncates Arabic input at normal typing speed. The diag log showing `query="الربا"` when she typed `الرباط` wasn't a partial-typing snapshot — it was the truncation. That made everything I'd done with `الربا` testing partially noise.

I stopped patching. Wrote the **state-of-standing snapshot** into the session log per SO #5: (a) verified-shipped, (b) in-flight, (c) known-broken, (d) pending, (e) documentation drift. Presented three options to Eisa: ship now, pause, or fix one of the sub-bugs today.

**15:00 — Eisa's decision: "Ship federation now — commit §K.2 + §K.3, ship §L PCS, open MIG-057/058/059 for the other three."**

Cleanest path. I stripped the diagnostic instrumentation, cleaned up the BOSS-TESTS doc (removed the §K.2.D/E/F/G debug stages — kept the canonical 5 stages), verified 833/833 lib tests pass, committed as one PCS commit (`0e094da0`), pushed to `origin/main`.

Then the §L PCS work: orientation v2.37 with the MIG-055 + MIG-056 preamble (this MoCh records the session-log details; the orientation captures the architectural snapshot), MIG-057/058/059 stub docs for the three deferred issues, milestone git tag.

## Key decisions Eisa made

1. **"Do v2 scatter-gather now."** Replaced §K.2's modified-DESC workaround with per-cUniverse Connections + RRF — the architecturally correct fix.
2. **"By getting the right term I am searching for, not only the semantic terms."** Success criterion for federated search: exact-term match wins.
3. **"Enough patching."** Stop after the §K.3.A round; don't keep iterating speculatively.
4. **"I cannot type the whole term, that's why it is truncated."** Surfaced MIG-058 as a separate input bug.
5. **"Ship federation now."** Commit + push the working state, open separate MIGs for the three pre-existing issues.

## Outputs delivered

- Commit `0e094da0` — MIG-056 §K.2+§K.3 federation completion (Rust scatter-gather + RRF + frontend loadAllStats re-fire).
- Pushed to `origin/main`.
- `docs/Constellation Orientation & Onboarding v2.37.md` — new preamble with MIG-055 + MIG-056 details + the three follow-up MIGs noted.
- `docs/MIG-057-lexicon-prefix-boundary-STUB.md` — open for lexicon-vs-prefix collision fix.
- `docs/MIG-058-quickswitcher-arabic-input-truncation-STUB.md` — open for IME / debounce / bind:value race.
- `docs/MIG-059-slow-federated-search-investigation-STUB.md` — open for the 25s per-search perf gap.
- `lab/reports/SESSION-LOG-2026-05-27.md` — state-of-standing record + §K.3 implementation log.
- `docs/MIG-056-BOSS-TESTS.md` — cleaned up to canonical 5-stage tutorial.
- Milestone git tag `milestone/mig-055-mig-056-combined` (pending — to be created next).

## What Eisa is left with

Working federation. Status bar correct (8751 notes). Federated search returns cUniverse notes with BM25 relevance ranking. Skip-unavailable behavior verified. Three known pre-existing issues catalogued as separate MIGs for future focused work. State-of-standing record in the session log so the next session picks up cleanly.

---

## Afternoon addendum — §L PCS ship + MIG-057 fix

After Eisa's "Ship federation now" decision: PCS landed (`0e094da0` federation completion + `0df05f00` orientation v2.37 + MIG-057/058/059 stubs + `fe1499f5` 15-locale help-doc i18n), milestone tag `milestone/mig-055-mig-056-combined` pushed, ZIP backup created.

Then Eisa: "Proceed." MIG-057 (lexicon expansion + prefix-wildcard coexistence) implemented + tested + Boss-tested. One-function fix in `expanded_match_query`; 3 new regression tests; 836/836 lib tests pass. Installer `Constellation_0.1.0_x64-setup.MIG057.exe` built and verified by Eisa via screenshot showing `الرباط` at rank 2 in QuickSwitcher results for query `الرباط` — the exact reverse of yesterday's failure.

Eisa: "Pass."

MIG-057 closed. Orientation bumped to v2.38. Session log updated with Block 3 detail.

### Remaining decisions for Eisa

- MIG-058 (QuickSwitcher Arabic input truncation) — investigation-heavy; pre-existing UX papercut.
- MIG-059 (slow per-cUniverse search ~25s) — investigation-heavy; pre-existing perf gap.

Neither breaks correctness. Both can wait for a future focused session.
