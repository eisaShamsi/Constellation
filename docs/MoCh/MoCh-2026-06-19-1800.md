# Minutes of Chating — 2026-06-19, 18:00 block

**Participants:** Eisa (Boss) ↔ Claude. **Topic:** MIG-081 — the Calendar's 4 new functions (Boss-requested 2026-06-17), then close-out.

## What Eisa asked for (the session's mandate)
The kickoff resume prompt set the task: build MIG-081's four new Calendar functions, in order —
1. **Month Correction** (±1 day per Hijri month + list + Clear All),
2. **Calculation Mode** selector (Astronomical ⟷ Tabular al-Tawfīqāt al-Ilhāmiyyah),
3. **Style Setter → "Calendar" tab** (wire the `--cal-*` tokens; full-center-zone preview),
4. **Retire the Daily-Note dock button** (the Calendar fully serves daily notes).

## Decisions Eisa made
- **Persistence (asked via options):** store corrections + calculation-mode in **appSettings (synced with the universe)**, not the engine's per-device localStorage — so they travel across his iOS/macOS/Windows devices.
- **Correction UI (asked via options):** **Settings-only** (all correction controls in Calendar Settings; the Calendar page stays display-only).

## Steers / corrections Eisa gave mid-build
- After Stage 1 ("All pass") he gave **two refinements**:
  1. The correction **offset dropdown should default to 0**, with two days above (+) and two below (−) → reordered `+2 / +1 / 0 / −1 / −2`, default 0 (and it now reflects the chosen month's existing correction).
  2. The **subtitle should show the *other* calendar** — Hijri month → Gregorian range, **and** Gregorian month → its Hijri equivalent (not a repeat of the primary). Implemented as a cross-reference range; Node-verified (June 2026 → "Dhul-Hijjah 1447 – Muharram 1448 AH"). Eisa: "All pass."
- On the Style-Setter text sizing: I first shipped a **single master Text-size slider**. Eisa: **"No. I prefer to give each element its own text sizing and coloring functions."** → reverted the master scale; gave each of 9 textual elements its own size token paired with its colour (33 controls total). Eisa: **"What a milestone. All passed perfectly."**

## Outputs delivered + validated this block
- §C.2f (Month Correction + Calculation Mode in Calendar Settings) — **"All pass"** (+ the two refinements, re-validated).
- §C.2d (Style-Setter "Calendar" category, full-center-zone live preview) — **"All pass"**, then per-element size+colour — **"All passed perfectly."**
- §C.2g (Daily-Note dock button retired; daily notes via the Calendar + command palette) — **"All pass."**
- Close-out (§C.2e) started: confirmed Amiri is already bundled+declared (fonts done, no code); translated the 34 `settings.calendar` keys + `calendarPanel.weekAbbrev` into all 14 non-English locales (one native translator per locale, written + validated); 3-agent migration audit; then PCS.

## Tone / notes
A milestone session — all four Boss asks plus two refinements and a per-element redesign landed and were validated the same session, each behind a staged tutorial test. Eisa engaged actively and iterated fast; corrections were taken immediately (Stop-On-Correction) without power-through.
