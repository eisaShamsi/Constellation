# Minutes of Chatting — 2026-06-23 — 15:30 block

Boss ↔ Claude conversational trace. (What shipped is in `lab/reports/SESSION-LOG-2026-06-23.md`.)

## The ask
Boss: resume **MIG-085 §B** (single-source maturity's inbound to `note_meta.incoming_count` across
`maturity.rs` / `inspector360.rs` / the `maturity_sql` trigger; add a test; verify Sky View doesn't
regress), then **MIG-086** (Tier-2 link suggestions for orphans/fragiles via the Index FTS5
co-occurrence). Read orientation v3.02 + the MIG-084 handover first.

## The turn that changed the job
Claude investigated §B by **measuring on the live 7,660-note universe** before writing code. Two
findings: (1) the expected COUNT(\*)-vs-DISTINCT difference is tiny (8 notes); (2) the *real* driver is
a **Unicode case-folding bug** — SQLite's `LOWER()` folds ASCII only, so **13 accented-title notes**
(`Île-de-France` 17 inbound, `Śramaṇa` 26, …) had `incoming_count = 0`: **false orphans in the new
Reviewer**, wrong Sky maturity, 0 backlinks. Crucially this **inverted §B's premise** — the DB surfaces
(incoming_count/Sky/Reviewer) were the WRONG ones; the FS-scan surfaces (maturity panel, 360) were
right. Claude stopped and surfaced this rather than coding the original plan.

## Boss decisions
1. **Scope:** when asked how to proceed given the discovery, Boss chose **"Fix accent bug first, then
   maturity."** (Recommended option.)
2. **NFC:** when asked how to handle the related cross-device "two ways to encode an accent" (NFC/NFD)
   issue, Boss chose **"Build now AND include NFC normalization."**

## Build + tests
Claude built §B.0 (Unicode name-fold: `fold_match_key`, `name_lower` column, `COALESCE` rollout gate,
`name_fold_backfill`) + §B.1 (maturity DISTINCT + `maturity.rs` DB rewrite + `inspector360` override).
Research-validated the approach (store a Rust-folded key column — the field's dominant pattern — not a
SQL function, not ICU). Ran a high-effort code review (6 findings, all fixed). Live-copy rehearsal on
the real universe: all 13 fixed, zero collateral.

## The tests (staged, as tutorials)
- **Stage 1 (accent fix):** Boss → **"Step 1: Pass · Step 2: Pass · Step 3: Pass."** `Île-de-France` left
  the orphan lens, showed ~17 backlinks, read evergreen.
- **Stage 2 (maturity agreement):** Boss confirmed **inbound in sync (17=17)** but **caught an outbound
  mismatch** — the 360 showed **↑34**, Backlinks showed **16** (with Sky View screenshots). Claude
  verified on the live DB (`outgoing_count = 16`; the 360 was counting 34 raw `[[link]]` occurrences of
  a Wikipedia import) and fixed it (§B.2 — override `total_outbound` from `outgoing_count` too).
- **Stage 2 re-test:** Boss → **"Pass."** 360 now ↑16 ↓17, matched Backlinks both directions.

## Side-thread (ultracode on)
While Boss tested, Claude ran the **MIG-086 architecture** in the background (6-agent workflow):
recommends **BM25 "More Like This"** over the note's top-IDF terms (reusing the existing FTS5 index +
the existing wikilink-create path; Rule-8 clean), rendered in the Reviewer's prescription block. Doc
saved; **5 open Boss decisions** pending (default link type, candidate count, fragile-vs-orphan,
Reviewer-only-vs-everywhere, the inert "Connect" button). No build until Boss approves.

## Net
MIG-085 §B **shipped + Boss-validated end-to-end**. MIG-086 **architected, awaiting Boss decisions.**
