# Minutes of Chating — 2026-06-26 13:00 block

Boss ↔ Claude direct interaction. (Companion shipping record: `lab/reports/SESSION-LOG-2026-06-26.md`
MIG-087 section. Orientation v3.08.)

## What Boss asked for
- Kickoff Prompt A: **the broader i18n localization pass.** Re-run the audit (locale value == en + Latin),
  reconcile against the handover §4 buckets; present the **brand-name ruling** before translating; then translate
  the ~20 clear UI labels ×14 via native-speaker agents; apply via the byte-identical round-trip; verify; rebuild;
  give a staged Arabic test; SO close-out.

## Decisions Boss made (AskUserQuestion)
- **app.name "Constellation" → keep English** (product brand, like Obsidian/Notion).
- **Feature names (Sight / Map / CNS) → translate to native** (Boss noted they're already native in their live
  panels — keeping the Settings labels English was inconsistent).
- **Dataview → translate to native.**
- **"Return to CNS / OrgChart" → translate verb + native feature name.**

## The pivot Boss drove mid-session
- Boss interrupted the cargo build with a screenshot: the **status bar Arabic plurals are wrong** —
  "19 مكتبات / 7659 ملاحظات". Boss taught the rule precisely: **مكتبة (1) / مكتبتان (2) / 3 مكتبات (3–10) /
  11 مكتبة (11+)** — and خاصية/حرف/كلمة/ملاحظة likewise. Then: **"And you have to consider other languages, if
  they have something similar."**
- I surfaced that the field standard is Unicode CLDR via `Intl.PluralRules`, showed that only ar/ru/he need >2
  forms, and asked two questions:
  - **Arabic 1 & 2:** Boss chose **word-only** (مكتبة / مكتبتان; number from 3 up).
  - **Scope:** Boss chose **everything app-wide in one pass.**

## What was delivered
- §A: 34 keys localized ×14 (the brand ruling applied). §B: the plural-aware `tn` engine + `plurals.*` ×15 wired
  to ~50 count displays. Boss Stage-1 (status bar, Arabic): **Pass** — every form grammatically correct.
- Boss then caught a second thing at the test: **"I cleared the content, but the app didn't update the info."**
  Diagnosed it as the editor-owns-content design (count read the stale tab store) + frontmatter inflating the
  count. Boss: **"Fix the live-counter first."** → §C one-way display-only live counter (body-only). Boss re-test:
  **Pass.**
- Boss: **"Pass. PCS + Orientation."** → this close-out.

## Tone / steers
- Boss steers by concrete observation (screenshots, exact Arabic examples) and expects the field-standard
  approach + app-wide completeness, but accepts a staged test (status bar first). Held the commit until each
  stage passed.
