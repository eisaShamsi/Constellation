# MoCh — 2026-06-17 (block start ~09:30 local)

> Minutes of Chating: the direct Eisa ↔ Claude interaction this (marathon) session. Not the commit log — the conversational trace. Session log of *what shipped*: `lab/reports/SESSION-LOG-2026-06-17.md`.

## The task that opened the session
- Picked up the handover: MIG-079 **§C.2b** (defer the 234k-row `read_links` off boot) + **§C.3** (covering index). Read orientation v2.86 + the plan + handover, mapped consumers, built it as one unit, ran the adversarial review (caught + fixed 2 P1 races), committed (`6a4d5e20`), gave Eisa a staged test.

## The pivot — Eisa's test exposed the real bottleneck
- **Eisa's §C.2b test:** boot ~20 s (not the hoped ~2 s); a ~7 s backlink-scroll freeze ("since we created it, nothing new"); Sky View pass; step-5 thrashing.
- I **measured** (boot-perf history) rather than guess: §C.2b worked (`read_links=None`) but `graph_ready` was still ~17 s — **15 s of it the graph IPC waiting behind the Sky read** (233,995-row `sky_links`, the twin of links). Reported honestly: the Plan mis-attributed the boot cost to links alone; the Sky read is the bigger half. Offered Eisa the fork.
- **Eisa chose:** "Kill the 234k in-memory array (fix the freezes)" — the structural per-note-query fix.

## The WA#5 discipline Eisa enforced
- I opened §C.2c (Architect + Plan). Eisa: **"Validate and cross-check it against best practices and proven solutions first. Then confirm it with me."** → ran the WA#5 cross-check (materialized-view-vs-index, IR posting-lists, SQLite-as-graph, Obsidian/Logseq). It CONFIRMED the design (count = write-time aggregate, rows = read-time indexed query). Eisa then approved, conditioned **"stop after Step 1 for proof."**
- §C.2c-1 (Rust per-note queries) built + **rehearsal-proven** on a copy of her real 1.97 GB DB (80 notes, 0 mismatch). Eisa: **"Go — build Step 2."**

## "Enough patching" — the turning point
- §C.2c-2 (panel swap) passed most steps but switching lagged. I patched it **3×** (removed the debounce → froze it; re-added coalescing → still laggy). Eisa, correctly: **"Enough patching."**
- Then: **"Employ SME agents to research, validate, and audit §C.2c issues, to provide sound solutions."** → I dispatched a 4-agent panel. Root cause: the un-virtualized RENDER (~55k DOM nodes/switch on a hub), not the fetch timing. Sound solution = virtualize (reuse `VirtualList`).
- Eisa: **"Build it as one complete unit"** — and added: **a toggle for note summaries in Summaries Settings, OFF by default.** Built §C.2c-3 (virtualize + the summaries toggle) as one unit.

## The validation rounds (all passed in the end)
- §C.2c-3 test: hub scrolls smoothly with its own scrollbar — Eisa: "When does it have its own scrollbar?" (answered: >50 items) "**We will keep it**" (= keep the scrollbar). Small notes unchanged; summaries off by default ("even better").
- Eisa clarified Step 4: she meant the note's own summary **under the title** (not the row headlines) — **"add a toggle switch for it as well."** And the brief hub open-lag: **"not keeping it"** (reduce it).
- Ultracode on → I ran a 2-agent Workflow to locate the title-summary + diagnose the open-lag, then built both: the **note-title-summary toggle** (default OFF) + a **leading-edge coalesce** (kills the ~180 ms felt open-lag). **All pass.**
- Last catch (Eisa): toggling the backlink summaries OFF didn't clear the already-shown ones → fixed (clear the map on toggle-off). **Pass.**

## Eisa's steers that shaped the session (the durable lessons)
- **Measure, don't guess** — the boot pivot came from the boot-perf history, not assumption.
- **Cross-check before building** (WA#5) — Eisa required it explicitly before approving §C.2c.
- **"Enough patching"** — the three-strike / Solve-the-Class line; the fix was the SME-designed whole, not another timing patch.
- **Leaner by default** — both summary surfaces now default OFF, toggleable.

## Close
- Whole MIG-079 §C.2c arc shipped + Boss-validated. Commits `6a4d5e20`→`44acd79d` on `main`. Next: **§C.2d** (defer the Sky read off boot — the remaining boot win). Full PCS done (orientation v2.88, session log, handover, next-prompt, help/manual).
