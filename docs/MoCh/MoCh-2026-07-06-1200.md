# Minutes of Chatting — 2026-07-06 ~12:00 → ~15:30 (MIG-093 · the Quick Switcher)

*The Boss ↔ Claude conversational trace. Not the session log (what shipped) — what was said.*

---

## The complaint → reproduce-first
- **Boss** (picking the queued item): *"Proceed."* Claude stated the function-in-hand
  (the Ctrl+O Quick Switcher) and asked what the slowness feels like. **Boss:** *"Typing
  the query is fine, but getting the results takes longer (over 2 minutes) and is
  accompanied by heavy thrashing."*
- Claude shipped a stopwatch build (no behavior change). **Boss ran it twice:**
  `knowledge` → local 3.5ms / rust 29.3s; `islam` → local 3.6ms / rust 25.0s — each
  showing two serialized searches queued on the DB lock. Diagnosis read off the trace:
  the switcher's embedded content search built highlighted snippets for EVERY matching
  note per keystroke-pause; stale runs never cancelled; the wanted TITLE hits buried by
  an unranked, path-matching, 20-cap local filter.

## The ruling that reshaped the design
- **Boss (interrupting the fix mid-build):** *"The Ctrl + O is useless. If I cannot get
  what I am looking for, then what is it for?"* → *"Ctrl + O should focus only on the
  titles. Do your research on how other apps are processing this kind of plain search."*
- Claude stopped all edits (Stop-On-Correction), summarized the half-threaded tree state,
  and ran the research: **Obsidian** (names+aliases only, never content), **VS Code**
  (banded scoring — exact beats prefix beats word-boundary beats fuzzy, by construction),
  **fzf/fzy** (subsequence scoring), peers that mix always demote content. Local checks
  found the title cache was main-schema-only (a ONE-universe violation) and aliases were
  unsearchable.
- **Boss asked-and-answered:** the note he couldn't find had the term **in its title** —
  confirming the ranking defect. Revised plan presented; **Boss: "go."**

## Build + staged tests
- §D two-phase engine → §A federated titles → §B shared Arabic/Latin fold → §C the
  switcher rewrite (titles+aliases, banded ranking, recents, Create-note +
  Search-in-Search-Hub rows). **Boss test:** Tests 1-4 all pass, with screenshots —
  `islam` at **2.7ms over 9,161 candidates**, exact title #1; Arabic alias row rendering
  right; create + hand-off rows working.
- **Boss, Test 5:** the Search Hub was *"still took some time, accompanied by a light
  thrashing"* → Claude found the Hub's default mode uses a SECOND command with the same
  per-match snippet disease → §D-2 same cure. **Boss re-test: "Much better. Pass."**

## Close-out
- §E: instrumentation stripped, the three new labels localized ×15, User Manual section,
  Orientation v3.27, 4-agent /simplify (one collator hoist applied; one false-positive
  refuted — the federation re-fetch already shipped in §A).

## Standing steers reinforced
- **Reproduce-first pays:** two Boss traces named the mechanism no code-reading had.
- **Concept before function, again:** the switcher's failure wasn't speed — it was a
  second search engine growing inside a title jumper. The Boss named the horse; the
  industry research unanimously agreed with him.
- **Stop-On-Correction honored mid-edit** (frozen half-threaded tree, stated, waited).
