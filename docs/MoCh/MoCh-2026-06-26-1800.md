# Minutes of Chating — 2026-06-26 18:00 block (Boss-prioritized backlog)

Boss ↔ Claude direct interaction. (Companion shipping record: `lab/reports/SESSION-LOG-2026-06-26.md`
"Continuation" section. Orientation v3.09.)

## What Boss asked for
- The consolidated kickoff prompt: work the **7-item backlog in priority order (1-2-5-4-7-6-3 reordered to
  1-2-3-4-5-6-7 in the prompt), ONE AT A TIME**, full SO close-out between each, don't batch. Ultracode on, use
  Workflows. SO #8 cross-check each PJ first.
- **Item 1 — i18n sentence-embeds** (finish MIG-087's filed remainder): the 3 full-sentence count strings
  (`dialogs.batchDelete*`, `settings.arabicOverrides.reindexed`, `importer.andMore`). Approach dictated: a `{noun}`
  placeholder ×15 in native word order, filled by `$tn('plurals.<noun>', count)`, **reuse `plurals.*`, no new noun
  data**; byte-identical round-trip; svelte-check 0; staged Arabic test at counts 1/2/3/11.

## What was delivered (Item 1)
- A Workflow of 15 native-speaker rewriters → 15 adversarial verifiers. Verifiers caught real out-of-`{noun}`
  agreement bugs (en count-1 pronoun + doubled "more"; he/es/fr/pt verb/participle; hi pronoun) and fixed them.
  Arabic solved via passive framing `ستُحذف {noun}` so the dual ملاحظتان is grammatical.
- Applied via byte-identical CRLF round-trip (60/60 keys, nothing else touched), svelte-check 0, runtime
  simulation proving ar/ru/en at 1/2/3/11, frontend+binary rebuilt.

## Boss steers / corrections this block
- **Test-tutorial correction #1:** "Ctrl and click doesn't work." → multi-select is the per-row **checkbox**, not
  Ctrl+click. I had the gesture wrong.
- **Test-tutorial correction #2 (with screenshot):** "I don't know why you assumed we have right-click in
  Constellation." The Back/Refresh/Save-as/Print menu is the **WebView2 native menu**, not ours. The **note list
  has no Constellation right-click**; the **File Explorer tree has no multi-select** (by design); the **Note
  Navigator** uses checkboxes + the bottom NavBatchBar. Logged as doc-truth in the session log + orientation v3.09.
- **Stage-1 result: "Pass."** (Arabic batch-delete dialog, the dual ملاحظتان confirmed.)

## Item 2 — live properties (خصائص) count
- Delivered: a one-way display-only `onLiveProps` observer (mirrors §C exactly) so the status-bar خصائص count
  updates live as properties are edited; both PropertyEditor instances + both NoteEditor mounts; never writes
  content; MIG-076 harness 28/28.
- **Boss Stage-1: "Pass."**
- **Boss refinement:** "if the property has only one, I want the Arabic equivalent to be 'خاصية واحدة' instead of
  'خاصية'." → AskUserQuestion on scope; Boss chose **all Arabic singular counts** (not just properties). Applied
  واحد/واحدة (gender-correct) to all 27 `ar plurals.*.one` via a native-Arabic verify pass.
- **Boss re-test: "Pass."**

## Tone / steers
- Boss tests by direct UI observation and is precise about the app's actual interaction model — corrects wrong
  assumptions about gestures/menus immediately. Keep test tutorials grounded in the real UI (checkboxes, no
  right-click on the note list), not assumed conventions.
- Boss refines localization toward EXPLICIT clarity (spell out "one") and wants such refinements applied
  CONSISTENTLY across the whole namespace, not just the one surface he happened to notice.
