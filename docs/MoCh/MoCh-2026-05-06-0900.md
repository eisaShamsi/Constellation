# Minutes of Chating — 2026-05-06 (block 0900–1200)

**Convention.** Per Eisa's instruction (2026-05-06): record direct Eisa ↔ Claude interaction in a fresh MD every ~3 hours. One file per block. Filename = `MoCh-YYYY-MM-DD-HHMM.md` where HHMM is the block's start. Records direct interaction only — no internal tool plumbing, no agent traces, no commit-message recitation. The MoCh sits next to the session log (`lab/reports/SESSION-LOG-YYYY-MM-DD.md` — captures *what shipped*); the MoCh captures *what was said*.

---

## Block summary

Session opened on the tail end of MIG-013 (CTSE Bridge Adapter) — second cross-check loop, pivot to Option B (query-time concept expansion), then closed-out tests. Pivoted to governance work: three top-principal rules added to CLAUDE.md (Cross-check, Predecessor Lookup, Stop-On-Correction, State the Function in Hand), Laws v1.0 → v1.3 authored, Pending Jobs v1.0 → v1.1 (stable reference numbers introduced), NotePane Specs v1.0 distilled from 121 commits. Then MIG-014 opened (Note-Stage Taxonomy / PJ-007): Architect → Plan → cascade through §1A → §1B → §1C. First user-visible change shipped (PropertyEditor stage combobox); first Boss test surfaced a UX correction (native `<datalist>` two-tier rendering) → §1C.5 added mid-cascade with a custom inline dropdown. NotePane Specs §3.5 doc-fix: the breadcrumb-as-dropdown claim was wrong; current state is promote/demote arrows + badge. MoCh convention established mid-block. Eisa's name preference recorded.

---

## Direct exchanges

### MIG-013 close-out (CTSE Bridge Adapter)

- **Eisa:** instructed cascade through §1C → §1D → §1E with a "How to resume" checklist.
- **Eisa:** "Build the binary for me." → built; tests delivered.
- **Eisa:** test results — screenshot showed 5,729,974 bigram concept rows + app "Not Responding."
- **Eisa:** "Yes, apply the fix and rebuild." → schema v2 sentinel migration applied.
- **Eisa:** test results — 529,777 concepts (down from 5.7M but still high). Asked: "Is half a million concepts acceptable?"
- **Eisa:** "Before I give you my OK, I want you to cross-check your fix with other proven methods/techniques among similar coding communities." → research delivered: Lucene SynonymGraphFilter pattern; presented Option A (further bigram pruning) vs Option B (query-time concept expansion).
- **Eisa:** "Go for Option B. And update [Claude.md] to follow this approach every time you work a fix, to avoid reinventing the wheel." → CLAUDE.md Working Agreement #5 added.
- **Eisa:** "Commit to update the orientation file among the OS's" → orientation v1.35 written.
- **Eisa:** confirmed "Stage 1 Passed", "Stage 2 Passed".

### Constellation Development Laws

- **Eisa:** "List all pending jobs." → list delivered.
- **Eisa:** "List all of it in a new MD file that we are going to title 'Constellation Pending Jobs', starting with v1.0. We will use this file for follow-up purposes." → `docs/Constellation Pending Jobs v1.0.md` authored.
- **Eisa:** during a separate diversion, requested a Laws document — `Constellation Development Laws v1.0.md` authored.
- **Eisa:** asked for Law 1.6 — "State the function in hand" — added in v1.1.
- **Eisa (Law 2.6 review, with image):** "While reviewing the laws, I need to confirm the following regarding Law 2.6: Is the five-level hierarchy correct (as per the diagram)? But also, it could be (Libraries) under the Universe directly, with each library having folders and notes." → diagram corrected (cUniverse moved to sibling federation, not required layer) → Laws v1.2.
- **Eisa (Law 2.6 follow-up):** "And we could also have folders and/or notes under the Universe root directly." → diagram corrected again to show Universe root as itself a default Library (`is_universe_notes` flag) → Laws v1.3.

### Pending Jobs reference numbering

- **Eisa:** "Then we will start with jobs 6 + 7."
- **Eisa (PJ-007 design — Note Stage Taxonomy, manifesto):** "Wait! The note is the heart and mind of Constellation system. It is the Key to Cognition & Knowledge. … It is not solid, but flexible and liquid. It can be formed in any way, shape, or method." → translated into design as "extensibility is non-negotiable."
- **Claude:** proposed three options including Zettelkasten (Fleeting/Literature/Permanent/Synthesis).
- **Eisa:** "The reason I asked you to retrieve everything related to Note Development is that you included 'Zettelkasten' in your options, and I think the Constellation note system surpasses this methodology." → Zettelkasten removed; revised to Constellation-native baselines only.
- **Eisa:** "I don't want you to remove the closed stage taxonomy. It will be the basic stages, but users could add their own, and Constellation should accommodate them." → final design: Living Link 6-stage baseline + extensible `custom_stages`.
- **Eisa:** "I confirm 'Living Link 6-stage as the baseline + the proposed defaults.'" → PJ-007 confirmed for build.
- **Eisa:** "Regarding the Pending Job document, I want each pending job to keep its unique sequence number. The numbers shouldn't be repeatable; they will act as reference numbers." → Stable Reference Numbers rule promoted to top of `Constellation Pending Jobs v1.1.md`.

### NotePane Specs

- **Eisa:** "Next, I want you to go through all the NotePane development history. To make sure we do not repeat or change anything proven to be good and final."
- **Eisa:** "And based on your findings, I want you to develop an MD file that we are going to title 'NotePane Specs'." → `docs/NotePane Specs v1.0.md` authored from 121-commit history.
- **Eisa:** "FYI, in order to develop the current NotePane, we started first with 'cNotePane' as an experiment. The 'c' stands for Constellation."

### MIG-014 — Note-Stage Taxonomy

- **Eisa:** "Architect doc OK, and I want the Plan doc in MD saved." → `lab/reports/MIG-014-NOTE-STAGE-PLAN.md` written, committed `1138aec`.
- **Eisa:** "Plan approved" → cascade kicked off per Plan-Approval-Equals-Build-Approval principle.
- **Phase 1A** committed `c3b9454` — Rust schema (`CustomStage` + `UniverseMeta.custom_stages`) + 5 Tauri commands (`read/add/update/remove/reorder_custom_stages`).
- **Phase 1B** committed `8a9ab3d` — frontend store + IPC wrappers (TS types + `LIVING_LINK_BASELINE` constant + `customStages` writable + 5 wrappers + `isKnownStage` helper). Flagged minor architectural deviation: routed `custom_stages` through the boot bundle instead of a separate IPC, to stay within the "ZERO extra boot IPCs" rule.
- **Phase 1C** committed `17bf474` — PropertyEditor stage field replaced with combobox sourced from `[...LIVING_LINK_BASELINE, ...$customStages]`. Inline-add: typing a value not in the list calls `addCustomStage` on commit. First user-visible change of MIG-014.

### Minutes of Chating instruction

- **Eisa:** "From now on, I want you to record our chatting history in an MD document that I will call 'Minutes of Chating (MoCh)'. It shall be written every 3 hours while we chat. Every copy will be uniquely identified by the date and time. It will record only our direct interaction." → MoCh convention established. Added to CLAUDE.md as Standing Order #7. First file is this document. Saved as memory feedback note.

### Name preference

- **Eisa:** "Address me as 'Eisa' from now on." → noted; conversational address switched. Project-doc terminology (`Boss`, `the user`) stays unchanged. Saved as user-memory note.

### MIG-014 §1C build + first test

- **Claude:** built MSI installer with §1A–§1C changes. Sent Stage 1 test tutorial covering Steps 1–3 (combobox + inline-add); held Steps 4–5 (cross-Universe scope + old-Zettelkasten-value preservation) for Stage 2.
- **Eisa (with screenshot):** "Step 1: Why is the dropdown listed this way?" Screenshot showed two-tier rendering: bold lowercase value (`spark`) on top, greyed `✨ Spark` below — Chromium/WebView2's native `<datalist>` style.
- **Claude:** explained the native rendering. Offered three paths: (1) accept and move on, (2) drop the inner label (breaks disk normalization — don't recommend), (3) replace `<datalist>` with a custom dropdown.
- **Eisa (with screenshot):** "I want them listed this way." Screenshot showed a clean single-row vertical list — emoji + capitalized label only (e.g. `✨ Spark`).

### §1D scope correction (mid-cascade)

- **Claude:** stated intent to extract the dropdown into a reusable `StageCombobox.svelte` for reuse in §1D NotePane breadcrumb.
- **Eisa:** "No, the breadcrumb will display only the Stage type and the promote arrow, as we decided before. Or did you forget?"
- **Claude:** acknowledged. Verified by reading current `NotePane.svelte:918-950` — confirmed the live design is `[← demote] [emoji label-badge] [promote →]`. NO dropdown. The Plan §1D I wrote was wrong, AND the NotePane Specs §3.5 entry I wrote earlier (citing commit `6cbe87c` introducing a dropdown) was also wrong — that dropdown experiment was undone at commit `90c1ea8` (§136, 2026-05-02 redesign).
- **Claude:** restated corrected scope:
  - **§1C.5** = inline custom dropdown in PropertyEditor only. Single consumer; do not extract a component.
  - **§1D** = update `stageOrder` and emoji map in NotePane breadcrumb to source from `[...LIVING_LINK_BASELINE, ...$customStages]`. Keep promote/demote-arrow nav. NO dropdown anywhere in breadcrumb.
- **Eisa:** "Proceed."

### MIG-014 §1C.5 — custom inline dropdown

- **Phase 1C.5** committed `9973e65` — replaced `<input list> + <datalist>` with a custom inline dropdown in PropertyEditor. Single-row items styled `emoji label`. Outside-click + ESC + Tab + Enter handled. ArrowUp/ArrowDown keyboard navigation. Inline-add behavior preserved. NotePane Specs §3.5 doc-fix landed in same commit (corrected dropdown-vs-arrows claim).
- **Session log** for 2026-05-06 written + committed `a65dc0f` (per Standing Order #1).
- **MSI rebuild** in flight at end of block.

### MoCh refresh request

- **Eisa:** "record our previous chats since we began this session." → this file (the 0900 block) refreshed to capture all exchanges since session start, including the post-§1C test → §1C.5 correction.

---

## Decisions made

1. CTSE Bridge Adapter switches index-time → query-time concept expansion (Option B). Locked into CLAUDE.md as Working Agreement #5.
2. Top-principal rules added to CLAUDE.md: Predecessor Lookup, Stop-On-Correction, State the Function in Hand.
3. PJ-007 baseline = Living Link 6-stage (`spark / birth / growth / maturity / dormancy / archival`) + per-Universe `custom_stages` extensibility. Zettelkasten model rejected.
4. Pending Jobs document uses stable reference numbers (PJ-NNN) that never get reissued.
5. Law 2.6 hierarchy: Universe (itself a default Library via `is_universe_notes`) → Library → Folder → Note, with cUniverse as sibling federation.
6. MoCh convention — `docs/MoCh/MoCh-YYYY-MM-DD-HHMM.md` every ~3 hours. Stored as Standing Order #7 in CLAUDE.md.
7. Eisa as the conversational name; project-doc terminology unchanged.
8. **MIG-014 §1D scope correction** — breadcrumb stays as promote/demote arrows + badge. NO dropdown.
9. **§1C.5 mid-cascade addition** — native `<datalist>` ditched for a custom inline dropdown after first Boss test surfaced two-tier value/label rendering.
10. Inlined dropdown stays in PropertyEditor (single consumer). Not extracted as a shared component since NotePane breadcrumb doesn't need it.

---

## Open at end of block

- MIG-014 §1C.5 MSI rebuild in flight (started 2026-05-06 ~11:00) — Eisa's retest pending on completion.
- §1D / §1E / §1F / §1G — pending in cascade.
- PJ-014 — 13-locale `propertyEditor.stagePlaceholder` backfill queued.
- Carried-over: `LinkLifecycle` dedupe (Option B approved 2026-05-01, deferred until post-CE); pre-MIG-013 backups hit blocking v2 sentinel migration.
- Next MoCh block: 2026-05-06 — 1200 onward.
