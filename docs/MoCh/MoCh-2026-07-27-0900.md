# Minutes of Chating — 2026-07-27, block starting ~09:00

*Direct Boss ↔ Claude interaction only. What shipped is in `lab/reports/SESSION-LOG-2026-07-27.md`.*

## 1. MIG-105 Architect closed; Boss approved and directed Stage 0

Boss read the Architect doc and the two team verdicts, then ruled: **"Root or System Library"** rejected
in favour of **Core Organizer** after Claude argued both names were wrong (one is the disease's name,
the other mislabels the head as a member of the category it rules); **"Core MOC"** rejected as the
*label* but adopted as the *front page*; **NO icon** (his own 07-25 steer, re-affirmed); and the **TOTAL
root rule** — *"The user cannot have notes in the root library. If they create a new note, they either
have to add it to the existing ones or create a new library for that reason."* Then: **"I'll go with (b)
archive it first"** on R2, with the reason that changed the migration's shape — *"I am planning to enable
Constellation in the near future to restore deleted notes/files. It will act like a time machine."*
That ruling made MIG-104 a hard prerequisite, and Boss agreed to sequence it first.

## 2. The Boss's testing found six defects that 52 passing tests did not

This was the session's defining pattern. Every one came from him **reading output**, not from the suite.

| # | What he did | What it exposed |
|---|---|---|
| 1 | Read `earned.jsonl` after Slice 4 | The same link written as `"France"` and `"france"` — two spellings for one link |
| 2 | Read it again after the seed | **44 lines where the truth was 38** — the join fanned out on his Arabic duplicate names (`السعودية` ×3, `فلسفة` ×2), inventing links he never walked |
| 3 | Same read | A Contested click at 09:21:25 recorded as 09:13:51 — a derived timestamp presented as observed |
| 4 | Renamed `search.db` aside | The restore **stamped itself complete** having restored nothing — a reconciler carrying a migration's stamp; the earned data would never have come back |
| 5 | Reported "stuck at 68 notes" | Index was progressing, not stuck — **Claude's own "~35 seconds" was a LOAD figure quoted for a REBUILD**, and he was sent into a 20-minute wait without warning |
| 6 | Booted after the small test | `no such tokenizer: constellation` — the root cause: a bare connection can't service the FTS trigger chain. **100% write loss, silent.** |

Boss's interventions were also corrective in kind: **"there is no 'retire' command. Check the image"**
(Claude had passed a UI affordance from the Plan without verifying it exists — the real label is
**Archive link**, and it lives in the sidebar panels, not the editor's right-click menu).

## 3. Boss asked for a smaller test, and it became the better one

After the 20-minute rebuild, Boss: **"Yes, draft the smaller test."** Claude proposed destroying only
the earned layer rather than the whole index, doing the database work himself (WA#1) and leaving the
Boss the GUI half. Two minutes instead of twenty — and it exercised his real data, including the
ambiguous `banana`/`السعودية` cases no synthetic corpus has.

## 4. The result

Boss wiped-and-booted; **34 of 34 written, 34 exact matches, 0 mismatches**, and **France came back
retired**. MIG-104's core promise proven on live data.

## 5. Two laws written from his findings

**LL-035** ("Feature X is off" must be proven by running it; never log a success you did not verify)
and **LL-036** (clone a pattern's preconditions, not just its code; build fixtures at the production
caller's privilege level). Both are his defects, generalised.

## 6. Close

Boss: **"Close the session with the full record and handover."**
