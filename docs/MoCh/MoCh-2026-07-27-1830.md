# Minutes of Chating — 2026-07-27 18:30 → 2026-07-29 06:00

*Boss ↔ Claude interaction only. What was asked, what was steered, what was decided.*

---

## 1. Opening — resume the MIG-104 cascade

Boss handed a resume prompt: continue at **Slice 7** (snapshot + 2 MB compactor), cascade the
slices, stop only at Boss-testable clauses. Standing constraints restated in the prompt itself
(name-keyed records restore only on exactly one row; weight and auto-tier are DERIVED; never hold
the DB lock across file I/O; the restore is a reconciler and carries no stamp; any dedicated
connection writing a trigger-bearing table must register the FTS tokenizer). Sight perf tests to run
as a **serial lane** (PJ-172).

## 2. Slice 7 — the compactor, and the Boss test that wasn't the compactor

Reported to Boss: the compactor itself has **no user-visible surface** (his store is 6 KB, ~340×
under the threshold) — but building it exposed that **review priorities were being written and
fsync'd since Slice 4 and never read back**, so losing the index still cost him every priority he
had set. That became the test instead.

Boss set **Priority 80** on `Africa`, confirmed the line in `earned.jsonl`, and handed back. Claude
erased it from the index directly (WA#1 — not handed to the Boss), verified `review_priority IS NOT
NULL` went from 1 row to 0 across the whole index and that `review-pulse.json` held no priority
data, so the ledger line was **the only copy on disk**. Boss reopened:

> *"Step 2: 1st image. Step 3: `1 priorities restored (0 already current, 0 unresolved)`"*

**Pass.** Priority 80 returned with the `manual` tag from a plain-text file the database had no
memory of.

## 3. THE PIVOT — Boss redirects to the app-killers

Claude surfaced the sweep register: **3 confirmed APP-KILLERs live in the build the Boss runs
daily**, none introduced by that slice. Put the fork to him with a recommendation.

**Boss: "Divert now — fix the 3 app-killers."**

SO#5 state-of-standing record written before any pivot work, per the standing order.

## 4. AK-1 — the rename cascade, and four holes

Boss tested in two stages and passed both:

> *"Stage 1 passed, sentence survived."*
> *"Pass. I saw the 'Updating links…'. It was so fast, I just got a glimpse of it."*

Claude reported honestly that **three further holes were found by inspecting the fix for the first**,
including an APP-KILLER in `renameItem` that was **Claude's own miss** — an ecosystem sweep run with
`grep -v` excluding the file being edited.

## 5. AK-2/3 — Boss asks for the design before the code

Claude proposed that AK-2 and AK-3 are ONE defect (props have no single owner) and that the project's
own Solve-the-Class rule forbids another symptom patch.

> **Boss: "reproduce it first, then show me the design."**

Reproduced RED, then MIG-107 written up as a combined Architect+Plan.

> **Boss: "write it up."** … then **"keep both panels, approved — start Slice 0."**

That ruling matters: Claude had recommended **against** the instinctive fix of deleting one of the
two Properties panels, on the grounds that two *views* of one truth is correct and two *copies* is
the bug. Boss approved the recommendation over the instinct.

## 6. Slices 0 → 5, and the four defects the Boss caught

Boss drove each slice with *"continue with Slice N"* and tested every user-visible one.

**Slice 4 — Boss found the tag regression.** Screenshot: a tag added in the in-note block appeared
there and **nowhere else**.

> *"Step 2: Failed. Check the image. I added a tag 'Eisa' in-note block, but didn't updated in the panel."*

Cause was Claude's: acting on an inspection finding, he had the panel *hand-mark* edited keys — wired
at **3 of that component's 16** mutation sites. Fix was to **delete the list, not complete it**.
Recorded as **LL-038 rule 6**.

**Slice 5 — Boss found the stage-surface gap.** Promoting from the sidebar updated both Properties
panels but not the header badge or the file-tree icon. Verified **pre-existing** (the panel has never
notified the tab store) and caused by a **wiring asymmetry**: the same component mounted twice, only
one mount given the callback. Fixed by removing the dependency rather than adding the missing wire.

**Boss also spotted a UX defect nobody had reported:**

> *"Shouldn't when trying to promote/demote a stage the list highlight the current one? Today, it highlight the top of the list, regardless."*

Right, and worse than cosmetic — a note at Growth offered Spark, two steps backwards. **PJ-179.**

## 7. Slice 5 pass, and the Boss's note

> *"1: Pass. 2: Pass. 3: Pass. 4: Pass."* … and earlier: **"You've done a great job."**

Claude also had to explain **Step 5 (Focus mode)** properly after giving it as a one-liner —

> **Boss: "Step 5: What do you mean?"**

— which was a fair hit on the Testing Instructions Rule: every test step is a tutorial, and that one
had been a fragment. Rewritten with what Focus mode is, why it is on the checklist, and the exact
click path; Boss then passed it.

## 8. Slice 6 — the Boss rules on the toggle

Claude put the trade honestly: a rollback flag is only worth having if the path behind it still
works, and that path had not been exercised since Slice 3 — against seven in-pass defects, four of
them Boss-found.

**Boss: keep the toggle one more cycle.** Removed when MIG-104 closes.

## 9. Close

> **Boss: "Close."**

Full PCS: orientation **v3.74**, PJ ledger **v1.56**, this MoCh, handover + next-session prompt.

---

## Decisions of record from this block

| # | Decision |
|---|---|
| 1 | **Divert** from the MIG-104 cascade to the three confirmed APP-KILLERs. |
| 2 | **Reproduce first, then show the design** — before any code on AK-2/3. |
| 3 | **Keep BOTH Properties panels** — two views of one truth is correct; two copies is the bug. |
| 4 | **Keep the `PROPS_SINGLE_OWNERSHIP` toggle** one more cycle, removed at the MIG-104 close. |
| 5 | The stage picker must **open on the note's current stage** (PJ-179). |

## What the Boss caught that the tests did not

Six defects, three of them introduced by Claude in this same session: the tag regression (Slice 4),
the stage-surface gap (Slice 5), the stage-picker highlight, and — via his insistence on testing each
slice — the conditions that exposed the rest. **The suite went 607 → 678 and still did not find
these.** That is the standing argument for the mandatory Boss test before every commit.
