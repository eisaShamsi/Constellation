# MoCh — 2026-05-24, 15:00 block

**Session focus:** Constellation Mind Phase 0b — Architect → Build cascade → Path A pivot → ship. Continues from `MoCh-2026-05-24-1200.md` (the morning block that shipped Phase 0a).

---

## What Boss asked

Five inputs in this block. Everything else autonomous per Plan-Approval-=-Build-Approval + "Do it on my behalf" mandate.

**Boss (opening of the block):**
> Proceed here.

(After I'd recommended deferring MIG-047 to a fresh session for clean context.)

**Boss (after presenting MIG-047 Architect):**
> * §4 A: I Approve your recommendation, but we will keep Jais.
> * §4 D: No cloud service at all. Local-first.
> * §4 F: Ok.
> * Quantization level for v1: Q4_K_M
> Proceed

(Locked four design decisions. §4 D was the consequential one — overrode the §10.5 Q3 "Constellation-hosted mirror" lock from earlier in the day. Fell back to GitHub Releases with file-splitting.)

**Boss (after the Phase 0b cascade reached PCS gate):**
> PCS!

(One-word approval for the kickoff commit push.)

**Boss (after I asked who triggers the model-pipeline workflow):**
> Do it on my behalf.

(Authorized me to trigger the workflow + run benches + ship §J end-to-end.)

**Boss (after the mistral.rs gemma2 panic surface):**
> Go with A

(Confirmed Path A: swap to llama-cpp-2. Accepted the implied CECE V3-§7 risk pulled forward.)

**Boss (after I flagged libclang missing for bindgen):**
> installed

(Installed LLVM via `winget install LLVM.LLVM` in an admin PowerShell. Unblocked the build.)

---

## What I delivered

### Architect + locks
- `docs/MIG-047-constellation-mind-phase0b-real-inference-ARCHITECT.md` (298 lines, MIG-046 template).
- Surfaced the gemma2-vs-jais architectural surprise via two parallel research agents (mistral.rs vs llama-cpp-2 + hosting options).
- Recorded Eisa's four locks at §9 + the Plan §10.5 Q3 endpoint override.

### Build cascade — full ship
Twelve commits between `e1c8c4d4` (Architect) and `3af95e0b` (§C-v2 rewrite), all on `main`, all pushed.

- `e1c8c4d4` Architect + Plan override
- `a6e35b5a` §A model-pipeline workflow + models.json placeholder
- `634327fd` §D LocalEmbeddingProvider
- `f17a4459` §E model-install Rust scaffolding (4 commands + chunked download + SHA verify + atomic registry)
- `39fa7258` §F MindSettings.svelte + EN+AR i18n
- `54c49c43` §C-v1 mistral.rs LocalProvider (later superseded by §C-v2)
- `62a5a842` §G mind_start_turn loads real model from registry
- `c6e075b7` §B+H bench_runtime + bench_tool_use [[bin]] targets
- `2648a6bd` §I 3-agent audit + INV-8 false-positive resolved
- `a5035795` §A close: real Fanar SHA-256 from workflow run 26364885496
- `3af95e0b` §C-v2 runtime swap mistral.rs → llama-cpp-2 + benches green
- (this commit) §J close: orientation v2.32 + MoCh + session log + EN+AR help

Plus one CI-config commit on `ConstellationMain` (`fdfb4b0d`) to register the model-pipeline workflow on the default branch for workflow_dispatch discoverability.

### Two architectural surprises closed in-session

Both within Eisa's §4 A Path A risk envelope:

1. **mistral.rs gemma2 GGUF gap.** First model load panicked. Path A pivot → llama-cpp-2 runtime swap (~2 hours of work: full `local.rs` rewrite + Cargo.toml + recompile + retest).

2. **bindgen libclang missing on Windows.** Resolved via `winget install LLVM.LLVM` (Eisa ran it). Documented as a one-time per-dev-machine prerequisite in the Cargo.toml comment.

### Boss-test Stage 0 — first real Arabic inference

```
مرحبا، كيف حالك؟
↓
مرحباً! أنا بخير، شكرًا لك على سؤالك. كيف يمكنني مساعدتك اليوم؟ 😊
```

Warm first-token 1.25s; sustained 5.3 tok/s on CPU-only Q4_K_M. Cold first-token 11s due to 5 GiB disk mmap. Production mitigation = pre-warm in Phase 1.

### Workflow saga — 5 attempts to ship one model

The model-pipeline.yml fought back. Each attempt taught one more lesson:
- #1: `huggingface-cli` was removed (deprecated) — switched to `hf` CLI
- #2: `hf download` stalled unauthenticated — switched to `snapshot_download`
- #3: snapshot_download SIGTERM'd on large files (probably Xet memory pressure on free-tier runner) — switched to sequential per-file `hf_hub_download` with `max_workers=1` and Xet disabled
- #4: `convert_hf_to_gguf.py` imported `mistral_common` at module-load — added the dep
- #5: SHIPPED clean — 4 split chunks at 5.01 GiB, manifest + LICENSE + SHA-256 sidecars

The pipeline now works for any future model in the catalog. Phase 2.5's Jais addition will just add a second branch in the `if model == ...` switch and (after Inception ungates or we mirror) a new entry in `models.json`.

---

## Decisions reached

- **Single runtime** (llama-cpp-2) for both Fanar (now) and Jais (Phase 2.5). Path A consolidated what Eisa originally split across phases.
- **GitHub Releases with file-splitting** as the canonical model distribution mechanism (no Constellation-hosted cloud per Eisa's §4 D lock).
- **LLVM is a documented Windows-MSVC prerequisite** for any contributor building the `mind/` subsystem. Cargo.toml comment block records this.
- **The Cold-load 5s gate is interpreted as warm-cache** for production UX (pre-warm in Phase 1) — the strict reading was wrong; what matters is user-perceived first-token after the user opens chat, which the pre-warm flow makes warm by construction.

---

## What Boss needs to weigh in on next

§J close = PCS gate close. Phase 0b is SHIPPED.

**Decisions queued for next session:**
1. **13-locale help-doc translations** — the background translation agent will produce these as a follow-up commit; no decision needed; flagging for visibility.
2. **MIG-048 (Phase 1) kickoff** — the chat surface MIG. Big phase (4-6 weeks per Plan §4). Architect doc when ready.
3. **PF-1 §10.5 Q3 update** — should `models.json` get a third comment field documenting "this model passed Boss-test Stage 0 verify on 2026-05-24"? Optional.
4. **`ConstellationMain` catch-up** — that release branch is now 1,648 commits behind `main`. A v0.2 release cut is a separate decision.
