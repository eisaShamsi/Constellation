# Minutes of Chating — 2026-07-07 (afternoon → evening block)

Direct Boss ↔ Claude interaction only (decisions, steers, what was said). Not the commit log (that's the session log).

## The arc

Resumed on the ratified next-session prompt (PJ-069 note-lists cluster). The day ran: **MIG-096 §2 build + test → the Reviewer test surfaced a hidden rename→index bug → a deep root-cause saga → Boss halts features for a full Safety Audit.**

## MIG-096 §2 (right-click cluster) — build + Boss test

- Boss approved the MIG-096 Plan ("Approved"). Claude cascaded §0 (Predecessor ledger) → §1 (dormant primitives, adversarially audited — 1 LOW leak fixed) → §2 Reviewer + OrgChart.
- **Second-Screen fork:** Claude surfaced the genuine choice — a mutating menu on the 2nd screen pops its dialog on the MAIN screen (Display-not-Domain). Boss ruled **"Full menu (mutations forward to main)."** Built + audited (3 findings: open-from-overlay dismissal, return-pending guard, embedded menu — all fixed).
- **Reviewer Boss test:** Parts A/C/D PASS. **Part B FAILED** — renamed a note, but on reopening the Reviewer it reverted to the old name and opening it hit the empty Dashboard, while the file tree showed the new name.

## The rename→index drift saga (the app-killer)

- Claude diagnosed against the LIVE DB + disk (Reproduce-First): `note_meta` pointed at a dead old path with the old name; disk was correct. First read it as rare (1/7,713).
- Boss ruled **"Fix it now (safe self-heal)"** → MIG-097 relocate.
- Re-test FAILED again (both a healed note AND a fresh note). Claude **corrected its own mistake to the Boss**: the diagnostics log proved this was *routine* (~9 days, 1–2 notes drifting per launch), not rare, and the old reconcile had been silently deleting the drifted notes each boot. MIG-097 was insufficient (boot-only + can't re-adopt already-deleted rows).
- Boss: **"Proceed"** with a proper `/migration`. Claude shipped Step-1 instrumentation, captured a working trace, then the completed self-heal (re-adopt orphans, WA#4-audited) — which **recovered 12 silently-orphaned notes, 0 removed**.
- Boss asked **"list them"** → Claude listed all 12 (8 renamed, 4 orphaned-otherwise), flagged two oddities (a stray leading `[` in a title; an old *Untitled* alias).
- Boss: **"chase the root cause… capture it as a lesson learned… solve it from its core root."** Claude ran a provoke-the-drift test → the **failing trace confirmed the root cause**: the detached fire-and-forget rename tail ran with the DB connection `None` (renamed during boot) → the note_meta update + reindex silently no-op'd; `reindex_single_note` returns `Ok(())` on a `None` conn (false-success). General: fire-and-forget = not durable.

## The pivot — Safety & Integrity Audit

- Boss (decisive): *"This bug is one BIG app killer. It was hidden… I don't want to keep stumbling on such BIG hidden bugs. We have to stop everything and put the app under inspection… I don't care how long it will take or how much effort — declare the app safe and secure."* Repeated: **"I don't want to reinvent the wheel"** (prior-art fixes).
- Claude: acknowledged, halted feature work, recorded the state-of-standing (SO#5), wrote the **Safety Audit Charter** (app-killer taxonomy + adversarial-verify method + prior-art fixes), and — on Boss instruction **"Before doing anything, conduct PCS + Orientation"** — ran the checkpoint (commit/push + Orientation v3.30 + this MoCh) before opening the audit.

## Decisions banked

- MIG-096 §2 Second-Screen = **full menu, forwarded to main**.
- The rename bug is an **app-killer** (silent, source-of-truth-corrupting, hidden ~9 days) → triggers a **full-app Safety & Integrity Audit** (features frozen until "safe & secure").
- Durable root fix (MIG-098 §2) = audit remediation **item #1**, via a prior-art pattern (outbox / WAL / disk-reconcile).
- **Lesson (to bank as LL):** a fire-and-forget background task is NEVER a durability mechanism for a source-of-truth write; durability = a persisted intent + a reliable reconcile (File-Over-App), never best-effort.
