# MoCh — 2026-07-24, 09:30 block

Conversational trace of direct Boss ↔ Claude interaction. What shipped is in
`lab/reports/SESSION-LOG-2026-07-24-B.md`; this is what was *said*.

---

## The Boss's instruction (session open)

> "Continue MIG-103. First: triage safety-inspection run wf_acc3ca2c-4f6 (in
> flight at PCS) and fix confirmed findings per WA#6. Then build the ruled
> destination-propose+show for new-note-from-template (reuse MoveDialog,
> +layout.svelte:6287); Boss-test per the Testing Instructions Rule. Then §1
> use-side remainder, then D4. Ledger v1.46 has the queue."

A fully specified queue — no ambiguity to resolve, so no questions asked. The
whole block ran on that one instruction.

## What I reported back

**1. The owed inspection was empty — and that mattered.**
The run left in flight at the last PCS had been killed ~2 minutes in by the
Boss-directed close: 14 agents started, **zero produced output**. The honest
reading is not "no findings" but "the inspection never happened" — so the
Slice-2 per-build inspection was still *owed*, and I re-ran it in full rather
than reporting a clean bill. That re-run found **62 confirmed issues including
two APP-KILLERs**. Had I read the empty journal as "nothing found", both
app-killers would have shipped.

**2. Two APP-KILLERs, both of the same family the project keeps meeting.**
- A property edit could silently delete frontmatter children — the **third**
  shape of the class PJ-136 and MIG-101 each closed once. Evidence for PJ-137
  (one YAML authority) is now overwhelming: six app-killers traced to the
  two-parser split.
- "Approve All" could erase the very batch it had just accepted — the *missed
  sibling* of a fix applied to four adjacent seams two days earlier. The lesson
  is the one already recorded as **Solve-the-class, not-the-instance**: fixing
  four of five call sites leaves the class alive.

**3. The right function existed and nobody called it.**
Four Rust sites plus the rename cascade resolved a note's library with a raw
first-match prefix test, while the canonical longest-root-wins resolver had been
sitting in the same file all along. Because `universe_notes` sits at index 0 with
its path equal to the Universe root, first-match *always* returned the root.

## Where I stopped, and why

I fixed **8** of the 62 and stopped: both app-killers, the three findings inside
the Slice-2 batch the inspection was owed for, and the library-resolver class
(one canonical-resolver swap per site).

The other **54** I did *not* fix, and did not quietly park either — they are
filed as **PJ-140** with the full register in the repo. The reasoning I owe the
Boss: they are whole-app findings unrelated to MIG-103, several crossing
subsystem boundaries. WA#6 says fix what you discover; it also says that when
something genuinely needs its own migration, say so and get a ruling rather than
silently defer. Fixing them as drive-bys inside a feature build is precisely the
unproven cross-cutting change WA#4 forbids. **A ruling is requested on how they
get sequenced.**

Four of them I flagged as standouts because they lose user data rather than
merely mis-index it: folder rename has no descendant index cascade; deleting a
folder purges nothing from the index; `review-pulse.json` is a non-atomic write
whose loader silently falls back to defaults, so a corrupt file loses all review
history with no error; and the schema-version rebuild deletes `search.db` — the
only store of user-earned Living-Link properties and review overrides — on every
version bump.

## A correction I made to myself, recorded

While fixing the frontmatter app-killer I made the refusal too broad and broke
`ikhtilāf`, the one seq-of-maps the system *does* round-trip losslessly. The
existing MIG-101 test suite caught it immediately — three tests went red. The
fix was to exempt it **by key**, and to move that key set to live beside the
serializer that makes the claim true, so the two can no longer drift apart. I
also wrote a first version of the parser fix that was convoluted enough that I
could not convince myself it was correct, and rewrote it — a data-protection fix
is the wrong place for cleverness.

Also: one of my own new test expectations was wrong (I asserted a blank-line list
should become read-only; the extent fix already preserves both items, so keeping
it editable is correct and strictly better for the user). I changed the test, not
the code — after checking which one was actually wrong.

## Delivered at block close

The destination flow is **built and the binary rebuilt**, with the test tutorial
written per the Testing Instructions Rule. **Nothing is committed** — per the
standing order that the Boss tests every build before it ships, the commit is
gated on that pass.
